load("@io_bazel_rules_go//go:def.bzl", "go_binary")

go_binary(
    name = "wasm",
    srcs = [
        "main.go",
    ],
    deps = [
        "//compiler:compiler_library",
    ],
    goarch = "wasm",
    goos = "js",
    pure = "on",
)
