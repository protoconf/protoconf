syntax = "proto3";

// Provider: google 4.69.1
package terraform.google.resources.v4;

import "terraform/v1/meta.proto";

// GoogleComputeAddress version is 0
message GoogleComputeAddress {
  // The static external IP address represented by this resource. Only
  // IPv4 is supported. An address may only be specified for INTERNAL
  // address types. The IP address must be inside the specified subnetwork,
  // if any. Set by the API if undefined.
  string address = 1;

  // The type of address to reserve.
  // Note: if you set this argument's value as 'INTERNAL' you need to leave the 'network_tier' argument unset in that resource block. Default value: "EXTERNAL" Possible values: ["INTERNAL", "EXTERNAL"]
  string address_type = 2 [json_name = "address_type"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 3 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 4;

  string id = 5;

  // Name of the resource. The name must be 1-63 characters long, and
  // comply with RFC1035. Specifically, the name must be 1-63 characters
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?'
  // which means the first character must be a lowercase letter, and all
  // following characters must be a dash, lowercase letter, or digit,
  // except the last character, which cannot be a dash.
  string name = 6;

  // The URL of the network in which to reserve the address. This field
  // can only be used with INTERNAL type with the VPC_PEERING and
  // IPSEC_INTERCONNECT purposes.
  string network = 7;

  // The networking tier used for configuring this address. If this field is not
  // specified, it is assumed to be PREMIUM.
  // This argument should not be used when configuring Internal addresses, because [network tier cannot be set for internal traffic; it's always Premium](https://cloud.google.com/network-tiers/docs/overview). Possible values: ["PREMIUM", "STANDARD"]
  string network_tier = 8 [json_name = "network_tier"];

  // The prefix length if the resource represents an IP range.
  int64 prefix_length = 9 [json_name = "prefix_length"];

  string project = 10;

  // The purpose of this resource, which can be one of the following values.
  //
  // * GCE_ENDPOINT for addresses that are used by VM instances, alias IP
  // ranges, load balancers, and similar resources.
  //
  // * SHARED_LOADBALANCER_VIP for an address that can be used by multiple
  // internal load balancers.
  //
  // * VPC_PEERING for addresses that are reserved for VPC peer networks.
  //
  // * IPSEC_INTERCONNECT for addresses created from a private IP range that
  // are reserved for a VLAN attachment in an HA VPN over Cloud Interconnect
  // configuration. These addresses are regional resources.
  //
  // * PRIVATE_SERVICE_CONNECT for a private network address that is used to
  // configure Private Service Connect. Only global internal addresses can use
  // this purpose.
  //
  //
  // This should only be set when using an Internal address.
  string purpose = 11;

  // The Region in which the created address should reside.
  // If it is not provided, the provider region is used.
  string region = 12;

  string self_link = 13 [json_name = "self_link"];

  // The URL of the subnetwork in which to reserve the address. If an IP
  // address is specified, it must be within the subnetwork's IP range.
  // This field can only be used with INTERNAL type with
  // GCE_ENDPOINT/DNS_RESOLVER purposes.
  string subnetwork = 14;

  // The URLs of the resources that are using this address.
  repeated string users = 15;

  repeated Timeouts timeouts = 16;

  map<string, string> for_each = 17 [json_name = "for_each"];

  repeated string depends_on = 18 [json_name = "depends_on"];

  int32 count = 19;

  string provider = 20;

  terraform.v1.Lifecycle lifecycle = 21;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeAttachedDisk version is 0
message GoogleComputeAttachedDisk {
  // Specifies a unique device name of your choice that is reflected into the /dev/disk/by-id/google-* tree of a Linux operating system running within the instance. This name can be used to reference the device for mounting, resizing, and so on, from within the instance. If not specified, the server chooses a default device name to apply to this disk, in the form persistent-disks-x, where x is a number assigned by Google Compute Engine.
  string device_name = 1 [json_name = "device_name"];

  // name or self_link of the disk that will be attached.
  string disk = 2;

  string id = 3;

  // name or self_link of the compute instance that the disk will be attached to. If the self_link is provided then zone and project are extracted from the self link. If only the name is used then zone and project must be defined as properties on the resource or provider.
  string instance = 4;

  // The mode in which to attach this disk, either READ_WRITE or READ_ONLY. If not specified, the default is to attach the disk in READ_WRITE mode.
  string mode = 5;

  // The project that the referenced compute instance is a part of. If instance is referenced by its self_link the project defined in the link will take precedence.
  string project = 6;

  // The zone that the referenced compute instance is located within. If instance is referenced by its self_link the zone defined in the link will take precedence.
  string zone = 7;

  repeated Timeouts timeouts = 8;

  map<string, string> for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeAutoscaler version is 0
message GoogleComputeAutoscaler {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 2;

  string id = 3;

  // Name of the resource. The name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 4;

  string project = 5;

  string self_link = 6 [json_name = "self_link"];

  // URL of the managed instance group that this autoscaler will scale.
  string target = 7;

  // URL of the zone where the instance group resides.
  string zone = 8;

  AutoscalingPolicy autoscaling_policy = 9 [json_name = "autoscaling_policy"];

  repeated Timeouts timeouts = 10;

  map<string, string> for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message AutoscalingPolicy {
    // The number of seconds that the autoscaler should wait before it
    // starts collecting information from a new instance. This prevents
    // the autoscaler from collecting information when the instance is
    // initializing, during which the collected usage would not be
    // reliable. The default time autoscaler waits is 60 seconds.
    //
    // Virtual machine initialization times might vary because of
    // numerous factors. We recommend that you test how long an
    // instance may take to initialize. To do this, create an instance
    // and time the startup process.
    int64 cooldown_period = 1 [json_name = "cooldown_period"];

    // The maximum number of instances that the autoscaler can scale up
    // to. This is required when creating or updating an autoscaler. The
    // maximum number of replicas should not be lower than minimal number
    // of replicas.
    int64 max_replicas = 2 [json_name = "max_replicas"];

    // The minimum number of replicas that the autoscaler can scale down
    // to. This cannot be less than 0. If not provided, autoscaler will
    // choose a default value depending on maximum number of instances
    // allowed.
    int64 min_replicas = 3 [json_name = "min_replicas"];

    // Defines operating mode for this policy. Default value: "ON" Possible values: ["OFF", "ONLY_UP", "ON"]
    string mode = 4;

    CpuUtilization cpu_utilization = 5 [json_name = "cpu_utilization"];

    LoadBalancingUtilization load_balancing_utilization = 6 [json_name = "load_balancing_utilization"];

    repeated Metric metric = 7;

    ScaleInControl scale_in_control = 8 [json_name = "scale_in_control"];

    repeated ScalingSchedules scaling_schedules = 9 [json_name = "scaling_schedules"];

    message CpuUtilization {
      // Indicates whether predictive autoscaling based on CPU metric is enabled. Valid values are:
      //
      // - NONE (default). No predictive method is used. The autoscaler scales the group to meet current demand based on real-time metrics.
      //
      // - OPTIMIZE_AVAILABILITY. Predictive autoscaling improves availability by monitoring daily and weekly load patterns and scaling out ahead of anticipated demand.
      string predictive_method = 1 [json_name = "predictive_method"];

      // The target CPU utilization that the autoscaler should maintain.
      // Must be a float value in the range (0, 1]. If not specified, the
      // default is 0.6.
      //
      // If the CPU level is below the target utilization, the autoscaler
      // scales down the number of instances until it reaches the minimum
      // number of instances you specified or until the average CPU of
      // your instances reaches the target utilization.
      //
      // If the average CPU is above the target utilization, the autoscaler
      // scales up until it reaches the maximum number of instances you
      // specified or until the average utilization reaches the target
      // utilization.
      int64 target = 2;
    }

    message LoadBalancingUtilization {
      // Fraction of backend capacity utilization (set in HTTP(s) load
      // balancing configuration) that autoscaler should maintain. Must
      // be a positive float value. If not defined, the default is 0.8.
      int64 target = 1;
    }

    message Metric {
      // The identifier (type) of the Stackdriver Monitoring metric.
      // The metric cannot have negative values.
      //
      // The metric must have a value type of INT64 or DOUBLE.
      string name = 1;

      // The target value of the metric that autoscaler should
      // maintain. This must be a positive value. A utilization
      // metric scales number of virtual machines handling requests
      // to increase or decrease proportionally to the metric.
      //
      // For example, a good metric to use as a utilizationTarget is
      // www.googleapis.com/compute/instance/network/received_bytes_count.
      // The autoscaler will work to keep this value constant for each
      // of the instances.
      int64 target = 2;

      // Defines how target utilization value is expressed for a
      // Stackdriver Monitoring metric. Possible values: ["GAUGE", "DELTA_PER_SECOND", "DELTA_PER_MINUTE"]
      string type = 3;
    }

    message ScaleInControl {
      // How long back autoscaling should look when computing recommendations
      // to include directives regarding slower scale down, as described above.
      int64 time_window_sec = 1 [json_name = "time_window_sec"];

      MaxScaledInReplicas max_scaled_in_replicas = 2 [json_name = "max_scaled_in_replicas"];

      message MaxScaledInReplicas {
        // Specifies a fixed number of VM instances. This must be a positive
        // integer.
        int64 fixed = 1;

        // Specifies a percentage of instances between 0 to 100%, inclusive.
        // For example, specify 80 for 80%.
        int64 percent = 2;
      }
    }

    message ScalingSchedules {
      // A description of a scaling schedule.
      string description = 1;

      // A boolean value that specifies if a scaling schedule can influence autoscaler recommendations. If set to true, then a scaling schedule has no effect.
      bool disabled = 2;

      // The duration of time intervals (in seconds) for which this scaling schedule will be running. The minimum allowed value is 300.
      int64 duration_sec = 3 [json_name = "duration_sec"];

      // Minimum number of VM instances that autoscaler will recommend in time intervals starting according to schedule.
      int64 min_required_replicas = 4 [json_name = "min_required_replicas"];

      string name = 5;

      // The start timestamps of time intervals when this scaling schedule should provide a scaling signal. This field uses the extended cron format (with an optional year field).
      string schedule = 6;

      // The time zone to be used when interpreting the schedule. The value of this field must be a time zone name from the tz database: http://en.wikipedia.org/wiki/Tz_database.
      string time_zone = 7 [json_name = "time_zone"];
    }
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeBackendBucket version is 0
message GoogleComputeBackendBucket {
  // Cloud Storage bucket name.
  string bucket_name = 1 [json_name = "bucket_name"];

  // Compress text responses using Brotli or gzip compression, based on the client's Accept-Encoding header. Possible values: ["AUTOMATIC", "DISABLED"]
  string compression_mode = 2 [json_name = "compression_mode"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 3 [json_name = "creation_timestamp"];

  // Headers that the HTTP/S load balancer should add to proxied responses.
  repeated string custom_response_headers = 4 [json_name = "custom_response_headers"];

  // An optional textual description of the resource; provided by the
  // client when the resource is created.
  string description = 5;

  // The security policy associated with this backend bucket.
  string edge_security_policy = 6 [json_name = "edge_security_policy"];

  // If true, enable Cloud CDN for this BackendBucket.
  bool enable_cdn = 7 [json_name = "enable_cdn"];

  string id = 8;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  // the first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the
  // last character, which cannot be a dash.
  string name = 9;

  string project = 10;

  string self_link = 11 [json_name = "self_link"];

  CdnPolicy cdn_policy = 12 [json_name = "cdn_policy"];

  repeated Timeouts timeouts = 13;

  map<string, string> for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  int32 count = 16;

  string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message CdnPolicy {
    // Specifies the cache setting for all responses from this backend.
    // The possible values are: USE_ORIGIN_HEADERS, FORCE_CACHE_ALL and CACHE_ALL_STATIC Possible values: ["USE_ORIGIN_HEADERS", "FORCE_CACHE_ALL", "CACHE_ALL_STATIC"]
    string cache_mode = 1 [json_name = "cache_mode"];

    // Specifies the maximum allowed TTL for cached content served by this origin.
    int64 client_ttl = 2 [json_name = "client_ttl"];

    // Specifies the default TTL for cached content served by this origin for responses
    // that do not have an existing valid TTL (max-age or s-max-age).
    int64 default_ttl = 3 [json_name = "default_ttl"];

    // Specifies the maximum allowed TTL for cached content served by this origin.
    int64 max_ttl = 4 [json_name = "max_ttl"];

    // Negative caching allows per-status code TTLs to be set, in order to apply fine-grained caching for common errors or redirects.
    bool negative_caching = 5 [json_name = "negative_caching"];

    // If true then Cloud CDN will combine multiple concurrent cache fill requests into a small number of requests to the origin.
    bool request_coalescing = 6 [json_name = "request_coalescing"];

    // Serve existing content from the cache (if available) when revalidating content with the origin, or when an error is encountered when refreshing the cache.
    int64 serve_while_stale = 7 [json_name = "serve_while_stale"];

    // Maximum number of seconds the response to a signed URL request will
    // be considered fresh. After this time period,
    // the response will be revalidated before being served.
    // When serving responses to signed URL requests,
    // Cloud CDN will internally behave as though
    // all responses from this backend had a "Cache-Control: public,
    // max-age=[TTL]" header, regardless of any existing Cache-Control
    // header. The actual headers served in responses will not be altered.
    int64 signed_url_cache_max_age_sec = 8 [json_name = "signed_url_cache_max_age_sec"];

    repeated BypassCacheOnRequestHeaders bypass_cache_on_request_headers = 9 [json_name = "bypass_cache_on_request_headers"];

    CacheKeyPolicy cache_key_policy = 10 [json_name = "cache_key_policy"];

    repeated NegativeCachingPolicy negative_caching_policy = 11 [json_name = "negative_caching_policy"];

    message BypassCacheOnRequestHeaders {
      // The header field name to match on when bypassing cache. Values are case-insensitive.
      string header_name = 1 [json_name = "header_name"];
    }

    message CacheKeyPolicy {
      // Allows HTTP request headers (by name) to be used in the
      // cache key.
      repeated string include_http_headers = 1 [json_name = "include_http_headers"];

      // Names of query string parameters to include in cache keys.
      // Default parameters are always included. '&' and '=' will
      // be percent encoded and not treated as delimiters.
      repeated string query_string_whitelist = 2 [json_name = "query_string_whitelist"];
    }

    message NegativeCachingPolicy {
      // The HTTP status code to define a TTL against. Only HTTP status codes 300, 301, 308, 404, 405, 410, 421, 451 and 501
      // can be specified as values, and you cannot specify a status code more than once.
      int64 code = 1;

      // The TTL (in seconds) for which to cache responses with the corresponding status code. The maximum allowed value is 1800s
      // (30 minutes), noting that infrequently accessed objects may be evicted from the cache before the defined TTL.
      int64 ttl = 2;
    }
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeBackendBucketSignedUrlKey version is 0
message GoogleComputeBackendBucketSignedUrlKey {
  // The backend bucket this signed URL key belongs.
  string backend_bucket = 1 [json_name = "backend_bucket"];

  string id = 2;

  // 128-bit key value used for signing the URL. The key value must be a
  // valid RFC 4648 Section 5 base64url encoded string.
  string key_value = 3 [json_name = "key_value"];

  // Name of the signed URL key.
  string name = 4;

  string project = 5;

  repeated Timeouts timeouts = 6;

  map<string, string> for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeBackendService version is 1
message GoogleComputeBackendService {
  // Lifetime of cookies in seconds if session_affinity is
  // GENERATED_COOKIE. If set to 0, the cookie is non-persistent and lasts
  // only until the end of the browser session (or equivalent). The
  // maximum allowed value for TTL is one day.
  //
  // When the load balancing scheme is INTERNAL, this field is not used.
  int64 affinity_cookie_ttl_sec = 1 [json_name = "affinity_cookie_ttl_sec"];

  // Compress text responses using Brotli or gzip compression, based on the client's Accept-Encoding header. Possible values: ["AUTOMATIC", "DISABLED"]
  string compression_mode = 2 [json_name = "compression_mode"];

  // Time for which instance will be drained (not accept new
  // connections, but still work to finish started).
  int64 connection_draining_timeout_sec = 3 [json_name = "connection_draining_timeout_sec"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 4 [json_name = "creation_timestamp"];

  // Headers that the HTTP/S load balancer should add to proxied
  // requests.
  repeated string custom_request_headers = 5 [json_name = "custom_request_headers"];

  // Headers that the HTTP/S load balancer should add to proxied
  // responses.
  repeated string custom_response_headers = 6 [json_name = "custom_response_headers"];

  // An optional description of this resource.
  string description = 7;

  // The resource URL for the edge security policy associated with this backend service.
  string edge_security_policy = 8 [json_name = "edge_security_policy"];

  // If true, enable Cloud CDN for this BackendService.
  bool enable_cdn = 9 [json_name = "enable_cdn"];

  // Fingerprint of this resource. A hash of the contents stored in this
  // object. This field is used in optimistic locking.
  string fingerprint = 10;

  // The unique identifier for the resource. This identifier is defined by the server.
  int64 generated_id = 11 [json_name = "generated_id"];

  // The set of URLs to the HttpHealthCheck or HttpsHealthCheck resource
  // for health checking this BackendService. Currently at most one health
  // check can be specified.
  //
  // A health check must be specified unless the backend service uses an internet
  // or serverless NEG as a backend.
  //
  // For internal load balancing, a URL to a HealthCheck resource must be specified instead.
  repeated string health_checks = 12 [json_name = "health_checks"];

  string id = 13;

  // Indicates whether the backend service will be used with internal or
  // external load balancing. A backend service created for one type of
  // load balancing cannot be used with the other. For more information, refer to
  // [Choosing a load balancer](https://cloud.google.com/load-balancing/docs/backend-service). Default value: "EXTERNAL" Possible values: ["EXTERNAL", "INTERNAL_SELF_MANAGED", "EXTERNAL_MANAGED"]
  string load_balancing_scheme = 14 [json_name = "load_balancing_scheme"];

  // The load balancing algorithm used within the scope of the locality.
  // The possible values are:
  //
  // * 'ROUND_ROBIN': This is a simple policy in which each healthy backend
  //                 is selected in round robin order.
  //
  // * 'LEAST_REQUEST': An O(1) algorithm which selects two random healthy
  //                   hosts and picks the host which has fewer active requests.
  //
  // * 'RING_HASH': The ring/modulo hash load balancer implements consistent
  //               hashing to backends. The algorithm has the property that the
  //               addition/removal of a host from a set of N hosts only affects
  //               1/N of the requests.
  //
  // * 'RANDOM': The load balancer selects a random healthy host.
  //
  // * 'ORIGINAL_DESTINATION': Backend host is selected based on the client
  //                          connection metadata, i.e., connections are opened
  //                          to the same address as the destination address of
  //                          the incoming connection before the connection
  //                          was redirected to the load balancer.
  //
  // * 'MAGLEV': used as a drop in replacement for the ring hash load balancer.
  //            Maglev is not as stable as ring hash but has faster table lookup
  //            build times and host selection times. For more information about
  //            Maglev, refer to https://ai.google/research/pubs/pub44824
  //
  // * 'WEIGHTED_MAGLEV': Per-instance weighted Load Balancing via health check
  //                     reported weights. If set, the Backend Service must
  //                     configure a non legacy HTTP-based Health Check, and
  //                     health check replies are expected to contain
  //                     non-standard HTTP response header field
  //                     X-Load-Balancing-Endpoint-Weight to specify the
  //                     per-instance weights. If set, Load Balancing is weight
  //                     based on the per-instance weights reported in the last
  //                     processed health check replies, as long as every
  //                     instance either reported a valid weight or had
  //                     UNAVAILABLE_WEIGHT. Otherwise, Load Balancing remains
  //                     equal-weight.
  //
  //
  // This field is applicable to either:
  //
  // * A regional backend service with the service_protocol set to HTTP, HTTPS, or HTTP2,
  //  and loadBalancingScheme set to INTERNAL_MANAGED.
  // * A global backend service with the load_balancing_scheme set to INTERNAL_SELF_MANAGED.
  // * A regional backend service with loadBalancingScheme set to EXTERNAL (External Network
  //  Load Balancing). Only MAGLEV and WEIGHTED_MAGLEV values are possible for External
  //  Network Load Balancing. The default is MAGLEV.
  //
  //
  // If session_affinity is not NONE, and this field is not set to MAGLEV, WEIGHTED_MAGLEV,
  // or RING_HASH, session affinity settings will not take effect.
  //
  // Only ROUND_ROBIN and RING_HASH are supported when the backend service is referenced
  // by a URL map that is bound to target gRPC proxy that has validate_for_proxyless
  // field set to true. Possible values: ["ROUND_ROBIN", "LEAST_REQUEST", "RING_HASH", "RANDOM", "ORIGINAL_DESTINATION", "MAGLEV", "WEIGHTED_MAGLEV"]
  string locality_lb_policy = 15 [json_name = "locality_lb_policy"];

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 16;

  // Name of backend port. The same name should appear in the instance
  // groups referenced by this service. Required when the load balancing
  // scheme is EXTERNAL.
  string port_name = 17 [json_name = "port_name"];

  string project = 18;

  // The protocol this BackendService uses to communicate with backends.
  // The default is HTTP. **NOTE**: HTTP2 is only valid for beta HTTP/2 load balancer
  // types and may result in errors if used with the GA API. Possible values: ["HTTP", "HTTPS", "HTTP2", "TCP", "SSL", "GRPC"]
  string protocol = 19;

  // The security policy associated with this backend service.
  string security_policy = 20 [json_name = "security_policy"];

  string self_link = 21 [json_name = "self_link"];

  // Type of session affinity to use. The default is NONE. Session affinity is
  // not applicable if the protocol is UDP. Possible values: ["NONE", "CLIENT_IP", "CLIENT_IP_PORT_PROTO", "CLIENT_IP_PROTO", "GENERATED_COOKIE", "HEADER_FIELD", "HTTP_COOKIE"]
  string session_affinity = 22 [json_name = "session_affinity"];

  // How many seconds to wait for the backend before considering it a
  // failed request. Default is 30 seconds. Valid range is [1, 86400].
  int64 timeout_sec = 23 [json_name = "timeout_sec"];

  repeated Backend backend = 24;

  CdnPolicy cdn_policy = 25 [json_name = "cdn_policy"];

  CircuitBreakers circuit_breakers = 26 [json_name = "circuit_breakers"];

  ConsistentHash consistent_hash = 27 [json_name = "consistent_hash"];

  Iap iap = 28;

  repeated LocalityLbPolicies locality_lb_policies = 29 [json_name = "locality_lb_policies"];

  LogConfig log_config = 30 [json_name = "log_config"];

  OutlierDetection outlier_detection = 31 [json_name = "outlier_detection"];

  SecuritySettings security_settings = 32 [json_name = "security_settings"];

  repeated Timeouts timeouts = 33;

  map<string, string> for_each = 34 [json_name = "for_each"];

  repeated string depends_on = 35 [json_name = "depends_on"];

  int32 count = 36;

  string provider = 37;

  terraform.v1.Lifecycle lifecycle = 38;

  message Backend {
    // Specifies the balancing mode for this backend.
    //
    // For global HTTP(S) or TCP/SSL load balancing, the default is
    // UTILIZATION. Valid values are UTILIZATION, RATE (for HTTP(S))
    // and CONNECTION (for TCP/SSL).
    //
    // See the [Backend Services Overview](https://cloud.google.com/load-balancing/docs/backend-service#balancing-mode)
    // for an explanation of load balancing modes. Default value: "UTILIZATION" Possible values: ["UTILIZATION", "RATE", "CONNECTION"]
    string balancing_mode = 1 [json_name = "balancing_mode"];

    // A multiplier applied to the group's maximum servicing capacity
    // (based on UTILIZATION, RATE or CONNECTION).
    //
    // Default value is 1, which means the group will serve up to 100%
    // of its configured capacity (depending on balancingMode). A
    // setting of 0 means the group is completely drained, offering
    // 0% of its available Capacity. Valid range is [0.0,1.0].
    int64 capacity_scaler = 2 [json_name = "capacity_scaler"];

    // An optional description of this resource.
    // Provide this property when you create the resource.
    string description = 3;

    // The fully-qualified URL of an Instance Group or Network Endpoint
    // Group resource. In case of instance group this defines the list
    // of instances that serve traffic. Member virtual machine
    // instances from each instance group must live in the same zone as
    // the instance group itself. No two backends in a backend service
    // are allowed to use same Instance Group resource.
    //
    // For Network Endpoint Groups this defines list of endpoints. All
    // endpoints of Network Endpoint Group must be hosted on instances
    // located in the same zone as the Network Endpoint Group.
    //
    // Backend services cannot mix Instance Group and
    // Network Endpoint Group backends.
    //
    // Note that you must specify an Instance Group or Network Endpoint
    // Group resource using the fully-qualified URL, rather than a
    // partial URL.
    string group = 4;

    // The max number of simultaneous connections for the group. Can
    // be used with either CONNECTION or UTILIZATION balancing modes.
    //
    // For CONNECTION mode, either maxConnections or one
    // of maxConnectionsPerInstance or maxConnectionsPerEndpoint,
    // as appropriate for group type, must be set.
    int64 max_connections = 5 [json_name = "max_connections"];

    // The max number of simultaneous connections that a single backend
    // network endpoint can handle. This is used to calculate the
    // capacity of the group. Can be used in either CONNECTION or
    // UTILIZATION balancing modes.
    //
    // For CONNECTION mode, either
    // maxConnections or maxConnectionsPerEndpoint must be set.
    int64 max_connections_per_endpoint = 6 [json_name = "max_connections_per_endpoint"];

    // The max number of simultaneous connections that a single
    // backend instance can handle. This is used to calculate the
    // capacity of the group. Can be used in either CONNECTION or
    // UTILIZATION balancing modes.
    //
    // For CONNECTION mode, either maxConnections or
    // maxConnectionsPerInstance must be set.
    int64 max_connections_per_instance = 7 [json_name = "max_connections_per_instance"];

    // The max requests per second (RPS) of the group.
    //
    // Can be used with either RATE or UTILIZATION balancing modes,
    // but required if RATE mode. For RATE mode, either maxRate or one
    // of maxRatePerInstance or maxRatePerEndpoint, as appropriate for
    // group type, must be set.
    int64 max_rate = 8 [json_name = "max_rate"];

    // The max requests per second (RPS) that a single backend network
    // endpoint can handle. This is used to calculate the capacity of
    // the group. Can be used in either balancing mode. For RATE mode,
    // either maxRate or maxRatePerEndpoint must be set.
    int64 max_rate_per_endpoint = 9 [json_name = "max_rate_per_endpoint"];

    // The max requests per second (RPS) that a single backend
    // instance can handle. This is used to calculate the capacity of
    // the group. Can be used in either balancing mode. For RATE mode,
    // either maxRate or maxRatePerInstance must be set.
    int64 max_rate_per_instance = 10 [json_name = "max_rate_per_instance"];

    // Used when balancingMode is UTILIZATION. This ratio defines the
    // CPU utilization target for the group. Valid range is [0.0, 1.0].
    int64 max_utilization = 11 [json_name = "max_utilization"];
  }

  message CdnPolicy {
    // Specifies the cache setting for all responses from this backend.
    // The possible values are: USE_ORIGIN_HEADERS, FORCE_CACHE_ALL and CACHE_ALL_STATIC Possible values: ["USE_ORIGIN_HEADERS", "FORCE_CACHE_ALL", "CACHE_ALL_STATIC"]
    string cache_mode = 1 [json_name = "cache_mode"];

    // Specifies the maximum allowed TTL for cached content served by this origin.
    int64 client_ttl = 2 [json_name = "client_ttl"];

    // Specifies the default TTL for cached content served by this origin for responses
    // that do not have an existing valid TTL (max-age or s-max-age).
    int64 default_ttl = 3 [json_name = "default_ttl"];

    // Specifies the maximum allowed TTL for cached content served by this origin.
    int64 max_ttl = 4 [json_name = "max_ttl"];

    // Negative caching allows per-status code TTLs to be set, in order to apply fine-grained caching for common errors or redirects.
    bool negative_caching = 5 [json_name = "negative_caching"];

    // Serve existing content from the cache (if available) when revalidating content with the origin, or when an error is encountered when refreshing the cache.
    int64 serve_while_stale = 6 [json_name = "serve_while_stale"];

    // Maximum number of seconds the response to a signed URL request
    // will be considered fresh, defaults to 1hr (3600s). After this
    // time period, the response will be revalidated before
    // being served.
    //
    // When serving responses to signed URL requests, Cloud CDN will
    // internally behave as though all responses from this backend had a
    // "Cache-Control: public, max-age=[TTL]" header, regardless of any
    // existing Cache-Control header. The actual headers served in
    // responses will not be altered.
    int64 signed_url_cache_max_age_sec = 7 [json_name = "signed_url_cache_max_age_sec"];

    repeated BypassCacheOnRequestHeaders bypass_cache_on_request_headers = 8 [json_name = "bypass_cache_on_request_headers"];

    CacheKeyPolicy cache_key_policy = 9 [json_name = "cache_key_policy"];

    repeated NegativeCachingPolicy negative_caching_policy = 10 [json_name = "negative_caching_policy"];

    message BypassCacheOnRequestHeaders {
      // The header field name to match on when bypassing cache. Values are case-insensitive.
      string header_name = 1 [json_name = "header_name"];
    }

    message CacheKeyPolicy {
      // If true requests to different hosts will be cached separately.
      bool include_host = 1 [json_name = "include_host"];

      // Allows HTTP request headers (by name) to be used in the
      // cache key.
      repeated string include_http_headers = 2 [json_name = "include_http_headers"];

      // Names of cookies to include in cache keys.
      repeated string include_named_cookies = 3 [json_name = "include_named_cookies"];

      // If true, http and https requests will be cached separately.
      bool include_protocol = 4 [json_name = "include_protocol"];

      // If true, include query string parameters in the cache key
      // according to query_string_whitelist and
      // query_string_blacklist. If neither is set, the entire query
      // string will be included.
      //
      // If false, the query string will be excluded from the cache
      // key entirely.
      bool include_query_string = 5 [json_name = "include_query_string"];

      // Names of query string parameters to exclude in cache keys.
      //
      // All other parameters will be included. Either specify
      // query_string_whitelist or query_string_blacklist, not both.
      // '&' and '=' will be percent encoded and not treated as
      // delimiters.
      repeated string query_string_blacklist = 6 [json_name = "query_string_blacklist"];

      // Names of query string parameters to include in cache keys.
      //
      // All other parameters will be excluded. Either specify
      // query_string_whitelist or query_string_blacklist, not both.
      // '&' and '=' will be percent encoded and not treated as
      // delimiters.
      repeated string query_string_whitelist = 7 [json_name = "query_string_whitelist"];
    }

    message NegativeCachingPolicy {
      // The HTTP status code to define a TTL against. Only HTTP status codes 300, 301, 308, 404, 405, 410, 421, 451 and 501
      // can be specified as values, and you cannot specify a status code more than once.
      int64 code = 1;

      // The TTL (in seconds) for which to cache responses with the corresponding status code. The maximum allowed value is 1800s
      // (30 minutes), noting that infrequently accessed objects may be evicted from the cache before the defined TTL.
      int64 ttl = 2;
    }
  }

  message CircuitBreakers {
    // The maximum number of connections to the backend cluster.
    // Defaults to 1024.
    int64 max_connections = 1 [json_name = "max_connections"];

    // The maximum number of pending requests to the backend cluster.
    // Defaults to 1024.
    int64 max_pending_requests = 2 [json_name = "max_pending_requests"];

    // The maximum number of parallel requests to the backend cluster.
    // Defaults to 1024.
    int64 max_requests = 3 [json_name = "max_requests"];

    // Maximum requests for a single backend connection. This parameter
    // is respected by both the HTTP/1.1 and HTTP/2 implementations. If
    // not specified, there is no limit. Setting this parameter to 1
    // will effectively disable keep alive.
    int64 max_requests_per_connection = 4 [json_name = "max_requests_per_connection"];

    // The maximum number of parallel retries to the backend cluster.
    // Defaults to 3.
    int64 max_retries = 5 [json_name = "max_retries"];
  }

  message ConsistentHash {
    // The hash based on the value of the specified header field.
    // This field is applicable if the sessionAffinity is set to HEADER_FIELD.
    string http_header_name = 1 [json_name = "http_header_name"];

    // The minimum number of virtual nodes to use for the hash ring.
    // Larger ring sizes result in more granular load
    // distributions. If the number of hosts in the load balancing pool
    // is larger than the ring size, each host will be assigned a single
    // virtual node.
    // Defaults to 1024.
    int64 minimum_ring_size = 2 [json_name = "minimum_ring_size"];

    HttpCookie http_cookie = 3 [json_name = "http_cookie"];

    message HttpCookie {
      // Name of the cookie.
      string name = 1;

      // Path to set for the cookie.
      string path = 2;

      Ttl ttl = 3;

      message Ttl {
        // Span of time that's a fraction of a second at nanosecond
        // resolution. Durations less than one second are represented
        // with a 0 seconds field and a positive nanos field. Must
        // be from 0 to 999,999,999 inclusive.
        int64 nanos = 1;

        // Span of time at a resolution of a second.
        // Must be from 0 to 315,576,000,000 inclusive.
        int64 seconds = 2;
      }
    }
  }

  message Iap {
    // OAuth2 Client ID for IAP
    string oauth2_client_id = 1 [json_name = "oauth2_client_id"];

    // OAuth2 Client Secret for IAP
    string oauth2_client_secret = 2 [json_name = "oauth2_client_secret"];

    // OAuth2 Client Secret SHA-256 for IAP
    string oauth2_client_secret_sha256 = 3 [json_name = "oauth2_client_secret_sha256"];
  }

  message LocalityLbPolicies {
    CustomPolicy custom_policy = 1 [json_name = "custom_policy"];

    Policy policy = 2;

    message CustomPolicy {
      // An optional, arbitrary JSON object with configuration data, understood
      // by a locally installed custom policy implementation.
      string data = 1;

      // Identifies the custom policy.
      //
      // The value should match the type the custom implementation is registered
      // with on the gRPC clients. It should follow protocol buffer
      // message naming conventions and include the full path (e.g.
      // myorg.CustomLbPolicy). The maximum length is 256 characters.
      //
      // Note that specifying the same custom policy more than once for a
      // backend is not a valid configuration and will be rejected.
      string name = 2;
    }

    message Policy {
      // The name of a locality load balancer policy to be used. The value
      // should be one of the predefined ones as supported by localityLbPolicy,
      // although at the moment only ROUND_ROBIN is supported.
      //
      // This field should only be populated when the customPolicy field is not
      // used.
      //
      // Note that specifying the same policy more than once for a backend is
      // not a valid configuration and will be rejected.
      //
      // The possible values are:
      //
      // * 'ROUND_ROBIN': This is a simple policy in which each healthy backend
      //                is selected in round robin order.
      //
      // * 'LEAST_REQUEST': An O(1) algorithm which selects two random healthy
      //                  hosts and picks the host which has fewer active requests.
      //
      // * 'RING_HASH': The ring/modulo hash load balancer implements consistent
      //              hashing to backends. The algorithm has the property that the
      //              addition/removal of a host from a set of N hosts only affects
      //              1/N of the requests.
      //
      // * 'RANDOM': The load balancer selects a random healthy host.
      //
      // * 'ORIGINAL_DESTINATION': Backend host is selected based on the client
      //                          connection metadata, i.e., connections are opened
      //                          to the same address as the destination address of
      //                          the incoming connection before the connection
      //                          was redirected to the load balancer.
      //
      // * 'MAGLEV': used as a drop in replacement for the ring hash load balancer.
      //            Maglev is not as stable as ring hash but has faster table lookup
      //            build times and host selection times. For more information about
      //            Maglev, refer to https://ai.google/research/pubs/pub44824 Possible values: ["ROUND_ROBIN", "LEAST_REQUEST", "RING_HASH", "RANDOM", "ORIGINAL_DESTINATION", "MAGLEV"]
      string name = 1;
    }
  }

  message LogConfig {
    // Whether to enable logging for the load balancer traffic served by this backend service.
    bool enable = 1;

    // This field can only be specified if logging is enabled for this backend service. The value of
    // the field must be in [0, 1]. This configures the sampling rate of requests to the load balancer
    // where 1.0 means all logged requests are reported and 0.0 means no logged requests are reported.
    // The default value is 1.0.
    int64 sample_rate = 2 [json_name = "sample_rate"];
  }

  message OutlierDetection {
    // Number of errors before a host is ejected from the connection pool. When the
    // backend host is accessed over HTTP, a 5xx return code qualifies as an error.
    // Defaults to 5.
    int64 consecutive_errors = 1 [json_name = "consecutive_errors"];

    // The number of consecutive gateway failures (502, 503, 504 status or connection
    // errors that are mapped to one of those status codes) before a consecutive
    // gateway failure ejection occurs. Defaults to 5.
    int64 consecutive_gateway_failure = 2 [json_name = "consecutive_gateway_failure"];

    // The percentage chance that a host will be actually ejected when an outlier
    // status is detected through consecutive 5xx. This setting can be used to disable
    // ejection or to ramp it up slowly. Defaults to 100.
    int64 enforcing_consecutive_errors = 3 [json_name = "enforcing_consecutive_errors"];

    // The percentage chance that a host will be actually ejected when an outlier
    // status is detected through consecutive gateway failures. This setting can be
    // used to disable ejection or to ramp it up slowly. Defaults to 0.
    int64 enforcing_consecutive_gateway_failure = 4 [
      json_name = "enforcing_consecutive_gateway_failure"
    ];

    // The percentage chance that a host will be actually ejected when an outlier
    // status is detected through success rate statistics. This setting can be used to
    // disable ejection or to ramp it up slowly. Defaults to 100.
    int64 enforcing_success_rate = 5 [json_name = "enforcing_success_rate"];

    // Maximum percentage of hosts in the load balancing pool for the backend service
    // that can be ejected. Defaults to 10%.
    int64 max_ejection_percent = 6 [json_name = "max_ejection_percent"];

    // The number of hosts in a cluster that must have enough request volume to detect
    // success rate outliers. If the number of hosts is less than this setting, outlier
    // detection via success rate statistics is not performed for any host in the
    // cluster. Defaults to 5.
    int64 success_rate_minimum_hosts = 7 [json_name = "success_rate_minimum_hosts"];

    // The minimum number of total requests that must be collected in one interval (as
    // defined by the interval duration above) to include this host in success rate
    // based outlier detection. If the volume is lower than this setting, outlier
    // detection via success rate statistics is not performed for that host. Defaults
    // to 100.
    int64 success_rate_request_volume = 8 [json_name = "success_rate_request_volume"];

    // This factor is used to determine the ejection threshold for success rate outlier
    // ejection. The ejection threshold is the difference between the mean success
    // rate, and the product of this factor and the standard deviation of the mean
    // success rate: mean - (stdev * success_rate_stdev_factor). This factor is divided
    // by a thousand to get a double. That is, if the desired factor is 1.9, the
    // runtime value should be 1900. Defaults to 1900.
    int64 success_rate_stdev_factor = 9 [json_name = "success_rate_stdev_factor"];

    BaseEjectionTime base_ejection_time = 10 [json_name = "base_ejection_time"];

    Interval interval = 11;

    message BaseEjectionTime {
      // Span of time that's a fraction of a second at nanosecond resolution. Durations
      // less than one second are represented with a 0 'seconds' field and a positive
      // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
      int64 nanos = 1;

      // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
      // inclusive.
      int64 seconds = 2;
    }

    message Interval {
      // Span of time that's a fraction of a second at nanosecond resolution. Durations
      // less than one second are represented with a 0 'seconds' field and a positive
      // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
      int64 nanos = 1;

      // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
      // inclusive.
      int64 seconds = 2;
    }
  }

  message SecuritySettings {
    // ClientTlsPolicy is a resource that specifies how a client should authenticate
    // connections to backends of a service. This resource itself does not affect
    // configuration unless it is attached to a backend service resource.
    string client_tls_policy = 1 [json_name = "client_tls_policy"];

    // A list of alternate names to verify the subject identity in the certificate.
    // If specified, the client will verify that the server certificate's subject
    // alt name matches one of the specified values.
    repeated string subject_alt_names = 2 [json_name = "subject_alt_names"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeBackendServiceSignedUrlKey version is 0
message GoogleComputeBackendServiceSignedUrlKey {
  // The backend service this signed URL key belongs.
  string backend_service = 1 [json_name = "backend_service"];

  string id = 2;

  // 128-bit key value used for signing the URL. The key value must be a
  // valid RFC 4648 Section 5 base64url encoded string.
  string key_value = 3 [json_name = "key_value"];

  // Name of the signed URL key.
  string name = 4;

  string project = 5;

  repeated Timeouts timeouts = 6;

  map<string, string> for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeDisk version is 0
message GoogleComputeDisk {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional description of this resource. Provide this property when
  // you create the resource.
  string description = 2;

  string id = 3;

  // The image from which to initialize this disk. This can be
  // one of: the image's 'self_link', 'projects/{project}/global/images/{image}',
  // 'projects/{project}/global/images/family/{family}', 'global/images/{image}',
  // 'global/images/family/{family}', 'family/{family}', '{project}/{family}',
  // '{project}/{image}', '{family}', or '{image}'. If referred by family, the
  // images names must include the family name. If they don't, use the
  // [google_compute_image data source](/docs/providers/google/d/compute_image.html).
  // For instance, the image 'centos-6-v20180104' includes its family name 'centos-6'.
  // These images can be referred by family name here.
  string image = 4;

  // The fingerprint used for optimistic locking of this resource.  Used
  // internally during updates.
  string label_fingerprint = 5 [json_name = "label_fingerprint"];

  // Labels to apply to this disk.  A list of key->value pairs.
  map<string, string> labels = 6;

  // Last attach timestamp in RFC3339 text format.
  string last_attach_timestamp = 7 [json_name = "last_attach_timestamp"];

  // Last detach timestamp in RFC3339 text format.
  string last_detach_timestamp = 8 [json_name = "last_detach_timestamp"];

  // Any applicable license URI.
  repeated string licenses = 9;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 10;

  // Physical block size of the persistent disk, in bytes. If not present
  // in a request, a default value is used. Currently supported sizes
  // are 4096 and 16384, other sizes may be added in the future.
  // If an unsupported value is requested, the error message will list
  // the supported values for the caller's project.
  int64 physical_block_size_bytes = 11 [json_name = "physical_block_size_bytes"];

  string project = 12;

  // Indicates how many IOPS must be provisioned for the disk.
  int64 provisioned_iops = 13 [json_name = "provisioned_iops"];

  string self_link = 14 [json_name = "self_link"];

  // Size of the persistent disk, specified in GB. You can specify this
  // field when creating a persistent disk using the 'image' or
  // 'snapshot' parameter, or specify it alone to create an empty
  // persistent disk.
  //
  // If you specify this field along with 'image' or 'snapshot',
  // the value must not be less than the size of the image
  // or the size of the snapshot.
  //
  // ~>**NOTE** If you change the size, Terraform updates the disk size
  // if upsizing is detected but recreates the disk if downsizing is requested.
  // You can add 'lifecycle.prevent_destroy' in the config to prevent destroying
  // and recreating.
  int64 size = 15;

  // The source snapshot used to create this disk. You can provide this as
  // a partial or full URL to the resource. If the snapshot is in another
  // project than this disk, you must supply a full URL. For example, the
  // following are valid values:
  //
  // * 'https://www.googleapis.com/compute/v1/projects/project/global/snapshots/snapshot'
  // * 'projects/project/global/snapshots/snapshot'
  // * 'global/snapshots/snapshot'
  // * 'snapshot'
  string snapshot = 16;

  // The source disk used to create this disk. You can provide this as a partial or full URL to the resource.
  // For example, the following are valid values:
  //
  // * https://www.googleapis.com/compute/v1/projects/{project}/zones/{zone}/disks/{disk}
  // * https://www.googleapis.com/compute/v1/projects/{project}/regions/{region}/disks/{disk}
  // * projects/{project}/zones/{zone}/disks/{disk}
  // * projects/{project}/regions/{region}/disks/{disk}
  // * zones/{zone}/disks/{disk}
  // * regions/{region}/disks/{disk}
  string source_disk = 17 [json_name = "source_disk"];

  // The ID value of the disk used to create this image. This value may
  // be used to determine whether the image was taken from the current
  // or a previous instance of a given disk name.
  string source_disk_id = 18 [json_name = "source_disk_id"];

  // The ID value of the image used to create this disk. This value
  // identifies the exact image that was used to create this persistent
  // disk. For example, if you created the persistent disk from an image
  // that was later deleted and recreated under the same name, the source
  // image ID would identify the exact version of the image that was used.
  string source_image_id = 19 [json_name = "source_image_id"];

  // The unique ID of the snapshot used to create this disk. This value
  // identifies the exact snapshot that was used to create this persistent
  // disk. For example, if you created the persistent disk from a snapshot
  // that was later deleted and recreated under the same name, the source
  // snapshot ID would identify the exact version of the snapshot that was
  // used.
  string source_snapshot_id = 20 [json_name = "source_snapshot_id"];

  // URL of the disk type resource describing which disk type to use to
  // create the disk. Provide this when creating the disk.
  string type = 21;

  // Links to the users of the disk (attached instances) in form:
  // project/zones/zone/instances/instance
  repeated string users = 22;

  // A reference to the zone where the disk resides.
  string zone = 23;

  DiskEncryptionKey disk_encryption_key = 24 [json_name = "disk_encryption_key"];

  repeated GuestOsFeatures guest_os_features = 25 [json_name = "guest_os_features"];

  SourceImageEncryptionKey source_image_encryption_key = 26 [json_name = "source_image_encryption_key"];

  SourceSnapshotEncryptionKey source_snapshot_encryption_key = 27 [json_name = "source_snapshot_encryption_key"];

  repeated Timeouts timeouts = 28;

  map<string, string> for_each = 29 [json_name = "for_each"];

  repeated string depends_on = 30 [json_name = "depends_on"];

  int32 count = 31;

  string provider = 32;

  terraform.v1.Lifecycle lifecycle = 33;

  message DiskEncryptionKey {
    // The self link of the encryption key used to encrypt the disk. Also called KmsKeyName
    // in the cloud console. Your project's Compute Engine System service account
    // ('service-{{PROJECT_NUMBER}}@compute-system.iam.gserviceaccount.com') must have
    // 'roles/cloudkms.cryptoKeyEncrypterDecrypter' to use this feature.
    // See https://cloud.google.com/compute/docs/disks/customer-managed-encryption#encrypt_a_new_persistent_disk_with_your_own_keys
    string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

    // The service account used for the encryption request for the given KMS key.
    // If absent, the Compute Engine Service Agent service account is used.
    string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    string raw_key = 3 [json_name = "raw_key"];

    // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit
    // customer-supplied encryption key to either encrypt or decrypt
    // this resource. You can provide either the rawKey or the rsaEncryptedKey.
    string rsa_encrypted_key = 4 [json_name = "rsa_encrypted_key"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied
    // encryption key that protects this resource.
    string sha256 = 5;
  }

  message GuestOsFeatures {
    // The type of supported feature. Read [Enabling guest operating system features](https://cloud.google.com/compute/docs/images/create-delete-deprecate-private-images#guest-os-features) to see a list of available options. Possible values: ["MULTI_IP_SUBNET", "SECURE_BOOT", "SEV_CAPABLE", "UEFI_COMPATIBLE", "VIRTIO_SCSI_MULTIQUEUE", "WINDOWS", "GVNIC", "SEV_LIVE_MIGRATABLE", "SEV_SNP_CAPABLE", "SUSPEND_RESUME_COMPATIBLE", "TDX_CAPABLE"]
    string type = 1;
  }

  message SourceImageEncryptionKey {
    // The self link of the encryption key used to encrypt the disk. Also called KmsKeyName
    // in the cloud console. Your project's Compute Engine System service account
    // ('service-{{PROJECT_NUMBER}}@compute-system.iam.gserviceaccount.com') must have
    // 'roles/cloudkms.cryptoKeyEncrypterDecrypter' to use this feature.
    // See https://cloud.google.com/compute/docs/disks/customer-managed-encryption#encrypt_a_new_persistent_disk_with_your_own_keys
    string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

    // The service account used for the encryption request for the given KMS key.
    // If absent, the Compute Engine Service Agent service account is used.
    string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    string raw_key = 3 [json_name = "raw_key"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied
    // encryption key that protects this resource.
    string sha256 = 4;
  }

  message SourceSnapshotEncryptionKey {
    // The self link of the encryption key used to encrypt the disk. Also called KmsKeyName
    // in the cloud console. Your project's Compute Engine System service account
    // ('service-{{PROJECT_NUMBER}}@compute-system.iam.gserviceaccount.com') must have
    // 'roles/cloudkms.cryptoKeyEncrypterDecrypter' to use this feature.
    // See https://cloud.google.com/compute/docs/disks/customer-managed-encryption#encrypt_a_new_persistent_disk_with_your_own_keys
    string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

    // The service account used for the encryption request for the given KMS key.
    // If absent, the Compute Engine Service Agent service account is used.
    string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    string raw_key = 3 [json_name = "raw_key"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied
    // encryption key that protects this resource.
    string sha256 = 4;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeDiskIamBinding version is 0
message GoogleComputeDiskIamBinding {
  string etag = 1;

  string id = 2;

  repeated string members = 3;

  string name = 4;

  string project = 5;

  string role = 6;

  string zone = 7;

  Condition condition = 8;

  map<string, string> for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    string description = 1;

    string expression = 2;

    string title = 3;
  }
}

// GoogleComputeDiskIamMember version is 0
message GoogleComputeDiskIamMember {
  string etag = 1;

  string id = 2;

  string member = 3;

  string name = 4;

  string project = 5;

  string role = 6;

  string zone = 7;

  Condition condition = 8;

  map<string, string> for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    string description = 1;

    string expression = 2;

    string title = 3;
  }
}

// GoogleComputeDiskIamPolicy version is 0
message GoogleComputeDiskIamPolicy {
  string etag = 1;

  string id = 2;

  string name = 3;

  string policy_data = 4 [json_name = "policy_data"];

  string project = 5;

  string zone = 6;

  map<string, string> for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// GoogleComputeDiskResourcePolicyAttachment version is 0
message GoogleComputeDiskResourcePolicyAttachment {
  // The name of the disk in which the resource policies are attached to.
  string disk = 1;

  string id = 2;

  // The resource policy to be attached to the disk for scheduling snapshot
  // creation. Do not specify the self link.
  string name = 3;

  string project = 4;

  // A reference to the zone where the disk resides.
  string zone = 5;

  repeated Timeouts timeouts = 6;

  map<string, string> for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeExternalVpnGateway version is 0
message GoogleComputeExternalVpnGateway {
  // An optional description of this resource.
  string description = 1;

  string id = 2;

  // Labels for the external VPN gateway resource.
  map<string, string> labels = 3;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  // the first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 4;

  string project = 5;

  // Indicates the redundancy type of this external VPN gateway Possible values: ["FOUR_IPS_REDUNDANCY", "SINGLE_IP_INTERNALLY_REDUNDANT", "TWO_IPS_REDUNDANCY"]
  string redundancy_type = 6 [json_name = "redundancy_type"];

  string self_link = 7 [json_name = "self_link"];

  repeated Interface interface = 8;

  repeated Timeouts timeouts = 9;

  map<string, string> for_each = 10 [json_name = "for_each"];

  repeated string depends_on = 11 [json_name = "depends_on"];

  int32 count = 12;

  string provider = 13;

  terraform.v1.Lifecycle lifecycle = 14;

  message Interface {
    // The numeric ID for this interface. Allowed values are based on the redundancy type
    // of this external VPN gateway
    // * '0 - SINGLE_IP_INTERNALLY_REDUNDANT'
    // * '0, 1 - TWO_IPS_REDUNDANCY'
    // * '0, 1, 2, 3 - FOUR_IPS_REDUNDANCY'
    int64 id = 1;

    // IP address of the interface in the external VPN gateway.
    // Only IPv4 is supported. This IP address can be either from
    // your on-premise gateway or another Cloud provider's VPN gateway,
    // it cannot be an IP address from Google Compute Engine.
    string ip_address = 2 [json_name = "ip_address"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeFirewall version is 1
message GoogleComputeFirewall {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional description of this resource. Provide this property when
  // you create the resource.
  string description = 2;

  // If destination ranges are specified, the firewall will apply only to
  // traffic that has destination IP address in these ranges. These ranges
  // must be expressed in CIDR format. IPv4 or IPv6 ranges are supported.
  repeated string destination_ranges = 3 [json_name = "destination_ranges"];

  // Direction of traffic to which this firewall applies; default is
  // INGRESS. Note: For INGRESS traffic, one of 'source_ranges',
  // 'source_tags' or 'source_service_accounts' is required. Possible values: ["INGRESS", "EGRESS"]
  string direction = 4;

  // Denotes whether the firewall rule is disabled, i.e not applied to the
  // network it is associated with. When set to true, the firewall rule is
  // not enforced and the network behaves as if it did not exist. If this
  // is unspecified, the firewall rule will be enabled.
  bool disabled = 5;

  // This field denotes whether to enable logging for a particular firewall rule. If logging is enabled, logs will be exported to Stackdriver.
  bool enable_logging = 6 [json_name = "enable_logging"];

  string id = 7;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 8;

  // The name or self_link of the network to attach this firewall to.
  string network = 9;

  // Priority for this rule. This is an integer between 0 and 65535, both
  // inclusive. When not specified, the value assumed is 1000. Relative
  // priorities determine precedence of conflicting rules. Lower value of
  // priority implies higher precedence (eg, a rule with priority 0 has
  // higher precedence than a rule with priority 1). DENY rules take
  // precedence over ALLOW rules having equal priority.
  int64 priority = 10;

  string project = 11;

  string self_link = 12 [json_name = "self_link"];

  // If source ranges are specified, the firewall will apply only to
  // traffic that has source IP address in these ranges. These ranges must
  // be expressed in CIDR format. One or both of sourceRanges and
  // sourceTags may be set. If both properties are set, the firewall will
  // apply to traffic that has source IP address within sourceRanges OR the
  // source IP that belongs to a tag listed in the sourceTags property. The
  // connection does not need to match both properties for the firewall to
  // apply. IPv4 or IPv6 ranges are supported. For INGRESS traffic, one of
  // 'source_ranges', 'source_tags' or 'source_service_accounts' is required.
  repeated string source_ranges = 13 [json_name = "source_ranges"];

  // If source service accounts are specified, the firewall will apply only
  // to traffic originating from an instance with a service account in this
  // list. Source service accounts cannot be used to control traffic to an
  // instance's external IP address because service accounts are associated
  // with an instance, not an IP address. sourceRanges can be set at the
  // same time as sourceServiceAccounts. If both are set, the firewall will
  // apply to traffic that has source IP address within sourceRanges OR the
  // source IP belongs to an instance with service account listed in
  // sourceServiceAccount. The connection does not need to match both
  // properties for the firewall to apply. sourceServiceAccounts cannot be
  // used at the same time as sourceTags or targetTags. For INGRESS traffic,
  // one of 'source_ranges', 'source_tags' or 'source_service_accounts' is required.
  repeated string source_service_accounts = 14 [json_name = "source_service_accounts"];

  // If source tags are specified, the firewall will apply only to traffic
  // with source IP that belongs to a tag listed in source tags. Source
  // tags cannot be used to control traffic to an instance's external IP
  // address. Because tags are associated with an instance, not an IP
  // address. One or both of sourceRanges and sourceTags may be set. If
  // both properties are set, the firewall will apply to traffic that has
  // source IP address within sourceRanges OR the source IP that belongs to
  // a tag listed in the sourceTags property. The connection does not need
  // to match both properties for the firewall to apply. For INGRESS traffic,
  // one of 'source_ranges', 'source_tags' or 'source_service_accounts' is required.
  repeated string source_tags = 15 [json_name = "source_tags"];

  // A list of service accounts indicating sets of instances located in the
  // network that may make network connections as specified in allowed[].
  // targetServiceAccounts cannot be used at the same time as targetTags or
  // sourceTags. If neither targetServiceAccounts nor targetTags are
  // specified, the firewall rule applies to all instances on the specified
  // network.
  repeated string target_service_accounts = 16 [json_name = "target_service_accounts"];

  // A list of instance tags indicating sets of instances located in the
  // network that may make network connections as specified in allowed[].
  // If no targetTags are specified, the firewall rule applies to all
  // instances on the specified network.
  repeated string target_tags = 17 [json_name = "target_tags"];

  repeated Allow allow = 18;

  repeated Deny deny = 19;

  LogConfig log_config = 20 [json_name = "log_config"];

  repeated Timeouts timeouts = 21;

  map<string, string> for_each = 22 [json_name = "for_each"];

  repeated string depends_on = 23 [json_name = "depends_on"];

  int32 count = 24;

  string provider = 25;

  terraform.v1.Lifecycle lifecycle = 26;

  message Allow {
    // An optional list of ports to which this rule applies. This field
    // is only applicable for UDP or TCP protocol. Each entry must be
    // either an integer or a range. If not specified, this rule
    // applies to connections through any port.
    //
    // Example inputs include: ["22"], ["80","443"], and
    // ["12345-12349"].
    repeated string ports = 1;

    // The IP protocol to which this rule applies. The protocol type is
    // required when creating a firewall rule. This value can either be
    // one of the following well known protocol strings (tcp, udp,
    // icmp, esp, ah, sctp, ipip, all), or the IP protocol number.
    string protocol = 2;
  }

  message Deny {
    // An optional list of ports to which this rule applies. This field
    // is only applicable for UDP or TCP protocol. Each entry must be
    // either an integer or a range. If not specified, this rule
    // applies to connections through any port.
    //
    // Example inputs include: ["22"], ["80","443"], and
    // ["12345-12349"].
    repeated string ports = 1;

    // The IP protocol to which this rule applies. The protocol type is
    // required when creating a firewall rule. This value can either be
    // one of the following well known protocol strings (tcp, udp,
    // icmp, esp, ah, sctp, ipip, all), or the IP protocol number.
    string protocol = 2;
  }

  message LogConfig {
    // This field denotes whether to include or exclude metadata for firewall logs. Possible values: ["EXCLUDE_ALL_METADATA", "INCLUDE_ALL_METADATA"]
    string metadata = 1;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeFirewallPolicy version is 0
message GoogleComputeFirewallPolicy {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional description of this resource. Provide this property when you create the resource.
  string description = 2;

  // Fingerprint of the resource. This field is used internally during updates of this resource.
  string fingerprint = 3;

  // The unique identifier for the resource. This identifier is defined by the server.
  string firewall_policy_id = 4 [json_name = "firewall_policy_id"];

  string id = 5;

  // Name of the resource. It is a numeric ID allocated by GCP which uniquely identifies the Firewall Policy.
  string name = 6;

  // The parent of the firewall policy.
  string parent = 7;

  // Total count of all firewall policy rule tuples. A firewall policy can not exceed a set number of tuples.
  int64 rule_tuple_count = 8 [json_name = "rule_tuple_count"];

  // Server-defined URL for the resource.
  string self_link = 9 [json_name = "self_link"];

  // Server-defined URL for this resource with the resource id.
  string self_link_with_id = 10 [json_name = "self_link_with_id"];

  // User-provided name of the Organization firewall policy. The name should be unique in the organization in which the firewall policy is created. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression [a-z]([-a-z0-9]*[a-z0-9])? which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash.
  string short_name = 11 [json_name = "short_name"];

  repeated Timeouts timeouts = 12;

  map<string, string> for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  int32 count = 15;

  string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeFirewallPolicyAssociation version is 0
message GoogleComputeFirewallPolicyAssociation {
  // The target that the firewall policy is attached to.
  string attachment_target = 1 [json_name = "attachment_target"];

  // The firewall policy ID of the association.
  string firewall_policy = 2 [json_name = "firewall_policy"];

  string id = 3;

  // The name for an association.
  string name = 4;

  // The short name of the firewall policy of the association.
  string short_name = 5 [json_name = "short_name"];

  repeated Timeouts timeouts = 6;

  map<string, string> for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeFirewallPolicyRule version is 0
message GoogleComputeFirewallPolicyRule {
  // The Action to perform when the client connection triggers the rule. Valid actions are "allow", "deny" and "goto_next".
  string action = 1;

  // An optional description for this resource.
  string description = 2;

  // The direction in which this rule applies. Possible values: INGRESS, EGRESS
  string direction = 3;

  // Denotes whether the firewall policy rule is disabled. When set to true, the firewall policy rule is not enforced and traffic behaves as if it did not exist. If this is unspecified, the firewall policy rule will be enabled.
  bool disabled = 4;

  // Denotes whether to enable logging for a particular rule. If logging is enabled, logs will be exported to the configured export destination in Stackdriver. Logs may be exported to BigQuery or Pub/Sub. Note: you cannot enable logging on "goto_next" rules.
  bool enable_logging = 5 [json_name = "enable_logging"];

  // The firewall policy of the resource.
  string firewall_policy = 6 [json_name = "firewall_policy"];

  string id = 7;

  // Type of the resource. Always `compute#firewallPolicyRule` for firewall policy rules
  string kind = 8;

  // An integer indicating the priority of a rule in the list. The priority must be a positive value between 0 and 2147483647. Rules are evaluated from highest to lowest priority where 0 is the highest priority and 2147483647 is the lowest prority.
  int64 priority = 9;

  // Calculation of the complexity of a single firewall policy rule.
  int64 rule_tuple_count = 10 [json_name = "rule_tuple_count"];

  // A list of network resource URLs to which this rule applies. This field allows you to control which network's VMs get this rule. If this field is left blank, all VMs within the organization will receive the rule.
  repeated string target_resources = 11 [json_name = "target_resources"];

  // A list of service accounts indicating the sets of instances that are applied with this rule.
  repeated string target_service_accounts = 12 [json_name = "target_service_accounts"];

  Match match = 13;

  repeated Timeouts timeouts = 14;

  map<string, string> for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  int32 count = 17;

  string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message Match {
    // Address groups which should be matched against the traffic destination. Maximum number of destination address groups is 10. Destination address groups is only supported in Egress rules.
    repeated string dest_address_groups = 1 [json_name = "dest_address_groups"];

    // Domain names that will be used to match against the resolved domain name of destination of traffic. Can only be specified if DIRECTION is egress.
    repeated string dest_fqdns = 2 [json_name = "dest_fqdns"];

    // CIDR IP address range. Maximum number of destination CIDR IP ranges allowed is 256.
    repeated string dest_ip_ranges = 3 [json_name = "dest_ip_ranges"];

    // The Unicode country codes whose IP addresses will be used to match against the source of traffic. Can only be specified if DIRECTION is egress.
    repeated string dest_region_codes = 4 [json_name = "dest_region_codes"];

    // Name of the Google Cloud Threat Intelligence list.
    repeated string dest_threat_intelligences = 5 [json_name = "dest_threat_intelligences"];

    // Address groups which should be matched against the traffic source. Maximum number of source address groups is 10. Source address groups is only supported in Ingress rules.
    repeated string src_address_groups = 6 [json_name = "src_address_groups"];

    // Domain names that will be used to match against the resolved domain name of source of traffic. Can only be specified if DIRECTION is ingress.
    repeated string src_fqdns = 7 [json_name = "src_fqdns"];

    // CIDR IP address range. Maximum number of source CIDR IP ranges allowed is 256.
    repeated string src_ip_ranges = 8 [json_name = "src_ip_ranges"];

    // The Unicode country codes whose IP addresses will be used to match against the source of traffic. Can only be specified if DIRECTION is ingress.
    repeated string src_region_codes = 9 [json_name = "src_region_codes"];

    // Name of the Google Cloud Threat Intelligence list.
    repeated string src_threat_intelligences = 10 [json_name = "src_threat_intelligences"];

    repeated Layer4Configs layer4_configs = 11 [json_name = "layer4_configs"];

    message Layer4Configs {
      // The IP protocol to which this rule applies. The protocol type is required when creating a firewall rule. This value can either be one of the following well known protocol strings (`tcp`, `udp`, `icmp`, `esp`, `ah`, `ipip`, `sctp`), or the IP protocol number.
      string ip_protocol = 1 [json_name = "ip_protocol"];

      // An optional list of ports to which this rule applies. This field is only applicable for UDP or TCP protocol. Each entry must be either an integer or a range. If not specified, this rule applies to connections through any port. Example inputs include: ``.
      repeated string ports = 2;
    }
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeForwardingRule version is 0
message GoogleComputeForwardingRule {
  // This field can only be used:
  // * If 'IPProtocol' is one of TCP, UDP, or SCTP.
  // * By internal TCP/UDP load balancers, backend service-based network load
  // balancers, and internal and external protocol forwarding.
  //
  //
  // Set this field to true to allow packets addressed to any port or packets
  // lacking destination port information (for example, UDP fragments after the
  // first fragment) to be forwarded to the backends configured with this
  // forwarding rule.
  //
  // The 'ports', 'port_range', and
  // 'allPorts' fields are mutually exclusive.
  bool all_ports = 1 [json_name = "all_ports"];

  // This field is used along with the 'backend_service' field for
  // internal load balancing or with the 'target' field for internal
  // TargetInstance.
  //
  // If the field is set to 'TRUE', clients can access ILB from all
  // regions.
  //
  // Otherwise only allows access from clients in the same region as the
  // internal load balancer.
  bool allow_global_access = 2 [json_name = "allow_global_access"];

  // This is used in PSC consumer ForwardingRule to control whether the PSC endpoint can be accessed from another region.
  bool allow_psc_global_access = 3 [json_name = "allow_psc_global_access"];

  // Identifies the backend service to which the forwarding rule sends traffic.
  //
  // Required for Internal TCP/UDP Load Balancing and Network Load Balancing;
  // must be omitted for all other load balancer types.
  string backend_service = 4 [json_name = "backend_service"];

  // [Output Only] The URL for the corresponding base Forwarding Rule. By base Forwarding Rule, we mean the Forwarding Rule that has the same IP address, protocol, and port settings with the current Forwarding Rule, but without sourceIPRanges specified. Always empty if the current Forwarding Rule does not have sourceIPRanges specified.
  string base_forwarding_rule = 5 [json_name = "base_forwarding_rule"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 6 [json_name = "creation_timestamp"];

  // An optional description of this resource. Provide this property when
  // you create the resource.
  string description = 7;

  string id = 8;

  // IP address for which this forwarding rule accepts traffic. When a client
  // sends traffic to this IP address, the forwarding rule directs the traffic
  // to the referenced 'target' or 'backendService'.
  //
  // While creating a forwarding rule, specifying an 'IPAddress' is
  // required under the following circumstances:
  //
  // * When the 'target' is set to 'targetGrpcProxy' and
  // 'validateForProxyless' is set to 'true', the
  // 'IPAddress' should be set to '0.0.0.0'.
  // * When the 'target' is a Private Service Connect Google APIs
  // bundle, you must specify an 'IPAddress'.
  //
  //
  // Otherwise, you can optionally specify an IP address that references an
  // existing static (reserved) IP address resource. When omitted, Google Cloud
  // assigns an ephemeral IP address.
  //
  // Use one of the following formats to specify an IP address while creating a
  // forwarding rule:
  //
  // * IP address number, as in '100.1.2.3'
  // * IPv6 address range, as in '2600:1234::/96'
  // * Full resource URL, as in
  // 'https://www.googleapis.com/compute/v1/projects/project_id/regions/region/addresses/address-name'
  // * Partial URL or by name, as in:
  //  * 'projects/project_id/regions/region/addresses/address-name'
  //  * 'regions/region/addresses/address-name'
  //  * 'global/addresses/address-name'
  //  * 'address-name'
  //
  //
  // The forwarding rule's 'target' or 'backendService',
  // and in most cases, also the 'loadBalancingScheme', determine the
  // type of IP address that you can use. For detailed information, see
  // [IP address
  // specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
  //
  // When reading an 'IPAddress', the API always returns the IP
  // address number.
  string ip_address = 9 [json_name = "ip_address"];

  // The IP protocol to which this rule applies.
  //
  // For protocol forwarding, valid
  // options are 'TCP', 'UDP', 'ESP',
  // 'AH', 'SCTP', 'ICMP' and
  // 'L3_DEFAULT'.
  //
  // The valid IP protocols are different for different load balancing products
  // as described in [Load balancing
  // features](https://cloud.google.com/load-balancing/docs/features#protocols_from_the_load_balancer_to_the_backends). Possible values: ["TCP", "UDP", "ESP", "AH", "SCTP", "ICMP", "L3_DEFAULT"]
  string ip_protocol = 10 [json_name = "ip_protocol"];

  // Indicates whether or not this load balancer can be used as a collector for
  // packet mirroring. To prevent mirroring loops, instances behind this
  // load balancer will not have their traffic mirrored even if a
  // 'PacketMirroring' rule applies to them.
  //
  // This can only be set to true for load balancers that have their
  // 'loadBalancingScheme' set to 'INTERNAL'.
  bool is_mirroring_collector = 11 [json_name = "is_mirroring_collector"];

  // The fingerprint used for optimistic locking of this resource.  Used
  // internally during updates.
  string label_fingerprint = 12 [json_name = "label_fingerprint"];

  // Labels to apply to this forwarding rule.  A list of key->value pairs.
  map<string, string> labels = 13;

  // Specifies the forwarding rule type.
  //
  // For more information about forwarding rules, refer to
  // [Forwarding rule concepts](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts). Default value: "EXTERNAL" Possible values: ["EXTERNAL", "EXTERNAL_MANAGED", "INTERNAL", "INTERNAL_MANAGED"]
  string load_balancing_scheme = 14 [json_name = "load_balancing_scheme"];

  // Name of the resource; provided by the client when the resource is created.
  // The name must be 1-63 characters long, and comply with
  // [RFC1035](https://www.ietf.org/rfc/rfc1035.txt).
  //
  // Specifically, the name must be 1-63 characters long and match the regular
  // expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first
  // character must be a lowercase letter, and all following characters must
  // be a dash, lowercase letter, or digit, except the last character, which
  // cannot be a dash.
  //
  // For Private Service Connect forwarding rules that forward traffic to Google
  // APIs, the forwarding rule name must be a 1-20 characters string with
  // lowercase letters and numbers and must start with a letter.
  string name = 15;

  // This field is not used for external load balancing.
  //
  // For Internal TCP/UDP Load Balancing, this field identifies the network that
  // the load balanced IP should belong to for this Forwarding Rule.
  // If the subnetwork is specified, the network of the subnetwork will be used.
  // If neither subnetwork nor this field is specified, the default network will
  // be used.
  //
  // For Private Service Connect forwarding rules that forward traffic to Google
  // APIs, a network must be provided.
  string network = 16;

  // This signifies the networking tier used for configuring
  // this load balancer and can only take the following values:
  // 'PREMIUM', 'STANDARD'.
  //
  // For regional ForwardingRule, the valid values are 'PREMIUM' and
  // 'STANDARD'. For GlobalForwardingRule, the valid value is
  // 'PREMIUM'.
  //
  // If this field is not specified, it is assumed to be 'PREMIUM'.
  // If 'IPAddress' is specified, this value must be equal to the
  // networkTier of the Address. Possible values: ["PREMIUM", "STANDARD"]
  string network_tier = 17 [json_name = "network_tier"];

  // This field can only be used:
  //
  // * If 'IPProtocol' is one of TCP, UDP, or SCTP.
  // * By backend service-based network load balancers, target pool-based
  // network load balancers, internal proxy load balancers, external proxy load
  // balancers, Traffic Director, external protocol forwarding, and Classic VPN.
  // Some products have restrictions on what ports can be used. See
  // [port specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#port_specifications)
  // for details.
  //
  //
  // Only packets addressed to ports in the specified range will be forwarded to
  // the backends configured with this forwarding rule.
  //
  // The 'ports' and 'port_range' fields are mutually exclusive.
  //
  // For external forwarding rules, two or more forwarding rules cannot use the
  // same '[IPAddress, IPProtocol]' pair, and cannot have
  // overlapping 'portRange's.
  //
  // For internal forwarding rules within the same VPC network, two or more
  // forwarding rules cannot use the same '[IPAddress, IPProtocol]'
  // pair, and cannot have overlapping 'portRange's.
  string port_range = 18 [json_name = "port_range"];

  // This field can only be used:
  //
  // * If 'IPProtocol' is one of TCP, UDP, or SCTP.
  // * By internal TCP/UDP load balancers, backend service-based network load
  // balancers, and internal protocol forwarding.
  //
  //
  // You can specify a list of up to five ports by number, separated by commas.
  // The ports can be contiguous or discontiguous. Only packets addressed to
  // these ports will be forwarded to the backends configured with this
  // forwarding rule.
  //
  // For external forwarding rules, two or more forwarding rules cannot use the
  // same '[IPAddress, IPProtocol]' pair, and cannot share any values
  // defined in 'ports'.
  //
  // For internal forwarding rules within the same VPC network, two or more
  // forwarding rules cannot use the same '[IPAddress, IPProtocol]'
  // pair, and cannot share any values defined in 'ports'.
  //
  // The 'ports' and 'port_range' fields are mutually exclusive.
  repeated string ports = 19;

  string project = 20;

  // The PSC connection id of the PSC Forwarding Rule.
  string psc_connection_id = 21 [json_name = "psc_connection_id"];

  // The PSC connection status of the PSC Forwarding Rule. Possible values: 'STATUS_UNSPECIFIED', 'PENDING', 'ACCEPTED', 'REJECTED', 'CLOSED'
  string psc_connection_status = 22 [json_name = "psc_connection_status"];

  // A reference to the region where the regional forwarding rule resides.
  //
  // This field is not applicable to global forwarding rules.
  string region = 23;

  string self_link = 24 [json_name = "self_link"];

  // An optional prefix to the service name for this Forwarding Rule.
  // If specified, will be the first label of the fully qualified service
  // name.
  //
  // The label must be 1-63 characters long, and comply with RFC1035.
  // Specifically, the label must be 1-63 characters long and match the
  // regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first
  // character must be a lowercase letter, and all following characters
  // must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  //
  // This field is only used for INTERNAL load balancing.
  string service_label = 25 [json_name = "service_label"];

  // The internal fully qualified service name for this Forwarding Rule.
  //
  // This field is only used for INTERNAL load balancing.
  string service_name = 26 [json_name = "service_name"];

  // If not empty, this Forwarding Rule will only forward the traffic when the source IP address matches one of the IP addresses or CIDR ranges set here. Note that a Forwarding Rule can only have up to 64 source IP ranges, and this field can only be used with a regional Forwarding Rule whose scheme is EXTERNAL. Each sourceIpRange entry should be either an IP address (for example, 1.2.3.4) or a CIDR range (for example, 1.2.3.0/24).
  repeated string source_ip_ranges = 27 [json_name = "source_ip_ranges"];

  // This field identifies the subnetwork that the load balanced IP should
  // belong to for this Forwarding Rule, used in internal load balancing and
  // network load balancing with IPv6.
  //
  // If the network specified is in auto subnet mode, this field is optional.
  // However, a subnetwork must be specified if the network is in custom subnet
  // mode or when creating external forwarding rule with IPv6.
  string subnetwork = 28;

  // The URL of the target resource to receive the matched traffic.  For
  // regional forwarding rules, this target must be in the same region as the
  // forwarding rule. For global forwarding rules, this target must be a global
  // load balancing resource.
  //
  // The forwarded traffic must be of a type appropriate to the target object.
  // *  For load balancers, see the "Target" column in [Port specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
  // *  For Private Service Connect forwarding rules that forward traffic to Google APIs, provide the name of a supported Google API bundle:
  //  *  'vpc-sc' - [ APIs that support VPC Service Controls](https://cloud.google.com/vpc-service-controls/docs/supported-products).
  //  *  'all-apis' - [All supported Google APIs](https://cloud.google.com/vpc/docs/private-service-connect#supported-apis).
  //
  //
  // For Private Service Connect forwarding rules that forward traffic to managed services, the target must be a service attachment.
  string target = 29;

  ServiceDirectoryRegistrations service_directory_registrations = 30 [json_name = "service_directory_registrations"];

  repeated Timeouts timeouts = 31;

  map<string, string> for_each = 32 [json_name = "for_each"];

  repeated string depends_on = 33 [json_name = "depends_on"];

  int32 count = 34;

  string provider = 35;

  terraform.v1.Lifecycle lifecycle = 36;

  message ServiceDirectoryRegistrations {
    // Service Directory namespace to register the forwarding rule under.
    string namespace = 1;

    // Service Directory service to register the forwarding rule under.
    string service = 2;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeGlobalAddress version is 0
message GoogleComputeGlobalAddress {
  // The IP address or beginning of the address range represented by this
  // resource. This can be supplied as an input to reserve a specific
  // address or omitted to allow GCP to choose a valid one for you.
  string address = 1;

  // The type of the address to reserve.
  //
  // * EXTERNAL indicates public/external single IP address.
  // * INTERNAL indicates internal IP ranges belonging to some network. Default value: "EXTERNAL" Possible values: ["EXTERNAL", "INTERNAL"]
  string address_type = 2 [json_name = "address_type"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 3 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 4;

  string id = 5;

  // The IP Version that will be used by this address. The default value is 'IPV4'. Possible values: ["IPV4", "IPV6"]
  string ip_version = 6 [json_name = "ip_version"];

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  // the first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 7;

  // The URL of the network in which to reserve the IP range. The IP range
  // must be in RFC1918 space. The network cannot be deleted if there are
  // any reserved IP ranges referring to it.
  //
  // This should only be set when using an Internal address.
  string network = 8;

  // The prefix length of the IP range. If not present, it means the
  // address field is a single IP address.
  //
  // This field is not applicable to addresses with addressType=EXTERNAL,
  // or addressType=INTERNAL when purpose=PRIVATE_SERVICE_CONNECT
  int64 prefix_length = 9 [json_name = "prefix_length"];

  string project = 10;

  // The purpose of the resource. Possible values include:
  //
  // * VPC_PEERING - for peer networks
  //
  // * PRIVATE_SERVICE_CONNECT - for ([Beta](https://terraform.io/docs/providers/google/guides/provider_versions.html) only) Private Service Connect networks
  string purpose = 11;

  string self_link = 12 [json_name = "self_link"];

  repeated Timeouts timeouts = 13;

  map<string, string> for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  int32 count = 16;

  string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeGlobalForwardingRule version is 0
message GoogleComputeGlobalForwardingRule {
  // [Output Only] The URL for the corresponding base Forwarding Rule. By base Forwarding Rule, we mean the Forwarding Rule that has the same IP address, protocol, and port settings with the current Forwarding Rule, but without sourceIPRanges specified. Always empty if the current Forwarding Rule does not have sourceIPRanges specified.
  string base_forwarding_rule = 1 [json_name = "base_forwarding_rule"];

  // An optional description of this resource. Provide this property when
  // you create the resource.
  string description = 2;

  string id = 3;

  // IP address for which this forwarding rule accepts traffic. When a client
  // sends traffic to this IP address, the forwarding rule directs the traffic
  // to the referenced 'target'.
  //
  // While creating a forwarding rule, specifying an 'IPAddress' is
  // required under the following circumstances:
  //
  // * When the 'target' is set to 'targetGrpcProxy' and
  // 'validateForProxyless' is set to 'true', the
  // 'IPAddress' should be set to '0.0.0.0'.
  // * When the 'target' is a Private Service Connect Google APIs
  // bundle, you must specify an 'IPAddress'.
  //
  //
  // Otherwise, you can optionally specify an IP address that references an
  // existing static (reserved) IP address resource. When omitted, Google Cloud
  // assigns an ephemeral IP address.
  //
  // Use one of the following formats to specify an IP address while creating a
  // forwarding rule:
  //
  // * IP address number, as in '100.1.2.3'
  // * IPv6 address range, as in '2600:1234::/96'
  // * Full resource URL, as in
  // 'https://www.googleapis.com/compute/v1/projects/project_id/regions/region/addresses/address-name'
  // * Partial URL or by name, as in:
  //  * 'projects/project_id/regions/region/addresses/address-name'
  //  * 'regions/region/addresses/address-name'
  //  * 'global/addresses/address-name'
  //  * 'address-name'
  //
  //
  // The forwarding rule's 'target',
  // and in most cases, also the 'loadBalancingScheme', determine the
  // type of IP address that you can use. For detailed information, see
  // [IP address
  // specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
  //
  // When reading an 'IPAddress', the API always returns the IP
  // address number.
  string ip_address = 4 [json_name = "ip_address"];

  // The IP protocol to which this rule applies.
  //
  // For protocol forwarding, valid
  // options are 'TCP', 'UDP', 'ESP',
  // 'AH', 'SCTP', 'ICMP' and
  // 'L3_DEFAULT'.
  //
  // The valid IP protocols are different for different load balancing products
  // as described in [Load balancing
  // features](https://cloud.google.com/load-balancing/docs/features#protocols_from_the_load_balancer_to_the_backends). Possible values: ["TCP", "UDP", "ESP", "AH", "SCTP", "ICMP"]
  string ip_protocol = 5 [json_name = "ip_protocol"];

  // The IP Version that will be used by this global forwarding rule. Possible values: ["IPV4", "IPV6"]
  string ip_version = 6 [json_name = "ip_version"];

  // The fingerprint used for optimistic locking of this resource.  Used
  // internally during updates.
  string label_fingerprint = 7 [json_name = "label_fingerprint"];

  // Labels to apply to this forwarding rule.  A list of key->value pairs.
  map<string, string> labels = 8;

  // Specifies the forwarding rule type.
  //
  // For more information about forwarding rules, refer to
  // [Forwarding rule concepts](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts). Default value: "EXTERNAL" Possible values: ["EXTERNAL", "EXTERNAL_MANAGED", "INTERNAL_SELF_MANAGED"]
  string load_balancing_scheme = 9 [json_name = "load_balancing_scheme"];

  // Name of the resource; provided by the client when the resource is created.
  // The name must be 1-63 characters long, and comply with
  // [RFC1035](https://www.ietf.org/rfc/rfc1035.txt).
  //
  // Specifically, the name must be 1-63 characters long and match the regular
  // expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first
  // character must be a lowercase letter, and all following characters must
  // be a dash, lowercase letter, or digit, except the last character, which
  // cannot be a dash.
  //
  // For Private Service Connect forwarding rules that forward traffic to Google
  // APIs, the forwarding rule name must be a 1-20 characters string with
  // lowercase letters and numbers and must start with a letter.
  string name = 10;

  // This field is not used for external load balancing.
  //
  // For Internal TCP/UDP Load Balancing, this field identifies the network that
  // the load balanced IP should belong to for this Forwarding Rule.
  // If the subnetwork is specified, the network of the subnetwork will be used.
  // If neither subnetwork nor this field is specified, the default network will
  // be used.
  //
  // For Private Service Connect forwarding rules that forward traffic to Google
  // APIs, a network must be provided.
  string network = 11;

  // This field can only be used:
  //
  // * If 'IPProtocol' is one of TCP, UDP, or SCTP.
  // * By backend service-based network load balancers, target pool-based
  // network load balancers, internal proxy load balancers, external proxy load
  // balancers, Traffic Director, external protocol forwarding, and Classic VPN.
  // Some products have restrictions on what ports can be used. See
  // [port specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#port_specifications)
  // for details.
  //
  //
  // * TargetHttpProxy: 80, 8080
  // * TargetHttpsProxy: 443
  // * TargetTcpProxy: 25, 43, 110, 143, 195, 443, 465, 587, 700, 993, 995,
  //                  1883, 5222
  // * TargetSslProxy: 25, 43, 110, 143, 195, 443, 465, 587, 700, 993, 995,
  //                  1883, 5222
  // * TargetVpnGateway: 500, 4500
  string port_range = 12 [json_name = "port_range"];

  string project = 13;

  // The PSC connection id of the PSC Forwarding Rule.
  string psc_connection_id = 14 [json_name = "psc_connection_id"];

  // The PSC connection status of the PSC Forwarding Rule. Possible values: 'STATUS_UNSPECIFIED', 'PENDING', 'ACCEPTED', 'REJECTED', 'CLOSED'
  string psc_connection_status = 15 [json_name = "psc_connection_status"];

  string self_link = 16 [json_name = "self_link"];

  // If not empty, this Forwarding Rule will only forward the traffic when the source IP address matches one of the IP addresses or CIDR ranges set here. Note that a Forwarding Rule can only have up to 64 source IP ranges, and this field can only be used with a regional Forwarding Rule whose scheme is EXTERNAL. Each sourceIpRange entry should be either an IP address (for example, 1.2.3.4) or a CIDR range (for example, 1.2.3.0/24).
  repeated string source_ip_ranges = 17 [json_name = "source_ip_ranges"];

  // The URL of the target resource to receive the matched traffic.  For
  // regional forwarding rules, this target must be in the same region as the
  // forwarding rule. For global forwarding rules, this target must be a global
  // load balancing resource.
  //
  // The forwarded traffic must be of a type appropriate to the target object.
  // *  For load balancers, see the "Target" column in [Port specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
  // *  For Private Service Connect forwarding rules that forward traffic to Google APIs, provide the name of a supported Google API bundle:
  //  *  'vpc-sc' - [ APIs that support VPC Service Controls](https://cloud.google.com/vpc-service-controls/docs/supported-products).
  //  *  'all-apis' - [All supported Google APIs](https://cloud.google.com/vpc/docs/private-service-connect#supported-apis).
  //
  //
  // For Private Service Connect forwarding rules that forward traffic to managed services, the target must be a service attachment.
  string target = 18;

  repeated MetadataFilters metadata_filters = 19 [json_name = "metadata_filters"];

  repeated Timeouts timeouts = 20;

  map<string, string> for_each = 21 [json_name = "for_each"];

  repeated string depends_on = 22 [json_name = "depends_on"];

  int32 count = 23;

  string provider = 24;

  terraform.v1.Lifecycle lifecycle = 25;

  message MetadataFilters {
    // Specifies how individual filterLabel matches within the list of
    // filterLabels contribute towards the overall metadataFilter match.
    //
    // MATCH_ANY - At least one of the filterLabels must have a matching
    // label in the provided metadata.
    // MATCH_ALL - All filterLabels must have matching labels in the
    // provided metadata. Possible values: ["MATCH_ANY", "MATCH_ALL"]
    string filter_match_criteria = 1 [json_name = "filter_match_criteria"];

    repeated FilterLabels filter_labels = 2 [json_name = "filter_labels"];

    message FilterLabels {
      // Name of the metadata label. The length must be between
      // 1 and 1024 characters, inclusive.
      string name = 1;

      // The value that the label must match. The value has a maximum
      // length of 1024 characters.
      string value = 2;
    }
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeGlobalNetworkEndpoint version is 0
message GoogleComputeGlobalNetworkEndpoint {
  // Fully qualified domain name of network endpoint.
  // This can only be specified when network_endpoint_type of the NEG is INTERNET_FQDN_PORT.
  string fqdn = 1;

  // The global network endpoint group this endpoint is part of.
  string global_network_endpoint_group = 2 [json_name = "global_network_endpoint_group"];

  string id = 3;

  // IPv4 address external endpoint.
  string ip_address = 4 [json_name = "ip_address"];

  // Port number of the external endpoint.
  int64 port = 5;

  string project = 6;

  repeated Timeouts timeouts = 7;

  map<string, string> for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeGlobalNetworkEndpointGroup version is 0
message GoogleComputeGlobalNetworkEndpointGroup {
  // The default port used if the port number is not specified in the
  // network endpoint.
  int64 default_port = 1 [json_name = "default_port"];

  // An optional description of this resource. Provide this property when
  // you create the resource.
  string description = 2;

  string id = 3;

  // Name of the resource; provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 4;

  // Type of network endpoints in this network endpoint group. Possible values: ["INTERNET_IP_PORT", "INTERNET_FQDN_PORT"]
  string network_endpoint_type = 5 [json_name = "network_endpoint_type"];

  string project = 6;

  string self_link = 7 [json_name = "self_link"];

  repeated Timeouts timeouts = 8;

  map<string, string> for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeHaVpnGateway version is 0
message GoogleComputeHaVpnGateway {
  // An optional description of this resource.
  string description = 1;

  string id = 2;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  // the first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 3;

  // The network this VPN gateway is accepting traffic for.
  string network = 4;

  string project = 5;

  // The region this gateway should sit in.
  string region = 6;

  string self_link = 7 [json_name = "self_link"];

  // The stack type for this VPN gateway to identify the IP protocols that are enabled.
  // If not specified, IPV4_ONLY will be used. Default value: "IPV4_ONLY" Possible values: ["IPV4_ONLY", "IPV4_IPV6"]
  string stack_type = 8 [json_name = "stack_type"];

  repeated Timeouts timeouts = 9;

  repeated VpnInterfaces vpn_interfaces = 10 [json_name = "vpn_interfaces"];

  map<string, string> for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }

  message VpnInterfaces {
    // The numeric ID of this VPN gateway interface.
    int64 id = 1;

    // URL of the interconnect attachment resource. When the value
    // of this field is present, the VPN Gateway will be used for
    // IPsec-encrypted Cloud Interconnect; all Egress or Ingress
    // traffic for this VPN Gateway interface will go through the
    // specified interconnect attachment resource.
    //
    // Not currently available publicly.
    string interconnect_attachment = 2 [json_name = "interconnect_attachment"];

    // The external IP address for this VPN gateway interface.
    string ip_address = 3 [json_name = "ip_address"];
  }
}

// GoogleComputeHealthCheck version is 0
message GoogleComputeHealthCheck {
  // How often (in seconds) to send a health check. The default value is 5
  // seconds.
  int64 check_interval_sec = 1 [json_name = "check_interval_sec"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // An optional description of this resource. Provide this property when
  // you create the resource.
  string description = 3;

  // A so-far unhealthy instance will be marked healthy after this many
  // consecutive successes. The default value is 2.
  int64 healthy_threshold = 4 [json_name = "healthy_threshold"];

  string id = 5;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  // the first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the
  // last character, which cannot be a dash.
  string name = 6;

  string project = 7;

  string self_link = 8 [json_name = "self_link"];

  // How long (in seconds) to wait before claiming failure.
  // The default value is 5 seconds.  It is invalid for timeoutSec to have
  // greater value than checkIntervalSec.
  int64 timeout_sec = 9 [json_name = "timeout_sec"];

  // The type of the health check. One of HTTP, HTTPS, TCP, or SSL.
  string type = 10;

  // A so-far healthy instance will be marked unhealthy after this many
  // consecutive failures. The default value is 2.
  int64 unhealthy_threshold = 11 [json_name = "unhealthy_threshold"];

  GrpcHealthCheck grpc_health_check = 12 [json_name = "grpc_health_check"];

  Http2HealthCheck http2_health_check = 13 [json_name = "http2_health_check"];

  HttpHealthCheck http_health_check = 14 [json_name = "http_health_check"];

  HttpsHealthCheck https_health_check = 15 [json_name = "https_health_check"];

  LogConfig log_config = 16 [json_name = "log_config"];

  SslHealthCheck ssl_health_check = 17 [json_name = "ssl_health_check"];

  TcpHealthCheck tcp_health_check = 18 [json_name = "tcp_health_check"];

  repeated Timeouts timeouts = 19;

  map<string, string> for_each = 20 [json_name = "for_each"];

  repeated string depends_on = 21 [json_name = "depends_on"];

  int32 count = 22;

  string provider = 23;

  terraform.v1.Lifecycle lifecycle = 24;

  message GrpcHealthCheck {
    // The gRPC service name for the health check.
    // The value of grpcServiceName has the following meanings by convention:
    //  - Empty serviceName means the overall status of all services at the backend.
    //  - Non-empty serviceName means the health of that gRPC service, as defined by the owner of the service.
    // The grpcServiceName can only be ASCII.
    string grpc_service_name = 1 [json_name = "grpc_service_name"];

    // The port number for the health check request.
    // Must be specified if portName and portSpecification are not set
    // or if port_specification is USE_FIXED_PORT. Valid values are 1 through 65535.
    int64 port = 2;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    // port_name are defined, port takes precedence.
    string port_name = 3 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    // following values:
    //
    //  * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //  * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //  * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //  network endpoint is used for health checking. For other backends, the
    //  port or named port specified in the Backend Service is used for health
    //  checking.
    //
    // If not specified, gRPC health check follows behavior specified in 'port' and
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"]
    string port_specification = 4 [json_name = "port_specification"];
  }

  message Http2HealthCheck {
    // The value of the host header in the HTTP2 health check request.
    // If left empty (default value), the public IP on behalf of which this health
    // check is performed will be used.
    string host = 1;

    // The TCP port number for the HTTP2 health check request.
    // The default value is 443.
    int64 port = 2;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    // port_name are defined, port takes precedence.
    string port_name = 3 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    // following values:
    //
    //  * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //  * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //  * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //  network endpoint is used for health checking. For other backends, the
    //  port or named port specified in the Backend Service is used for health
    //  checking.
    //
    // If not specified, HTTP2 health check follows behavior specified in 'port' and
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"]
    string port_specification = 4 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    string proxy_header = 5 [json_name = "proxy_header"];

    // The request path of the HTTP2 health check request.
    // The default value is /.
    string request_path = 6 [json_name = "request_path"];

    // The bytes to match against the beginning of the response data. If left empty
    // (the default value), any response will indicate health. The response data
    // can only be ASCII.
    string response = 7;
  }

  message HttpHealthCheck {
    // The value of the host header in the HTTP health check request.
    // If left empty (default value), the public IP on behalf of which this health
    // check is performed will be used.
    string host = 1;

    // The TCP port number for the HTTP health check request.
    // The default value is 80.
    int64 port = 2;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    // port_name are defined, port takes precedence.
    string port_name = 3 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    // following values:
    //
    //  * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //  * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //  * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //  network endpoint is used for health checking. For other backends, the
    //  port or named port specified in the Backend Service is used for health
    //  checking.
    //
    // If not specified, HTTP health check follows behavior specified in 'port' and
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"]
    string port_specification = 4 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    string proxy_header = 5 [json_name = "proxy_header"];

    // The request path of the HTTP health check request.
    // The default value is /.
    string request_path = 6 [json_name = "request_path"];

    // The bytes to match against the beginning of the response data. If left empty
    // (the default value), any response will indicate health. The response data
    // can only be ASCII.
    string response = 7;
  }

  message HttpsHealthCheck {
    // The value of the host header in the HTTPS health check request.
    // If left empty (default value), the public IP on behalf of which this health
    // check is performed will be used.
    string host = 1;

    // The TCP port number for the HTTPS health check request.
    // The default value is 443.
    int64 port = 2;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    // port_name are defined, port takes precedence.
    string port_name = 3 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    // following values:
    //
    //  * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //  * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //  * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //  network endpoint is used for health checking. For other backends, the
    //  port or named port specified in the Backend Service is used for health
    //  checking.
    //
    // If not specified, HTTPS health check follows behavior specified in 'port' and
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"]
    string port_specification = 4 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    string proxy_header = 5 [json_name = "proxy_header"];

    // The request path of the HTTPS health check request.
    // The default value is /.
    string request_path = 6 [json_name = "request_path"];

    // The bytes to match against the beginning of the response data. If left empty
    // (the default value), any response will indicate health. The response data
    // can only be ASCII.
    string response = 7;
  }

  message LogConfig {
    // Indicates whether or not to export logs. This is false by default,
    // which means no health check logging will be done.
    bool enable = 1;
  }

  message SslHealthCheck {
    // The TCP port number for the SSL health check request.
    // The default value is 443.
    int64 port = 1;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    // port_name are defined, port takes precedence.
    string port_name = 2 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    // following values:
    //
    //  * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //  * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //  * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //  network endpoint is used for health checking. For other backends, the
    //  port or named port specified in the Backend Service is used for health
    //  checking.
    //
    // If not specified, SSL health check follows behavior specified in 'port' and
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"]
    string port_specification = 3 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    string proxy_header = 4 [json_name = "proxy_header"];

    // The application data to send once the SSL connection has been
    // established (default value is empty). If both request and response are
    // empty, the connection establishment alone will indicate health. The request
    // data can only be ASCII.
    string request = 5;

    // The bytes to match against the beginning of the response data. If left empty
    // (the default value), any response will indicate health. The response data
    // can only be ASCII.
    string response = 6;
  }

  message TcpHealthCheck {
    // The TCP port number for the TCP health check request.
    // The default value is 443.
    int64 port = 1;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    // port_name are defined, port takes precedence.
    string port_name = 2 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    // following values:
    //
    //  * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //  * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //  * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //  network endpoint is used for health checking. For other backends, the
    //  port or named port specified in the Backend Service is used for health
    //  checking.
    //
    // If not specified, TCP health check follows behavior specified in 'port' and
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"]
    string port_specification = 3 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    string proxy_header = 4 [json_name = "proxy_header"];

    // The application data to send once the TCP connection has been
    // established (default value is empty). If both request and response are
    // empty, the connection establishment alone will indicate health. The request
    // data can only be ASCII.
    string request = 5;

    // The bytes to match against the beginning of the response data. If left empty
    // (the default value), any response will indicate health. The response data
    // can only be ASCII.
    string response = 6;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeHttpHealthCheck version is 0
message GoogleComputeHttpHealthCheck {
  // How often (in seconds) to send a health check. The default value is 5
  // seconds.
  int64 check_interval_sec = 1 [json_name = "check_interval_sec"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // An optional description of this resource. Provide this property when
  // you create the resource.
  string description = 3;

  // A so-far unhealthy instance will be marked healthy after this many
  // consecutive successes. The default value is 2.
  int64 healthy_threshold = 4 [json_name = "healthy_threshold"];

  // The value of the host header in the HTTP health check request. If
  // left empty (default value), the public IP on behalf of which this
  // health check is performed will be used.
  string host = 5;

  string id = 6;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  // the first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the
  // last character, which cannot be a dash.
  string name = 7;

  // The TCP port number for the HTTP health check request.
  // The default value is 80.
  int64 port = 8;

  string project = 9;

  // The request path of the HTTP health check request.
  // The default value is /.
  string request_path = 10 [json_name = "request_path"];

  string self_link = 11 [json_name = "self_link"];

  // How long (in seconds) to wait before claiming failure.
  // The default value is 5 seconds.  It is invalid for timeoutSec to have
  // greater value than checkIntervalSec.
  int64 timeout_sec = 12 [json_name = "timeout_sec"];

  // A so-far healthy instance will be marked unhealthy after this many
  // consecutive failures. The default value is 2.
  int64 unhealthy_threshold = 13 [json_name = "unhealthy_threshold"];

  repeated Timeouts timeouts = 14;

  map<string, string> for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  int32 count = 17;

  string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeHttpsHealthCheck version is 0
message GoogleComputeHttpsHealthCheck {
  // How often (in seconds) to send a health check. The default value is 5
  // seconds.
  int64 check_interval_sec = 1 [json_name = "check_interval_sec"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // An optional description of this resource. Provide this property when
  // you create the resource.
  string description = 3;

  // A so-far unhealthy instance will be marked healthy after this many
  // consecutive successes. The default value is 2.
  int64 healthy_threshold = 4 [json_name = "healthy_threshold"];

  // The value of the host header in the HTTPS health check request. If
  // left empty (default value), the public IP on behalf of which this
  // health check is performed will be used.
  string host = 5;

  string id = 6;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  // the first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the
  // last character, which cannot be a dash.
  string name = 7;

  // The TCP port number for the HTTPS health check request.
  // The default value is 443.
  int64 port = 8;

  string project = 9;

  // The request path of the HTTPS health check request.
  // The default value is /.
  string request_path = 10 [json_name = "request_path"];

  string self_link = 11 [json_name = "self_link"];

  // How long (in seconds) to wait before claiming failure.
  // The default value is 5 seconds.  It is invalid for timeoutSec to have
  // greater value than checkIntervalSec.
  int64 timeout_sec = 12 [json_name = "timeout_sec"];

  // A so-far healthy instance will be marked unhealthy after this many
  // consecutive failures. The default value is 2.
  int64 unhealthy_threshold = 13 [json_name = "unhealthy_threshold"];

  repeated Timeouts timeouts = 14;

  map<string, string> for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  int32 count = 17;

  string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeImage version is 0
message GoogleComputeImage {
  // Size of the image tar.gz archive stored in Google Cloud Storage (in
  // bytes).
  int64 archive_size_bytes = 1 [json_name = "archive_size_bytes"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // An optional description of this resource. Provide this property when
  // you create the resource.
  string description = 3;

  // Size of the image when restored onto a persistent disk (in GB).
  int64 disk_size_gb = 4 [json_name = "disk_size_gb"];

  // The name of the image family to which this image belongs. You can
  // create disks by specifying an image family instead of a specific
  // image name. The image family always returns its latest image that is
  // not deprecated. The name of the image family must comply with
  // RFC1035.
  string family = 5;

  string id = 6;

  // The fingerprint used for optimistic locking of this resource. Used
  // internally during updates.
  string label_fingerprint = 7 [json_name = "label_fingerprint"];

  // Labels to apply to this Image.
  map<string, string> labels = 8;

  // Any applicable license URI.
  repeated string licenses = 9;

  // Name of the resource; provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  // the first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the
  // last character, which cannot be a dash.
  string name = 10;

  string project = 11;

  string self_link = 12 [json_name = "self_link"];

  // The source disk to create this image based on.
  // You must provide either this property or the
  // rawDisk.source property but not both to create an image.
  string source_disk = 13 [json_name = "source_disk"];

  // URL of the source image used to create this image. In order to create an image, you must provide the full or partial
  // URL of one of the following:
  //
  // * The selfLink URL
  // * This property
  // * The rawDisk.source URL
  // * The sourceDisk URL
  string source_image = 14 [json_name = "source_image"];

  // URL of the source snapshot used to create this image.
  //
  // In order to create an image, you must provide the full or partial URL of one of the following:
  //
  // * The selfLink URL
  // * This property
  // * The sourceImage URL
  // * The rawDisk.source URL
  // * The sourceDisk URL
  string source_snapshot = 15 [json_name = "source_snapshot"];

  // Cloud Storage bucket storage location of the image
  // (regional or multi-regional).
  // Reference link: https://cloud.google.com/compute/docs/reference/rest/v1/images
  repeated string storage_locations = 16 [json_name = "storage_locations"];

  repeated GuestOsFeatures guest_os_features = 17 [json_name = "guest_os_features"];

  ImageEncryptionKey image_encryption_key = 18 [json_name = "image_encryption_key"];

  RawDisk raw_disk = 19 [json_name = "raw_disk"];

  repeated Timeouts timeouts = 20;

  map<string, string> for_each = 21 [json_name = "for_each"];

  repeated string depends_on = 22 [json_name = "depends_on"];

  int32 count = 23;

  string provider = 24;

  terraform.v1.Lifecycle lifecycle = 25;

  message GuestOsFeatures {
    // The type of supported feature. Read [Enabling guest operating system features](https://cloud.google.com/compute/docs/images/create-delete-deprecate-private-images#guest-os-features) to see a list of available options. Possible values: ["MULTI_IP_SUBNET", "SECURE_BOOT", "SEV_CAPABLE", "UEFI_COMPATIBLE", "VIRTIO_SCSI_MULTIQUEUE", "WINDOWS", "GVNIC", "SEV_LIVE_MIGRATABLE", "SEV_SNP_CAPABLE", "SUSPEND_RESUME_COMPATIBLE", "TDX_CAPABLE"]
    string type = 1;
  }

  message ImageEncryptionKey {
    // The self link of the encryption key that is stored in Google Cloud
    // KMS.
    string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

    // The service account being used for the encryption request for the
    // given KMS key. If absent, the Compute Engine default service
    // account is used.
    string kms_key_service_account = 2 [json_name = "kms_key_service_account"];
  }

  message RawDisk {
    // The format used to encode and transmit the block device, which
    // should be TAR. This is just a container and transmission format
    // and not a runtime format. Provided by the client when the disk
    // image is created. Default value: "TAR" Possible values: ["TAR"]
    string container_type = 1 [json_name = "container_type"];

    // An optional SHA1 checksum of the disk image before unpackaging.
    // This is provided by the client when the disk image is created.
    string sha1 = 2;

    // The full Google Cloud Storage URL where disk storage is stored
    // You must provide either this property or the sourceDisk property
    // but not both.
    string source = 3;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeImageIamBinding version is 0
message GoogleComputeImageIamBinding {
  string etag = 1;

  string id = 2;

  string image = 3;

  repeated string members = 4;

  string project = 5;

  string role = 6;

  Condition condition = 7;

  map<string, string> for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Condition {
    string description = 1;

    string expression = 2;

    string title = 3;
  }
}

// GoogleComputeImageIamMember version is 0
message GoogleComputeImageIamMember {
  string etag = 1;

  string id = 2;

  string image = 3;

  string member = 4;

  string project = 5;

  string role = 6;

  Condition condition = 7;

  map<string, string> for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Condition {
    string description = 1;

    string expression = 2;

    string title = 3;
  }
}

// GoogleComputeImageIamPolicy version is 0
message GoogleComputeImageIamPolicy {
  string etag = 1;

  string id = 2;

  string image = 3;

  string policy_data = 4 [json_name = "policy_data"];

  string project = 5;

  map<string, string> for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  int32 count = 8;

  string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;
}

// GoogleComputeInstance version is 6
message GoogleComputeInstance {
  // If true, allows Terraform to stop the instance to update its properties. If you try to update a property that requires stopping the instance without setting this field, the update will fail.
  bool allow_stopping_for_update = 1 [json_name = "allow_stopping_for_update"];

  // Whether sending and receiving of packets with non-matching source or destination IPs is allowed.
  bool can_ip_forward = 2 [json_name = "can_ip_forward"];

  // The CPU platform used by this instance.
  string cpu_platform = 3 [json_name = "cpu_platform"];

  // 					Current status of the instance.
  // 					This could be one of the following values: PROVISIONING, STAGING, RUNNING, STOPPING, SUSPENDING, SUSPENDED, REPAIRING, and TERMINATED.
  // 					For more information about the status of the instance, see [Instance life cycle](https://cloud.google.com/compute/docs/instances/instance-life-cycle).
  string current_status = 4 [json_name = "current_status"];

  // Whether deletion protection is enabled on this instance.
  bool deletion_protection = 5 [json_name = "deletion_protection"];

  // A brief description of the resource.
  string description = 6;

  // Desired status of the instance. Either "RUNNING" or "TERMINATED".
  string desired_status = 7 [json_name = "desired_status"];

  // Whether the instance has virtual displays enabled.
  bool enable_display = 8 [json_name = "enable_display"];

  // List of the type and count of accelerator cards attached to the instance.
  repeated GuestAccelerator guest_accelerator = 9 [json_name = "guest_accelerator"];

  // A custom hostname for the instance. Must be a fully qualified DNS name and RFC-1035-valid. Valid format is a series of labels 1-63 characters long matching the regular expression [a-z]([-a-z0-9]*[a-z0-9]), concatenated with periods. The entire hostname must not exceed 253 characters. Changing this forces a new resource to be created.
  string hostname = 10;

  string id = 11;

  // The server-assigned unique identifier of this instance.
  string instance_id = 12 [json_name = "instance_id"];

  // The unique fingerprint of the labels.
  string label_fingerprint = 13 [json_name = "label_fingerprint"];

  // A set of key/value label pairs assigned to the instance.
  map<string, string> labels = 14;

  // The machine type to create.
  string machine_type = 15 [json_name = "machine_type"];

  // Metadata key/value pairs made available within the instance.
  map<string, string> metadata = 16;

  // The unique fingerprint of the metadata.
  string metadata_fingerprint = 17 [json_name = "metadata_fingerprint"];

  // Metadata startup scripts made available within the instance.
  string metadata_startup_script = 18 [json_name = "metadata_startup_script"];

  // The minimum CPU platform specified for the VM instance.
  string min_cpu_platform = 19 [json_name = "min_cpu_platform"];

  // The name of the instance. One of name or self_link must be provided.
  string name = 20;

  // The ID of the project in which the resource belongs. If self_link is provided, this value is ignored. If neither self_link nor project are provided, the provider project is used.
  string project = 21;

  // A list of self_links of resource policies to attach to the instance. Currently a max of 1 resource policy is supported.
  repeated string resource_policies = 22 [json_name = "resource_policies"];

  // The URI of the created resource.
  string self_link = 23 [json_name = "self_link"];

  // The list of tags attached to the instance.
  repeated string tags = 24;

  // The unique fingerprint of the tags.
  string tags_fingerprint = 25 [json_name = "tags_fingerprint"];

  // The zone of the instance. If self_link is provided, this value is ignored. If neither self_link nor zone are provided, the provider zone is used.
  string zone = 26;

  AdvancedMachineFeatures advanced_machine_features = 27 [json_name = "advanced_machine_features"];

  repeated AttachedDisk attached_disk = 28 [json_name = "attached_disk"];

  BootDisk boot_disk = 29 [json_name = "boot_disk"];

  ConfidentialInstanceConfig confidential_instance_config = 30 [json_name = "confidential_instance_config"];

  repeated NetworkInterface network_interface = 31 [json_name = "network_interface"];

  NetworkPerformanceConfig network_performance_config = 32 [json_name = "network_performance_config"];

  ReservationAffinity reservation_affinity = 33 [json_name = "reservation_affinity"];

  Scheduling scheduling = 34;

  repeated ScratchDisk scratch_disk = 35 [json_name = "scratch_disk"];

  ServiceAccount service_account = 36 [json_name = "service_account"];

  ShieldedInstanceConfig shielded_instance_config = 37 [json_name = "shielded_instance_config"];

  repeated Timeouts timeouts = 38;

  map<string, string> for_each = 39 [json_name = "for_each"];

  repeated string depends_on = 40 [json_name = "depends_on"];

  int32 count = 41;

  string provider = 42;

  terraform.v1.Lifecycle lifecycle = 43;

  message GuestAccelerator {
    // count: number
    int64 count = 1;

    // type: string
    string type = 2;
  }

  message AdvancedMachineFeatures {
    // Whether to enable nested virtualization or not.
    bool enable_nested_virtualization = 1 [json_name = "enable_nested_virtualization"];

    // The number of threads per physical core. To disable simultaneous multithreading (SMT) set this to 1. If unset, the maximum number of threads supported per core by the underlying processor is assumed.
    int64 threads_per_core = 2 [json_name = "threads_per_core"];

    // The number of physical cores to expose to an instance. Multiply by the number of threads per core to compute the total number of virtual CPUs to expose to the instance. If unset, the number of cores is inferred from the instance\'s nominal CPU count and the underlying platform\'s SMT width.
    int64 visible_core_count = 3 [json_name = "visible_core_count"];
  }

  message AttachedDisk {
    // Name with which the attached disk is accessible under /dev/disk/by-id/
    string device_name = 1 [json_name = "device_name"];

    // A 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to encrypt this disk. Only one of kms_key_self_link and disk_encryption_key_raw may be set.
    string disk_encryption_key_raw = 2 [json_name = "disk_encryption_key_raw"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key that protects this resource.
    string disk_encryption_key_sha256 = 3 [json_name = "disk_encryption_key_sha256"];

    // The self_link of the encryption key that is stored in Google Cloud KMS to encrypt this disk. Only one of kms_key_self_link and disk_encryption_key_raw may be set.
    string kms_key_self_link = 4 [json_name = "kms_key_self_link"];

    // Read/write mode for the disk. One of "READ_ONLY" or "READ_WRITE".
    string mode = 5;

    // The name or self_link of the disk attached to this instance.
    string source = 6;
  }

  message BootDisk {
    // Whether the disk will be auto-deleted when the instance is deleted.
    bool auto_delete = 1 [json_name = "auto_delete"];

    // Name with which attached disk will be accessible under /dev/disk/by-id/
    string device_name = 2 [json_name = "device_name"];

    // A 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to encrypt this disk. Only one of kms_key_self_link and disk_encryption_key_raw may be set.
    string disk_encryption_key_raw = 3 [json_name = "disk_encryption_key_raw"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key that protects this resource.
    string disk_encryption_key_sha256 = 4 [json_name = "disk_encryption_key_sha256"];

    // The self_link of the encryption key that is stored in Google Cloud KMS to encrypt this disk. Only one of kms_key_self_link and disk_encryption_key_raw may be set.
    string kms_key_self_link = 5 [json_name = "kms_key_self_link"];

    // Read/write mode for the disk. One of "READ_ONLY" or "READ_WRITE".
    string mode = 6;

    // The name or self_link of the disk attached to this instance.
    string source = 7;

    InitializeParams initialize_params = 8 [json_name = "initialize_params"];

    message InitializeParams {
      // The image from which this disk was initialised.
      string image = 1;

      // A set of key/value label pairs assigned to the disk.
      map<string, string> labels = 2;

      // The size of the image in gigabytes.
      int64 size = 3;

      // The Google Compute Engine disk type. Such as pd-standard, pd-ssd or pd-balanced.
      string type = 4;
    }
  }

  message ConfidentialInstanceConfig {
    // Defines whether the instance should have confidential compute enabled.
    bool enable_confidential_compute = 1 [json_name = "enable_confidential_compute"];
  }

  message NetworkInterface {
    // One of EXTERNAL, INTERNAL to indicate whether the IP can be accessed from the Internet. This field is always inherited from its subnetwork.
    string ipv6_access_type = 1 [json_name = "ipv6_access_type"];

    // The name of the interface
    string name = 2;

    // The name or self_link of the network attached to this interface.
    string network = 3;

    // The private IP address assigned to the instance.
    string network_ip = 4 [json_name = "network_ip"];

    // The type of vNIC to be used on this interface. Possible values:GVNIC, VIRTIO_NET
    string nic_type = 5 [json_name = "nic_type"];

    // The networking queue count that's specified by users for the network interface. Both Rx and Tx queues will be set to this number. It will be empty if not specified.
    int64 queue_count = 6 [json_name = "queue_count"];

    // The stack type for this network interface to identify whether the IPv6 feature is enabled or not. If not specified, IPV4_ONLY will be used.
    string stack_type = 7 [json_name = "stack_type"];

    // The name or self_link of the subnetwork attached to this interface.
    string subnetwork = 8;

    // The project in which the subnetwork belongs.
    string subnetwork_project = 9 [json_name = "subnetwork_project"];

    repeated AccessConfig access_config = 10 [json_name = "access_config"];

    repeated AliasIpRange alias_ip_range = 11 [json_name = "alias_ip_range"];

    repeated Ipv6AccessConfig ipv6_access_config = 12 [json_name = "ipv6_access_config"];

    message AccessConfig {
      // The IP address that is be 1:1 mapped to the instance's network ip.
      string nat_ip = 1 [json_name = "nat_ip"];

      // The networking tier used for configuring this instance. One of PREMIUM or STANDARD.
      string network_tier = 2 [json_name = "network_tier"];

      // The DNS domain name for the public PTR record.
      string public_ptr_domain_name = 3 [json_name = "public_ptr_domain_name"];
    }

    message AliasIpRange {
      // The IP CIDR range represented by this alias IP range.
      string ip_cidr_range = 1 [json_name = "ip_cidr_range"];

      // The subnetwork secondary range name specifying the secondary range from which to allocate the IP CIDR range for this alias IP range.
      string subnetwork_range_name = 2 [json_name = "subnetwork_range_name"];
    }

    message Ipv6AccessConfig {
      // The first IPv6 address of the external IPv6 range associated with this instance, prefix length is stored in externalIpv6PrefixLength in ipv6AccessConfig. The field is output only, an IPv6 address from a subnetwork associated with the instance will be allocated dynamically.
      string external_ipv6 = 1 [json_name = "external_ipv6"];

      // The prefix length of the external IPv6 range.
      string external_ipv6_prefix_length = 2 [json_name = "external_ipv6_prefix_length"];

      // The service-level to be provided for IPv6 traffic when the subnet has an external subnet. Only PREMIUM tier is valid for IPv6
      string network_tier = 3 [json_name = "network_tier"];

      // The domain name to be used when creating DNSv6 records for the external IPv6 ranges.
      string public_ptr_domain_name = 4 [json_name = "public_ptr_domain_name"];
    }
  }

  message NetworkPerformanceConfig {
    // The egress bandwidth tier to enable. Possible values:TIER_1, DEFAULT
    string total_egress_bandwidth_tier = 1 [json_name = "total_egress_bandwidth_tier"];
  }

  message ReservationAffinity {
    // The type of reservation from which this instance can consume resources.
    string type = 1;

    SpecificReservation specific_reservation = 2 [json_name = "specific_reservation"];

    message SpecificReservation {
      // Corresponds to the label key of a reservation resource. To target a SPECIFIC_RESERVATION by name, specify compute.googleapis.com/reservation-name as the key and specify the name of your reservation as the only value.
      string key = 1;

      // Corresponds to the label values of a reservation resource.
      repeated string values = 2;
    }
  }

  message Scheduling {
    // Specifies if the instance should be restarted if it was terminated by Compute Engine (not a user).
    bool automatic_restart = 1 [json_name = "automatic_restart"];

    // Specifies the action GCE should take when SPOT VM is preempted.
    string instance_termination_action = 2 [json_name = "instance_termination_action"];

    int64 min_node_cpus = 3 [json_name = "min_node_cpus"];

    // Describes maintenance behavior for the instance. One of MIGRATE or TERMINATE,
    string on_host_maintenance = 4 [json_name = "on_host_maintenance"];

    // Whether the instance is preemptible.
    bool preemptible = 5;

    // Whether the instance is spot. If this is set as SPOT.
    string provisioning_model = 6 [json_name = "provisioning_model"];

    repeated NodeAffinities node_affinities = 7 [json_name = "node_affinities"];

    message NodeAffinities {
      string key = 1;

      string operator = 2;

      repeated string values = 3;
    }
  }

  message ScratchDisk {
    // The disk interface used for attaching this disk. One of SCSI or NVME.
    string interface = 1;

    // The size of the disk in gigabytes. One of 375 or 3000.
    int64 size = 2;
  }

  message ServiceAccount {
    // The service account e-mail address.
    string email = 1;

    // A list of service scopes.
    repeated string scopes = 2;
  }

  message ShieldedInstanceConfig {
    // Whether integrity monitoring is enabled for the instance.
    bool enable_integrity_monitoring = 1 [json_name = "enable_integrity_monitoring"];

    // Whether secure boot is enabled for the instance.
    bool enable_secure_boot = 2 [json_name = "enable_secure_boot"];

    // Whether the instance uses vTPM.
    bool enable_vtpm = 3 [json_name = "enable_vtpm"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeInstanceFromTemplate version is 0
message GoogleComputeInstanceFromTemplate {
  // If true, allows Terraform to stop the instance to update its properties. If you try to update a property that requires stopping the instance without setting this field, the update will fail.
  bool allow_stopping_for_update = 1 [json_name = "allow_stopping_for_update"];

  // List of disks attached to the instance
  repeated AttachedDisk attached_disk = 2 [json_name = "attached_disk"];

  // Whether sending and receiving of packets with non-matching source or destination IPs is allowed.
  bool can_ip_forward = 3 [json_name = "can_ip_forward"];

  // The CPU platform used by this instance.
  string cpu_platform = 4 [json_name = "cpu_platform"];

  // 					Current status of the instance.
  // 					This could be one of the following values: PROVISIONING, STAGING, RUNNING, STOPPING, SUSPENDING, SUSPENDED, REPAIRING, and TERMINATED.
  // 					For more information about the status of the instance, see [Instance life cycle](https://cloud.google.com/compute/docs/instances/instance-life-cycle).
  string current_status = 5 [json_name = "current_status"];

  // Whether deletion protection is enabled on this instance.
  bool deletion_protection = 6 [json_name = "deletion_protection"];

  // A brief description of the resource.
  string description = 7;

  // Desired status of the instance. Either "RUNNING" or "TERMINATED".
  string desired_status = 8 [json_name = "desired_status"];

  // Whether the instance has virtual displays enabled.
  bool enable_display = 9 [json_name = "enable_display"];

  // List of the type and count of accelerator cards attached to the instance.
  repeated GuestAccelerator guest_accelerator = 10 [json_name = "guest_accelerator"];

  // A custom hostname for the instance. Must be a fully qualified DNS name and RFC-1035-valid. Valid format is a series of labels 1-63 characters long matching the regular expression [a-z]([-a-z0-9]*[a-z0-9]), concatenated with periods. The entire hostname must not exceed 253 characters. Changing this forces a new resource to be created.
  string hostname = 11;

  string id = 12;

  // The server-assigned unique identifier of this instance.
  string instance_id = 13 [json_name = "instance_id"];

  // The unique fingerprint of the labels.
  string label_fingerprint = 14 [json_name = "label_fingerprint"];

  // A set of key/value label pairs assigned to the instance.
  map<string, string> labels = 15;

  // The machine type to create.
  string machine_type = 16 [json_name = "machine_type"];

  // Metadata key/value pairs made available within the instance.
  map<string, string> metadata = 17;

  // The unique fingerprint of the metadata.
  string metadata_fingerprint = 18 [json_name = "metadata_fingerprint"];

  // Metadata startup scripts made available within the instance.
  string metadata_startup_script = 19 [json_name = "metadata_startup_script"];

  // The minimum CPU platform specified for the VM instance.
  string min_cpu_platform = 20 [json_name = "min_cpu_platform"];

  // The name of the instance. One of name or self_link must be provided.
  string name = 21;

  // The ID of the project in which the resource belongs. If self_link is provided, this value is ignored. If neither self_link nor project are provided, the provider project is used.
  string project = 22;

  // A list of self_links of resource policies to attach to the instance. Currently a max of 1 resource policy is supported.
  repeated string resource_policies = 23 [json_name = "resource_policies"];

  // The scratch disks attached to the instance.
  repeated ScratchDisk scratch_disk = 24 [json_name = "scratch_disk"];

  // The URI of the created resource.
  string self_link = 25 [json_name = "self_link"];

  // The service account to attach to the instance.
  repeated ServiceAccount service_account = 26 [json_name = "service_account"];

  // Name or self link of an instance template to create the instance based on.
  string source_instance_template = 27 [json_name = "source_instance_template"];

  // The list of tags attached to the instance.
  repeated string tags = 28;

  // The unique fingerprint of the tags.
  string tags_fingerprint = 29 [json_name = "tags_fingerprint"];

  // The zone of the instance. If self_link is provided, this value is ignored. If neither self_link nor zone are provided, the provider zone is used.
  string zone = 30;

  AdvancedMachineFeatures advanced_machine_features = 31 [json_name = "advanced_machine_features"];

  BootDisk boot_disk = 32 [json_name = "boot_disk"];

  ConfidentialInstanceConfig confidential_instance_config = 33 [json_name = "confidential_instance_config"];

  repeated NetworkInterface network_interface = 34 [json_name = "network_interface"];

  NetworkPerformanceConfig network_performance_config = 35 [json_name = "network_performance_config"];

  ReservationAffinity reservation_affinity = 36 [json_name = "reservation_affinity"];

  Scheduling scheduling = 37;

  ShieldedInstanceConfig shielded_instance_config = 38 [json_name = "shielded_instance_config"];

  repeated Timeouts timeouts = 39;

  map<string, string> for_each = 40 [json_name = "for_each"];

  repeated string depends_on = 41 [json_name = "depends_on"];

  int32 count = 42;

  string provider = 43;

  terraform.v1.Lifecycle lifecycle = 44;

  message AttachedDisk {
    // device_name: string
    string device_name = 1 [json_name = "device_name"];

    // disk_encryption_key_raw: string
    string disk_encryption_key_raw = 2 [json_name = "disk_encryption_key_raw"];

    // disk_encryption_key_sha256: string
    string disk_encryption_key_sha256 = 3 [json_name = "disk_encryption_key_sha256"];

    // kms_key_self_link: string
    string kms_key_self_link = 4 [json_name = "kms_key_self_link"];

    // mode: string
    string mode = 5;

    // source: string
    string source = 6;
  }

  message GuestAccelerator {
    // count: number
    int64 count = 1;

    // type: string
    string type = 2;
  }

  message ScratchDisk {
    // interface: string
    string interface = 1;

    // size: number
    int64 size = 2;
  }

  message ServiceAccount {
    // email: string
    string email = 1;

    // scopes: set of string
    repeated string scopes = 2;
  }

  message AdvancedMachineFeatures {
    // Whether to enable nested virtualization or not.
    bool enable_nested_virtualization = 1 [json_name = "enable_nested_virtualization"];

    // The number of threads per physical core. To disable simultaneous multithreading (SMT) set this to 1. If unset, the maximum number of threads supported per core by the underlying processor is assumed.
    int64 threads_per_core = 2 [json_name = "threads_per_core"];

    // The number of physical cores to expose to an instance. Multiply by the number of threads per core to compute the total number of virtual CPUs to expose to the instance. If unset, the number of cores is inferred from the instance\'s nominal CPU count and the underlying platform\'s SMT width.
    int64 visible_core_count = 3 [json_name = "visible_core_count"];
  }

  message BootDisk {
    // Whether the disk will be auto-deleted when the instance is deleted.
    bool auto_delete = 1 [json_name = "auto_delete"];

    // Name with which attached disk will be accessible under /dev/disk/by-id/
    string device_name = 2 [json_name = "device_name"];

    // A 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to encrypt this disk. Only one of kms_key_self_link and disk_encryption_key_raw may be set.
    string disk_encryption_key_raw = 3 [json_name = "disk_encryption_key_raw"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key that protects this resource.
    string disk_encryption_key_sha256 = 4 [json_name = "disk_encryption_key_sha256"];

    // The self_link of the encryption key that is stored in Google Cloud KMS to encrypt this disk. Only one of kms_key_self_link and disk_encryption_key_raw may be set.
    string kms_key_self_link = 5 [json_name = "kms_key_self_link"];

    // Read/write mode for the disk. One of "READ_ONLY" or "READ_WRITE".
    string mode = 6;

    // The name or self_link of the disk attached to this instance.
    string source = 7;

    InitializeParams initialize_params = 8 [json_name = "initialize_params"];

    message InitializeParams {
      // The image from which this disk was initialised.
      string image = 1;

      // A set of key/value label pairs assigned to the disk.
      map<string, string> labels = 2;

      // The size of the image in gigabytes.
      int64 size = 3;

      // The Google Compute Engine disk type. Such as pd-standard, pd-ssd or pd-balanced.
      string type = 4;
    }
  }

  message ConfidentialInstanceConfig {
    // Defines whether the instance should have confidential compute enabled.
    bool enable_confidential_compute = 1 [json_name = "enable_confidential_compute"];
  }

  message NetworkInterface {
    // Access configurations, i.e. IPs via which this instance can be accessed via the Internet.
    repeated AccessConfig access_config = 1 [json_name = "access_config"];

    // An array of alias IP ranges for this network interface.
    repeated AliasIpRange alias_ip_range = 2 [json_name = "alias_ip_range"];

    // One of EXTERNAL, INTERNAL to indicate whether the IP can be accessed from the Internet. This field is always inherited from its subnetwork.
    string ipv6_access_type = 3 [json_name = "ipv6_access_type"];

    // The name of the interface
    string name = 4;

    // The name or self_link of the network attached to this interface.
    string network = 5;

    // The private IP address assigned to the instance.
    string network_ip = 6 [json_name = "network_ip"];

    // The type of vNIC to be used on this interface. Possible values:GVNIC, VIRTIO_NET
    string nic_type = 7 [json_name = "nic_type"];

    // The networking queue count that's specified by users for the network interface. Both Rx and Tx queues will be set to this number. It will be empty if not specified.
    int64 queue_count = 8 [json_name = "queue_count"];

    // The stack type for this network interface to identify whether the IPv6 feature is enabled or not. If not specified, IPV4_ONLY will be used.
    string stack_type = 9 [json_name = "stack_type"];

    // The name or self_link of the subnetwork attached to this interface.
    string subnetwork = 10;

    // The project in which the subnetwork belongs.
    string subnetwork_project = 11 [json_name = "subnetwork_project"];

    repeated Ipv6AccessConfig ipv6_access_config = 12 [json_name = "ipv6_access_config"];

    message AccessConfig {
      // nat_ip: string
      string nat_ip = 1 [json_name = "nat_ip"];

      // network_tier: string
      string network_tier = 2 [json_name = "network_tier"];

      // public_ptr_domain_name: string
      string public_ptr_domain_name = 3 [json_name = "public_ptr_domain_name"];
    }

    message AliasIpRange {
      // ip_cidr_range: string
      string ip_cidr_range = 1 [json_name = "ip_cidr_range"];

      // subnetwork_range_name: string
      string subnetwork_range_name = 2 [json_name = "subnetwork_range_name"];
    }

    message Ipv6AccessConfig {
      // The first IPv6 address of the external IPv6 range associated with this instance, prefix length is stored in externalIpv6PrefixLength in ipv6AccessConfig. The field is output only, an IPv6 address from a subnetwork associated with the instance will be allocated dynamically.
      string external_ipv6 = 1 [json_name = "external_ipv6"];

      // The prefix length of the external IPv6 range.
      string external_ipv6_prefix_length = 2 [json_name = "external_ipv6_prefix_length"];

      // The service-level to be provided for IPv6 traffic when the subnet has an external subnet. Only PREMIUM tier is valid for IPv6
      string network_tier = 3 [json_name = "network_tier"];

      // The domain name to be used when creating DNSv6 records for the external IPv6 ranges.
      string public_ptr_domain_name = 4 [json_name = "public_ptr_domain_name"];
    }
  }

  message NetworkPerformanceConfig {
    // The egress bandwidth tier to enable. Possible values:TIER_1, DEFAULT
    string total_egress_bandwidth_tier = 1 [json_name = "total_egress_bandwidth_tier"];
  }

  message ReservationAffinity {
    // The type of reservation from which this instance can consume resources.
    string type = 1;

    SpecificReservation specific_reservation = 2 [json_name = "specific_reservation"];

    message SpecificReservation {
      // Corresponds to the label key of a reservation resource. To target a SPECIFIC_RESERVATION by name, specify compute.googleapis.com/reservation-name as the key and specify the name of your reservation as the only value.
      string key = 1;

      // Corresponds to the label values of a reservation resource.
      repeated string values = 2;
    }
  }

  message Scheduling {
    // Specifies if the instance should be restarted if it was terminated by Compute Engine (not a user).
    bool automatic_restart = 1 [json_name = "automatic_restart"];

    // Specifies the action GCE should take when SPOT VM is preempted.
    string instance_termination_action = 2 [json_name = "instance_termination_action"];

    int64 min_node_cpus = 3 [json_name = "min_node_cpus"];

    // Describes maintenance behavior for the instance. One of MIGRATE or TERMINATE,
    string on_host_maintenance = 4 [json_name = "on_host_maintenance"];

    // Whether the instance is preemptible.
    bool preemptible = 5;

    // Whether the instance is spot. If this is set as SPOT.
    string provisioning_model = 6 [json_name = "provisioning_model"];

    repeated NodeAffinities node_affinities = 7 [json_name = "node_affinities"];

    message NodeAffinities {
      string key = 1;

      string operator = 2;

      repeated string values = 3;
    }
  }

  message ShieldedInstanceConfig {
    // Whether integrity monitoring is enabled for the instance.
    bool enable_integrity_monitoring = 1 [json_name = "enable_integrity_monitoring"];

    // Whether secure boot is enabled for the instance.
    bool enable_secure_boot = 2 [json_name = "enable_secure_boot"];

    // Whether the instance uses vTPM.
    bool enable_vtpm = 3 [json_name = "enable_vtpm"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeInstanceGroup version is 2
message GoogleComputeInstanceGroup {
  // An optional textual description of the instance group.
  string description = 1;

  string id = 2;

  // The list of instances in the group, in self_link format. When adding instances they must all be in the same network and zone as the instance group.
  repeated string instances = 3;

  // The name of the instance group. Must be 1-63 characters long and comply with RFC1035. Supported characters include lowercase letters, numbers, and hyphens.
  string name = 4;

  // The URL of the network the instance group is in. If this is different from the network where the instances are in, the creation fails. Defaults to the network where the instances are in (if neither network nor instances is specified, this field will be blank).
  string network = 5;

  // The ID of the project in which the resource belongs. If it is not provided, the provider project is used.
  string project = 6;

  // The URI of the created resource.
  string self_link = 7 [json_name = "self_link"];

  // The number of instances in the group.
  int64 size = 8;

  // The zone that this instance group should be created in.
  string zone = 9;

  repeated NamedPort named_port = 10 [json_name = "named_port"];

  repeated Timeouts timeouts = 11;

  map<string, string> for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  int32 count = 14;

  string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;

  message NamedPort {
    // The name which the port will be mapped to.
    string name = 1;

    // The port number to map the name to.
    int64 port = 2;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeInstanceGroupManager version is 0
message GoogleComputeInstanceGroupManager {
  // The base instance name to use for instances in this group. The value must be a valid RFC1035 name. Supported characters are lowercase letters, numbers, and hyphens (-). Instances are named by appending a hyphen and a random four-character string to the base instance name.
  string base_instance_name = 1 [json_name = "base_instance_name"];

  // An optional textual description of the instance group manager.
  string description = 2;

  // The fingerprint of the instance group manager.
  string fingerprint = 3;

  string id = 4;

  // The full URL of the instance group created by the manager.
  string instance_group = 5 [json_name = "instance_group"];

  // Pagination behavior of the listManagedInstances API method for this managed instance group. Valid values are: "PAGELESS", "PAGINATED". If PAGELESS (default), Pagination is disabled for the group's listManagedInstances API method. maxResults and pageToken query parameters are ignored and all instances are returned in a single response. If PAGINATED, pagination is enabled, maxResults and pageToken query parameters are respected.
  string list_managed_instances_results = 6 [json_name = "list_managed_instances_results"];

  // The name of the instance group manager. Must be 1-63 characters long and comply with RFC1035. Supported characters include lowercase letters, numbers, and hyphens.
  string name = 7;

  string operation = 8;

  // The ID of the project in which the resource belongs. If it is not provided, the provider project is used.
  string project = 9;

  // The URL of the created resource.
  string self_link = 10 [json_name = "self_link"];

  // The status of this managed instance group.
  repeated Status status = 11;

  // The full URL of all target pools to which new instances in the group are added. Updating the target pools attribute does not affect existing instances.
  repeated string target_pools = 12 [json_name = "target_pools"];

  // The target number of running instances for this managed instance group. This value should always be explicitly set unless this resource is attached to an autoscaler, in which case it should never be set. Defaults to 0.
  int64 target_size = 13 [json_name = "target_size"];

  // Whether to wait for all instances to be created/updated before returning. Note that if this is set to true and the operation does not succeed, Terraform will continue trying until it times out.
  bool wait_for_instances = 14 [json_name = "wait_for_instances"];

  // When used with wait_for_instances specifies the status to wait for. When STABLE is specified this resource will wait until the instances are stable before returning. When UPDATED is set, it will wait for the version target to be reached and any per instance configs to be effective as well as all instances to be stable before returning.
  string wait_for_instances_status = 15 [json_name = "wait_for_instances_status"];

  // The zone that instances in this group should be created in.
  string zone = 16;

  AutoHealingPolicies auto_healing_policies = 17 [json_name = "auto_healing_policies"];

  repeated NamedPort named_port = 18 [json_name = "named_port"];

  repeated StatefulDisk stateful_disk = 19 [json_name = "stateful_disk"];

  repeated Timeouts timeouts = 20;

  UpdatePolicy update_policy = 21 [json_name = "update_policy"];

  repeated Version version = 22;

  map<string, string> for_each = 23 [json_name = "for_each"];

  repeated string depends_on = 24 [json_name = "depends_on"];

  int32 count = 25;

  string provider = 26;

  terraform.v1.Lifecycle lifecycle = 27;

  message Status {
    // is_stable: bool
    bool is_stable = 1 [json_name = "is_stable"];

    // stateful: list of object
    repeated Stateful stateful = 2;

    // version_target: list of object
    repeated VersionTarget version_target = 3 [json_name = "version_target"];

    message Stateful {
      // has_stateful_config: bool
      bool has_stateful_config = 1 [json_name = "has_stateful_config"];

      // per_instance_configs: list of object
      repeated PerInstanceConfigs per_instance_configs = 2 [json_name = "per_instance_configs"];

      message PerInstanceConfigs {
        // all_effective: bool
        bool all_effective = 1 [json_name = "all_effective"];
      }
    }

    message VersionTarget {
      // is_reached: bool
      bool is_reached = 1 [json_name = "is_reached"];
    }
  }

  message AutoHealingPolicies {
    // The health check resource that signals autohealing.
    string health_check = 1 [json_name = "health_check"];

    // The number of seconds that the managed instance group waits before it applies autohealing policies to new instances or recently recreated instances. Between 0 and 3600.
    int64 initial_delay_sec = 2 [json_name = "initial_delay_sec"];
  }

  message NamedPort {
    // The name of the port.
    string name = 1;

    // The port number.
    int64 port = 2;
  }

  message StatefulDisk {
    // A value that prescribes what should happen to the stateful disk when the VM instance is deleted. The available options are NEVER and ON_PERMANENT_INSTANCE_DELETION. NEVER - detach the disk when the VM is deleted, but do not delete the disk. ON_PERMANENT_INSTANCE_DELETION will delete the stateful disk when the VM is permanently deleted from the instance group. The default is NEVER.
    string delete_rule = 1 [json_name = "delete_rule"];

    // The device name of the disk to be attached.
    string device_name = 2 [json_name = "device_name"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }

  message UpdatePolicy {
    // The maximum number of instances that can be created above the specified targetSize during the update process. Conflicts with max_surge_percent. If neither is set, defaults to 1
    int64 max_surge_fixed = 1 [json_name = "max_surge_fixed"];

    // The maximum number of instances(calculated as percentage) that can be created above the specified targetSize during the update process. Conflicts with max_surge_fixed.
    int64 max_surge_percent = 2 [json_name = "max_surge_percent"];

    // The maximum number of instances that can be unavailable during the update process. Conflicts with max_unavailable_percent. If neither is set, defaults to 1.
    int64 max_unavailable_fixed = 3 [json_name = "max_unavailable_fixed"];

    // The maximum number of instances(calculated as percentage) that can be unavailable during the update process. Conflicts with max_unavailable_fixed.
    int64 max_unavailable_percent = 4 [json_name = "max_unavailable_percent"];

    // Minimal action to be taken on an instance. You can specify either REFRESH to update without stopping instances, RESTART to restart existing instances or REPLACE to delete and create new instances from the target template. If you specify a REFRESH, the Updater will attempt to perform that action only. However, if the Updater determines that the minimal action you specify is not enough to perform the update, it might perform a more disruptive action.
    string minimal_action = 5 [json_name = "minimal_action"];

    // Most disruptive action that is allowed to be taken on an instance. You can specify either NONE to forbid any actions, REFRESH to allow actions that do not need instance restart, RESTART to allow actions that can be applied without instance replacing or REPLACE to allow all possible actions. If the Updater determines that the minimal update action needed is more disruptive than most disruptive allowed action you specify it will not perform the update at all.
    string most_disruptive_allowed_action = 6 [json_name = "most_disruptive_allowed_action"];

    // The instance replacement method for managed instance groups. Valid values are: "RECREATE", "SUBSTITUTE". If SUBSTITUTE (default), the group replaces VM instances with new instances that have randomly generated names. If RECREATE, instance names are preserved.  You must also set max_unavailable_fixed or max_unavailable_percent to be greater than 0.
    string replacement_method = 7 [json_name = "replacement_method"];

    // The type of update process. You can specify either PROACTIVE so that the instance group manager proactively executes actions in order to bring instances to their target versions or OPPORTUNISTIC so that no action is proactively executed but the update will be performed as part of other actions (for example, resizes or recreateInstances calls).
    string type = 8;
  }

  message Version {
    // The full URL to an instance template from which all new instances of this version will be created.
    string instance_template = 1 [json_name = "instance_template"];

    // Version name.
    string name = 2;

    TargetSize target_size = 3 [json_name = "target_size"];

    message TargetSize {
      // The number of instances which are managed for this version. Conflicts with percent.
      int64 fixed = 1;

      // The number of instances (calculated as percentage) which are managed for this version. Conflicts with fixed. Note that when using percent, rounding will be in favor of explicitly set target_size values; a managed instance group with 2 instances and 2 versions, one of which has a target_size.percent of 60 will create 2 instances of that version.
      int64 percent = 2;
    }
  }
}

// GoogleComputeInstanceGroupNamedPort version is 0
message GoogleComputeInstanceGroupNamedPort {
  // The name of the instance group.
  string group = 1;

  string id = 2;

  // The name for this named port. The name must be 1-63 characters
  // long, and comply with RFC1035.
  string name = 3;

  // The port number, which can be a value between 1 and 65535.
  int64 port = 4;

  string project = 5;

  // The zone of the instance group.
  string zone = 6;

  repeated Timeouts timeouts = 7;

  map<string, string> for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeInstanceIamBinding version is 0
message GoogleComputeInstanceIamBinding {
  string etag = 1;

  string id = 2;

  string instance_name = 3 [json_name = "instance_name"];

  repeated string members = 4;

  string project = 5;

  string role = 6;

  string zone = 7;

  Condition condition = 8;

  map<string, string> for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    string description = 1;

    string expression = 2;

    string title = 3;
  }
}

// GoogleComputeInstanceIamMember version is 0
message GoogleComputeInstanceIamMember {
  string etag = 1;

  string id = 2;

  string instance_name = 3 [json_name = "instance_name"];

  string member = 4;

  string project = 5;

  string role = 6;

  string zone = 7;

  Condition condition = 8;

  map<string, string> for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    string description = 1;

    string expression = 2;

    string title = 3;
  }
}

// GoogleComputeInstanceIamPolicy version is 0
message GoogleComputeInstanceIamPolicy {
  string etag = 1;

  string id = 2;

  string instance_name = 3 [json_name = "instance_name"];

  string policy_data = 4 [json_name = "policy_data"];

  string project = 5;

  string zone = 6;

  map<string, string> for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// GoogleComputeInstanceTemplate version is 1
message GoogleComputeInstanceTemplate {
  // Whether to allow sending and receiving of packets with non-matching source or destination IPs. This defaults to false.
  bool can_ip_forward = 1 [json_name = "can_ip_forward"];

  // A brief description of this resource.
  string description = 2;

  string id = 3;

  // A description of the instance.
  string instance_description = 4 [json_name = "instance_description"];

  // A set of key/value label pairs to assign to instances created from this template,
  map<string, string> labels = 5;

  // The machine type to create. To create a machine with a custom type (such as extended memory), format the value like custom-VCPUS-MEM_IN_MB like custom-6-20480 for 6 vCPU and 20GB of RAM.
  string machine_type = 6 [json_name = "machine_type"];

  // Metadata key/value pairs to make available from within instances created from this template.
  map<string, string> metadata = 7;

  // The unique fingerprint of the metadata.
  string metadata_fingerprint = 8 [json_name = "metadata_fingerprint"];

  // An alternative to using the startup-script metadata key, mostly to match the compute_instance resource. This replaces the startup-script metadata key on the created instance and thus the two mechanisms are not allowed to be used simultaneously.
  string metadata_startup_script = 9 [json_name = "metadata_startup_script"];

  // Specifies a minimum CPU platform. Applicable values are the friendly names of CPU platforms, such as Intel Haswell or Intel Skylake.
  string min_cpu_platform = 10 [json_name = "min_cpu_platform"];

  // The name of the instance template. If you leave this blank, Terraform will auto-generate a unique name.
  string name = 11;

  // Creates a unique name beginning with the specified prefix. Conflicts with name.
  string name_prefix = 12 [json_name = "name_prefix"];

  // The ID of the project in which the resource belongs. If it is not provided, the provider project is used.
  string project = 13;

  // An instance template is a global resource that is not bound to a zone or a region. However, you can still specify some regional resources in an instance template, which restricts the template to the region where that resource resides. For example, a custom subnetwork resource is tied to a specific region. Defaults to the region of the Provider if no value is given.
  string region = 14;

  // A list of self_links of resource policies to attach to the instance. Currently a max of 1 resource policy is supported.
  repeated string resource_policies = 15 [json_name = "resource_policies"];

  // The URI of the created resource.
  string self_link = 16 [json_name = "self_link"];

  // A special URI of the created resource that uniquely identifies this instance template.
  string self_link_unique = 17 [json_name = "self_link_unique"];

  // Tags to attach to the instance.
  repeated string tags = 18;

  // The unique fingerprint of the tags.
  string tags_fingerprint = 19 [json_name = "tags_fingerprint"];

  AdvancedMachineFeatures advanced_machine_features = 20 [json_name = "advanced_machine_features"];

  ConfidentialInstanceConfig confidential_instance_config = 21 [json_name = "confidential_instance_config"];

  repeated Disk disk = 22;

  repeated GuestAccelerator guest_accelerator = 23 [json_name = "guest_accelerator"];

  repeated NetworkInterface network_interface = 24 [json_name = "network_interface"];

  NetworkPerformanceConfig network_performance_config = 25 [json_name = "network_performance_config"];

  ReservationAffinity reservation_affinity = 26 [json_name = "reservation_affinity"];

  Scheduling scheduling = 27;

  ServiceAccount service_account = 28 [json_name = "service_account"];

  ShieldedInstanceConfig shielded_instance_config = 29 [json_name = "shielded_instance_config"];

  repeated Timeouts timeouts = 30;

  map<string, string> for_each = 31 [json_name = "for_each"];

  repeated string depends_on = 32 [json_name = "depends_on"];

  int32 count = 33;

  string provider = 34;

  terraform.v1.Lifecycle lifecycle = 35;

  message AdvancedMachineFeatures {
    // Whether to enable nested virtualization or not.
    bool enable_nested_virtualization = 1 [json_name = "enable_nested_virtualization"];

    // The number of threads per physical core. To disable simultaneous multithreading (SMT) set this to 1. If unset, the maximum number of threads supported per core by the underlying processor is assumed.
    int64 threads_per_core = 2 [json_name = "threads_per_core"];

    // The number of physical cores to expose to an instance. Multiply by the number of threads per core to compute the total number of virtual CPUs to expose to the instance. If unset, the number of cores is inferred from the instance\'s nominal CPU count and the underlying platform\'s SMT width.
    int64 visible_core_count = 3 [json_name = "visible_core_count"];
  }

  message ConfidentialInstanceConfig {
    // Defines whether the instance should have confidential compute enabled.
    bool enable_confidential_compute = 1 [json_name = "enable_confidential_compute"];
  }

  message Disk {
    // Whether or not the disk should be auto-deleted. This defaults to true.
    bool auto_delete = 1 [json_name = "auto_delete"];

    // Indicates that this is a boot disk.
    bool boot = 2;

    // A unique device name that is reflected into the /dev/ tree of a Linux operating system running within the instance. If not specified, the server chooses a default device name to apply to this disk.
    string device_name = 3 [json_name = "device_name"];

    // Name of the disk. When not provided, this defaults to the name of the instance.
    string disk_name = 4 [json_name = "disk_name"];

    // The size of the image in gigabytes. If not specified, it will inherit the size of its base image. For SCRATCH disks, the size must be one of 375 or 3000 GB, with a default of 375 GB.
    int64 disk_size_gb = 5 [json_name = "disk_size_gb"];

    // The Google Compute Engine disk type. Such as "pd-ssd", "local-ssd", "pd-balanced" or "pd-standard".
    string disk_type = 6 [json_name = "disk_type"];

    // Specifies the disk interface to use for attaching this disk.
    string interface = 7;

    // A set of key/value label pairs to assign to disks,
    map<string, string> labels = 8;

    // The mode in which to attach this disk, either READ_WRITE or READ_ONLY. If you are attaching or creating a boot disk, this must read-write mode.
    string mode = 9;

    // A list (short name or id) of resource policies to attach to this disk. Currently a max of 1 resource policy is supported.
    repeated string resource_policies = 10 [json_name = "resource_policies"];

    // The name (not self_link) of the disk (such as those managed by google_compute_disk) to attach. ~> Note: Either source or source_image is required when creating a new instance except for when creating a local SSD.
    string source = 11;

    // The image from which to initialize this disk. This can be one of: the image's self_link, projects/{project}/global/images/{image}, projects/{project}/global/images/family/{family}, global/images/{image}, global/images/family/{family}, family/{family}, {project}/{family}, {project}/{image}, {family}, or {image}. ~> Note: Either source or source_image is required when creating a new instance except for when creating a local SSD.
    string source_image = 12 [json_name = "source_image"];

    // The source snapshot to create this disk. When creating
    // a new instance, one of initializeParams.sourceSnapshot,
    // initializeParams.sourceImage, or disks.source is
    // required except for local SSD.
    string source_snapshot = 13 [json_name = "source_snapshot"];

    // The type of Google Compute Engine disk, can be either "SCRATCH" or "PERSISTENT".
    string type = 14;

    DiskEncryptionKey disk_encryption_key = 15 [json_name = "disk_encryption_key"];

    SourceImageEncryptionKey source_image_encryption_key = 16 [json_name = "source_image_encryption_key"];

    SourceSnapshotEncryptionKey source_snapshot_encryption_key = 17 [json_name = "source_snapshot_encryption_key"];

    message DiskEncryptionKey {
      // The self link of the encryption key that is stored in Google Cloud KMS.
      string kms_key_self_link = 1 [json_name = "kms_key_self_link"];
    }

    message SourceImageEncryptionKey {
      // The self link of the encryption key that is stored in
      // Google Cloud KMS.
      string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

      // The service account being used for the encryption
      // request for the given KMS key. If absent, the Compute
      // Engine default service account is used.
      string kms_key_service_account = 2 [json_name = "kms_key_service_account"];
    }

    message SourceSnapshotEncryptionKey {
      // The self link of the encryption key that is stored in
      // Google Cloud KMS.
      string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

      // The service account being used for the encryption
      // request for the given KMS key. If absent, the Compute
      // Engine default service account is used.
      string kms_key_service_account = 2 [json_name = "kms_key_service_account"];
    }
  }

  message GuestAccelerator {
    // The number of the guest accelerator cards exposed to this instance.
    int64 count = 1;

    // The accelerator type resource to expose to this instance. E.g. nvidia-tesla-k80.
    string type = 2;
  }

  message NetworkInterface {
    // One of EXTERNAL, INTERNAL to indicate whether the IP can be accessed from the Internet. This field is always inherited from its subnetwork.
    string ipv6_access_type = 1 [json_name = "ipv6_access_type"];

    // The name of the network_interface.
    string name = 2;

    // The name or self_link of the network to attach this interface to. Use network attribute for Legacy or Auto subnetted networks and subnetwork for custom subnetted networks.
    string network = 3;

    // The private IP address to assign to the instance. If empty, the address will be automatically assigned.
    string network_ip = 4 [json_name = "network_ip"];

    // The type of vNIC to be used on this interface. Possible values:GVNIC, VIRTIO_NET
    string nic_type = 5 [json_name = "nic_type"];

    // The networking queue count that's specified by users for the network interface. Both Rx and Tx queues will be set to this number. It will be empty if not specified.
    int64 queue_count = 6 [json_name = "queue_count"];

    // The stack type for this network interface to identify whether the IPv6 feature is enabled or not. If not specified, IPV4_ONLY will be used.
    string stack_type = 7 [json_name = "stack_type"];

    // The name of the subnetwork to attach this interface to. The subnetwork must exist in the same region this instance will be created in. Either network or subnetwork must be provided.
    string subnetwork = 8;

    // The ID of the project in which the subnetwork belongs. If it is not provided, the provider project is used.
    string subnetwork_project = 9 [json_name = "subnetwork_project"];

    repeated AccessConfig access_config = 10 [json_name = "access_config"];

    repeated AliasIpRange alias_ip_range = 11 [json_name = "alias_ip_range"];

    repeated Ipv6AccessConfig ipv6_access_config = 12 [json_name = "ipv6_access_config"];

    message AccessConfig {
      // The IP address that will be 1:1 mapped to the instance's network ip. If not given, one will be generated.
      string nat_ip = 1 [json_name = "nat_ip"];

      // The networking tier used for configuring this instance template. This field can take the following values: PREMIUM, STANDARD, FIXED_STANDARD. If this field is not specified, it is assumed to be PREMIUM.
      string network_tier = 2 [json_name = "network_tier"];

      // The DNS domain name for the public PTR record.The DNS domain name for the public PTR record.
      string public_ptr_domain_name = 3 [json_name = "public_ptr_domain_name"];
    }

    message AliasIpRange {
      // The IP CIDR range represented by this alias IP range. This IP CIDR range must belong to the specified subnetwork and cannot contain IP addresses reserved by system or used by other network interfaces. At the time of writing only a netmask (e.g. /24) may be supplied, with a CIDR format resulting in an API error.
      string ip_cidr_range = 1 [json_name = "ip_cidr_range"];

      // The subnetwork secondary range name specifying the secondary range from which to allocate the IP CIDR range for this alias IP range. If left unspecified, the primary range of the subnetwork will be used.
      string subnetwork_range_name = 2 [json_name = "subnetwork_range_name"];
    }

    message Ipv6AccessConfig {
      // The first IPv6 address of the external IPv6 range associated with this instance, prefix length is stored in externalIpv6PrefixLength in ipv6AccessConfig. The field is output only, an IPv6 address from a subnetwork associated with the instance will be allocated dynamically.
      string external_ipv6 = 1 [json_name = "external_ipv6"];

      // The prefix length of the external IPv6 range.
      string external_ipv6_prefix_length = 2 [json_name = "external_ipv6_prefix_length"];

      // The service-level to be provided for IPv6 traffic when the subnet has an external subnet. Only PREMIUM tier is valid for IPv6
      string network_tier = 3 [json_name = "network_tier"];

      // The domain name to be used when creating DNSv6 records for the external IPv6 ranges.
      string public_ptr_domain_name = 4 [json_name = "public_ptr_domain_name"];
    }
  }

  message NetworkPerformanceConfig {
    // The egress bandwidth tier to enable. Possible values:TIER_1, DEFAULT
    string total_egress_bandwidth_tier = 1 [json_name = "total_egress_bandwidth_tier"];
  }

  message ReservationAffinity {
    // The type of reservation from which this instance can consume resources.
    string type = 1;

    SpecificReservation specific_reservation = 2 [json_name = "specific_reservation"];

    message SpecificReservation {
      // Corresponds to the label key of a reservation resource. To target a SPECIFIC_RESERVATION by name, specify compute.googleapis.com/reservation-name as the key and specify the name of your reservation as the only value.
      string key = 1;

      // Corresponds to the label values of a reservation resource.
      repeated string values = 2;
    }
  }

  message Scheduling {
    // Specifies whether the instance should be automatically restarted if it is terminated by Compute Engine (not terminated by a user). This defaults to true.
    bool automatic_restart = 1 [json_name = "automatic_restart"];

    // Specifies the action GCE should take when SPOT VM is preempted.
    string instance_termination_action = 2 [json_name = "instance_termination_action"];

    // Minimum number of cpus for the instance.
    int64 min_node_cpus = 3 [json_name = "min_node_cpus"];

    // Defines the maintenance behavior for this instance.
    string on_host_maintenance = 4 [json_name = "on_host_maintenance"];

    // Allows instance to be preempted. This defaults to false.
    bool preemptible = 5;

    // Whether the instance is spot. If this is set as SPOT.
    string provisioning_model = 6 [json_name = "provisioning_model"];

    repeated NodeAffinities node_affinities = 7 [json_name = "node_affinities"];

    message NodeAffinities {
      string key = 1;

      string operator = 2;

      repeated string values = 3;
    }
  }

  message ServiceAccount {
    // The service account e-mail address. If not given, the default Google Compute Engine service account is used.
    string email = 1;

    // A list of service scopes. Both OAuth2 URLs and gcloud short names are supported. To allow full access to all Cloud APIs, use the cloud-platform scope.
    repeated string scopes = 2;
  }

  message ShieldedInstanceConfig {
    // Compare the most recent boot measurements to the integrity policy baseline and return a pair of pass/fail results depending on whether they match or not. Defaults to true.
    bool enable_integrity_monitoring = 1 [json_name = "enable_integrity_monitoring"];

    // Verify the digital signature of all boot components, and halt the boot process if signature verification fails. Defaults to false.
    bool enable_secure_boot = 2 [json_name = "enable_secure_boot"];

    // Use a virtualized trusted platform module, which is a specialized computer chip you can use to encrypt objects like keys and certificates. Defaults to true.
    bool enable_vtpm = 3 [json_name = "enable_vtpm"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeInterconnectAttachment version is 0
message GoogleComputeInterconnectAttachment {
  // Whether the VLAN attachment is enabled or disabled.  When using
  // PARTNER type this will Pre-Activate the interconnect attachment
  bool admin_enabled = 1 [json_name = "admin_enabled"];

  // Provisioned bandwidth capacity for the interconnect attachment.
  // For attachments of type DEDICATED, the user can set the bandwidth.
  // For attachments of type PARTNER, the Google Partner that is operating the interconnect must set the bandwidth.
  // Output only for PARTNER type, mutable for PARTNER_PROVIDER and DEDICATED,
  // Defaults to BPS_10G Possible values: ["BPS_50M", "BPS_100M", "BPS_200M", "BPS_300M", "BPS_400M", "BPS_500M", "BPS_1G", "BPS_2G", "BPS_5G", "BPS_10G", "BPS_20G", "BPS_50G"]
  string bandwidth = 2;

  // Up to 16 candidate prefixes that can be used to restrict the allocation
  // of cloudRouterIpAddress and customerRouterIpAddress for this attachment.
  // All prefixes must be within link-local address space (169.254.0.0/16)
  // and must be /29 or shorter (/28, /27, etc). Google will attempt to select
  // an unused /29 from the supplied candidate prefix(es). The request will
  // fail if all possible /29s are in use on Google's edge. If not supplied,
  // Google will randomly select an unused /29 from all of link-local space.
  repeated string candidate_subnets = 3 [json_name = "candidate_subnets"];

  // IPv4 address + prefix length to be configured on Cloud Router
  // Interface for this interconnect attachment.
  string cloud_router_ip_address = 4 [json_name = "cloud_router_ip_address"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 5 [json_name = "creation_timestamp"];

  // IPv4 address + prefix length to be configured on the customer
  // router subinterface for this interconnect attachment.
  string customer_router_ip_address = 6 [json_name = "customer_router_ip_address"];

  // An optional description of this resource.
  string description = 7;

  // Desired availability domain for the attachment. Only available for type
  // PARTNER, at creation time. For improved reliability, customers should
  // configure a pair of attachments with one per availability domain. The
  // selected availability domain will be provided to the Partner via the
  // pairing key so that the provisioned circuit will lie in the specified
  // domain. If not specified, the value will default to AVAILABILITY_DOMAIN_ANY.
  string edge_availability_domain = 8 [json_name = "edge_availability_domain"];

  // Indicates the user-supplied encryption option of this interconnect
  // attachment. Can only be specified at attachment creation for PARTNER or
  // DEDICATED attachments.
  //
  // * NONE - This is the default value, which means that the VLAN attachment
  // carries unencrypted traffic. VMs are able to send traffic to, or receive
  // traffic from, such a VLAN attachment.
  //
  // * IPSEC - The VLAN attachment carries only encrypted traffic that is
  // encrypted by an IPsec device, such as an HA VPN gateway or third-party
  // IPsec VPN. VMs cannot directly send traffic to, or receive traffic from,
  // such a VLAN attachment. To use HA VPN over Cloud Interconnect, the VLAN
  // attachment must be created with this option. Default value: "NONE" Possible values: ["NONE", "IPSEC"]
  string encryption = 9;

  // Google reference ID, to be used when raising support tickets with
  // Google or otherwise to debug backend connectivity issues.
  string google_reference_id = 10 [json_name = "google_reference_id"];

  string id = 11;

  // URL of the underlying Interconnect object that this attachment's
  // traffic will traverse through. Required if type is DEDICATED, must not
  // be set if type is PARTNER.
  string interconnect = 12;

  // URL of addresses that have been reserved for the interconnect attachment,
  // Used only for interconnect attachment that has the encryption option as
  // IPSEC.
  //
  // The addresses must be RFC 1918 IP address ranges. When creating HA VPN
  // gateway over the interconnect attachment, if the attachment is configured
  // to use an RFC 1918 IP address, then the VPN gateway's IP address will be
  // allocated from the IP address range specified here.
  //
  // For example, if the HA VPN gateway's interface 0 is paired to this
  // interconnect attachment, then an RFC 1918 IP address for the VPN gateway
  // interface 0 will be allocated from the IP address specified for this
  // interconnect attachment.
  //
  // If this field is not specified for interconnect attachment that has
  // encryption option as IPSEC, later on when creating HA VPN gateway on this
  // interconnect attachment, the HA VPN gateway's IP address will be
  // allocated from regional external IP address pool.
  repeated string ipsec_internal_addresses = 13 [json_name = "ipsec_internal_addresses"];

  // Maximum Transmission Unit (MTU), in bytes, of packets passing through
  // this interconnect attachment. Currently, only 1440 and 1500 are allowed. If not specified, the value will default to 1440.
  string mtu = 14;

  // Name of the resource. Provided by the client when the resource is created. The
  // name must be 1-63 characters long, and comply with RFC1035. Specifically, the
  // name must be 1-63 characters long and match the regular expression
  // '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character must be a
  // lowercase letter, and all following characters must be a dash, lowercase
  // letter, or digit, except the last character, which cannot be a dash.
  string name = 15;

  // [Output only for type PARTNER. Not present for DEDICATED]. The opaque
  // identifier of an PARTNER attachment used to initiate provisioning with
  // a selected partner. Of the form "XXXXX/region/domain"
  string pairing_key = 16 [json_name = "pairing_key"];

  // [Output only for type PARTNER. Not present for DEDICATED]. Optional
  // BGP ASN for the router that should be supplied by a layer 3 Partner if
  // they configured BGP on behalf of the customer.
  string partner_asn = 17 [json_name = "partner_asn"];

  // Information specific to an InterconnectAttachment. This property
  // is populated if the interconnect that this is attached to is of type DEDICATED.
  repeated PrivateInterconnectInfo private_interconnect_info = 18 [json_name = "private_interconnect_info"];

  string project = 19;

  // Region where the regional interconnect attachment resides.
  string region = 20;

  // URL of the cloud router to be used for dynamic routing. This router must be in
  // the same region as this InterconnectAttachment. The InterconnectAttachment will
  // automatically connect the Interconnect to the network & region within which the
  // Cloud Router is configured.
  string router = 21;

  string self_link = 22 [json_name = "self_link"];

  // [Output Only] The current state of this attachment's functionality.
  string state = 23;

  // The type of InterconnectAttachment you wish to create. Defaults to
  // DEDICATED. Possible values: ["DEDICATED", "PARTNER", "PARTNER_PROVIDER"]
  string type = 24;

  // The IEEE 802.1Q VLAN tag for this attachment, in the range 2-4094. When
  // using PARTNER type this will be managed upstream.
  int64 vlan_tag8021q = 25 [json_name = "vlan_tag8021q"];

  repeated Timeouts timeouts = 26;

  map<string, string> for_each = 27 [json_name = "for_each"];

  repeated string depends_on = 28 [json_name = "depends_on"];

  int32 count = 29;

  string provider = 30;

  terraform.v1.Lifecycle lifecycle = 31;

  message PrivateInterconnectInfo {
    // tag8021q: number
    int64 tag8021q = 1;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeManagedSslCertificate version is 0
message GoogleComputeManagedSslCertificate {
  // The unique identifier for the resource.
  int64 certificate_id = 1 [json_name = "certificate_id"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 3;

  // Expire time of the certificate in RFC3339 text format.
  string expire_time = 4 [json_name = "expire_time"];

  string id = 5;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  //
  //
  // These are in the same namespace as the managed SSL certificates.
  string name = 6;

  string project = 7;

  string self_link = 8 [json_name = "self_link"];

  // Domains associated with the certificate via Subject Alternative Name.
  repeated string subject_alternative_names = 9 [json_name = "subject_alternative_names"];

  // Enum field whose value is always 'MANAGED' - used to signal to the API
  // which type this is. Default value: "MANAGED" Possible values: ["MANAGED"]
  string type = 10;

  Managed managed = 11;

  repeated Timeouts timeouts = 12;

  map<string, string> for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  int32 count = 15;

  string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Managed {
    // Domains for which a managed SSL certificate will be valid.  Currently,
    // there can be up to 100 domains in this list.
    repeated string domains = 1;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeNetwork version is 0
message GoogleComputeNetwork {
  // When set to 'true', the network is created in "auto subnet mode" and
  // it will create a subnet for each region automatically across the
  // '10.128.0.0/9' address range.
  //
  // When set to 'false', the network is created in "custom subnet mode" so
  // the user can explicitly connect subnetwork resources.
  bool auto_create_subnetworks = 1 [json_name = "auto_create_subnetworks"];

  // If set to 'true', default routes ('0.0.0.0/0') will be deleted
  // immediately after network creation. Defaults to 'false'.
  bool delete_default_routes_on_create = 2 [json_name = "delete_default_routes_on_create"];

  // An optional description of this resource. The resource must be
  // recreated to modify this field.
  string description = 3;

  // Enable ULA internal ipv6 on this network. Enabling this feature will assign
  // a /48 from google defined ULA prefix fd20::/20.
  bool enable_ula_internal_ipv6 = 4 [json_name = "enable_ula_internal_ipv6"];

  // The gateway address for default routing out of the network. This value
  // is selected by GCP.
  string gateway_ipv4 = 5 [json_name = "gateway_ipv4"];

  string id = 6;

  // When enabling ula internal ipv6, caller optionally can specify the /48 range
  // they want from the google defined ULA prefix fd20::/20. The input must be a
  // valid /48 ULA IPv6 address and must be within the fd20::/20. Operation will
  // fail if the speficied /48 is already in used by another resource.
  // If the field is not speficied, then a /48 range will be randomly allocated from fd20::/20 and returned via this field.
  string internal_ipv6_range = 7 [json_name = "internal_ipv6_range"];

  // Maximum Transmission Unit in bytes. The default value is 1460 bytes.
  // The minimum value for this field is 1300 and the maximum value is 8896 bytes (jumbo frames).
  // Note that packets larger than 1500 bytes (standard Ethernet) can be subject to TCP-MSS clamping or dropped
  // with an ICMP 'Fragmentation-Needed' message if the packets are routed to the Internet or other VPCs
  // with varying MTUs.
  int64 mtu = 8;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 9;

  // Set the order that Firewall Rules and Firewall Policies are evaluated. Default value: "AFTER_CLASSIC_FIREWALL" Possible values: ["BEFORE_CLASSIC_FIREWALL", "AFTER_CLASSIC_FIREWALL"]
  string network_firewall_policy_enforcement_order = 10 [
    json_name = "network_firewall_policy_enforcement_order"
  ];

  string project = 11;

  // The network-wide routing mode to use. If set to 'REGIONAL', this
  // network's cloud routers will only advertise routes with subnetworks
  // of this network in the same region as the router. If set to 'GLOBAL',
  // this network's cloud routers will advertise routes with all
  // subnetworks of this network, across regions. Possible values: ["REGIONAL", "GLOBAL"]
  string routing_mode = 12 [json_name = "routing_mode"];

  string self_link = 13 [json_name = "self_link"];

  repeated Timeouts timeouts = 14;

  map<string, string> for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  int32 count = 17;

  string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeNetworkEndpoint version is 0
message GoogleComputeNetworkEndpoint {
  string id = 1;

  // The name for a specific VM instance that the IP address belongs to.
  // This is required for network endpoints of type GCE_VM_IP_PORT.
  // The instance must be in the same zone of network endpoint group.
  string instance = 2;

  // IPv4 address of network endpoint. The IP address must belong
  // to a VM in GCE (either the primary IP or as part of an aliased IP
  // range).
  string ip_address = 3 [json_name = "ip_address"];

  // The network endpoint group this endpoint is part of.
  string network_endpoint_group = 4 [json_name = "network_endpoint_group"];

  // Port number of network endpoint.
  // **Note** 'port' is required unless the Network Endpoint Group is created
  // with the type of 'GCE_VM_IP'
  int64 port = 5;

  string project = 6;

  // Zone where the containing network endpoint group is located.
  string zone = 7;

  repeated Timeouts timeouts = 8;

  map<string, string> for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeNetworkEndpointGroup version is 0
message GoogleComputeNetworkEndpointGroup {
  // The default port used if the port number is not specified in the
  // network endpoint.
  int64 default_port = 1 [json_name = "default_port"];

  // An optional description of this resource. Provide this property when
  // you create the resource.
  string description = 2;

  string id = 3;

  // Name of the resource; provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 4;

  // The network to which all network endpoints in the NEG belong.
  // Uses "default" project network if unspecified.
  string network = 5;

  // Type of network endpoints in this network endpoint group.
  // NON_GCP_PRIVATE_IP_PORT is used for hybrid connectivity network
  // endpoint groups (see https://cloud.google.com/load-balancing/docs/hybrid).
  // Note that NON_GCP_PRIVATE_IP_PORT can only be used with Backend Services
  // that 1) have the following load balancing schemes: EXTERNAL, EXTERNAL_MANAGED,
  // INTERNAL_MANAGED, and INTERNAL_SELF_MANAGED and 2) support the RATE or
  // CONNECTION balancing modes.
  //
  // Possible values include: GCE_VM_IP, GCE_VM_IP_PORT, and NON_GCP_PRIVATE_IP_PORT. Default value: "GCE_VM_IP_PORT" Possible values: ["GCE_VM_IP", "GCE_VM_IP_PORT", "NON_GCP_PRIVATE_IP_PORT"]
  string network_endpoint_type = 6 [json_name = "network_endpoint_type"];

  string project = 7;

  string self_link = 8 [json_name = "self_link"];

  // Number of network endpoints in the network endpoint group.
  int64 size = 9;

  // Optional subnetwork to which all network endpoints in the NEG belong.
  string subnetwork = 10;

  // Zone where the network endpoint group is located.
  string zone = 11;

  repeated Timeouts timeouts = 12;

  map<string, string> for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  int32 count = 15;

  string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeNetworkFirewallPolicy version is 0
message GoogleComputeNetworkFirewallPolicy {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional description of this resource. Provide this property when you create the resource.
  string description = 2;

  // Fingerprint of the resource. This field is used internally during updates of this resource.
  string fingerprint = 3;

  string id = 4;

  // User-provided name of the Network firewall policy. The name should be unique in the project in which the firewall policy is created. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression [a-z]([-a-z0-9]*[a-z0-9])? which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash.
  string name = 5;

  // The unique identifier for the resource. This identifier is defined by the server.
  string network_firewall_policy_id = 6 [json_name = "network_firewall_policy_id"];

  // The project for the resource
  string project = 7;

  // Total count of all firewall policy rule tuples. A firewall policy can not exceed a set number of tuples.
  int64 rule_tuple_count = 8 [json_name = "rule_tuple_count"];

  // Server-defined URL for the resource.
  string self_link = 9 [json_name = "self_link"];

  // Server-defined URL for this resource with the resource id.
  string self_link_with_id = 10 [json_name = "self_link_with_id"];

  repeated Timeouts timeouts = 11;

  map<string, string> for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  int32 count = 14;

  string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeNetworkFirewallPolicyAssociation version is 0
message GoogleComputeNetworkFirewallPolicyAssociation {
  // The target that the firewall policy is attached to.
  string attachment_target = 1 [json_name = "attachment_target"];

  // The firewall policy ID of the association.
  string firewall_policy = 2 [json_name = "firewall_policy"];

  string id = 3;

  // The name for an association.
  string name = 4;

  // The project for the resource
  string project = 5;

  // The short name of the firewall policy of the association.
  string short_name = 6 [json_name = "short_name"];

  repeated Timeouts timeouts = 7;

  map<string, string> for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeNetworkFirewallPolicyRule version is 0
message GoogleComputeNetworkFirewallPolicyRule {
  // The Action to perform when the client connection triggers the rule. Can currently be either "allow" or "deny()" where valid values for status are 403, 404, and 502.
  string action = 1;

  // An optional description for this resource.
  string description = 2;

  // The direction in which this rule applies. Possible values: INGRESS, EGRESS
  string direction = 3;

  // Denotes whether the firewall policy rule is disabled. When set to true, the firewall policy rule is not enforced and traffic behaves as if it did not exist. If this is unspecified, the firewall policy rule will be enabled.
  bool disabled = 4;

  // Denotes whether to enable logging for a particular rule. If logging is enabled, logs will be exported to the configured export destination in Stackdriver. Logs may be exported to BigQuery or Pub/Sub. Note: you cannot enable logging on "goto_next" rules.
  bool enable_logging = 5 [json_name = "enable_logging"];

  // The firewall policy of the resource.
  string firewall_policy = 6 [json_name = "firewall_policy"];

  string id = 7;

  // Type of the resource. Always `compute#firewallPolicyRule` for firewall policy rules
  string kind = 8;

  // An integer indicating the priority of a rule in the list. The priority must be a positive value between 0 and 2147483647. Rules are evaluated from highest to lowest priority where 0 is the highest priority and 2147483647 is the lowest prority.
  int64 priority = 9;

  // The project for the resource
  string project = 10;

  // An optional name for the rule. This field is not a unique identifier and can be updated.
  string rule_name = 11 [json_name = "rule_name"];

  // Calculation of the complexity of a single firewall policy rule.
  int64 rule_tuple_count = 12 [json_name = "rule_tuple_count"];

  // A list of service accounts indicating the sets of instances that are applied with this rule.
  repeated string target_service_accounts = 13 [json_name = "target_service_accounts"];

  Match match = 14;

  repeated TargetSecureTags target_secure_tags = 15 [json_name = "target_secure_tags"];

  repeated Timeouts timeouts = 16;

  map<string, string> for_each = 17 [json_name = "for_each"];

  repeated string depends_on = 18 [json_name = "depends_on"];

  int32 count = 19;

  string provider = 20;

  terraform.v1.Lifecycle lifecycle = 21;

  message Match {
    // Address groups which should be matched against the traffic destination. Maximum number of destination address groups is 10. Destination address groups is only supported in Egress rules.
    repeated string dest_address_groups = 1 [json_name = "dest_address_groups"];

    // Domain names that will be used to match against the resolved domain name of destination of traffic. Can only be specified if DIRECTION is egress.
    repeated string dest_fqdns = 2 [json_name = "dest_fqdns"];

    // CIDR IP address range. Maximum number of destination CIDR IP ranges allowed is 5000.
    repeated string dest_ip_ranges = 3 [json_name = "dest_ip_ranges"];

    // The Unicode country codes whose IP addresses will be used to match against the source of traffic. Can only be specified if DIRECTION is egress.
    repeated string dest_region_codes = 4 [json_name = "dest_region_codes"];

    // Name of the Google Cloud Threat Intelligence list.
    repeated string dest_threat_intelligences = 5 [json_name = "dest_threat_intelligences"];

    // Address groups which should be matched against the traffic source. Maximum number of source address groups is 10. Source address groups is only supported in Ingress rules.
    repeated string src_address_groups = 6 [json_name = "src_address_groups"];

    // Domain names that will be used to match against the resolved domain name of source of traffic. Can only be specified if DIRECTION is ingress.
    repeated string src_fqdns = 7 [json_name = "src_fqdns"];

    // CIDR IP address range. Maximum number of source CIDR IP ranges allowed is 5000.
    repeated string src_ip_ranges = 8 [json_name = "src_ip_ranges"];

    // The Unicode country codes whose IP addresses will be used to match against the source of traffic. Can only be specified if DIRECTION is ingress.
    repeated string src_region_codes = 9 [json_name = "src_region_codes"];

    // Name of the Google Cloud Threat Intelligence list.
    repeated string src_threat_intelligences = 10 [json_name = "src_threat_intelligences"];

    repeated Layer4Configs layer4_configs = 11 [json_name = "layer4_configs"];

    repeated SrcSecureTags src_secure_tags = 12 [json_name = "src_secure_tags"];

    message Layer4Configs {
      // The IP protocol to which this rule applies. The protocol type is required when creating a firewall rule. This value can either be one of the following well known protocol strings (`tcp`, `udp`, `icmp`, `esp`, `ah`, `ipip`, `sctp`), or the IP protocol number.
      string ip_protocol = 1 [json_name = "ip_protocol"];

      // An optional list of ports to which this rule applies. This field is only applicable for UDP or TCP protocol. Each entry must be either an integer or a range. If not specified, this rule applies to connections through any port. Example inputs include: ``.
      repeated string ports = 2;
    }

    message SrcSecureTags {
      // Name of the secure tag, created with TagManager's TagValue API. @pattern tagValues/[0-9]+
      string name = 1;

      // [Output Only] State of the secure tag, either `EFFECTIVE` or `INEFFECTIVE`. A secure tag is `INEFFECTIVE` when it is deleted or its network is deleted.
      string state = 2;
    }
  }

  message TargetSecureTags {
    // Name of the secure tag, created with TagManager's TagValue API. @pattern tagValues/[0-9]+
    string name = 1;

    // [Output Only] State of the secure tag, either `EFFECTIVE` or `INEFFECTIVE`. A secure tag is `INEFFECTIVE` when it is deleted or its network is deleted.
    string state = 2;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeNetworkPeering version is 0
message GoogleComputeNetworkPeering {
  // Whether to export the custom routes to the peer network. Defaults to false.
  bool export_custom_routes = 1 [json_name = "export_custom_routes"];

  bool export_subnet_routes_with_public_ip = 2 [json_name = "export_subnet_routes_with_public_ip"];

  string id = 3;

  // Whether to export the custom routes from the peer network. Defaults to false.
  bool import_custom_routes = 4 [json_name = "import_custom_routes"];

  bool import_subnet_routes_with_public_ip = 5 [json_name = "import_subnet_routes_with_public_ip"];

  // Name of the peering.
  string name = 6;

  // The primary network of the peering.
  string network = 7;

  // The peer network in the peering. The peer network may belong to a different project.
  string peer_network = 8 [json_name = "peer_network"];

  // Which IP version(s) of traffic and routes are allowed to be imported or exported between peer networks. The default value is IPV4_ONLY. Possible values: ["IPV4_ONLY", "IPV4_IPV6"]
  string stack_type = 9 [json_name = "stack_type"];

  // State for the peering, either ACTIVE or INACTIVE. The peering is ACTIVE when there's a matching configuration in the peer network.
  string state = 10;

  // Details about the current state of the peering.
  string state_details = 11 [json_name = "state_details"];

  repeated Timeouts timeouts = 12;

  map<string, string> for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  int32 count = 15;

  string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeNetworkPeeringRoutesConfig version is 0
message GoogleComputeNetworkPeeringRoutesConfig {
  // Whether to export the custom routes to the peer network.
  bool export_custom_routes = 1 [json_name = "export_custom_routes"];

  string id = 2;

  // Whether to import the custom routes to the peer network.
  bool import_custom_routes = 3 [json_name = "import_custom_routes"];

  // The name of the primary network for the peering.
  string network = 4;

  // Name of the peering.
  string peering = 5;

  string project = 6;

  repeated Timeouts timeouts = 7;

  map<string, string> for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeNodeGroup version is 0
message GoogleComputeNodeGroup {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional textual description of the resource.
  string description = 2;

  string id = 3;

  // The initial number of nodes in the node group. One of 'initial_size' or 'size' must be specified.
  int64 initial_size = 4 [json_name = "initial_size"];

  // Specifies how to handle instances when a node in the group undergoes maintenance. Set to one of: DEFAULT, RESTART_IN_PLACE, or MIGRATE_WITHIN_NODE_GROUP. The default value is DEFAULT.
  string maintenance_policy = 5 [json_name = "maintenance_policy"];

  // Name of the resource.
  string name = 6;

  // The URL of the node template to which this node group belongs.
  string node_template = 7 [json_name = "node_template"];

  string project = 8;

  string self_link = 9 [json_name = "self_link"];

  // The total number of nodes in the node group. One of 'initial_size' or 'size' must be specified.
  int64 size = 10;

  // Zone where this node group is located
  string zone = 11;

  AutoscalingPolicy autoscaling_policy = 12 [json_name = "autoscaling_policy"];

  MaintenanceWindow maintenance_window = 13 [json_name = "maintenance_window"];

  ShareSettings share_settings = 14 [json_name = "share_settings"];

  repeated Timeouts timeouts = 15;

  map<string, string> for_each = 16 [json_name = "for_each"];

  repeated string depends_on = 17 [json_name = "depends_on"];

  int32 count = 18;

  string provider = 19;

  terraform.v1.Lifecycle lifecycle = 20;

  message AutoscalingPolicy {
    // Maximum size of the node group. Set to a value less than or equal
    // to 100 and greater than or equal to min-nodes.
    int64 max_nodes = 1 [json_name = "max_nodes"];

    // Minimum size of the node group. Must be less
    // than or equal to max-nodes. The default value is 0.
    int64 min_nodes = 2 [json_name = "min_nodes"];

    // The autoscaling mode. Set to one of the following:
    //  - OFF: Disables the autoscaler.
    //  - ON: Enables scaling in and scaling out.
    //  - ONLY_SCALE_OUT: Enables only scaling out.
    //  You must use this mode if your node groups are configured to
    //  restart their hosted VMs on minimal servers. Possible values: ["OFF", "ON", "ONLY_SCALE_OUT"]
    string mode = 3;
  }

  message MaintenanceWindow {
    // instances.start time of the window. This must be in UTC format that resolves to one of 00:00, 04:00, 08:00, 12:00, 16:00, or 20:00. For example, both 13:00-5 and 08:00 are valid.
    string start_time = 1 [json_name = "start_time"];
  }

  message ShareSettings {
    // Node group sharing type. Possible values: ["ORGANIZATION", "SPECIFIC_PROJECTS", "LOCAL"]
    string share_type = 1 [json_name = "share_type"];

    repeated ProjectMap project_map = 2 [json_name = "project_map"];

    message ProjectMap {
      string id = 1;

      // The project id/number should be the same as the key of this project config in the project map.
      string project_id = 2 [json_name = "project_id"];
    }
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeNodeTemplate version is 0
message GoogleComputeNodeTemplate {
  // CPU overcommit. Default value: "NONE" Possible values: ["ENABLED", "NONE"]
  string cpu_overcommit_type = 1 [json_name = "cpu_overcommit_type"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // An optional textual description of the resource.
  string description = 3;

  string id = 4;

  // Name of the resource.
  string name = 5;

  // Labels to use for node affinity, which will be used in
  // instance scheduling.
  map<string, string> node_affinity_labels = 6 [json_name = "node_affinity_labels"];

  // Node type to use for nodes group that are created from this template.
  // Only one of nodeTypeFlexibility and nodeType can be specified.
  string node_type = 7 [json_name = "node_type"];

  string project = 8;

  // Region where nodes using the node template will be created.
  // If it is not provided, the provider region is used.
  string region = 9;

  string self_link = 10 [json_name = "self_link"];

  NodeTypeFlexibility node_type_flexibility = 11 [json_name = "node_type_flexibility"];

  ServerBinding server_binding = 12 [json_name = "server_binding"];

  repeated Timeouts timeouts = 13;

  map<string, string> for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  int32 count = 16;

  string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message NodeTypeFlexibility {
    // Number of virtual CPUs to use.
    string cpus = 1;

    // Use local SSD
    string local_ssd = 2 [json_name = "local_ssd"];

    // Physical memory available to the node, defined in MB.
    string memory = 3;
  }

  message ServerBinding {
    // Type of server binding policy. If 'RESTART_NODE_ON_ANY_SERVER',
    // nodes using this template will restart on any physical server
    // following a maintenance event.
    //
    // If 'RESTART_NODE_ON_MINIMAL_SERVER', nodes using this template
    // will restart on the same physical server following a maintenance
    // event, instead of being live migrated to or restarted on a new
    // physical server. This option may be useful if you are using
    // software licenses tied to the underlying server characteristics
    // such as physical sockets or cores, to avoid the need for
    // additional licenses when maintenance occurs. However, VMs on such
    // nodes will experience outages while maintenance is applied. Possible values: ["RESTART_NODE_ON_ANY_SERVER", "RESTART_NODE_ON_MINIMAL_SERVERS"]
    string type = 1;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputePacketMirroring version is 0
message GoogleComputePacketMirroring {
  // A human-readable description of the rule.
  string description = 1;

  string id = 2;

  // The name of the packet mirroring rule
  string name = 3;

  // Since only one rule can be active at a time, priority is
  // used to break ties in the case of two rules that apply to
  // the same instances.
  int64 priority = 4;

  string project = 5;

  // The Region in which the created address should reside.
  // If it is not provided, the provider region is used.
  string region = 6;

  CollectorIlb collector_ilb = 7 [json_name = "collector_ilb"];

  Filter filter = 8;

  MirroredResources mirrored_resources = 9 [json_name = "mirrored_resources"];

  Network network = 10;

  repeated Timeouts timeouts = 11;

  map<string, string> for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  int32 count = 14;

  string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;

  message CollectorIlb {
    // The URL of the forwarding rule.
    string url = 1;
  }

  message Filter {
    // IP CIDR ranges that apply as a filter on the source (ingress) or
    // destination (egress) IP in the IP header. Only IPv4 is supported.
    repeated string cidr_ranges = 1 [json_name = "cidr_ranges"];

    // Direction of traffic to mirror. Default value: "BOTH" Possible values: ["INGRESS", "EGRESS", "BOTH"]
    string direction = 2;

    // Possible IP protocols including tcp, udp, icmp and esp
    repeated string ip_protocols = 3 [json_name = "ip_protocols"];
  }

  message MirroredResources {
    // All instances with these tags will be mirrored.
    repeated string tags = 1;

    repeated Instances instances = 2;

    repeated Subnetworks subnetworks = 3;

    message Instances {
      // The URL of the instances where this rule should be active.
      string url = 1;
    }

    message Subnetworks {
      // The URL of the subnetwork where this rule should be active.
      string url = 1;
    }
  }

  message Network {
    // The full self_link URL of the network where this rule is active.
    string url = 1;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputePerInstanceConfig version is 0
message GoogleComputePerInstanceConfig {
  string id = 1;

  // The instance group manager this instance config is part of.
  string instance_group_manager = 2 [json_name = "instance_group_manager"];

  // The minimal action to perform on the instance during an update.
  // Default is 'NONE'. Possible values are:
  // * REPLACE
  // * RESTART
  // * REFRESH
  // * NONE
  string minimal_action = 3 [json_name = "minimal_action"];

  // The most disruptive action to perform on the instance during an update.
  // Default is 'REPLACE'. Possible values are:
  // * REPLACE
  // * RESTART
  // * REFRESH
  // * NONE
  string most_disruptive_allowed_action = 4 [json_name = "most_disruptive_allowed_action"];

  // The name for this per-instance config and its corresponding instance.
  string name = 5;

  string project = 6;

  // When true, deleting this config will immediately remove any specified state from the underlying instance.
  // When false, deleting this config will *not* immediately remove any state from the underlying instance.
  // State will be removed on the next instance recreation or update.
  bool remove_instance_state_on_destroy = 7 [json_name = "remove_instance_state_on_destroy"];

  // Zone where the containing instance group manager is located
  string zone = 8;

  PreservedState preserved_state = 9 [json_name = "preserved_state"];

  repeated Timeouts timeouts = 10;

  map<string, string> for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message PreservedState {
    // Preserved metadata defined for this instance. This is a list of key->value pairs.
    map<string, string> metadata = 1;

    repeated Disk disk = 2;

    message Disk {
      // A value that prescribes what should happen to the stateful disk when the VM instance is deleted.
      // The available options are 'NEVER' and 'ON_PERMANENT_INSTANCE_DELETION'.
      // 'NEVER' - detach the disk when the VM is deleted, but do not delete the disk.
      // 'ON_PERMANENT_INSTANCE_DELETION' will delete the stateful disk when the VM is permanently
      // deleted from the instance group. Default value: "NEVER" Possible values: ["NEVER", "ON_PERMANENT_INSTANCE_DELETION"]
      string delete_rule = 1 [json_name = "delete_rule"];

      // A unique device name that is reflected into the /dev/ tree of a Linux operating system running within the instance.
      string device_name = 2 [json_name = "device_name"];

      // The mode of the disk. Default value: "READ_WRITE" Possible values: ["READ_ONLY", "READ_WRITE"]
      string mode = 3;

      // The URI of an existing persistent disk to attach under the specified device-name in the format
      // 'projects/project-id/zones/zone/disks/disk-name'.
      string source = 4;
    }
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeProjectDefaultNetworkTier version is 0
message GoogleComputeProjectDefaultNetworkTier {
  string id = 1;

  // The default network tier to be configured for the project. This field can take the following values: PREMIUM or STANDARD.
  string network_tier = 2 [json_name = "network_tier"];

  // The ID of the project in which the resource belongs. If it is not provided, the provider project is used.
  string project = 3;

  repeated Timeouts timeouts = 4;

  map<string, string> for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;

  message Timeouts {
    string create = 1;
  }
}

// GoogleComputeProjectMetadata version is 0
message GoogleComputeProjectMetadata {
  string id = 1;

  // A series of key value pairs.
  map<string, string> metadata = 2;

  // The ID of the project in which the resource belongs. If it is not provided, the provider project is used.
  string project = 3;

  repeated Timeouts timeouts = 4;

  map<string, string> for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeProjectMetadataItem version is 0
message GoogleComputeProjectMetadataItem {
  string id = 1;

  // The metadata key to set.
  string key = 2;

  // The ID of the project in which the resource belongs. If it is not provided, the provider project is used.
  string project = 3;

  // The value to set for the given metadata key.
  string value = 4;

  repeated Timeouts timeouts = 5;

  map<string, string> for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  int32 count = 8;

  string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputePublicAdvertisedPrefix version is 0
message GoogleComputePublicAdvertisedPrefix {
  // An optional description of this resource.
  string description = 1;

  // The IPv4 address to be used for reverse DNS verification.
  string dns_verification_ip = 2 [json_name = "dns_verification_ip"];

  string id = 3;

  // The IPv4 address range, in CIDR format, represented by this public advertised prefix.
  string ip_cidr_range = 4 [json_name = "ip_cidr_range"];

  // Name of the resource. The name must be 1-63 characters long, and
  // comply with RFC1035. Specifically, the name must be 1-63 characters
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?'
  // which means the first character must be a lowercase letter, and all
  // following characters must be a dash, lowercase letter, or digit,
  // except the last character, which cannot be a dash.
  string name = 5;

  string project = 6;

  string self_link = 7 [json_name = "self_link"];

  repeated Timeouts timeouts = 8;

  map<string, string> for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputePublicDelegatedPrefix version is 0
message GoogleComputePublicDelegatedPrefix {
  // An optional description of this resource.
  string description = 1;

  string id = 2;

  // The IPv4 address range, in CIDR format, represented by this public advertised prefix.
  string ip_cidr_range = 3 [json_name = "ip_cidr_range"];

  // If true, the prefix will be live migrated.
  bool is_live_migration = 4 [json_name = "is_live_migration"];

  // Name of the resource. The name must be 1-63 characters long, and
  // comply with RFC1035. Specifically, the name must be 1-63 characters
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?'
  // which means the first character must be a lowercase letter, and all
  // following characters must be a dash, lowercase letter, or digit,
  // except the last character, which cannot be a dash.
  string name = 5;

  // The URL of parent prefix. Either PublicAdvertisedPrefix or PublicDelegatedPrefix.
  string parent_prefix = 6 [json_name = "parent_prefix"];

  string project = 7;

  // A region where the prefix will reside.
  string region = 8;

  string self_link = 9 [json_name = "self_link"];

  repeated Timeouts timeouts = 10;

  map<string, string> for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeRegionAutoscaler version is 0
message GoogleComputeRegionAutoscaler {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 2;

  string id = 3;

  // Name of the resource. The name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 4;

  string project = 5;

  // URL of the region where the instance group resides.
  string region = 6;

  string self_link = 7 [json_name = "self_link"];

  // URL of the managed instance group that this autoscaler will scale.
  string target = 8;

  AutoscalingPolicy autoscaling_policy = 9 [json_name = "autoscaling_policy"];

  repeated Timeouts timeouts = 10;

  map<string, string> for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message AutoscalingPolicy {
    // The number of seconds that the autoscaler should wait before it
    // starts collecting information from a new instance. This prevents
    // the autoscaler from collecting information when the instance is
    // initializing, during which the collected usage would not be
    // reliable. The default time autoscaler waits is 60 seconds.
    //
    // Virtual machine initialization times might vary because of
    // numerous factors. We recommend that you test how long an
    // instance may take to initialize. To do this, create an instance
    // and time the startup process.
    int64 cooldown_period = 1 [json_name = "cooldown_period"];

    // The maximum number of instances that the autoscaler can scale up
    // to. This is required when creating or updating an autoscaler. The
    // maximum number of replicas should not be lower than minimal number
    // of replicas.
    int64 max_replicas = 2 [json_name = "max_replicas"];

    // The minimum number of replicas that the autoscaler can scale down
    // to. This cannot be less than 0. If not provided, autoscaler will
    // choose a default value depending on maximum number of instances
    // allowed.
    int64 min_replicas = 3 [json_name = "min_replicas"];

    // Defines operating mode for this policy. Default value: "ON" Possible values: ["OFF", "ONLY_UP", "ON"]
    string mode = 4;

    CpuUtilization cpu_utilization = 5 [json_name = "cpu_utilization"];

    LoadBalancingUtilization load_balancing_utilization = 6 [json_name = "load_balancing_utilization"];

    repeated Metric metric = 7;

    ScaleInControl scale_in_control = 8 [json_name = "scale_in_control"];

    repeated ScalingSchedules scaling_schedules = 9 [json_name = "scaling_schedules"];

    message CpuUtilization {
      // Indicates whether predictive autoscaling based on CPU metric is enabled. Valid values are:
      //
      // - NONE (default). No predictive method is used. The autoscaler scales the group to meet current demand based on real-time metrics.
      //
      // - OPTIMIZE_AVAILABILITY. Predictive autoscaling improves availability by monitoring daily and weekly load patterns and scaling out ahead of anticipated demand.
      string predictive_method = 1 [json_name = "predictive_method"];

      // The target CPU utilization that the autoscaler should maintain.
      // Must be a float value in the range (0, 1]. If not specified, the
      // default is 0.6.
      //
      // If the CPU level is below the target utilization, the autoscaler
      // scales down the number of instances until it reaches the minimum
      // number of instances you specified or until the average CPU of
      // your instances reaches the target utilization.
      //
      // If the average CPU is above the target utilization, the autoscaler
      // scales up until it reaches the maximum number of instances you
      // specified or until the average utilization reaches the target
      // utilization.
      int64 target = 2;
    }

    message LoadBalancingUtilization {
      // Fraction of backend capacity utilization (set in HTTP(s) load
      // balancing configuration) that autoscaler should maintain. Must
      // be a positive float value. If not defined, the default is 0.8.
      int64 target = 1;
    }

    message Metric {
      // The identifier (type) of the Stackdriver Monitoring metric.
      // The metric cannot have negative values.
      //
      // The metric must have a value type of INT64 or DOUBLE.
      string name = 1;

      // The target value of the metric that autoscaler should
      // maintain. This must be a positive value. A utilization
      // metric scales number of virtual machines handling requests
      // to increase or decrease proportionally to the metric.
      //
      // For example, a good metric to use as a utilizationTarget is
      // www.googleapis.com/compute/instance/network/received_bytes_count.
      // The autoscaler will work to keep this value constant for each
      // of the instances.
      int64 target = 2;

      // Defines how target utilization value is expressed for a
      // Stackdriver Monitoring metric. Possible values: ["GAUGE", "DELTA_PER_SECOND", "DELTA_PER_MINUTE"]
      string type = 3;
    }

    message ScaleInControl {
      // How long back autoscaling should look when computing recommendations
      // to include directives regarding slower scale down, as described above.
      int64 time_window_sec = 1 [json_name = "time_window_sec"];

      MaxScaledInReplicas max_scaled_in_replicas = 2 [json_name = "max_scaled_in_replicas"];

      message MaxScaledInReplicas {
        // Specifies a fixed number of VM instances. This must be a positive
        // integer.
        int64 fixed = 1;

        // Specifies a percentage of instances between 0 to 100%, inclusive.
        // For example, specify 80 for 80%.
        int64 percent = 2;
      }
    }

    message ScalingSchedules {
      // A description of a scaling schedule.
      string description = 1;

      // A boolean value that specifies if a scaling schedule can influence autoscaler recommendations. If set to true, then a scaling schedule has no effect.
      bool disabled = 2;

      // The duration of time intervals (in seconds) for which this scaling schedule will be running. The minimum allowed value is 300.
      int64 duration_sec = 3 [json_name = "duration_sec"];

      // Minimum number of VM instances that autoscaler will recommend in time intervals starting according to schedule.
      int64 min_required_replicas = 4 [json_name = "min_required_replicas"];

      string name = 5;

      // The start timestamps of time intervals when this scaling schedule should provide a scaling signal. This field uses the extended cron format (with an optional year field).
      string schedule = 6;

      // The time zone to be used when interpreting the schedule. The value of this field must be a time zone name from the tz database: http://en.wikipedia.org/wiki/Tz_database.
      string time_zone = 7 [json_name = "time_zone"];
    }
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeRegionBackendService version is 1
message GoogleComputeRegionBackendService {
  // Lifetime of cookies in seconds if session_affinity is
  // GENERATED_COOKIE. If set to 0, the cookie is non-persistent and lasts
  // only until the end of the browser session (or equivalent). The
  // maximum allowed value for TTL is one day.
  //
  // When the load balancing scheme is INTERNAL, this field is not used.
  int64 affinity_cookie_ttl_sec = 1 [json_name = "affinity_cookie_ttl_sec"];

  // Time for which instance will be drained (not accept new
  // connections, but still work to finish started).
  int64 connection_draining_timeout_sec = 2 [json_name = "connection_draining_timeout_sec"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 3 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 4;

  // If true, enable Cloud CDN for this RegionBackendService.
  bool enable_cdn = 5 [json_name = "enable_cdn"];

  // Fingerprint of this resource. A hash of the contents stored in this
  // object. This field is used in optimistic locking.
  string fingerprint = 6;

  // The set of URLs to HealthCheck resources for health checking
  // this RegionBackendService. Currently at most one health
  // check can be specified.
  //
  // A health check must be specified unless the backend service uses an internet
  // or serverless NEG as a backend.
  repeated string health_checks = 7 [json_name = "health_checks"];

  string id = 8;

  // Indicates what kind of load balancing this regional backend service
  // will be used for. A backend service created for one type of load
  // balancing cannot be used with the other(s). For more information, refer to
  // [Choosing a load balancer](https://cloud.google.com/load-balancing/docs/backend-service). Default value: "INTERNAL" Possible values: ["EXTERNAL", "EXTERNAL_MANAGED", "INTERNAL", "INTERNAL_MANAGED"]
  string load_balancing_scheme = 9 [json_name = "load_balancing_scheme"];

  // The load balancing algorithm used within the scope of the locality.
  // The possible values are:
  //
  // * 'ROUND_ROBIN': This is a simple policy in which each healthy backend
  //                 is selected in round robin order.
  //
  // * 'LEAST_REQUEST': An O(1) algorithm which selects two random healthy
  //                   hosts and picks the host which has fewer active requests.
  //
  // * 'RING_HASH': The ring/modulo hash load balancer implements consistent
  //               hashing to backends. The algorithm has the property that the
  //               addition/removal of a host from a set of N hosts only affects
  //               1/N of the requests.
  //
  // * 'RANDOM': The load balancer selects a random healthy host.
  //
  // * 'ORIGINAL_DESTINATION': Backend host is selected based on the client
  //                          connection metadata, i.e., connections are opened
  //                          to the same address as the destination address of
  //                          the incoming connection before the connection
  //                          was redirected to the load balancer.
  //
  // * 'MAGLEV': used as a drop in replacement for the ring hash load balancer.
  //            Maglev is not as stable as ring hash but has faster table lookup
  //            build times and host selection times. For more information about
  //            Maglev, refer to https://ai.google/research/pubs/pub44824
  //
  // * 'WEIGHTED_MAGLEV': Per-instance weighted Load Balancing via health check
  //                     reported weights. If set, the Backend Service must
  //                     configure a non legacy HTTP-based Health Check, and
  //                     health check replies are expected to contain
  //                     non-standard HTTP response header field
  //                     X-Load-Balancing-Endpoint-Weight to specify the
  //                     per-instance weights. If set, Load Balancing is weight
  //                     based on the per-instance weights reported in the last
  //                     processed health check replies, as long as every
  //                     instance either reported a valid weight or had
  //                     UNAVAILABLE_WEIGHT. Otherwise, Load Balancing remains
  //                     equal-weight.
  //
  //
  // This field is applicable to either:
  //
  // * A regional backend service with the service_protocol set to HTTP, HTTPS, or HTTP2,
  //  and loadBalancingScheme set to INTERNAL_MANAGED.
  // * A global backend service with the load_balancing_scheme set to INTERNAL_SELF_MANAGED.
  // * A regional backend service with loadBalancingScheme set to EXTERNAL (External Network
  //  Load Balancing). Only MAGLEV and WEIGHTED_MAGLEV values are possible for External
  //  Network Load Balancing. The default is MAGLEV.
  //
  //
  // If session_affinity is not NONE, and this field is not set to MAGLEV, WEIGHTED_MAGLEV,
  // or RING_HASH, session affinity settings will not take effect.
  //
  // Only ROUND_ROBIN and RING_HASH are supported when the backend service is referenced
  // by a URL map that is bound to target gRPC proxy that has validate_for_proxyless
  // field set to true. Possible values: ["ROUND_ROBIN", "LEAST_REQUEST", "RING_HASH", "RANDOM", "ORIGINAL_DESTINATION", "MAGLEV", "WEIGHTED_MAGLEV"]
  string locality_lb_policy = 10 [json_name = "locality_lb_policy"];

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 11;

  // The URL of the network to which this backend service belongs.
  // This field can only be specified when the load balancing scheme is set to INTERNAL.
  string network = 12;

  // A named port on a backend instance group representing the port for
  // communication to the backend VMs in that group. Required when the
  // loadBalancingScheme is EXTERNAL, EXTERNAL_MANAGED, INTERNAL_MANAGED, or INTERNAL_SELF_MANAGED
  // and the backends are instance groups. The named port must be defined on each
  // backend instance group. This parameter has no meaning if the backends are NEGs. API sets a
  // default of "http" if not given.
  // Must be omitted when the loadBalancingScheme is INTERNAL (Internal TCP/UDP Load Balancing).
  string port_name = 13 [json_name = "port_name"];

  string project = 14;

  // The protocol this RegionBackendService uses to communicate with backends.
  // The default is HTTP. **NOTE**: HTTP2 is only valid for beta HTTP/2 load balancer
  // types and may result in errors if used with the GA API. Possible values: ["HTTP", "HTTPS", "HTTP2", "SSL", "TCP", "UDP", "GRPC", "UNSPECIFIED"]
  string protocol = 15;

  // The Region in which the created backend service should reside.
  // If it is not provided, the provider region is used.
  string region = 16;

  string self_link = 17 [json_name = "self_link"];

  // Type of session affinity to use. The default is NONE. Session affinity is
  // not applicable if the protocol is UDP. Possible values: ["NONE", "CLIENT_IP", "CLIENT_IP_PORT_PROTO", "CLIENT_IP_PROTO", "GENERATED_COOKIE", "HEADER_FIELD", "HTTP_COOKIE", "CLIENT_IP_NO_DESTINATION"]
  string session_affinity = 18 [json_name = "session_affinity"];

  // How many seconds to wait for the backend before considering it a
  // failed request. Default is 30 seconds. Valid range is [1, 86400].
  int64 timeout_sec = 19 [json_name = "timeout_sec"];

  repeated Backend backend = 20;

  CdnPolicy cdn_policy = 21 [json_name = "cdn_policy"];

  CircuitBreakers circuit_breakers = 22 [json_name = "circuit_breakers"];

  ConsistentHash consistent_hash = 23 [json_name = "consistent_hash"];

  FailoverPolicy failover_policy = 24 [json_name = "failover_policy"];

  Iap iap = 25;

  LogConfig log_config = 26 [json_name = "log_config"];

  OutlierDetection outlier_detection = 27 [json_name = "outlier_detection"];

  repeated Timeouts timeouts = 28;

  map<string, string> for_each = 29 [json_name = "for_each"];

  repeated string depends_on = 30 [json_name = "depends_on"];

  int32 count = 31;

  string provider = 32;

  terraform.v1.Lifecycle lifecycle = 33;

  message Backend {
    // Specifies the balancing mode for this backend.
    //
    // See the [Backend Services Overview](https://cloud.google.com/load-balancing/docs/backend-service#balancing-mode)
    // for an explanation of load balancing modes. Default value: "CONNECTION" Possible values: ["UTILIZATION", "RATE", "CONNECTION"]
    string balancing_mode = 1 [json_name = "balancing_mode"];

    // A multiplier applied to the group's maximum servicing capacity
    // (based on UTILIZATION, RATE or CONNECTION).
    //
    // ~>**NOTE**: This field cannot be set for
    // INTERNAL region backend services (default loadBalancingScheme),
    // but is required for non-INTERNAL backend service. The total
    // capacity_scaler for all backends must be non-zero.
    //
    // A setting of 0 means the group is completely drained, offering
    // 0% of its available Capacity. Valid range is [0.0,1.0].
    int64 capacity_scaler = 2 [json_name = "capacity_scaler"];

    // An optional description of this resource.
    // Provide this property when you create the resource.
    string description = 3;

    // This field designates whether this is a failover backend. More
    // than one failover backend can be configured for a given RegionBackendService.
    bool failover = 4;

    // The fully-qualified URL of an Instance Group or Network Endpoint
    // Group resource. In case of instance group this defines the list
    // of instances that serve traffic. Member virtual machine
    // instances from each instance group must live in the same zone as
    // the instance group itself. No two backends in a backend service
    // are allowed to use same Instance Group resource.
    //
    // For Network Endpoint Groups this defines list of endpoints. All
    // endpoints of Network Endpoint Group must be hosted on instances
    // located in the same zone as the Network Endpoint Group.
    //
    // Backend services cannot mix Instance Group and
    // Network Endpoint Group backends.
    //
    // When the 'load_balancing_scheme' is INTERNAL, only instance groups
    // are supported.
    //
    // Note that you must specify an Instance Group or Network Endpoint
    // Group resource using the fully-qualified URL, rather than a
    // partial URL.
    string group = 5;

    // The max number of simultaneous connections for the group. Can
    // be used with either CONNECTION or UTILIZATION balancing modes.
    // Cannot be set for INTERNAL backend services.
    //
    // For CONNECTION mode, either maxConnections or one
    // of maxConnectionsPerInstance or maxConnectionsPerEndpoint,
    // as appropriate for group type, must be set.
    int64 max_connections = 6 [json_name = "max_connections"];

    // The max number of simultaneous connections that a single backend
    // network endpoint can handle. Cannot be set
    // for INTERNAL backend services.
    //
    // This is used to calculate the capacity of the group. Can be
    // used in either CONNECTION or UTILIZATION balancing modes. For
    // CONNECTION mode, either maxConnections or
    // maxConnectionsPerEndpoint must be set.
    int64 max_connections_per_endpoint = 7 [json_name = "max_connections_per_endpoint"];

    // The max number of simultaneous connections that a single
    // backend instance can handle. Cannot be set for INTERNAL backend
    // services.
    //
    // This is used to calculate the capacity of the group.
    // Can be used in either CONNECTION or UTILIZATION balancing modes.
    // For CONNECTION mode, either maxConnections or
    // maxConnectionsPerInstance must be set.
    int64 max_connections_per_instance = 8 [json_name = "max_connections_per_instance"];

    // The max requests per second (RPS) of the group. Cannot be set
    // for INTERNAL backend services.
    //
    // Can be used with either RATE or UTILIZATION balancing modes,
    // but required if RATE mode. Either maxRate or one
    // of maxRatePerInstance or maxRatePerEndpoint, as appropriate for
    // group type, must be set.
    int64 max_rate = 9 [json_name = "max_rate"];

    // The max requests per second (RPS) that a single backend network
    // endpoint can handle. This is used to calculate the capacity of
    // the group. Can be used in either balancing mode. For RATE mode,
    // either maxRate or maxRatePerEndpoint must be set. Cannot be set
    // for INTERNAL backend services.
    int64 max_rate_per_endpoint = 10 [json_name = "max_rate_per_endpoint"];

    // The max requests per second (RPS) that a single backend
    // instance can handle. This is used to calculate the capacity of
    // the group. Can be used in either balancing mode. For RATE mode,
    // either maxRate or maxRatePerInstance must be set. Cannot be set
    // for INTERNAL backend services.
    int64 max_rate_per_instance = 11 [json_name = "max_rate_per_instance"];

    // Used when balancingMode is UTILIZATION. This ratio defines the
    // CPU utilization target for the group. Valid range is [0.0, 1.0].
    // Cannot be set for INTERNAL backend services.
    int64 max_utilization = 12 [json_name = "max_utilization"];
  }

  message CdnPolicy {
    // Specifies the cache setting for all responses from this backend.
    // The possible values are: USE_ORIGIN_HEADERS, FORCE_CACHE_ALL and CACHE_ALL_STATIC Possible values: ["USE_ORIGIN_HEADERS", "FORCE_CACHE_ALL", "CACHE_ALL_STATIC"]
    string cache_mode = 1 [json_name = "cache_mode"];

    // Specifies the maximum allowed TTL for cached content served by this origin.
    int64 client_ttl = 2 [json_name = "client_ttl"];

    // Specifies the default TTL for cached content served by this origin for responses
    // that do not have an existing valid TTL (max-age or s-max-age).
    int64 default_ttl = 3 [json_name = "default_ttl"];

    // Specifies the maximum allowed TTL for cached content served by this origin.
    int64 max_ttl = 4 [json_name = "max_ttl"];

    // Negative caching allows per-status code TTLs to be set, in order to apply fine-grained caching for common errors or redirects.
    bool negative_caching = 5 [json_name = "negative_caching"];

    // Serve existing content from the cache (if available) when revalidating content with the origin, or when an error is encountered when refreshing the cache.
    int64 serve_while_stale = 6 [json_name = "serve_while_stale"];

    // Maximum number of seconds the response to a signed URL request
    // will be considered fresh, defaults to 1hr (3600s). After this
    // time period, the response will be revalidated before
    // being served.
    //
    // When serving responses to signed URL requests, Cloud CDN will
    // internally behave as though all responses from this backend had a
    // "Cache-Control: public, max-age=[TTL]" header, regardless of any
    // existing Cache-Control header. The actual headers served in
    // responses will not be altered.
    int64 signed_url_cache_max_age_sec = 7 [json_name = "signed_url_cache_max_age_sec"];

    CacheKeyPolicy cache_key_policy = 8 [json_name = "cache_key_policy"];

    repeated NegativeCachingPolicy negative_caching_policy = 9 [json_name = "negative_caching_policy"];

    message CacheKeyPolicy {
      // If true requests to different hosts will be cached separately.
      bool include_host = 1 [json_name = "include_host"];

      // Names of cookies to include in cache keys.
      repeated string include_named_cookies = 2 [json_name = "include_named_cookies"];

      // If true, http and https requests will be cached separately.
      bool include_protocol = 3 [json_name = "include_protocol"];

      // If true, include query string parameters in the cache key
      // according to query_string_whitelist and
      // query_string_blacklist. If neither is set, the entire query
      // string will be included.
      //
      // If false, the query string will be excluded from the cache
      // key entirely.
      bool include_query_string = 4 [json_name = "include_query_string"];

      // Names of query string parameters to exclude in cache keys.
      //
      // All other parameters will be included. Either specify
      // query_string_whitelist or query_string_blacklist, not both.
      // '&' and '=' will be percent encoded and not treated as
      // delimiters.
      repeated string query_string_blacklist = 5 [json_name = "query_string_blacklist"];

      // Names of query string parameters to include in cache keys.
      //
      // All other parameters will be excluded. Either specify
      // query_string_whitelist or query_string_blacklist, not both.
      // '&' and '=' will be percent encoded and not treated as
      // delimiters.
      repeated string query_string_whitelist = 6 [json_name = "query_string_whitelist"];
    }

    message NegativeCachingPolicy {
      // The HTTP status code to define a TTL against. Only HTTP status codes 300, 301, 308, 404, 405, 410, 421, 451 and 501
      // can be specified as values, and you cannot specify a status code more than once.
      int64 code = 1;
    }
  }

  message CircuitBreakers {
    // The maximum number of connections to the backend cluster.
    // Defaults to 1024.
    int64 max_connections = 1 [json_name = "max_connections"];

    // The maximum number of pending requests to the backend cluster.
    // Defaults to 1024.
    int64 max_pending_requests = 2 [json_name = "max_pending_requests"];

    // The maximum number of parallel requests to the backend cluster.
    // Defaults to 1024.
    int64 max_requests = 3 [json_name = "max_requests"];

    // Maximum requests for a single backend connection. This parameter
    // is respected by both the HTTP/1.1 and HTTP/2 implementations. If
    // not specified, there is no limit. Setting this parameter to 1
    // will effectively disable keep alive.
    int64 max_requests_per_connection = 4 [json_name = "max_requests_per_connection"];

    // The maximum number of parallel retries to the backend cluster.
    // Defaults to 3.
    int64 max_retries = 5 [json_name = "max_retries"];
  }

  message ConsistentHash {
    // The hash based on the value of the specified header field.
    // This field is applicable if the sessionAffinity is set to HEADER_FIELD.
    string http_header_name = 1 [json_name = "http_header_name"];

    // The minimum number of virtual nodes to use for the hash ring.
    // Larger ring sizes result in more granular load
    // distributions. If the number of hosts in the load balancing pool
    // is larger than the ring size, each host will be assigned a single
    // virtual node.
    // Defaults to 1024.
    int64 minimum_ring_size = 2 [json_name = "minimum_ring_size"];

    HttpCookie http_cookie = 3 [json_name = "http_cookie"];

    message HttpCookie {
      // Name of the cookie.
      string name = 1;

      // Path to set for the cookie.
      string path = 2;

      Ttl ttl = 3;

      message Ttl {
        // Span of time that's a fraction of a second at nanosecond
        // resolution. Durations less than one second are represented
        // with a 0 seconds field and a positive nanos field. Must
        // be from 0 to 999,999,999 inclusive.
        int64 nanos = 1;

        // Span of time at a resolution of a second.
        // Must be from 0 to 315,576,000,000 inclusive.
        int64 seconds = 2;
      }
    }
  }

  message FailoverPolicy {
    // On failover or failback, this field indicates whether connection drain
    // will be honored. Setting this to true has the following effect: connections
    // to the old active pool are not drained. Connections to the new active pool
    // use the timeout of 10 min (currently fixed). Setting to false has the
    // following effect: both old and new connections will have a drain timeout
    // of 10 min.
    // This can be set to true only if the protocol is TCP.
    // The default is false.
    bool disable_connection_drain_on_failover = 1 [json_name = "disable_connection_drain_on_failover"];

    // This option is used only when no healthy VMs are detected in the primary
    // and backup instance groups. When set to true, traffic is dropped. When
    // set to false, new connections are sent across all VMs in the primary group.
    // The default is false.
    bool drop_traffic_if_unhealthy = 2 [json_name = "drop_traffic_if_unhealthy"];

    // The value of the field must be in [0, 1]. If the ratio of the healthy
    // VMs in the primary backend is at or below this number, traffic arriving
    // at the load-balanced IP will be directed to the failover backend.
    // In case where 'failoverRatio' is not set or all the VMs in the backup
    // backend are unhealthy, the traffic will be directed back to the primary
    // backend in the "force" mode, where traffic will be spread to the healthy
    // VMs with the best effort, or to all VMs when no VM is healthy.
    // This field is only used with l4 load balancing.
    int64 failover_ratio = 3 [json_name = "failover_ratio"];
  }

  message Iap {
    // OAuth2 Client ID for IAP
    string oauth2_client_id = 1 [json_name = "oauth2_client_id"];

    // OAuth2 Client Secret for IAP
    string oauth2_client_secret = 2 [json_name = "oauth2_client_secret"];

    // OAuth2 Client Secret SHA-256 for IAP
    string oauth2_client_secret_sha256 = 3 [json_name = "oauth2_client_secret_sha256"];
  }

  message LogConfig {
    // Whether to enable logging for the load balancer traffic served by this backend service.
    bool enable = 1;

    // This field can only be specified if logging is enabled for this backend service. The value of
    // the field must be in [0, 1]. This configures the sampling rate of requests to the load balancer
    // where 1.0 means all logged requests are reported and 0.0 means no logged requests are reported.
    // The default value is 1.0.
    int64 sample_rate = 2 [json_name = "sample_rate"];
  }

  message OutlierDetection {
    // Number of errors before a host is ejected from the connection pool. When the
    // backend host is accessed over HTTP, a 5xx return code qualifies as an error.
    // Defaults to 5.
    int64 consecutive_errors = 1 [json_name = "consecutive_errors"];

    // The number of consecutive gateway failures (502, 503, 504 status or connection
    // errors that are mapped to one of those status codes) before a consecutive
    // gateway failure ejection occurs. Defaults to 5.
    int64 consecutive_gateway_failure = 2 [json_name = "consecutive_gateway_failure"];

    // The percentage chance that a host will be actually ejected when an outlier
    // status is detected through consecutive 5xx. This setting can be used to disable
    // ejection or to ramp it up slowly. Defaults to 100.
    int64 enforcing_consecutive_errors = 3 [json_name = "enforcing_consecutive_errors"];

    // The percentage chance that a host will be actually ejected when an outlier
    // status is detected through consecutive gateway failures. This setting can be
    // used to disable ejection or to ramp it up slowly. Defaults to 0.
    int64 enforcing_consecutive_gateway_failure = 4 [
      json_name = "enforcing_consecutive_gateway_failure"
    ];

    // The percentage chance that a host will be actually ejected when an outlier
    // status is detected through success rate statistics. This setting can be used to
    // disable ejection or to ramp it up slowly. Defaults to 100.
    int64 enforcing_success_rate = 5 [json_name = "enforcing_success_rate"];

    // Maximum percentage of hosts in the load balancing pool for the backend service
    // that can be ejected. Defaults to 10%.
    int64 max_ejection_percent = 6 [json_name = "max_ejection_percent"];

    // The number of hosts in a cluster that must have enough request volume to detect
    // success rate outliers. If the number of hosts is less than this setting, outlier
    // detection via success rate statistics is not performed for any host in the
    // cluster. Defaults to 5.
    int64 success_rate_minimum_hosts = 7 [json_name = "success_rate_minimum_hosts"];

    // The minimum number of total requests that must be collected in one interval (as
    // defined by the interval duration above) to include this host in success rate
    // based outlier detection. If the volume is lower than this setting, outlier
    // detection via success rate statistics is not performed for that host. Defaults
    // to 100.
    int64 success_rate_request_volume = 8 [json_name = "success_rate_request_volume"];

    // This factor is used to determine the ejection threshold for success rate outlier
    // ejection. The ejection threshold is the difference between the mean success
    // rate, and the product of this factor and the standard deviation of the mean
    // success rate: mean - (stdev * success_rate_stdev_factor). This factor is divided
    // by a thousand to get a double. That is, if the desired factor is 1.9, the
    // runtime value should be 1900. Defaults to 1900.
    int64 success_rate_stdev_factor = 9 [json_name = "success_rate_stdev_factor"];

    BaseEjectionTime base_ejection_time = 10 [json_name = "base_ejection_time"];

    Interval interval = 11;

    message BaseEjectionTime {
      // Span of time that's a fraction of a second at nanosecond resolution. Durations
      // less than one second are represented with a 0 'seconds' field and a positive
      // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
      int64 nanos = 1;

      // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
      // inclusive.
      int64 seconds = 2;
    }

    message Interval {
      // Span of time that's a fraction of a second at nanosecond resolution. Durations
      // less than one second are represented with a 0 'seconds' field and a positive
      // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
      int64 nanos = 1;

      // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
      // inclusive.
      int64 seconds = 2;
    }
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeRegionCommitment version is 0
message GoogleComputeRegionCommitment {
  // Specifies whether to enable automatic renewal for the commitment.
  // The default value is false if not specified.
  // If the field is set to true, the commitment will be automatically renewed for either
  // one or three years according to the terms of the existing commitment.
  bool auto_renew = 1 [json_name = "auto_renew"];

  // The category of the commitment. Category MACHINE specifies commitments composed of
  // machine resources such as VCPU or MEMORY, listed in resources. Category LICENSE
  // specifies commitments composed of software licenses, listed in licenseResources.
  // Note that only MACHINE commitments should have a Type specified. Possible values: ["LICENSE", "MACHINE"]
  string category = 2;

  // Unique identifier for the resource.
  int64 commitment_id = 3 [json_name = "commitment_id"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 4 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 5;

  // Commitment end time in RFC3339 text format.
  string end_timestamp = 6 [json_name = "end_timestamp"];

  string id = 7;

  // Name of the resource. The name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 8;

  // The plan for this commitment, which determines duration and discount rate.
  // The currently supported plans are TWELVE_MONTH (1 year), and THIRTY_SIX_MONTH (3 years). Possible values: ["TWELVE_MONTH", "THIRTY_SIX_MONTH"]
  string plan = 9;

  string project = 10;

  // URL of the region where this commitment may be used.
  string region = 11;

  string self_link = 12 [json_name = "self_link"];

  // Commitment start time in RFC3339 text format.
  string start_timestamp = 13 [json_name = "start_timestamp"];

  // Status of the commitment with regards to eventual expiration
  // (each commitment has an end date defined).
  string status = 14;

  // A human-readable explanation of the status.
  string status_message = 15 [json_name = "status_message"];

  // The type of commitment, which affects the discount rate and the eligible resources.
  // The type could be one of the following value: 'MEMORY_OPTIMIZED', 'ACCELERATOR_OPTIMIZED',
  // 'GENERAL_PURPOSE_N1', 'GENERAL_PURPOSE_N2', 'GENERAL_PURPOSE_N2D', 'GENERAL_PURPOSE_E2',
  // 'GENERAL_PURPOSE_T2D', 'GENERAL_PURPOSE_C3', 'COMPUTE_OPTIMIZED_C2', 'COMPUTE_OPTIMIZED_C2D' and
  // 'GRAPHICS_OPTIMIZED_G2'
  string type = 16;

  LicenseResource license_resource = 17 [json_name = "license_resource"];

  repeated Resources resources = 18;

  repeated Timeouts timeouts = 19;

  map<string, string> for_each = 20 [json_name = "for_each"];

  repeated string depends_on = 21 [json_name = "depends_on"];

  int32 count = 22;

  string provider = 23;

  terraform.v1.Lifecycle lifecycle = 24;

  message LicenseResource {
    // The number of licenses purchased.
    string amount = 1;

    // Specifies the core range of the instance for which this license applies.
    string cores_per_license = 2 [json_name = "cores_per_license"];

    // Any applicable license URI.
    string license = 3;
  }

  message Resources {
    // Name of the accelerator type resource. Applicable only when the type is ACCELERATOR.
    string accelerator_type = 1 [json_name = "accelerator_type"];

    // The amount of the resource purchased (in a type-dependent unit,
    // such as bytes). For vCPUs, this can just be an integer. For memory,
    // this must be provided in MB. Memory must be a multiple of 256 MB,
    // with up to 6.5GB of memory per every vCPU.
    string amount = 2;

    // Type of resource for which this commitment applies.
    // Possible values are VCPU, MEMORY, LOCAL_SSD, and ACCELERATOR.
    string type = 3;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeRegionDisk version is 0
message GoogleComputeRegionDisk {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional description of this resource. Provide this property when
  // you create the resource.
  string description = 2;

  string id = 3;

  // The fingerprint used for optimistic locking of this resource.  Used
  // internally during updates.
  string label_fingerprint = 4 [json_name = "label_fingerprint"];

  // Labels to apply to this disk.  A list of key->value pairs.
  map<string, string> labels = 5;

  // Last attach timestamp in RFC3339 text format.
  string last_attach_timestamp = 6 [json_name = "last_attach_timestamp"];

  // Last detach timestamp in RFC3339 text format.
  string last_detach_timestamp = 7 [json_name = "last_detach_timestamp"];

  // Any applicable license URI.
  repeated string licenses = 8;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 9;

  // Physical block size of the persistent disk, in bytes. If not present
  // in a request, a default value is used. Currently supported sizes
  // are 4096 and 16384, other sizes may be added in the future.
  // If an unsupported value is requested, the error message will list
  // the supported values for the caller's project.
  int64 physical_block_size_bytes = 10 [json_name = "physical_block_size_bytes"];

  string project = 11;

  // A reference to the region where the disk resides.
  string region = 12;

  // URLs of the zones where the disk should be replicated to.
  repeated string replica_zones = 13 [json_name = "replica_zones"];

  string self_link = 14 [json_name = "self_link"];

  // Size of the persistent disk, specified in GB. You can specify this
  // field when creating a persistent disk using the sourceImage or
  // sourceSnapshot parameter, or specify it alone to create an empty
  // persistent disk.
  //
  // If you specify this field along with sourceImage or sourceSnapshot,
  // the value of sizeGb must not be less than the size of the sourceImage
  // or the size of the snapshot.
  int64 size = 15;

  // The source snapshot used to create this disk. You can provide this as
  // a partial or full URL to the resource. For example, the following are
  // valid values:
  //
  // * 'https://www.googleapis.com/compute/v1/projects/project/global/snapshots/snapshot'
  // * 'projects/project/global/snapshots/snapshot'
  // * 'global/snapshots/snapshot'
  // * 'snapshot'
  string snapshot = 16;

  // The source disk used to create this disk. You can provide this as a partial or full URL to the resource.
  // For example, the following are valid values:
  //
  // * https://www.googleapis.com/compute/v1/projects/{project}/zones/{zone}/disks/{disk}
  // * https://www.googleapis.com/compute/v1/projects/{project}/regions/{region}/disks/{disk}
  // * projects/{project}/zones/{zone}/disks/{disk}
  // * projects/{project}/regions/{region}/disks/{disk}
  // * zones/{zone}/disks/{disk}
  // * regions/{region}/disks/{disk}
  string source_disk = 17 [json_name = "source_disk"];

  // The ID value of the disk used to create this image. This value may
  // be used to determine whether the image was taken from the current
  // or a previous instance of a given disk name.
  string source_disk_id = 18 [json_name = "source_disk_id"];

  // The unique ID of the snapshot used to create this disk. This value
  // identifies the exact snapshot that was used to create this persistent
  // disk. For example, if you created the persistent disk from a snapshot
  // that was later deleted and recreated under the same name, the source
  // snapshot ID would identify the exact version of the snapshot that was
  // used.
  string source_snapshot_id = 19 [json_name = "source_snapshot_id"];

  // URL of the disk type resource describing which disk type to use to
  // create the disk. Provide this when creating the disk.
  string type = 20;

  // Links to the users of the disk (attached instances) in form:
  // project/zones/zone/instances/instance
  repeated string users = 21;

  DiskEncryptionKey disk_encryption_key = 22 [json_name = "disk_encryption_key"];

  repeated GuestOsFeatures guest_os_features = 23 [json_name = "guest_os_features"];

  SourceSnapshotEncryptionKey source_snapshot_encryption_key = 24 [json_name = "source_snapshot_encryption_key"];

  repeated Timeouts timeouts = 25;

  map<string, string> for_each = 26 [json_name = "for_each"];

  repeated string depends_on = 27 [json_name = "depends_on"];

  int32 count = 28;

  string provider = 29;

  terraform.v1.Lifecycle lifecycle = 30;

  message DiskEncryptionKey {
    // The name of the encryption key that is stored in Google Cloud KMS.
    string kms_key_name = 1 [json_name = "kms_key_name"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    string raw_key = 2 [json_name = "raw_key"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied
    // encryption key that protects this resource.
    string sha256 = 3;
  }

  message GuestOsFeatures {
    // The type of supported feature. Read [Enabling guest operating system features](https://cloud.google.com/compute/docs/images/create-delete-deprecate-private-images#guest-os-features) to see a list of available options. Possible values: ["MULTI_IP_SUBNET", "SECURE_BOOT", "SEV_CAPABLE", "UEFI_COMPATIBLE", "VIRTIO_SCSI_MULTIQUEUE", "WINDOWS", "GVNIC", "SEV_LIVE_MIGRATABLE", "SEV_SNP_CAPABLE", "SUSPEND_RESUME_COMPATIBLE", "TDX_CAPABLE"]
    string type = 1;
  }

  message SourceSnapshotEncryptionKey {
    // Specifies a 256-bit customer-supplied encryption key, encoded in
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    string raw_key = 1 [json_name = "raw_key"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied
    // encryption key that protects this resource.
    string sha256 = 2;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeRegionDiskIamBinding version is 0
message GoogleComputeRegionDiskIamBinding {
  string etag = 1;

  string id = 2;

  repeated string members = 3;

  string name = 4;

  string project = 5;

  string region = 6;

  string role = 7;

  Condition condition = 8;

  map<string, string> for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    string description = 1;

    string expression = 2;

    string title = 3;
  }
}

// GoogleComputeRegionDiskIamMember version is 0
message GoogleComputeRegionDiskIamMember {
  string etag = 1;

  string id = 2;

  string member = 3;

  string name = 4;

  string project = 5;

  string region = 6;

  string role = 7;

  Condition condition = 8;

  map<string, string> for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    string description = 1;

    string expression = 2;

    string title = 3;
  }
}

// GoogleComputeRegionDiskIamPolicy version is 0
message GoogleComputeRegionDiskIamPolicy {
  string etag = 1;

  string id = 2;

  string name = 3;

  string policy_data = 4 [json_name = "policy_data"];

  string project = 5;

  string region = 6;

  map<string, string> for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// GoogleComputeRegionDiskResourcePolicyAttachment version is 0
message GoogleComputeRegionDiskResourcePolicyAttachment {
  // The name of the regional disk in which the resource policies are attached to.
  string disk = 1;

  string id = 2;

  // The resource policy to be attached to the disk for scheduling snapshot
  // creation. Do not specify the self link.
  string name = 3;

  string project = 4;

  // A reference to the region where the disk resides.
  string region = 5;

  repeated Timeouts timeouts = 6;

  map<string, string> for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeRegionHealthCheck version is 0
message GoogleComputeRegionHealthCheck {
  // How often (in seconds) to send a health check. The default value is 5
  // seconds.
  int64 check_interval_sec = 1 [json_name = "check_interval_sec"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // An optional description of this resource. Provide this property when
  // you create the resource.
  string description = 3;

  // A so-far unhealthy instance will be marked healthy after this many
  // consecutive successes. The default value is 2.
  int64 healthy_threshold = 4 [json_name = "healthy_threshold"];

  string id = 5;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  // the first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the
  // last character, which cannot be a dash.
  string name = 6;

  string project = 7;

  // The Region in which the created health check should reside.
  // If it is not provided, the provider region is used.
  string region = 8;

  string self_link = 9 [json_name = "self_link"];

  // How long (in seconds) to wait before claiming failure.
  // The default value is 5 seconds.  It is invalid for timeoutSec to have
  // greater value than checkIntervalSec.
  int64 timeout_sec = 10 [json_name = "timeout_sec"];

  // The type of the health check. One of HTTP, HTTP2, HTTPS, TCP, or SSL.
  string type = 11;

  // A so-far healthy instance will be marked unhealthy after this many
  // consecutive failures. The default value is 2.
  int64 unhealthy_threshold = 12 [json_name = "unhealthy_threshold"];

  GrpcHealthCheck grpc_health_check = 13 [json_name = "grpc_health_check"];

  Http2HealthCheck http2_health_check = 14 [json_name = "http2_health_check"];

  HttpHealthCheck http_health_check = 15 [json_name = "http_health_check"];

  HttpsHealthCheck https_health_check = 16 [json_name = "https_health_check"];

  LogConfig log_config = 17 [json_name = "log_config"];

  SslHealthCheck ssl_health_check = 18 [json_name = "ssl_health_check"];

  TcpHealthCheck tcp_health_check = 19 [json_name = "tcp_health_check"];

  repeated Timeouts timeouts = 20;

  map<string, string> for_each = 21 [json_name = "for_each"];

  repeated string depends_on = 22 [json_name = "depends_on"];

  int32 count = 23;

  string provider = 24;

  terraform.v1.Lifecycle lifecycle = 25;

  message GrpcHealthCheck {
    // The gRPC service name for the health check.
    // The value of grpcServiceName has the following meanings by convention:
    //
    // * Empty serviceName means the overall status of all services at the backend.
    // * Non-empty serviceName means the health of that gRPC service, as defined by the owner of the service.
    //
    // The grpcServiceName can only be ASCII.
    string grpc_service_name = 1 [json_name = "grpc_service_name"];

    // The port number for the health check request.
    // Must be specified if portName and portSpecification are not set
    // or if port_specification is USE_FIXED_PORT. Valid values are 1 through 65535.
    int64 port = 2;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    // port_name are defined, port takes precedence.
    string port_name = 3 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    // following values:
    //
    //  * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //  * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //  * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //  network endpoint is used for health checking. For other backends, the
    //  port or named port specified in the Backend Service is used for health
    //  checking.
    //
    // If not specified, gRPC health check follows behavior specified in 'port' and
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"]
    string port_specification = 4 [json_name = "port_specification"];
  }

  message Http2HealthCheck {
    // The value of the host header in the HTTP2 health check request.
    // If left empty (default value), the public IP on behalf of which this health
    // check is performed will be used.
    string host = 1;

    // The TCP port number for the HTTP2 health check request.
    // The default value is 443.
    int64 port = 2;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    // port_name are defined, port takes precedence.
    string port_name = 3 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    // following values:
    //
    //  * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //  * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //  * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //  network endpoint is used for health checking. For other backends, the
    //  port or named port specified in the Backend Service is used for health
    //  checking.
    //
    // If not specified, HTTP2 health check follows behavior specified in 'port' and
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"]
    string port_specification = 4 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    string proxy_header = 5 [json_name = "proxy_header"];

    // The request path of the HTTP2 health check request.
    // The default value is /.
    string request_path = 6 [json_name = "request_path"];

    // The bytes to match against the beginning of the response data. If left empty
    // (the default value), any response will indicate health. The response data
    // can only be ASCII.
    string response = 7;
  }

  message HttpHealthCheck {
    // The value of the host header in the HTTP health check request.
    // If left empty (default value), the public IP on behalf of which this health
    // check is performed will be used.
    string host = 1;

    // The TCP port number for the HTTP health check request.
    // The default value is 80.
    int64 port = 2;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    // port_name are defined, port takes precedence.
    string port_name = 3 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    // following values:
    //
    //  * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //  * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //  * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //  network endpoint is used for health checking. For other backends, the
    //  port or named port specified in the Backend Service is used for health
    //  checking.
    //
    // If not specified, HTTP health check follows behavior specified in 'port' and
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"]
    string port_specification = 4 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    string proxy_header = 5 [json_name = "proxy_header"];

    // The request path of the HTTP health check request.
    // The default value is /.
    string request_path = 6 [json_name = "request_path"];

    // The bytes to match against the beginning of the response data. If left empty
    // (the default value), any response will indicate health. The response data
    // can only be ASCII.
    string response = 7;
  }

  message HttpsHealthCheck {
    // The value of the host header in the HTTPS health check request.
    // If left empty (default value), the public IP on behalf of which this health
    // check is performed will be used.
    string host = 1;

    // The TCP port number for the HTTPS health check request.
    // The default value is 443.
    int64 port = 2;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    // port_name are defined, port takes precedence.
    string port_name = 3 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    // following values:
    //
    //  * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //  * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //  * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //  network endpoint is used for health checking. For other backends, the
    //  port or named port specified in the Backend Service is used for health
    //  checking.
    //
    // If not specified, HTTPS health check follows behavior specified in 'port' and
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"]
    string port_specification = 4 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    string proxy_header = 5 [json_name = "proxy_header"];

    // The request path of the HTTPS health check request.
    // The default value is /.
    string request_path = 6 [json_name = "request_path"];

    // The bytes to match against the beginning of the response data. If left empty
    // (the default value), any response will indicate health. The response data
    // can only be ASCII.
    string response = 7;
  }

  message LogConfig {
    // Indicates whether or not to export logs. This is false by default,
    // which means no health check logging will be done.
    bool enable = 1;
  }

  message SslHealthCheck {
    // The TCP port number for the SSL health check request.
    // The default value is 443.
    int64 port = 1;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    // port_name are defined, port takes precedence.
    string port_name = 2 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    // following values:
    //
    //  * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //  * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //  * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //  network endpoint is used for health checking. For other backends, the
    //  port or named port specified in the Backend Service is used for health
    //  checking.
    //
    // If not specified, SSL health check follows behavior specified in 'port' and
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"]
    string port_specification = 3 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    string proxy_header = 4 [json_name = "proxy_header"];

    // The application data to send once the SSL connection has been
    // established (default value is empty). If both request and response are
    // empty, the connection establishment alone will indicate health. The request
    // data can only be ASCII.
    string request = 5;

    // The bytes to match against the beginning of the response data. If left empty
    // (the default value), any response will indicate health. The response data
    // can only be ASCII.
    string response = 6;
  }

  message TcpHealthCheck {
    // The TCP port number for the TCP health check request.
    // The default value is 80.
    int64 port = 1;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    // port_name are defined, port takes precedence.
    string port_name = 2 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    // following values:
    //
    //  * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //  * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //  * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //  network endpoint is used for health checking. For other backends, the
    //  port or named port specified in the Backend Service is used for health
    //  checking.
    //
    // If not specified, TCP health check follows behavior specified in 'port' and
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"]
    string port_specification = 3 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    string proxy_header = 4 [json_name = "proxy_header"];

    // The application data to send once the TCP connection has been
    // established (default value is empty). If both request and response are
    // empty, the connection establishment alone will indicate health. The request
    // data can only be ASCII.
    string request = 5;

    // The bytes to match against the beginning of the response data. If left empty
    // (the default value), any response will indicate health. The response data
    // can only be ASCII.
    string response = 6;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeRegionInstanceGroupManager version is 0
message GoogleComputeRegionInstanceGroupManager {
  // The base instance name to use for instances in this group. The value must be a valid RFC1035 name. Supported characters are lowercase letters, numbers, and hyphens (-). Instances are named by appending a hyphen and a random four-character string to the base instance name.
  string base_instance_name = 1 [json_name = "base_instance_name"];

  // An optional textual description of the instance group manager.
  string description = 2;

  // The shape to which the group converges either proactively or on resize events (depending on the value set in updatePolicy.instanceRedistributionType).
  string distribution_policy_target_shape = 3 [json_name = "distribution_policy_target_shape"];

  // The distribution policy for this managed instance group. You can specify one or more values.
  repeated string distribution_policy_zones = 4 [json_name = "distribution_policy_zones"];

  // The fingerprint of the instance group manager.
  string fingerprint = 5;

  string id = 6;

  // The full URL of the instance group created by the manager.
  string instance_group = 7 [json_name = "instance_group"];

  // Pagination behavior of the listManagedInstances API method for this managed instance group. Valid values are: "PAGELESS", "PAGINATED". If PAGELESS (default), Pagination is disabled for the group's listManagedInstances API method. maxResults and pageToken query parameters are ignored and all instances are returned in a single response. If PAGINATED, pagination is enabled, maxResults and pageToken query parameters are respected.
  string list_managed_instances_results = 8 [json_name = "list_managed_instances_results"];

  // The name of the instance group manager. Must be 1-63 characters long and comply with RFC1035. Supported characters include lowercase letters, numbers, and hyphens.
  string name = 9;

  // The ID of the project in which the resource belongs. If it is not provided, the provider project is used.
  string project = 10;

  // The region where the managed instance group resides.
  string region = 11;

  // The URL of the created resource.
  string self_link = 12 [json_name = "self_link"];

  // The status of this managed instance group.
  repeated Status status = 13;

  // The full URL of all target pools to which new instances in the group are added. Updating the target pools attribute does not affect existing instances.
  repeated string target_pools = 14 [json_name = "target_pools"];

  // The target number of running instances for this managed instance group. This value should always be explicitly set unless this resource is attached to an autoscaler, in which case it should never be set. Defaults to 0.
  int64 target_size = 15 [json_name = "target_size"];

  // Whether to wait for all instances to be created/updated before returning. Note that if this is set to true and the operation does not succeed, Terraform will continue trying until it times out.
  bool wait_for_instances = 16 [json_name = "wait_for_instances"];

  // When used with wait_for_instances specifies the status to wait for. When STABLE is specified this resource will wait until the instances are stable before returning. When UPDATED is set, it will wait for the version target to be reached and any per instance configs to be effective as well as all instances to be stable before returning.
  string wait_for_instances_status = 17 [json_name = "wait_for_instances_status"];

  AutoHealingPolicies auto_healing_policies = 18 [json_name = "auto_healing_policies"];

  repeated NamedPort named_port = 19 [json_name = "named_port"];

  repeated StatefulDisk stateful_disk = 20 [json_name = "stateful_disk"];

  repeated Timeouts timeouts = 21;

  UpdatePolicy update_policy = 22 [json_name = "update_policy"];

  repeated Version version = 23;

  map<string, string> for_each = 24 [json_name = "for_each"];

  repeated string depends_on = 25 [json_name = "depends_on"];

  int32 count = 26;

  string provider = 27;

  terraform.v1.Lifecycle lifecycle = 28;

  message Status {
    // is_stable: bool
    bool is_stable = 1 [json_name = "is_stable"];

    // stateful: list of object
    repeated Stateful stateful = 2;

    // version_target: list of object
    repeated VersionTarget version_target = 3 [json_name = "version_target"];

    message Stateful {
      // has_stateful_config: bool
      bool has_stateful_config = 1 [json_name = "has_stateful_config"];

      // per_instance_configs: list of object
      repeated PerInstanceConfigs per_instance_configs = 2 [json_name = "per_instance_configs"];

      message PerInstanceConfigs {
        // all_effective: bool
        bool all_effective = 1 [json_name = "all_effective"];
      }
    }

    message VersionTarget {
      // is_reached: bool
      bool is_reached = 1 [json_name = "is_reached"];
    }
  }

  message AutoHealingPolicies {
    // The health check resource that signals autohealing.
    string health_check = 1 [json_name = "health_check"];

    // The number of seconds that the managed instance group waits before it applies autohealing policies to new instances or recently recreated instances. Between 0 and 3600.
    int64 initial_delay_sec = 2 [json_name = "initial_delay_sec"];
  }

  message NamedPort {
    // The name of the port.
    string name = 1;

    // The port number.
    int64 port = 2;
  }

  message StatefulDisk {
    // A value that prescribes what should happen to the stateful disk when the VM instance is deleted. The available options are NEVER and ON_PERMANENT_INSTANCE_DELETION. NEVER - detach the disk when the VM is deleted, but do not delete the disk. ON_PERMANENT_INSTANCE_DELETION will delete the stateful disk when the VM is permanently deleted from the instance group. The default is NEVER.
    string delete_rule = 1 [json_name = "delete_rule"];

    // The device name of the disk to be attached.
    string device_name = 2 [json_name = "device_name"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }

  message UpdatePolicy {
    // The instance redistribution policy for regional managed instance groups. Valid values are: "PROACTIVE", "NONE". If PROACTIVE (default), the group attempts to maintain an even distribution of VM instances across zones in the region. If NONE, proactive redistribution is disabled.
    string instance_redistribution_type = 1 [json_name = "instance_redistribution_type"];

    // The maximum number of instances that can be created above the specified targetSize during the update process. Conflicts with max_surge_percent. It has to be either 0 or at least equal to the number of zones. If fixed values are used, at least one of max_unavailable_fixed or max_surge_fixed must be greater than 0.
    int64 max_surge_fixed = 2 [json_name = "max_surge_fixed"];

    // The maximum number of instances(calculated as percentage) that can be created above the specified targetSize during the update process. Conflicts with max_surge_fixed. Percent value is only allowed for regional managed instance groups with size at least 10.
    int64 max_surge_percent = 3 [json_name = "max_surge_percent"];

    // The maximum number of instances that can be unavailable during the update process. Conflicts with max_unavailable_percent. It has to be either 0 or at least equal to the number of zones. If fixed values are used, at least one of max_unavailable_fixed or max_surge_fixed must be greater than 0.
    int64 max_unavailable_fixed = 4 [json_name = "max_unavailable_fixed"];

    // The maximum number of instances(calculated as percentage) that can be unavailable during the update process. Conflicts with max_unavailable_fixed. Percent value is only allowed for regional managed instance groups with size at least 10.
    int64 max_unavailable_percent = 5 [json_name = "max_unavailable_percent"];

    // Minimal action to be taken on an instance. You can specify either REFRESH to update without stopping instances, RESTART to restart existing instances or REPLACE to delete and create new instances from the target template. If you specify a REFRESH, the Updater will attempt to perform that action only. However, if the Updater determines that the minimal action you specify is not enough to perform the update, it might perform a more disruptive action.
    string minimal_action = 6 [json_name = "minimal_action"];

    // Most disruptive action that is allowed to be taken on an instance. You can specify either NONE to forbid any actions, REFRESH to allow actions that do not need instance restart, RESTART to allow actions that can be applied without instance replacing or REPLACE to allow all possible actions. If the Updater determines that the minimal update action needed is more disruptive than most disruptive allowed action you specify it will not perform the update at all.
    string most_disruptive_allowed_action = 7 [json_name = "most_disruptive_allowed_action"];

    // The instance replacement method for regional managed instance groups. Valid values are: "RECREATE", "SUBSTITUTE". If SUBSTITUTE (default), the group replaces VM instances with new instances that have randomly generated names. If RECREATE, instance names are preserved.  You must also set max_unavailable_fixed or max_unavailable_percent to be greater than 0.
    string replacement_method = 8 [json_name = "replacement_method"];

    // The type of update process. You can specify either PROACTIVE so that the instance group manager proactively executes actions in order to bring instances to their target versions or OPPORTUNISTIC so that no action is proactively executed but the update will be performed as part of other actions (for example, resizes or recreateInstances calls).
    string type = 9;
  }

  message Version {
    // The full URL to an instance template from which all new instances of this version will be created.
    string instance_template = 1 [json_name = "instance_template"];

    // Version name.
    string name = 2;

    TargetSize target_size = 3 [json_name = "target_size"];

    message TargetSize {
      // The number of instances which are managed for this version. Conflicts with percent.
      int64 fixed = 1;

      // The number of instances (calculated as percentage) which are managed for this version. Conflicts with fixed. Note that when using percent, rounding will be in favor of explicitly set target_size values; a managed instance group with 2 instances and 2 versions, one of which has a target_size.percent of 60 will create 2 instances of that version.
      int64 percent = 2;
    }
  }
}

// GoogleComputeRegionNetworkEndpointGroup version is 0
message GoogleComputeRegionNetworkEndpointGroup {
  // An optional description of this resource. Provide this property when
  // you create the resource.
  string description = 1;

  string id = 2;

  // Name of the resource; provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 3;

  // This field is only used for PSC.
  // The URL of the network to which all network endpoints in the NEG belong. Uses
  // "default" project network if unspecified.
  string network = 4;

  // Type of network endpoints in this network endpoint group. Defaults to SERVERLESS Default value: "SERVERLESS" Possible values: ["SERVERLESS", "PRIVATE_SERVICE_CONNECT"]
  string network_endpoint_type = 5 [json_name = "network_endpoint_type"];

  string project = 6;

  // The target service url used to set up private service connection to
  // a Google API or a PSC Producer Service Attachment.
  string psc_target_service = 7 [json_name = "psc_target_service"];

  // A reference to the region where the Serverless NEGs Reside.
  string region = 8;

  string self_link = 9 [json_name = "self_link"];

  // This field is only used for PSC.
  // Optional URL of the subnetwork to which all network endpoints in the NEG belong.
  string subnetwork = 10;

  AppEngine app_engine = 11 [json_name = "app_engine"];

  CloudFunction cloud_function = 12 [json_name = "cloud_function"];

  CloudRun cloud_run = 13 [json_name = "cloud_run"];

  repeated Timeouts timeouts = 14;

  map<string, string> for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  int32 count = 17;

  string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message AppEngine {
    // Optional serving service.
    // The service name must be 1-63 characters long, and comply with RFC1035.
    // Example value: "default", "my-service".
    string service = 1;

    // A template to parse service and version fields from a request URL.
    // URL mask allows for routing to multiple App Engine services without
    // having to create multiple Network Endpoint Groups and backend services.
    //
    // For example, the request URLs "foo1-dot-appname.appspot.com/v1" and
    // "foo1-dot-appname.appspot.com/v2" can be backed by the same Serverless NEG with
    // URL mask "-dot-appname.appspot.com/". The URL mask will parse
    // them to { service = "foo1", version = "v1" } and { service = "foo1", version = "v2" } respectively.
    string url_mask = 2 [json_name = "url_mask"];

    // Optional serving version.
    // The version must be 1-63 characters long, and comply with RFC1035.
    // Example value: "v1", "v2".
    string version = 3;
  }

  message CloudFunction {
    // A user-defined name of the Cloud Function.
    // The function name is case-sensitive and must be 1-63 characters long.
    // Example value: "func1".
    string function = 1;

    // A template to parse function field from a request URL. URL mask allows
    // for routing to multiple Cloud Functions without having to create
    // multiple Network Endpoint Groups and backend services.
    //
    // For example, request URLs "mydomain.com/function1" and "mydomain.com/function2"
    // can be backed by the same Serverless NEG with URL mask "/". The URL mask
    // will parse them to { function = "function1" } and { function = "function2" } respectively.
    string url_mask = 2 [json_name = "url_mask"];
  }

  message CloudRun {
    // Cloud Run service is the main resource of Cloud Run.
    // The service must be 1-63 characters long, and comply with RFC1035.
    // Example value: "run-service".
    string service = 1;

    // Cloud Run tag represents the "named-revision" to provide
    // additional fine-grained traffic routing information.
    // The tag must be 1-63 characters long, and comply with RFC1035.
    // Example value: "revision-0010".
    string tag = 2;

    // A template to parse service and tag fields from a request URL.
    // URL mask allows for routing to multiple Run services without having
    // to create multiple network endpoint groups and backend services.
    //
    // For example, request URLs "foo1.domain.com/bar1" and "foo1.domain.com/bar2"
    // an be backed by the same Serverless Network Endpoint Group (NEG) with
    // URL mask ".domain.com/". The URL mask will parse them to { service="bar1", tag="foo1" }
    // and { service="bar2", tag="foo2" } respectively.
    string url_mask = 3 [json_name = "url_mask"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeRegionNetworkFirewallPolicy version is 0
message GoogleComputeRegionNetworkFirewallPolicy {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional description of this resource. Provide this property when you create the resource.
  string description = 2;

  // Fingerprint of the resource. This field is used internally during updates of this resource.
  string fingerprint = 3;

  string id = 4;

  // User-provided name of the Network firewall policy. The name should be unique in the project in which the firewall policy is created. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression [a-z]([-a-z0-9]*[a-z0-9])? which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash.
  string name = 5;

  // The project for the resource
  string project = 6;

  // The location of this resource.
  string region = 7;

  // The unique identifier for the resource. This identifier is defined by the server.
  string region_network_firewall_policy_id = 8 [json_name = "region_network_firewall_policy_id"];

  // Total count of all firewall policy rule tuples. A firewall policy can not exceed a set number of tuples.
  int64 rule_tuple_count = 9 [json_name = "rule_tuple_count"];

  // Server-defined URL for the resource.
  string self_link = 10 [json_name = "self_link"];

  // Server-defined URL for this resource with the resource id.
  string self_link_with_id = 11 [json_name = "self_link_with_id"];

  repeated Timeouts timeouts = 12;

  map<string, string> for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  int32 count = 15;

  string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeRegionNetworkFirewallPolicyAssociation version is 0
message GoogleComputeRegionNetworkFirewallPolicyAssociation {
  // The target that the firewall policy is attached to.
  string attachment_target = 1 [json_name = "attachment_target"];

  // The firewall policy ID of the association.
  string firewall_policy = 2 [json_name = "firewall_policy"];

  string id = 3;

  // The name for an association.
  string name = 4;

  // The project for the resource
  string project = 5;

  // The location of this resource.
  string region = 6;

  // The short name of the firewall policy of the association.
  string short_name = 7 [json_name = "short_name"];

  repeated Timeouts timeouts = 8;

  map<string, string> for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeRegionNetworkFirewallPolicyRule version is 0
message GoogleComputeRegionNetworkFirewallPolicyRule {
  // The Action to perform when the client connection triggers the rule. Can currently be either "allow" or "deny()" where valid values for status are 403, 404, and 502.
  string action = 1;

  // An optional description for this resource.
  string description = 2;

  // The direction in which this rule applies. Possible values: INGRESS, EGRESS
  string direction = 3;

  // Denotes whether the firewall policy rule is disabled. When set to true, the firewall policy rule is not enforced and traffic behaves as if it did not exist. If this is unspecified, the firewall policy rule will be enabled.
  bool disabled = 4;

  // Denotes whether to enable logging for a particular rule. If logging is enabled, logs will be exported to the configured export destination in Stackdriver. Logs may be exported to BigQuery or Pub/Sub. Note: you cannot enable logging on "goto_next" rules.
  bool enable_logging = 5 [json_name = "enable_logging"];

  // The firewall policy of the resource.
  string firewall_policy = 6 [json_name = "firewall_policy"];

  string id = 7;

  // Type of the resource. Always `compute#firewallPolicyRule` for firewall policy rules
  string kind = 8;

  // An integer indicating the priority of a rule in the list. The priority must be a positive value between 0 and 2147483647. Rules are evaluated from highest to lowest priority where 0 is the highest priority and 2147483647 is the lowest prority.
  int64 priority = 9;

  // The project for the resource
  string project = 10;

  // The location of this resource.
  string region = 11;

  // An optional name for the rule. This field is not a unique identifier and can be updated.
  string rule_name = 12 [json_name = "rule_name"];

  // Calculation of the complexity of a single firewall policy rule.
  int64 rule_tuple_count = 13 [json_name = "rule_tuple_count"];

  // A list of service accounts indicating the sets of instances that are applied with this rule.
  repeated string target_service_accounts = 14 [json_name = "target_service_accounts"];

  Match match = 15;

  repeated TargetSecureTags target_secure_tags = 16 [json_name = "target_secure_tags"];

  repeated Timeouts timeouts = 17;

  map<string, string> for_each = 18 [json_name = "for_each"];

  repeated string depends_on = 19 [json_name = "depends_on"];

  int32 count = 20;

  string provider = 21;

  terraform.v1.Lifecycle lifecycle = 22;

  message Match {
    // Address groups which should be matched against the traffic destination. Maximum number of destination address groups is 10. Destination address groups is only supported in Egress rules.
    repeated string dest_address_groups = 1 [json_name = "dest_address_groups"];

    // Domain names that will be used to match against the resolved domain name of destination of traffic. Can only be specified if DIRECTION is egress.
    repeated string dest_fqdns = 2 [json_name = "dest_fqdns"];

    // CIDR IP address range. Maximum number of destination CIDR IP ranges allowed is 5000.
    repeated string dest_ip_ranges = 3 [json_name = "dest_ip_ranges"];

    // The Unicode country codes whose IP addresses will be used to match against the source of traffic. Can only be specified if DIRECTION is egress.
    repeated string dest_region_codes = 4 [json_name = "dest_region_codes"];

    // Name of the Google Cloud Threat Intelligence list.
    repeated string dest_threat_intelligences = 5 [json_name = "dest_threat_intelligences"];

    // Address groups which should be matched against the traffic source. Maximum number of source address groups is 10. Source address groups is only supported in Ingress rules.
    repeated string src_address_groups = 6 [json_name = "src_address_groups"];

    // Domain names that will be used to match against the resolved domain name of source of traffic. Can only be specified if DIRECTION is ingress.
    repeated string src_fqdns = 7 [json_name = "src_fqdns"];

    // CIDR IP address range. Maximum number of source CIDR IP ranges allowed is 5000.
    repeated string src_ip_ranges = 8 [json_name = "src_ip_ranges"];

    // The Unicode country codes whose IP addresses will be used to match against the source of traffic. Can only be specified if DIRECTION is ingress.
    repeated string src_region_codes = 9 [json_name = "src_region_codes"];

    // Name of the Google Cloud Threat Intelligence list.
    repeated string src_threat_intelligences = 10 [json_name = "src_threat_intelligences"];

    repeated Layer4Configs layer4_configs = 11 [json_name = "layer4_configs"];

    repeated SrcSecureTags src_secure_tags = 12 [json_name = "src_secure_tags"];

    message Layer4Configs {
      // The IP protocol to which this rule applies. The protocol type is required when creating a firewall rule. This value can either be one of the following well known protocol strings (`tcp`, `udp`, `icmp`, `esp`, `ah`, `ipip`, `sctp`), or the IP protocol number.
      string ip_protocol = 1 [json_name = "ip_protocol"];

      // An optional list of ports to which this rule applies. This field is only applicable for UDP or TCP protocol. Each entry must be either an integer or a range. If not specified, this rule applies to connections through any port. Example inputs include: ``.
      repeated string ports = 2;
    }

    message SrcSecureTags {
      // Name of the secure tag, created with TagManager's TagValue API. @pattern tagValues/[0-9]+
      string name = 1;

      // [Output Only] State of the secure tag, either `EFFECTIVE` or `INEFFECTIVE`. A secure tag is `INEFFECTIVE` when it is deleted or its network is deleted.
      string state = 2;
    }
  }

  message TargetSecureTags {
    // Name of the secure tag, created with TagManager's TagValue API. @pattern tagValues/[0-9]+
    string name = 1;

    // [Output Only] State of the secure tag, either `EFFECTIVE` or `INEFFECTIVE`. A secure tag is `INEFFECTIVE` when it is deleted or its network is deleted.
    string state = 2;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeRegionPerInstanceConfig version is 0
message GoogleComputeRegionPerInstanceConfig {
  string id = 1;

  // The minimal action to perform on the instance during an update.
  // Default is 'NONE'. Possible values are:
  // * REPLACE
  // * RESTART
  // * REFRESH
  // * NONE
  string minimal_action = 2 [json_name = "minimal_action"];

  // The most disruptive action to perform on the instance during an update.
  // Default is 'REPLACE'. Possible values are:
  // * REPLACE
  // * RESTART
  // * REFRESH
  // * NONE
  string most_disruptive_allowed_action = 3 [json_name = "most_disruptive_allowed_action"];

  // The name for this per-instance config and its corresponding instance.
  string name = 4;

  string project = 5;

  // Region where the containing instance group manager is located
  string region = 6;

  // The region instance group manager this instance config is part of.
  string region_instance_group_manager = 7 [json_name = "region_instance_group_manager"];

  // When true, deleting this config will immediately remove any specified state from the underlying instance.
  // When false, deleting this config will *not* immediately remove any state from the underlying instance.
  // State will be removed on the next instance recreation or update.
  bool remove_instance_state_on_destroy = 8 [json_name = "remove_instance_state_on_destroy"];

  PreservedState preserved_state = 9 [json_name = "preserved_state"];

  repeated Timeouts timeouts = 10;

  map<string, string> for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message PreservedState {
    // Preserved metadata defined for this instance. This is a list of key->value pairs.
    map<string, string> metadata = 1;

    repeated Disk disk = 2;

    message Disk {
      // A value that prescribes what should happen to the stateful disk when the VM instance is deleted.
      // The available options are 'NEVER' and 'ON_PERMANENT_INSTANCE_DELETION'.
      // 'NEVER' - detach the disk when the VM is deleted, but do not delete the disk.
      // 'ON_PERMANENT_INSTANCE_DELETION' will delete the stateful disk when the VM is permanently
      // deleted from the instance group. Default value: "NEVER" Possible values: ["NEVER", "ON_PERMANENT_INSTANCE_DELETION"]
      string delete_rule = 1 [json_name = "delete_rule"];

      // A unique device name that is reflected into the /dev/ tree of a Linux operating system running within the instance.
      string device_name = 2 [json_name = "device_name"];

      // The mode of the disk. Default value: "READ_WRITE" Possible values: ["READ_ONLY", "READ_WRITE"]
      string mode = 3;

      // The URI of an existing persistent disk to attach under the specified device-name in the format
      // 'projects/project-id/zones/zone/disks/disk-name'.
      string source = 4;
    }
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeRegionSslCertificate version is 0
message GoogleComputeRegionSslCertificate {
  // The certificate in PEM format.
  // The certificate chain must be no greater than 5 certs long.
  // The chain must include at least one intermediate cert.
  string certificate = 1;

  // The unique identifier for the resource.
  int64 certificate_id = 2 [json_name = "certificate_id"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 3 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 4;

  // Expire time of the certificate in RFC3339 text format.
  string expire_time = 5 [json_name = "expire_time"];

  string id = 6;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  //
  //
  // These are in the same namespace as the managed SSL certificates.
  string name = 7;

  // Creates a unique name beginning with the specified prefix. Conflicts with name.
  string name_prefix = 8 [json_name = "name_prefix"];

  // The write-only private key in PEM format.
  string private_key = 9 [json_name = "private_key"];

  string project = 10;

  // The Region in which the created regional ssl certificate should reside.
  // If it is not provided, the provider region is used.
  string region = 11;

  string self_link = 12 [json_name = "self_link"];

  repeated Timeouts timeouts = 13;

  map<string, string> for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  int32 count = 16;

  string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeRegionTargetHttpProxy version is 0
message GoogleComputeRegionTargetHttpProxy {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 2;

  string id = 3;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 4;

  string project = 5;

  // The unique identifier for the resource.
  int64 proxy_id = 6 [json_name = "proxy_id"];

  // The Region in which the created target https proxy should reside.
  // If it is not provided, the provider region is used.
  string region = 7;

  string self_link = 8 [json_name = "self_link"];

  // A reference to the RegionUrlMap resource that defines the mapping from URL
  // to the BackendService.
  string url_map = 9 [json_name = "url_map"];

  repeated Timeouts timeouts = 10;

  map<string, string> for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeRegionTargetHttpsProxy version is 0
message GoogleComputeRegionTargetHttpsProxy {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 2;

  string id = 3;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 4;

  string project = 5;

  // The unique identifier for the resource.
  int64 proxy_id = 6 [json_name = "proxy_id"];

  // The Region in which the created target https proxy should reside.
  // If it is not provided, the provider region is used.
  string region = 7;

  string self_link = 8 [json_name = "self_link"];

  // A list of RegionSslCertificate resources that are used to authenticate
  // connections between users and the load balancer. Currently, exactly
  // one SSL certificate must be specified.
  repeated string ssl_certificates = 9 [json_name = "ssl_certificates"];

  // A reference to the RegionUrlMap resource that defines the mapping from URL
  // to the RegionBackendService.
  string url_map = 10 [json_name = "url_map"];

  repeated Timeouts timeouts = 11;

  map<string, string> for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  int32 count = 14;

  string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeRegionTargetTcpProxy version is 0
message GoogleComputeRegionTargetTcpProxy {
  // A reference to the BackendService resource.
  string backend_service = 1 [json_name = "backend_service"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 3;

  string id = 4;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 5;

  string project = 6;

  // This field only applies when the forwarding rule that references
  // this target proxy has a loadBalancingScheme set to INTERNAL_SELF_MANAGED.
  bool proxy_bind = 7 [json_name = "proxy_bind"];

  // Specifies the type of proxy header to append before sending data to
  // the backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
  string proxy_header = 8 [json_name = "proxy_header"];

  // The unique identifier for the resource.
  int64 proxy_id = 9 [json_name = "proxy_id"];

  // The Region in which the created target TCP proxy should reside.
  // If it is not provided, the provider region is used.
  string region = 10;

  string self_link = 11 [json_name = "self_link"];

  repeated Timeouts timeouts = 12;

  map<string, string> for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  int32 count = 15;

  string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeRegionUrlMap version is 0
message GoogleComputeRegionUrlMap {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // The full or partial URL of the defaultService resource to which traffic is directed if
  // none of the hostRules match. If defaultRouteAction is additionally specified, advanced
  // routing actions like URL Rewrites, etc. take effect prior to sending the request to the
  // backend. However, if defaultService is specified, defaultRouteAction cannot contain any
  // weightedBackendServices. Conversely, if routeAction specifies any
  // weightedBackendServices, service must not be specified.  Only one of defaultService,
  // defaultUrlRedirect or defaultRouteAction.weightedBackendService must be set.
  string default_service = 2 [json_name = "default_service"];

  // An optional description of this resource. Provide this property when
  // you create the resource.
  string description = 3;

  // Fingerprint of this resource. This field is used internally during
  // updates of this resource.
  string fingerprint = 4;

  string id = 5;

  // The unique identifier for the resource.
  int64 map_id = 6 [json_name = "map_id"];

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 7;

  string project = 8;

  // The Region in which the url map should reside.
  // If it is not provided, the provider region is used.
  string region = 9;

  string self_link = 10 [json_name = "self_link"];

  DefaultRouteAction default_route_action = 11 [json_name = "default_route_action"];

  DefaultUrlRedirect default_url_redirect = 12 [json_name = "default_url_redirect"];

  repeated HostRule host_rule = 13 [json_name = "host_rule"];

  repeated PathMatcher path_matcher = 14 [json_name = "path_matcher"];

  repeated Test test = 15;

  repeated Timeouts timeouts = 16;

  map<string, string> for_each = 17 [json_name = "for_each"];

  repeated string depends_on = 18 [json_name = "depends_on"];

  int32 count = 19;

  string provider = 20;

  terraform.v1.Lifecycle lifecycle = 21;

  message DefaultRouteAction {
    CorsPolicy cors_policy = 1 [json_name = "cors_policy"];

    FaultInjectionPolicy fault_injection_policy = 2 [json_name = "fault_injection_policy"];

    RequestMirrorPolicy request_mirror_policy = 3 [json_name = "request_mirror_policy"];

    RetryPolicy retry_policy = 4 [json_name = "retry_policy"];

    Timeout timeout = 5;

    UrlRewrite url_rewrite = 6 [json_name = "url_rewrite"];

    repeated WeightedBackendServices weighted_backend_services = 7 [json_name = "weighted_backend_services"];

    message CorsPolicy {
      // In response to a preflight request, setting this to true indicates that the actual request can include user credentials. This field translates to the Access-Control-Allow-Credentials header.
      // Default is false.
      bool allow_credentials = 1 [json_name = "allow_credentials"];

      // Specifies the content for the Access-Control-Allow-Headers header.
      repeated string allow_headers = 2 [json_name = "allow_headers"];

      // Specifies the content for the Access-Control-Allow-Methods header.
      repeated string allow_methods = 3 [json_name = "allow_methods"];

      // Specifies the regualar expression patterns that match allowed origins. For regular expression grammar
      // please see en.cppreference.com/w/cpp/regex/ecmascript
      // An origin is allowed if it matches either an item in allowOrigins or an item in allowOriginRegexes.
      repeated string allow_origin_regexes = 4 [json_name = "allow_origin_regexes"];

      // Specifies the list of origins that will be allowed to do CORS requests.
      // An origin is allowed if it matches either an item in allowOrigins or an item in allowOriginRegexes.
      repeated string allow_origins = 5 [json_name = "allow_origins"];

      // If true, the setting specifies the CORS policy is disabled. The default value of false, which indicates that the CORS policy is in effect.
      bool disabled = 6;

      // Specifies the content for the Access-Control-Expose-Headers header.
      repeated string expose_headers = 7 [json_name = "expose_headers"];

      // Specifies how long results of a preflight request can be cached in seconds.
      // This translates to the Access-Control-Max-Age header.
      int64 max_age = 8 [json_name = "max_age"];
    }

    message FaultInjectionPolicy {
      Abort abort = 1;

      Delay delay = 2;

      message Abort {
        // The HTTP status code used to abort the request.
        // The value must be between 200 and 599 inclusive.
        int64 http_status = 1 [json_name = "http_status"];

        // The percentage of traffic (connections/operations/requests) which will be aborted as part of fault injection.
        // The value must be between 0.0 and 100.0 inclusive.
        int64 percentage = 2;
      }

      message Delay {
        // The percentage of traffic (connections/operations/requests) on which delay will be introduced as part of fault injection.
        // The value must be between 0.0 and 100.0 inclusive.
        int64 percentage = 1;

        FixedDelay fixed_delay = 2 [json_name = "fixed_delay"];

        message FixedDelay {
          // Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are
          // represented with a 0 seconds field and a positive nanos field. Must be from 0 to 999,999,999 inclusive.
          int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive.
          // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years
          string seconds = 2;
        }
      }
    }

    message RequestMirrorPolicy {
      // The full or partial URL to the RegionBackendService resource being mirrored to.
      // The backend service configured for a mirroring policy must reference backends that are of the same type as the original backend service matched in the URL map.
      // Serverless NEG backends are not currently supported as a mirrored backend service.
      string backend_service = 1 [json_name = "backend_service"];
    }

    message RetryPolicy {
      // Specifies the allowed number retries. This number must be > 0. If not specified, defaults to 1.
      int64 num_retries = 1 [json_name = "num_retries"];

      // Specifies one or more conditions when this retry policy applies.
      // Valid values are listed below. Only the following codes are supported when the URL map is bound to target gRPC proxy that has validateForProxyless field set to true: cancelled, deadline-exceeded, internal, resource-exhausted, unavailable.
      //  - 5xx : retry is attempted if the instance or endpoint responds with any 5xx response code, or if the instance or endpoint does not respond at all. For example, disconnects, reset, read timeout, connection failure, and refused streams.
      //  - gateway-error : Similar to 5xx, but only applies to response codes 502, 503 or 504.
      //  - connect-failure : a retry is attempted on failures connecting to the instance or endpoint. For example, connection timeouts.
      //  - retriable-4xx : a retry is attempted if the instance or endpoint responds with a 4xx response code. The only error that you can retry is error code 409.
      //  - refused-stream : a retry is attempted if the instance or endpoint resets the stream with a REFUSED_STREAM error code. This reset type indicates that it is safe to retry.
      //  - cancelled : a retry is attempted if the gRPC status code in the response header is set to cancelled.
      //  - deadline-exceeded : a retry is attempted if the gRPC status code in the response header is set to deadline-exceeded.
      //  - internal :  a retry is attempted if the gRPC status code in the response header is set to internal.
      //  - resource-exhausted : a retry is attempted if the gRPC status code in the response header is set to resource-exhausted.
      //  - unavailable : a retry is attempted if the gRPC status code in the response header is set to unavailable.
      repeated string retry_conditions = 2 [json_name = "retry_conditions"];

      PerTryTimeout per_try_timeout = 3 [json_name = "per_try_timeout"];

      message PerTryTimeout {
        // Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are
        // represented with a 0 seconds field and a positive nanos field. Must be from 0 to 999,999,999 inclusive.
        int64 nanos = 1;

        // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive.
        // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years
        string seconds = 2;
      }
    }

    message Timeout {
      // Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 seconds field and a positive nanos field. Must be from 0 to 999,999,999 inclusive.
      int64 nanos = 1;

      // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years
      string seconds = 2;
    }

    message UrlRewrite {
      // Before forwarding the request to the selected service, the request's host header is replaced with contents of hostRewrite.
      // The value must be from 1 to 255 characters.
      string host_rewrite = 1 [json_name = "host_rewrite"];

      // Before forwarding the request to the selected backend service, the matching portion of the request's path is replaced by pathPrefixRewrite.
      // The value must be from 1 to 1024 characters.
      string path_prefix_rewrite = 2 [json_name = "path_prefix_rewrite"];
    }

    message WeightedBackendServices {
      // The full or partial URL to the default BackendService resource. Before forwarding the request to backendService, the load balancer applies any relevant headerActions specified as part of this backendServiceWeight.
      string backend_service = 1 [json_name = "backend_service"];

      // Specifies the fraction of traffic sent to a backend service, computed as weight / (sum of all weightedBackendService weights in routeAction) .
      // The selection of a backend service is determined only for new traffic. Once a user's request has been directed to a backend service, subsequent requests are sent to the same backend service as determined by the backend service's session affinity policy.
      // The value must be from 0 to 1000.
      int64 weight = 2;

      HeaderAction header_action = 3 [json_name = "header_action"];

      message HeaderAction {
        // A list of header names for headers that need to be removed from the request before forwarding the request to the backendService.
        repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

        // A list of header names for headers that need to be removed from the response before sending the response back to the client.
        repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

        repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

        repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

        message RequestHeadersToAdd {
          // The name of the header.
          string header_name = 1 [json_name = "header_name"];

          // The value of the header to add.
          string header_value = 2 [json_name = "header_value"];

          // If false, headerValue is appended to any values that already exist for the header. If true, headerValue is set for the header, discarding any values that were set for that header.
          // The default value is false.
          bool replace = 3;
        }

        message ResponseHeadersToAdd {
          // The name of the header.
          string header_name = 1 [json_name = "header_name"];

          // The value of the header to add.
          string header_value = 2 [json_name = "header_value"];

          // If false, headerValue is appended to any values that already exist for the header. If true, headerValue is set for the header, discarding any values that were set for that header.
          // The default value is false.
          bool replace = 3;
        }
      }
    }
  }

  message DefaultUrlRedirect {
    // The host that will be used in the redirect response instead of the one that was
    // supplied in the request. The value must be between 1 and 255 characters.
    string host_redirect = 1 [json_name = "host_redirect"];

    // If set to true, the URL scheme in the redirected request is set to https. If set to
    // false, the URL scheme of the redirected request will remain the same as that of the
    // request. This must only be set for UrlMaps used in TargetHttpProxys. Setting this
    // true for TargetHttpsProxy is not permitted. The default is set to false.
    bool https_redirect = 2 [json_name = "https_redirect"];

    // The path that will be used in the redirect response instead of the one that was
    // supplied in the request. pathRedirect cannot be supplied together with
    // prefixRedirect. Supply one alone or neither. If neither is supplied, the path of the
    // original request will be used for the redirect. The value must be between 1 and 1024
    // characters.
    string path_redirect = 3 [json_name = "path_redirect"];

    // The prefix that replaces the prefixMatch specified in the HttpRouteRuleMatch,
    // retaining the remaining portion of the URL before redirecting the request.
    // prefixRedirect cannot be supplied together with pathRedirect. Supply one alone or
    // neither. If neither is supplied, the path of the original request will be used for
    // the redirect. The value must be between 1 and 1024 characters.
    string prefix_redirect = 4 [json_name = "prefix_redirect"];

    // The HTTP Status code to use for this RedirectAction. Supported values are:
    //
    // * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301.
    //
    // * FOUND, which corresponds to 302.
    //
    // * SEE_OTHER which corresponds to 303.
    //
    // * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method
    // will be retained.
    //
    // * PERMANENT_REDIRECT, which corresponds to 308. In this case,
    // the request method will be retained. Possible values: ["FOUND", "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER", "TEMPORARY_REDIRECT"]
    string redirect_response_code = 5 [json_name = "redirect_response_code"];

    // If set to true, any accompanying query portion of the original URL is removed prior
    // to redirecting the request. If set to false, the query portion of the original URL is
    // retained.
    // This field is required to ensure an empty block is not set. The normal default value is false.
    bool strip_query = 6 [json_name = "strip_query"];
  }

  message HostRule {
    // An optional description of this HostRule. Provide this property
    // when you create the resource.
    string description = 1;

    // The list of host patterns to match. They must be valid
    // hostnames, except * will match any string of ([a-z0-9-.]*). In
    // that case, * must be the first character and must be followed in
    // the pattern by either - or ..
    repeated string hosts = 2;

    // The name of the PathMatcher to use to match the path portion of
    // the URL if the hostRule matches the URL's host portion.
    string path_matcher = 3 [json_name = "path_matcher"];
  }

  message PathMatcher {
    // A reference to a RegionBackendService resource. This will be used if
    // none of the pathRules defined by this PathMatcher is matched by
    // the URL's path portion.
    string default_service = 1 [json_name = "default_service"];

    // An optional description of this resource.
    string description = 2;

    // The name to which this PathMatcher is referred by the HostRule.
    string name = 3;

    DefaultUrlRedirect default_url_redirect = 4 [json_name = "default_url_redirect"];

    repeated PathRule path_rule = 5 [json_name = "path_rule"];

    repeated RouteRules route_rules = 6 [json_name = "route_rules"];

    message DefaultUrlRedirect {
      // The host that will be used in the redirect response instead of the one that was
      // supplied in the request. The value must be between 1 and 255 characters.
      string host_redirect = 1 [json_name = "host_redirect"];

      // If set to true, the URL scheme in the redirected request is set to https. If set to
      // false, the URL scheme of the redirected request will remain the same as that of the
      // request. This must only be set for UrlMaps used in TargetHttpProxys. Setting this
      // true for TargetHttpsProxy is not permitted. The default is set to false.
      bool https_redirect = 2 [json_name = "https_redirect"];

      // The path that will be used in the redirect response instead of the one that was
      // supplied in the request. pathRedirect cannot be supplied together with
      // prefixRedirect. Supply one alone or neither. If neither is supplied, the path of the
      // original request will be used for the redirect. The value must be between 1 and 1024
      // characters.
      string path_redirect = 3 [json_name = "path_redirect"];

      // The prefix that replaces the prefixMatch specified in the HttpRouteRuleMatch,
      // retaining the remaining portion of the URL before redirecting the request.
      // prefixRedirect cannot be supplied together with pathRedirect. Supply one alone or
      // neither. If neither is supplied, the path of the original request will be used for
      // the redirect. The value must be between 1 and 1024 characters.
      string prefix_redirect = 4 [json_name = "prefix_redirect"];

      // The HTTP Status code to use for this RedirectAction. Supported values are:
      //
      // * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301.
      //
      // * FOUND, which corresponds to 302.
      //
      // * SEE_OTHER which corresponds to 303.
      //
      // * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method
      // will be retained.
      //
      // * PERMANENT_REDIRECT, which corresponds to 308. In this case,
      // the request method will be retained. Possible values: ["FOUND", "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER", "TEMPORARY_REDIRECT"]
      string redirect_response_code = 5 [json_name = "redirect_response_code"];

      // If set to true, any accompanying query portion of the original URL is removed prior
      // to redirecting the request. If set to false, the query portion of the original URL is
      // retained.
      // This field is required to ensure an empty block is not set. The normal default value is false.
      bool strip_query = 6 [json_name = "strip_query"];
    }

    message PathRule {
      // The list of path patterns to match. Each must start with / and the only place a
      // \* is allowed is at the end following a /. The string fed to the path matcher
      // does not include any text after the first ? or #, and those chars are not
      // allowed here.
      repeated string paths = 1;

      // The region backend service resource to which traffic is
      // directed if this rule is matched. If routeAction is additionally specified,
      // advanced routing actions like URL Rewrites, etc. take effect prior to sending
      // the request to the backend. However, if service is specified, routeAction cannot
      // contain any weightedBackendService s. Conversely, if routeAction specifies any
      // weightedBackendServices, service must not be specified. Only one of urlRedirect,
      // service or routeAction.weightedBackendService must be set.
      string service = 2;

      RouteAction route_action = 3 [json_name = "route_action"];

      UrlRedirect url_redirect = 4 [json_name = "url_redirect"];

      message RouteAction {
        CorsPolicy cors_policy = 1 [json_name = "cors_policy"];

        FaultInjectionPolicy fault_injection_policy = 2 [json_name = "fault_injection_policy"];

        RequestMirrorPolicy request_mirror_policy = 3 [json_name = "request_mirror_policy"];

        RetryPolicy retry_policy = 4 [json_name = "retry_policy"];

        Timeout timeout = 5;

        UrlRewrite url_rewrite = 6 [json_name = "url_rewrite"];

        repeated WeightedBackendServices weighted_backend_services = 7 [json_name = "weighted_backend_services"];

        message CorsPolicy {
          // In response to a preflight request, setting this to true indicates that the
          // actual request can include user credentials. This translates to the Access-
          // Control-Allow-Credentials header. Defaults to false.
          bool allow_credentials = 1 [json_name = "allow_credentials"];

          // Specifies the content for the Access-Control-Allow-Headers header.
          repeated string allow_headers = 2 [json_name = "allow_headers"];

          // Specifies the content for the Access-Control-Allow-Methods header.
          repeated string allow_methods = 3 [json_name = "allow_methods"];

          // Specifies the regular expression patterns that match allowed origins. For
          // regular expression grammar please see en.cppreference.com/w/cpp/regex/ecmascript
          // An origin is allowed if it matches either allow_origins or allow_origin_regex.
          repeated string allow_origin_regexes = 4 [json_name = "allow_origin_regexes"];

          // Specifies the list of origins that will be allowed to do CORS requests. An
          // origin is allowed if it matches either allow_origins or allow_origin_regex.
          repeated string allow_origins = 5 [json_name = "allow_origins"];

          // If true, specifies the CORS policy is disabled.
          bool disabled = 6;

          // Specifies the content for the Access-Control-Expose-Headers header.
          repeated string expose_headers = 7 [json_name = "expose_headers"];

          // Specifies how long the results of a preflight request can be cached. This
          // translates to the content for the Access-Control-Max-Age header.
          int64 max_age = 8 [json_name = "max_age"];
        }

        message FaultInjectionPolicy {
          Abort abort = 1;

          Delay delay = 2;

          message Abort {
            // The HTTP status code used to abort the request. The value must be between 200
            // and 599 inclusive.
            int64 http_status = 1 [json_name = "http_status"];

            // The percentage of traffic (connections/operations/requests) which will be
            // aborted as part of fault injection. The value must be between 0.0 and 100.0
            // inclusive.
            int64 percentage = 2;
          }

          message Delay {
            // The percentage of traffic (connections/operations/requests) on which delay will
            // be introduced as part of fault injection. The value must be between 0.0 and
            // 100.0 inclusive.
            int64 percentage = 1;

            FixedDelay fixed_delay = 2 [json_name = "fixed_delay"];

            message FixedDelay {
              // Span of time that's a fraction of a second at nanosecond resolution. Durations
              // less than one second are represented with a 0 'seconds' field and a positive
              // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
              int64 nanos = 1;

              // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
              // inclusive.
              string seconds = 2;
            }
          }
        }

        message RequestMirrorPolicy {
          // The RegionBackendService resource being mirrored to.
          string backend_service = 1 [json_name = "backend_service"];
        }

        message RetryPolicy {
          // Specifies the allowed number retries. This number must be > 0.
          int64 num_retries = 1 [json_name = "num_retries"];

          // Specifies one or more conditions when this retry rule applies. Valid values are:
          //
          // - 5xx: Loadbalancer will attempt a retry if the backend service responds with
          // any 5xx response code, or if the backend service does not respond at all,
          // example: disconnects, reset, read timeout, connection failure, and refused
          // streams.
          // - gateway-error: Similar to 5xx, but only applies to response codes
          // 502, 503 or 504.
          // - connect-failure: Loadbalancer will retry on failures
          // connecting to backend services, for example due to connection timeouts.
          // - retriable-4xx: Loadbalancer will retry for retriable 4xx response codes.
          // Currently the only retriable error supported is 409.
          // - refused-stream: Loadbalancer will retry if the backend service resets the stream with a
          // REFUSED_STREAM error code. This reset type indicates that it is safe to retry.
          // - cancelled: Loadbalancer will retry if the gRPC status code in the response
          // header is set to cancelled
          // - deadline-exceeded: Loadbalancer will retry if the
          // gRPC status code in the response header is set to deadline-exceeded
          // - resource-exhausted: Loadbalancer will retry if the gRPC status code in the response
          // header is set to resource-exhausted
          // - unavailable: Loadbalancer will retry if
          // the gRPC status code in the response header is set to unavailable
          repeated string retry_conditions = 2 [json_name = "retry_conditions"];

          PerTryTimeout per_try_timeout = 3 [json_name = "per_try_timeout"];

          message PerTryTimeout {
            // Span of time that's a fraction of a second at nanosecond resolution. Durations
            // less than one second are represented with a 0 'seconds' field and a positive
            // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
            int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            // inclusive.
            string seconds = 2;
          }
        }

        message Timeout {
          // Span of time that's a fraction of a second at nanosecond resolution. Durations
          // less than one second are represented with a 0 'seconds' field and a positive
          // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
          int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          // inclusive.
          string seconds = 2;
        }

        message UrlRewrite {
          // Prior to forwarding the request to the selected service, the request's host
          // header is replaced with contents of hostRewrite. The value must be between 1 and
          // 255 characters.
          string host_rewrite = 1 [json_name = "host_rewrite"];

          // Prior to forwarding the request to the selected backend service, the matching
          // portion of the request's path is replaced by pathPrefixRewrite. The value must
          // be between 1 and 1024 characters.
          string path_prefix_rewrite = 2 [json_name = "path_prefix_rewrite"];
        }

        message WeightedBackendServices {
          // The default RegionBackendService resource. Before
          // forwarding the request to backendService, the loadbalancer applies any relevant
          // headerActions specified as part of this backendServiceWeight.
          string backend_service = 1 [json_name = "backend_service"];

          // Specifies the fraction of traffic sent to backendService, computed as weight /
          // (sum of all weightedBackendService weights in routeAction) . The selection of a
          // backend service is determined only for new traffic. Once a user's request has
          // been directed to a backendService, subsequent requests will be sent to the same
          // backendService as determined by the BackendService's session affinity policy.
          // The value must be between 0 and 1000
          int64 weight = 2;

          HeaderAction header_action = 3 [json_name = "header_action"];

          message HeaderAction {
            // A list of header names for headers that need to be removed from the request
            // prior to forwarding the request to the backendService.
            repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

            // A list of header names for headers that need to be removed from the response
            // prior to sending the response back to the client.
            repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

            repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

            repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

            message RequestHeadersToAdd {
              // The name of the header.
              string header_name = 1 [json_name = "header_name"];

              // The value of the header to add.
              string header_value = 2 [json_name = "header_value"];

              // If false, headerValue is appended to any values that already exist for the
              // header. If true, headerValue is set for the header, discarding any values that
              // were set for that header.
              bool replace = 3;
            }

            message ResponseHeadersToAdd {
              // The name of the header.
              string header_name = 1 [json_name = "header_name"];

              // The value of the header to add.
              string header_value = 2 [json_name = "header_value"];

              // If false, headerValue is appended to any values that already exist for the
              // header. If true, headerValue is set for the header, discarding any values that
              // were set for that header.
              bool replace = 3;
            }
          }
        }
      }

      message UrlRedirect {
        // The host that will be used in the redirect response instead of the one
        // that was supplied in the request. The value must be between 1 and 255
        // characters.
        string host_redirect = 1 [json_name = "host_redirect"];

        // If set to true, the URL scheme in the redirected request is set to https.
        // If set to false, the URL scheme of the redirected request will remain the
        // same as that of the request. This must only be set for UrlMaps used in
        // TargetHttpProxys. Setting this true for TargetHttpsProxy is not
        // permitted. The default is set to false.
        bool https_redirect = 2 [json_name = "https_redirect"];

        // The path that will be used in the redirect response instead of the one
        // that was supplied in the request. pathRedirect cannot be supplied
        // together with prefixRedirect. Supply one alone or neither. If neither is
        // supplied, the path of the original request will be used for the redirect.
        // The value must be between 1 and 1024 characters.
        string path_redirect = 3 [json_name = "path_redirect"];

        // The prefix that replaces the prefixMatch specified in the
        // HttpRouteRuleMatch, retaining the remaining portion of the URL before
        // redirecting the request. prefixRedirect cannot be supplied together with
        // pathRedirect. Supply one alone or neither. If neither is supplied, the
        // path of the original request will be used for the redirect. The value
        // must be between 1 and 1024 characters.
        string prefix_redirect = 4 [json_name = "prefix_redirect"];

        // The HTTP Status code to use for this RedirectAction. Supported values are:
        //
        // * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301.
        //
        // * FOUND, which corresponds to 302.
        //
        // * SEE_OTHER which corresponds to 303.
        //
        // * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method
        // will be retained.
        //
        // * PERMANENT_REDIRECT, which corresponds to 308. In this case,
        // the request method will be retained. Possible values: ["FOUND", "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER", "TEMPORARY_REDIRECT"]
        string redirect_response_code = 5 [json_name = "redirect_response_code"];

        // If set to true, any accompanying query portion of the original URL is removed
        // prior to redirecting the request. If set to false, the query portion of the
        // original URL is retained.
        // This field is required to ensure an empty block is not set. The normal default value is false.
        bool strip_query = 6 [json_name = "strip_query"];
      }
    }

    message RouteRules {
      // For routeRules within a given pathMatcher, priority determines the order
      // in which load balancer will interpret routeRules. RouteRules are evaluated
      // in order of priority, from the lowest to highest number. The priority of
      // a rule decreases as its number increases (1, 2, 3, N+1). The first rule
      // that matches the request is applied.
      //
      // You cannot configure two or more routeRules with the same priority.
      // Priority for each rule must be set to a number between 0 and
      // 2147483647 inclusive.
      //
      // Priority numbers can have gaps, which enable you to add or remove rules
      // in the future without affecting the rest of the rules. For example,
      // 1, 2, 3, 4, 5, 9, 12, 16 is a valid series of priority numbers to which
      // you could add rules numbered from 6 to 8, 10 to 11, and 13 to 15 in the
      // future without any impact on existing rules.
      int64 priority = 1;

      // The region backend service resource to which traffic is
      // directed if this rule is matched. If routeAction is additionally specified,
      // advanced routing actions like URL Rewrites, etc. take effect prior to sending
      // the request to the backend. However, if service is specified, routeAction cannot
      // contain any weightedBackendService s. Conversely, if routeAction specifies any
      // weightedBackendServices, service must not be specified. Only one of urlRedirect,
      // service or routeAction.weightedBackendService must be set.
      string service = 2;

      HeaderAction header_action = 3 [json_name = "header_action"];

      repeated MatchRules match_rules = 4 [json_name = "match_rules"];

      RouteAction route_action = 5 [json_name = "route_action"];

      UrlRedirect url_redirect = 6 [json_name = "url_redirect"];

      message HeaderAction {
        // A list of header names for headers that need to be removed from the request
        // prior to forwarding the request to the backendService.
        repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

        // A list of header names for headers that need to be removed from the response
        // prior to sending the response back to the client.
        repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

        repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

        repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

        message RequestHeadersToAdd {
          // The name of the header.
          string header_name = 1 [json_name = "header_name"];

          // The value of the header to add.
          string header_value = 2 [json_name = "header_value"];

          // If false, headerValue is appended to any values that already exist for the
          // header. If true, headerValue is set for the header, discarding any values that
          // were set for that header.
          bool replace = 3;
        }

        message ResponseHeadersToAdd {
          // The name of the header.
          string header_name = 1 [json_name = "header_name"];

          // The value of the header to add.
          string header_value = 2 [json_name = "header_value"];

          // If false, headerValue is appended to any values that already exist for the
          // header. If true, headerValue is set for the header, discarding any values that
          // were set for that header.
          bool replace = 3;
        }
      }

      message MatchRules {
        // For satisfying the matchRule condition, the path of the request must exactly
        // match the value specified in fullPathMatch after removing any query parameters
        // and anchor that may be part of the original URL. FullPathMatch must be between 1
        // and 1024 characters. Only one of prefixMatch, fullPathMatch or regexMatch must
        // be specified.
        string full_path_match = 1 [json_name = "full_path_match"];

        // Specifies that prefixMatch and fullPathMatch matches are case sensitive.
        // Defaults to false.
        bool ignore_case = 2 [json_name = "ignore_case"];

        // For satisfying the matchRule condition, the request's path must begin with the
        // specified prefixMatch. prefixMatch must begin with a /. The value must be
        // between 1 and 1024 characters. Only one of prefixMatch, fullPathMatch or
        // regexMatch must be specified.
        string prefix_match = 3 [json_name = "prefix_match"];

        // For satisfying the matchRule condition, the path of the request must satisfy the
        // regular expression specified in regexMatch after removing any query parameters
        // and anchor supplied with the original URL. For regular expression grammar please
        // see en.cppreference.com/w/cpp/regex/ecmascript  Only one of prefixMatch,
        // fullPathMatch or regexMatch must be specified.
        string regex_match = 4 [json_name = "regex_match"];

        repeated HeaderMatches header_matches = 5 [json_name = "header_matches"];

        repeated MetadataFilters metadata_filters = 6 [json_name = "metadata_filters"];

        repeated QueryParameterMatches query_parameter_matches = 7 [json_name = "query_parameter_matches"];

        message HeaderMatches {
          // The value should exactly match contents of exactMatch. Only one of exactMatch,
          // prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch must be set.
          string exact_match = 1 [json_name = "exact_match"];

          // The name of the HTTP header to match. For matching against the HTTP request's
          // authority, use a headerMatch with the header name ":authority". For matching a
          // request's method, use the headerName ":method".
          string header_name = 2 [json_name = "header_name"];

          // If set to false, the headerMatch is considered a match if the match criteria
          // above are met. If set to true, the headerMatch is considered a match if the
          // match criteria above are NOT met. Defaults to false.
          bool invert_match = 3 [json_name = "invert_match"];

          // The value of the header must start with the contents of prefixMatch. Only one of
          // exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch
          // must be set.
          string prefix_match = 4 [json_name = "prefix_match"];

          // A header with the contents of headerName must exist. The match takes place
          // whether or not the request's header has a value or not. Only one of exactMatch,
          // prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch must be set.
          bool present_match = 5 [json_name = "present_match"];

          // The value of the header must match the regular expression specified in
          // regexMatch. For regular expression grammar, please see:
          // en.cppreference.com/w/cpp/regex/ecmascript  For matching against a port
          // specified in the HTTP request, use a headerMatch with headerName set to PORT and
          // a regular expression that satisfies the RFC2616 Host header's port specifier.
          // Only one of exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or
          // rangeMatch must be set.
          string regex_match = 6 [json_name = "regex_match"];

          // The value of the header must end with the contents of suffixMatch. Only one of
          // exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch
          // must be set.
          string suffix_match = 7 [json_name = "suffix_match"];

          RangeMatch range_match = 8 [json_name = "range_match"];

          message RangeMatch {
            // The end of the range (exclusive).
            int64 range_end = 1 [json_name = "range_end"];

            // The start of the range (inclusive).
            int64 range_start = 2 [json_name = "range_start"];
          }
        }

        message MetadataFilters {
          // Specifies how individual filterLabel matches within the list of filterLabels
          // contribute towards the overall metadataFilter match. Supported values are:
          //
          // * MATCH_ANY: At least one of the filterLabels must have a matching label in the
          // provided metadata.
          // * MATCH_ALL: All filterLabels must have matching labels in
          // the provided metadata. Possible values: ["MATCH_ALL", "MATCH_ANY"]
          string filter_match_criteria = 1 [json_name = "filter_match_criteria"];

          repeated FilterLabels filter_labels = 2 [json_name = "filter_labels"];

          message FilterLabels {
            // Name of metadata label. The name can have a maximum length of 1024 characters
            // and must be at least 1 character long.
            string name = 1;

            // The value of the label must match the specified value. value can have a maximum
            // length of 1024 characters.
            string value = 2;
          }
        }

        message QueryParameterMatches {
          // The queryParameterMatch matches if the value of the parameter exactly matches
          // the contents of exactMatch. Only one of presentMatch, exactMatch and regexMatch
          // must be set.
          string exact_match = 1 [json_name = "exact_match"];

          // The name of the query parameter to match. The query parameter must exist in the
          // request, in the absence of which the request match fails.
          string name = 2;

          // Specifies that the queryParameterMatch matches if the request contains the query
          // parameter, irrespective of whether the parameter has a value or not. Only one of
          // presentMatch, exactMatch and regexMatch must be set.
          bool present_match = 3 [json_name = "present_match"];

          // The queryParameterMatch matches if the value of the parameter matches the
          // regular expression specified by regexMatch. For the regular expression grammar,
          // please see en.cppreference.com/w/cpp/regex/ecmascript  Only one of presentMatch,
          // exactMatch and regexMatch must be set.
          string regex_match = 4 [json_name = "regex_match"];
        }
      }

      message RouteAction {
        CorsPolicy cors_policy = 1 [json_name = "cors_policy"];

        FaultInjectionPolicy fault_injection_policy = 2 [json_name = "fault_injection_policy"];

        RequestMirrorPolicy request_mirror_policy = 3 [json_name = "request_mirror_policy"];

        RetryPolicy retry_policy = 4 [json_name = "retry_policy"];

        Timeout timeout = 5;

        UrlRewrite url_rewrite = 6 [json_name = "url_rewrite"];

        repeated WeightedBackendServices weighted_backend_services = 7 [json_name = "weighted_backend_services"];

        message CorsPolicy {
          // In response to a preflight request, setting this to true indicates that the
          // actual request can include user credentials. This translates to the Access-
          // Control-Allow-Credentials header. Defaults to false.
          bool allow_credentials = 1 [json_name = "allow_credentials"];

          // Specifies the content for the Access-Control-Allow-Headers header.
          repeated string allow_headers = 2 [json_name = "allow_headers"];

          // Specifies the content for the Access-Control-Allow-Methods header.
          repeated string allow_methods = 3 [json_name = "allow_methods"];

          // Specifies the regular expression patterns that match allowed origins. For
          // regular expression grammar please see en.cppreference.com/w/cpp/regex/ecmascript
          // An origin is allowed if it matches either allow_origins or allow_origin_regex.
          repeated string allow_origin_regexes = 4 [json_name = "allow_origin_regexes"];

          // Specifies the list of origins that will be allowed to do CORS requests. An
          // origin is allowed if it matches either allow_origins or allow_origin_regex.
          repeated string allow_origins = 5 [json_name = "allow_origins"];

          // If true, specifies the CORS policy is disabled.
          // which indicates that the CORS policy is in effect. Defaults to false.
          bool disabled = 6;

          // Specifies the content for the Access-Control-Expose-Headers header.
          repeated string expose_headers = 7 [json_name = "expose_headers"];

          // Specifies how long the results of a preflight request can be cached. This
          // translates to the content for the Access-Control-Max-Age header.
          int64 max_age = 8 [json_name = "max_age"];
        }

        message FaultInjectionPolicy {
          Abort abort = 1;

          Delay delay = 2;

          message Abort {
            // The HTTP status code used to abort the request. The value must be between 200
            // and 599 inclusive.
            int64 http_status = 1 [json_name = "http_status"];

            // The percentage of traffic (connections/operations/requests) which will be
            // aborted as part of fault injection. The value must be between 0.0 and 100.0
            // inclusive.
            int64 percentage = 2;
          }

          message Delay {
            // The percentage of traffic (connections/operations/requests) on which delay will
            // be introduced as part of fault injection. The value must be between 0.0 and
            // 100.0 inclusive.
            int64 percentage = 1;

            FixedDelay fixed_delay = 2 [json_name = "fixed_delay"];

            message FixedDelay {
              // Span of time that's a fraction of a second at nanosecond resolution. Durations
              // less than one second are represented with a 0 'seconds' field and a positive
              // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
              int64 nanos = 1;

              // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
              // inclusive.
              string seconds = 2;
            }
          }
        }

        message RequestMirrorPolicy {
          // The RegionBackendService resource being mirrored to.
          string backend_service = 1 [json_name = "backend_service"];
        }

        message RetryPolicy {
          // Specifies the allowed number retries. This number must be > 0.
          int64 num_retries = 1 [json_name = "num_retries"];

          // Specifies one or more conditions when this retry rule applies. Valid values are:
          //
          // * 5xx: Loadbalancer will attempt a retry if the backend service responds with
          //  any 5xx response code, or if the backend service does not respond at all,
          //  example: disconnects, reset, read timeout, connection failure, and refused
          //  streams.
          // * gateway-error: Similar to 5xx, but only applies to response codes
          //  502, 503 or 504.
          // * connect-failure: Loadbalancer will retry on failures
          //  connecting to backend services, for example due to connection timeouts.
          // * retriable-4xx: Loadbalancer will retry for retriable 4xx response codes.
          //  Currently the only retriable error supported is 409.
          // * refused-stream: Loadbalancer will retry if the backend service resets the stream with a
          //  REFUSED_STREAM error code. This reset type indicates that it is safe to retry.
          // * cancelled: Loadbalancer will retry if the gRPC status code in the response
          //  header is set to cancelled
          // * deadline-exceeded: Loadbalancer will retry if the
          //  gRPC status code in the response header is set to deadline-exceeded
          // * resource-exhausted: Loadbalancer will retry if the gRPC status code in the response
          //  header is set to resource-exhausted
          // * unavailable: Loadbalancer will retry if the gRPC status code in
          //  the response header is set to unavailable
          repeated string retry_conditions = 2 [json_name = "retry_conditions"];

          PerTryTimeout per_try_timeout = 3 [json_name = "per_try_timeout"];

          message PerTryTimeout {
            // Span of time that's a fraction of a second at nanosecond resolution. Durations
            // less than one second are represented with a 0 'seconds' field and a positive
            // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
            int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            // inclusive.
            string seconds = 2;
          }
        }

        message Timeout {
          // Span of time that's a fraction of a second at nanosecond resolution. Durations
          // less than one second are represented with a 0 'seconds' field and a positive
          // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
          int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          // inclusive.
          string seconds = 2;
        }

        message UrlRewrite {
          // Prior to forwarding the request to the selected service, the request's host
          // header is replaced with contents of hostRewrite. The value must be between 1 and
          // 255 characters.
          string host_rewrite = 1 [json_name = "host_rewrite"];

          // Prior to forwarding the request to the selected backend service, the matching
          // portion of the request's path is replaced by pathPrefixRewrite. The value must
          // be between 1 and 1024 characters.
          string path_prefix_rewrite = 2 [json_name = "path_prefix_rewrite"];
        }

        message WeightedBackendServices {
          // The default RegionBackendService resource. Before
          // forwarding the request to backendService, the loadbalancer applies any relevant
          // headerActions specified as part of this backendServiceWeight.
          string backend_service = 1 [json_name = "backend_service"];

          // Specifies the fraction of traffic sent to backendService, computed as weight /
          // (sum of all weightedBackendService weights in routeAction) . The selection of a
          // backend service is determined only for new traffic. Once a user's request has
          // been directed to a backendService, subsequent requests will be sent to the same
          // backendService as determined by the BackendService's session affinity policy.
          // The value must be between 0 and 1000
          int64 weight = 2;

          HeaderAction header_action = 3 [json_name = "header_action"];

          message HeaderAction {
            // A list of header names for headers that need to be removed from the request
            // prior to forwarding the request to the backendService.
            repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

            // A list of header names for headers that need to be removed from the response
            // prior to sending the response back to the client.
            repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

            repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

            repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

            message RequestHeadersToAdd {
              // The name of the header.
              string header_name = 1 [json_name = "header_name"];

              // The value of the header to add.
              string header_value = 2 [json_name = "header_value"];

              // If false, headerValue is appended to any values that already exist for the
              // header. If true, headerValue is set for the header, discarding any values that
              // were set for that header.
              bool replace = 3;
            }

            message ResponseHeadersToAdd {
              // The name of the header.
              string header_name = 1 [json_name = "header_name"];

              // The value of the header to add.
              string header_value = 2 [json_name = "header_value"];

              // If false, headerValue is appended to any values that already exist for the
              // header. If true, headerValue is set for the header, discarding any values that
              // were set for that header.
              bool replace = 3;
            }
          }
        }
      }

      message UrlRedirect {
        // The host that will be used in the redirect response instead of the one
        // that was supplied in the request. The value must be between 1 and 255
        // characters.
        string host_redirect = 1 [json_name = "host_redirect"];

        // If set to true, the URL scheme in the redirected request is set to https.
        // If set to false, the URL scheme of the redirected request will remain the
        // same as that of the request. This must only be set for UrlMaps used in
        // TargetHttpProxys. Setting this true for TargetHttpsProxy is not
        // permitted. The default is set to false.
        bool https_redirect = 2 [json_name = "https_redirect"];

        // The path that will be used in the redirect response instead of the one
        // that was supplied in the request. pathRedirect cannot be supplied
        // together with prefixRedirect. Supply one alone or neither. If neither is
        // supplied, the path of the original request will be used for the redirect.
        // The value must be between 1 and 1024 characters.
        string path_redirect = 3 [json_name = "path_redirect"];

        // The prefix that replaces the prefixMatch specified in the
        // HttpRouteRuleMatch, retaining the remaining portion of the URL before
        // redirecting the request. prefixRedirect cannot be supplied together with
        // pathRedirect. Supply one alone or neither. If neither is supplied, the
        // path of the original request will be used for the redirect. The value
        // must be between 1 and 1024 characters.
        string prefix_redirect = 4 [json_name = "prefix_redirect"];

        // The HTTP Status code to use for this RedirectAction. Supported values are:
        //
        // * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301.
        //
        // * FOUND, which corresponds to 302.
        //
        // * SEE_OTHER which corresponds to 303.
        //
        // * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method
        // will be retained.
        //
        // * PERMANENT_REDIRECT, which corresponds to 308. In this case,
        // the request method will be retained. Possible values: ["FOUND", "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER", "TEMPORARY_REDIRECT"]
        string redirect_response_code = 5 [json_name = "redirect_response_code"];

        // If set to true, any accompanying query portion of the original URL is
        // removed prior to redirecting the request. If set to false, the query
        // portion of the original URL is retained. The default value is false.
        bool strip_query = 6 [json_name = "strip_query"];
      }
    }
  }

  message Test {
    // Description of this test case.
    string description = 1;

    // Host portion of the URL.
    string host = 2;

    // Path portion of the URL.
    string path = 3;

    // A reference to expected RegionBackendService resource the given URL should be mapped to.
    string service = 4;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeReservation version is 0
message GoogleComputeReservation {
  // Full or partial URL to a parent commitment. This field displays for
  // reservations that are tied to a commitment.
  string commitment = 1;

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 3;

  string id = 4;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 5;

  string project = 6;

  string self_link = 7 [json_name = "self_link"];

  // When set to true, only VMs that target this reservation by name can
  // consume this reservation. Otherwise, it can be consumed by VMs with
  // affinity for any reservation. Defaults to false.
  bool specific_reservation_required = 8 [json_name = "specific_reservation_required"];

  // The status of the reservation.
  string status = 9;

  // The zone where the reservation is made.
  string zone = 10;

  ShareSettings share_settings = 11 [json_name = "share_settings"];

  SpecificReservation specific_reservation = 12 [json_name = "specific_reservation"];

  repeated Timeouts timeouts = 13;

  map<string, string> for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  int32 count = 16;

  string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message ShareSettings {
    // Type of sharing for this shared-reservation Possible values: ["LOCAL", "SPECIFIC_PROJECTS"]
    string share_type = 1 [json_name = "share_type"];

    repeated ProjectMap project_map = 2 [json_name = "project_map"];

    message ProjectMap {
      string id = 1;

      // The project id/number, should be same as the key of this project config in the project map.
      string project_id = 2 [json_name = "project_id"];
    }
  }

  message SpecificReservation {
    // The number of resources that are allocated.
    int64 count = 1;

    // How many instances are in use.
    int64 in_use_count = 2 [json_name = "in_use_count"];

    InstanceProperties instance_properties = 3 [json_name = "instance_properties"];

    message InstanceProperties {
      // The name of the machine type to reserve.
      string machine_type = 1 [json_name = "machine_type"];

      // The minimum CPU platform for the reservation. For example,
      // '"Intel Skylake"'. See
      // the CPU platform availability reference](https://cloud.google.com/compute/docs/instances/specify-min-cpu-platform#availablezones)
      // for information on available CPU platforms.
      string min_cpu_platform = 2 [json_name = "min_cpu_platform"];

      repeated GuestAccelerators guest_accelerators = 3 [json_name = "guest_accelerators"];

      repeated LocalSsds local_ssds = 4 [json_name = "local_ssds"];

      message GuestAccelerators {
        // The number of the guest accelerator cards exposed to
        // this instance.
        int64 accelerator_count = 1 [json_name = "accelerator_count"];

        // The full or partial URL of the accelerator type to
        // attach to this instance. For example:
        // 'projects/my-project/zones/us-central1-c/acceleratorTypes/nvidia-tesla-p100'
        //
        // If you are creating an instance template, specify only the accelerator name.
        string accelerator_type = 2 [json_name = "accelerator_type"];
      }

      message LocalSsds {
        // The size of the disk in base-2 GB.
        int64 disk_size_gb = 1 [json_name = "disk_size_gb"];

        // The disk interface to use for attaching this disk. Default value: "SCSI" Possible values: ["SCSI", "NVME"]
        string interface = 2;
      }
    }
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeResourcePolicy version is 0
message GoogleComputeResourcePolicy {
  // An optional description of this resource. Provide this property when you create the resource.
  string description = 1;

  string id = 2;

  // The name of the resource, provided by the client when initially creating
  // the resource. The resource name must be 1-63 characters long, and comply
  // with RFC1035. Specifically, the name must be 1-63 characters long and
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])'? which means the
  // first character must be a lowercase letter, and all following characters
  // must be a dash, lowercase letter, or digit, except the last character,
  // which cannot be a dash.
  string name = 3;

  string project = 4;

  // Region where resource policy resides.
  string region = 5;

  string self_link = 6 [json_name = "self_link"];

  GroupPlacementPolicy group_placement_policy = 7 [json_name = "group_placement_policy"];

  InstanceSchedulePolicy instance_schedule_policy = 8 [json_name = "instance_schedule_policy"];

  SnapshotSchedulePolicy snapshot_schedule_policy = 9 [json_name = "snapshot_schedule_policy"];

  repeated Timeouts timeouts = 10;

  map<string, string> for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message GroupPlacementPolicy {
    // The number of availability domains instances will be spread across. If two instances are in different
    // availability domain, they will not be put in the same low latency network
    int64 availability_domain_count = 1 [json_name = "availability_domain_count"];

    // Collocation specifies whether to place VMs inside the same availability domain on the same low-latency network.
    // Specify 'COLLOCATED' to enable collocation. Can only be specified with 'vm_count'. If compute instances are created
    // with a COLLOCATED policy, then exactly 'vm_count' instances must be created at the same time with the resource policy
    // attached. Possible values: ["COLLOCATED"]
    string collocation = 2;

    // Number of VMs in this placement group. Google does not recommend that you use this field
    // unless you use a compact policy and you want your policy to work only if it contains this
    // exact number of VMs.
    int64 vm_count = 3 [json_name = "vm_count"];
  }

  message InstanceSchedulePolicy {
    // The expiration time of the schedule. The timestamp is an RFC3339 string.
    string expiration_time = 1 [json_name = "expiration_time"];

    // The start time of the schedule. The timestamp is an RFC3339 string.
    string start_time = 2 [json_name = "start_time"];

    // Specifies the time zone to be used in interpreting the schedule. The value of this field must be a time zone name
    // from the tz database: http://en.wikipedia.org/wiki/Tz_database.
    string time_zone = 3 [json_name = "time_zone"];

    VmStartSchedule vm_start_schedule = 4 [json_name = "vm_start_schedule"];

    VmStopSchedule vm_stop_schedule = 5 [json_name = "vm_stop_schedule"];

    message VmStartSchedule {
      // Specifies the frequency for the operation, using the unix-cron format.
      string schedule = 1;
    }

    message VmStopSchedule {
      // Specifies the frequency for the operation, using the unix-cron format.
      string schedule = 1;
    }
  }

  message SnapshotSchedulePolicy {
    RetentionPolicy retention_policy = 1 [json_name = "retention_policy"];

    Schedule schedule = 2;

    SnapshotProperties snapshot_properties = 3 [json_name = "snapshot_properties"];

    message RetentionPolicy {
      // Maximum age of the snapshot that is allowed to be kept.
      int64 max_retention_days = 1 [json_name = "max_retention_days"];

      // Specifies the behavior to apply to scheduled snapshots when
      // the source disk is deleted. Default value: "KEEP_AUTO_SNAPSHOTS" Possible values: ["KEEP_AUTO_SNAPSHOTS", "APPLY_RETENTION_POLICY"]
      string on_source_disk_delete = 2 [json_name = "on_source_disk_delete"];
    }

    message Schedule {
      DailySchedule daily_schedule = 1 [json_name = "daily_schedule"];

      HourlySchedule hourly_schedule = 2 [json_name = "hourly_schedule"];

      WeeklySchedule weekly_schedule = 3 [json_name = "weekly_schedule"];

      message DailySchedule {
        // The number of days between snapshots.
        int64 days_in_cycle = 1 [json_name = "days_in_cycle"];

        // This must be in UTC format that resolves to one of
        // 00:00, 04:00, 08:00, 12:00, 16:00, or 20:00. For example,
        // both 13:00-5 and 08:00 are valid.
        string start_time = 2 [json_name = "start_time"];
      }

      message HourlySchedule {
        // The number of hours between snapshots.
        int64 hours_in_cycle = 1 [json_name = "hours_in_cycle"];

        // Time within the window to start the operations.
        // It must be in an hourly format "HH:MM",
        // where HH : [00-23] and MM : [00] GMT.
        // eg: 21:00
        string start_time = 2 [json_name = "start_time"];
      }

      message WeeklySchedule {
        repeated DayOfWeeks day_of_weeks = 1 [json_name = "day_of_weeks"];

        message DayOfWeeks {
          // The day of the week to create the snapshot. e.g. MONDAY Possible values: ["MONDAY", "TUESDAY", "WEDNESDAY", "THURSDAY", "FRIDAY", "SATURDAY", "SUNDAY"]
          string day = 1;

          // Time within the window to start the operations.
          // It must be in format "HH:MM", where HH : [00-23] and MM : [00-00] GMT.
          string start_time = 2 [json_name = "start_time"];
        }
      }
    }

    message SnapshotProperties {
      // Creates the new snapshot in the snapshot chain labeled with the
      // specified name. The chain name must be 1-63 characters long and comply
      // with RFC1035.
      string chain_name = 1 [json_name = "chain_name"];

      // Whether to perform a 'guest aware' snapshot.
      bool guest_flush = 2 [json_name = "guest_flush"];

      // A set of key-value pairs.
      map<string, string> labels = 3;

      // Cloud Storage bucket location to store the auto snapshot
      // (regional or multi-regional)
      repeated string storage_locations = 4 [json_name = "storage_locations"];
    }
  }

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeRoute version is 0
message GoogleComputeRoute {
  // An optional description of this resource. Provide this property
  // when you create the resource.
  string description = 1;

  // The destination range of outgoing packets that this route applies to.
  // Only IPv4 is supported.
  string dest_range = 2 [json_name = "dest_range"];

  string id = 3;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  // the first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the
  // last character, which cannot be a dash.
  string name = 4;

  // The network that this route applies to.
  string network = 5;

  // URL to a gateway that should handle matching packets.
  // Currently, you can only specify the internet gateway, using a full or
  // partial valid URL:
  // * 'https://www.googleapis.com/compute/v1/projects/project/global/gateways/default-internet-gateway'
  // * 'projects/project/global/gateways/default-internet-gateway'
  // * 'global/gateways/default-internet-gateway'
  // * The string 'default-internet-gateway'.
  string next_hop_gateway = 6 [json_name = "next_hop_gateway"];

  // The IP address or URL to a forwarding rule of type
  // loadBalancingScheme=INTERNAL that should handle matching
  // packets.
  //
  // With the GA provider you can only specify the forwarding
  // rule as a partial or full URL. For example, the following
  // are all valid values:
  // * 10.128.0.56
  // * https://www.googleapis.com/compute/v1/projects/project/regions/region/forwardingRules/forwardingRule
  // * regions/region/forwardingRules/forwardingRule
  //
  // When the beta provider, you can also specify the IP address
  // of a forwarding rule from the same VPC or any peered VPC.
  //
  // Note that this can only be used when the destinationRange is
  // a public (non-RFC 1918) IP CIDR range.
  string next_hop_ilb = 7 [json_name = "next_hop_ilb"];

  // URL to an instance that should handle matching packets.
  // You can specify this as a full or partial URL. For example:
  // * 'https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance'
  // * 'projects/project/zones/zone/instances/instance'
  // * 'zones/zone/instances/instance'
  // * Just the instance name, with the zone in 'next_hop_instance_zone'.
  string next_hop_instance = 8 [json_name = "next_hop_instance"];

  // The zone of the instance specified in next_hop_instance. Omit if next_hop_instance is specified as a URL.
  string next_hop_instance_zone = 9 [json_name = "next_hop_instance_zone"];

  // Network IP address of an instance that should handle matching packets.
  string next_hop_ip = 10 [json_name = "next_hop_ip"];

  // URL to a Network that should handle matching packets.
  string next_hop_network = 11 [json_name = "next_hop_network"];

  // URL to a VpnTunnel that should handle matching packets.
  string next_hop_vpn_tunnel = 12 [json_name = "next_hop_vpn_tunnel"];

  // The priority of this route. Priority is used to break ties in cases
  // where there is more than one matching route of equal prefix length.
  //
  // In the case of two routes with equal prefix length, the one with the
  // lowest-numbered priority value wins.
  //
  // Default value is 1000. Valid range is 0 through 65535.
  int64 priority = 13;

  string project = 14;

  string self_link = 15 [json_name = "self_link"];

  // A list of instance tags to which this route applies.
  repeated string tags = 16;

  repeated Timeouts timeouts = 17;

  map<string, string> for_each = 18 [json_name = "for_each"];

  repeated string depends_on = 19 [json_name = "depends_on"];

  int32 count = 20;

  string provider = 21;

  terraform.v1.Lifecycle lifecycle = 22;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeRouter version is 0
message GoogleComputeRouter {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 2;

  // Indicates if a router is dedicated for use with encrypted VLAN
  // attachments (interconnectAttachments).
  bool encrypted_interconnect_router = 3 [json_name = "encrypted_interconnect_router"];

  string id = 4;

  // Name of the resource. The name must be 1-63 characters long, and
  // comply with RFC1035. Specifically, the name must be 1-63 characters
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?'
  // which means the first character must be a lowercase letter, and all
  // following characters must be a dash, lowercase letter, or digit,
  // except the last character, which cannot be a dash.
  string name = 5;

  // A reference to the network to which this router belongs.
  string network = 6;

  string project = 7;

  // Region where the router resides.
  string region = 8;

  string self_link = 9 [json_name = "self_link"];

  Bgp bgp = 10;

  repeated Timeouts timeouts = 11;

  map<string, string> for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  int32 count = 14;

  string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;

  message Bgp {
    // User-specified flag to indicate which mode to use for advertisement. Default value: "DEFAULT" Possible values: ["DEFAULT", "CUSTOM"]
    string advertise_mode = 1 [json_name = "advertise_mode"];

    // User-specified list of prefix groups to advertise in custom mode.
    // This field can only be populated if advertiseMode is CUSTOM and
    // is advertised to all peers of the router. These groups will be
    // advertised in addition to any specified prefixes. Leave this field
    // blank to advertise no custom groups.
    //
    // This enum field has the one valid value: ALL_SUBNETS
    repeated string advertised_groups = 2 [json_name = "advertised_groups"];

    // Local BGP Autonomous System Number (ASN). Must be an RFC6996
    // private ASN, either 16-bit or 32-bit. The value will be fixed for
    // this router resource. All VPN tunnels that link to this router
    // will have the same local ASN.
    int64 asn = 3;

    // The interval in seconds between BGP keepalive messages that are sent
    // to the peer. Hold time is three times the interval at which keepalive
    // messages are sent, and the hold time is the maximum number of seconds
    // allowed to elapse between successive keepalive messages that BGP
    // receives from a peer.
    //
    // BGP will use the smaller of either the local hold time value or the
    // peer's hold time value as the hold time for the BGP connection
    // between the two peers. If set, this value must be between 20 and 60.
    // The default is 20.
    int64 keepalive_interval = 4 [json_name = "keepalive_interval"];

    repeated AdvertisedIpRanges advertised_ip_ranges = 5 [json_name = "advertised_ip_ranges"];

    message AdvertisedIpRanges {
      // User-specified description for the IP range.
      string description = 1;

      // The IP range to advertise. The value must be a
      // CIDR-formatted string.
      string range = 2;
    }
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeRouterInterface version is 0
message GoogleComputeRouterInterface {
  string id = 1;

  // The name or resource link to the VLAN interconnect for this interface. Changing this forces a new interface to be created. Only one of interconnect_attachment, subnetwork or vpn_tunnel can be specified.
  string interconnect_attachment = 2 [json_name = "interconnect_attachment"];

  // The IP address and range of the interface. The IP range must be in the RFC3927 link-local IP space. Changing this forces a new interface to be created.
  string ip_range = 3 [json_name = "ip_range"];

  // A unique name for the interface, required by GCE. Changing this forces a new interface to be created.
  string name = 4;

  // The regional private internal IP address that is used to establish BGP sessions to a VM instance acting as a third-party Router Appliance. Changing this forces a new interface to be created.
  string private_ip_address = 5 [json_name = "private_ip_address"];

  // The ID of the project in which this interface's router belongs. If it is not provided, the provider project is used. Changing this forces a new interface to be created.
  string project = 6;

  // The name of the interface that is redundant to this interface. Changing this forces a new interface to be created.
  string redundant_interface = 7 [json_name = "redundant_interface"];

  // The region this interface's router sits in. If not specified, the project region will be used. Changing this forces a new interface to be created.
  string region = 8;

  // The name of the router this interface will be attached to. Changing this forces a new interface to be created.
  string router = 9;

  // The URI of the subnetwork resource that this interface belongs to, which must be in the same region as the Cloud Router. Changing this forces a new interface to be created. Only one of subnetwork, interconnect_attachment or vpn_tunnel can be specified.
  string subnetwork = 10;

  // The name or resource link to the VPN tunnel this interface will be linked to. Changing this forces a new interface to be created. Only one of vpn_tunnel, interconnect_attachment or subnetwork can be specified.
  string vpn_tunnel = 11 [json_name = "vpn_tunnel"];

  repeated Timeouts timeouts = 12;

  map<string, string> for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  int32 count = 15;

  string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeRouterNat version is 0
message GoogleComputeRouterNat {
  // A list of URLs of the IP resources to be drained. These IPs must be
  // valid static external IPs that have been assigned to the NAT.
  repeated string drain_nat_ips = 1 [json_name = "drain_nat_ips"];

  // Enable Dynamic Port Allocation.
  // If minPortsPerVm is set, minPortsPerVm must be set to a power of two greater than or equal to 32.
  // If minPortsPerVm is not set, a minimum of 32 ports will be allocated to a VM from this NAT config.
  // If maxPortsPerVm is set, maxPortsPerVm must be set to a power of two greater than minPortsPerVm.
  // If maxPortsPerVm is not set, a maximum of 65536 ports will be allocated to a VM from this NAT config.
  //
  // Mutually exclusive with enableEndpointIndependentMapping.
  bool enable_dynamic_port_allocation = 2 [json_name = "enable_dynamic_port_allocation"];

  // Specifies if endpoint independent mapping is enabled. This is enabled by default. For more information
  // see the [official documentation](https://cloud.google.com/nat/docs/overview#specs-rfcs).
  bool enable_endpoint_independent_mapping = 3 [json_name = "enable_endpoint_independent_mapping"];

  // Timeout (in seconds) for ICMP connections. Defaults to 30s if not set.
  int64 icmp_idle_timeout_sec = 4 [json_name = "icmp_idle_timeout_sec"];

  string id = 5;

  // Maximum number of ports allocated to a VM from this NAT.
  // This field can only be set when enableDynamicPortAllocation is enabled.
  int64 max_ports_per_vm = 6 [json_name = "max_ports_per_vm"];

  // Minimum number of ports allocated to a VM from this NAT.
  int64 min_ports_per_vm = 7 [json_name = "min_ports_per_vm"];

  // Name of the NAT service. The name must be 1-63 characters long and
  // comply with RFC1035.
  string name = 8;

  // How external IPs should be allocated for this NAT. Valid values are
  // 'AUTO_ONLY' for only allowing NAT IPs allocated by Google Cloud
  // Platform, or 'MANUAL_ONLY' for only user-allocated NAT IP addresses. Possible values: ["MANUAL_ONLY", "AUTO_ONLY"]
  string nat_ip_allocate_option = 9 [json_name = "nat_ip_allocate_option"];

  // Self-links of NAT IPs. Only valid if natIpAllocateOption
  // is set to MANUAL_ONLY.
  repeated string nat_ips = 10 [json_name = "nat_ips"];

  string project = 11;

  // Region where the router and NAT reside.
  string region = 12;

  // The name of the Cloud Router in which this NAT will be configured.
  string router = 13;

  // How NAT should be configured per Subnetwork.
  // If 'ALL_SUBNETWORKS_ALL_IP_RANGES', all of the
  // IP ranges in every Subnetwork are allowed to Nat.
  // If 'ALL_SUBNETWORKS_ALL_PRIMARY_IP_RANGES', all of the primary IP
  // ranges in every Subnetwork are allowed to Nat.
  // 'LIST_OF_SUBNETWORKS': A list of Subnetworks are allowed to Nat
  // (specified in the field subnetwork below). Note that if this field
  // contains ALL_SUBNETWORKS_ALL_IP_RANGES or
  // ALL_SUBNETWORKS_ALL_PRIMARY_IP_RANGES, then there should not be any
  // other RouterNat section in any Router for this network in this region. Possible values: ["ALL_SUBNETWORKS_ALL_IP_RANGES", "ALL_SUBNETWORKS_ALL_PRIMARY_IP_RANGES", "LIST_OF_SUBNETWORKS"]
  string source_subnetwork_ip_ranges_to_nat = 14 [json_name = "source_subnetwork_ip_ranges_to_nat"];

  // Timeout (in seconds) for TCP established connections.
  // Defaults to 1200s if not set.
  int64 tcp_established_idle_timeout_sec = 15 [json_name = "tcp_established_idle_timeout_sec"];

  // Timeout (in seconds) for TCP connections that are in TIME_WAIT state.
  // Defaults to 120s if not set.
  int64 tcp_time_wait_timeout_sec = 16 [json_name = "tcp_time_wait_timeout_sec"];

  // Timeout (in seconds) for TCP transitory connections.
  // Defaults to 30s if not set.
  int64 tcp_transitory_idle_timeout_sec = 17 [json_name = "tcp_transitory_idle_timeout_sec"];

  // Timeout (in seconds) for UDP connections. Defaults to 30s if not set.
  int64 udp_idle_timeout_sec = 18 [json_name = "udp_idle_timeout_sec"];

  LogConfig log_config = 19 [json_name = "log_config"];

  repeated Rules rules = 20;

  repeated Subnetwork subnetwork = 21;

  repeated Timeouts timeouts = 22;

  map<string, string> for_each = 23 [json_name = "for_each"];

  repeated string depends_on = 24 [json_name = "depends_on"];

  int32 count = 25;

  string provider = 26;

  terraform.v1.Lifecycle lifecycle = 27;

  message LogConfig {
    // Indicates whether or not to export logs.
    bool enable = 1;

    // Specifies the desired filtering of logs on this NAT. Possible values: ["ERRORS_ONLY", "TRANSLATIONS_ONLY", "ALL"]
    string filter = 2;
  }

  message Rules {
    // An optional description of this rule.
    string description = 1;

    // CEL expression that specifies the match condition that egress traffic from a VM is evaluated against.
    // If it evaluates to true, the corresponding action is enforced.
    //
    // The following examples are valid match expressions for public NAT:
    //
    // "inIpRange(destination.ip, '1.1.0.0/16') || inIpRange(destination.ip, '2.2.0.0/16')"
    //
    // "destination.ip == '1.1.0.1' || destination.ip == '8.8.8.8'"
    //
    // The following example is a valid match expression for private NAT:
    //
    // "nexthop.hub == 'https://networkconnectivity.googleapis.com/v1alpha1/projects/my-project/global/hub/hub-1'"
    string match = 2;

    // An integer uniquely identifying a rule in the list.
    // The rule number must be a positive value between 0 and 65000, and must be unique among rules within a NAT.
    int64 rule_number = 3 [json_name = "rule_number"];

    Action action = 4;

    message Action {
      // A list of URLs of the IP resources used for this NAT rule.
      // These IP addresses must be valid static external IP addresses assigned to the project.
      // This field is used for public NAT.
      repeated string source_nat_active_ips = 1 [json_name = "source_nat_active_ips"];

      // A list of URLs of the IP resources to be drained.
      // These IPs must be valid static external IPs that have been assigned to the NAT.
      // These IPs should be used for updating/patching a NAT rule only.
      // This field is used for public NAT.
      repeated string source_nat_drain_ips = 2 [json_name = "source_nat_drain_ips"];
    }
  }

  message Subnetwork {
    // Self-link of subnetwork to NAT
    string name = 1;

    // List of the secondary ranges of the subnetwork that are allowed
    // to use NAT. This can be populated only if
    // 'LIST_OF_SECONDARY_IP_RANGES' is one of the values in
    // sourceIpRangesToNat
    repeated string secondary_ip_range_names = 2 [json_name = "secondary_ip_range_names"];

    // List of options for which source IPs in the subnetwork
    // should have NAT enabled. Supported values include:
    // 'ALL_IP_RANGES', 'LIST_OF_SECONDARY_IP_RANGES',
    // 'PRIMARY_IP_RANGE'.
    repeated string source_ip_ranges_to_nat = 3 [json_name = "source_ip_ranges_to_nat"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeRouterPeer version is 0
message GoogleComputeRouterPeer {
  // User-specified flag to indicate which mode to use for advertisement.
  // Valid values of this enum field are: 'DEFAULT', 'CUSTOM' Default value: "DEFAULT" Possible values: ["DEFAULT", "CUSTOM"]
  string advertise_mode = 1 [json_name = "advertise_mode"];

  // User-specified list of prefix groups to advertise in custom
  // mode, which can take one of the following options:
  //
  // * 'ALL_SUBNETS': Advertises all available subnets, including peer VPC subnets.
  // * 'ALL_VPC_SUBNETS': Advertises the router's own VPC subnets.
  // * 'ALL_PEER_VPC_SUBNETS': Advertises peer subnets of the router's VPC network.
  //
  //
  // Note that this field can only be populated if advertiseMode is 'CUSTOM'
  // and overrides the list defined for the router (in the "bgp" message).
  // These groups are advertised in addition to any specified prefixes.
  // Leave this field blank to advertise no custom groups.
  repeated string advertised_groups = 2 [json_name = "advertised_groups"];

  // The priority of routes advertised to this BGP peer.
  // Where there is more than one matching route of maximum
  // length, the routes with the lowest priority value win.
  int64 advertised_route_priority = 3 [json_name = "advertised_route_priority"];

  // The status of the BGP peer connection. If set to false, any active session
  // with the peer is terminated and all associated routing information is removed.
  // If set to true, the peer connection can be established with routing information.
  // The default is true.
  bool enable = 4;

  // Enable IPv6 traffic over BGP Peer. If not specified, it is disabled by default.
  bool enable_ipv6 = 5 [json_name = "enable_ipv6"];

  string id = 6;

  // Name of the interface the BGP peer is associated with.
  string interface = 7;

  // IP address of the interface inside Google Cloud Platform.
  // Only IPv4 is supported.
  string ip_address = 8 [json_name = "ip_address"];

  // IPv6 address of the interface inside Google Cloud Platform.
  // The address must be in the range 2600:2d00:0:2::/64 or 2600:2d00:0:3::/64.
  // If you do not specify the next hop addresses, Google Cloud automatically
  // assigns unused addresses from the 2600:2d00:0:2::/64 or 2600:2d00:0:3::/64 range for you.
  string ipv6_nexthop_address = 9 [json_name = "ipv6_nexthop_address"];

  // The resource that configures and manages this BGP peer.
  //
  // * 'MANAGED_BY_USER' is the default value and can be managed by
  // you or other users
  // * 'MANAGED_BY_ATTACHMENT' is a BGP peer that is configured and
  // managed by Cloud Interconnect, specifically by an
  // InterconnectAttachment of type PARTNER. Google automatically
  // creates, updates, and deletes this type of BGP peer when the
  // PARTNER InterconnectAttachment is created, updated,
  // or deleted.
  string management_type = 10 [json_name = "management_type"];

  // Name of this BGP peer. The name must be 1-63 characters long,
  // and comply with RFC1035. Specifically, the name must be 1-63 characters
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which
  // means the first character must be a lowercase letter, and all
  // following characters must be a dash, lowercase letter, or digit,
  // except the last character, which cannot be a dash.
  string name = 11;

  // Peer BGP Autonomous System Number (ASN).
  // Each BGP interface may use a different value.
  int64 peer_asn = 12 [json_name = "peer_asn"];

  // IP address of the BGP interface outside Google Cloud Platform.
  // Only IPv4 is supported.
  string peer_ip_address = 13 [json_name = "peer_ip_address"];

  // IPv6 address of the BGP interface outside Google Cloud Platform.
  // The address must be in the range 2600:2d00:0:2::/64 or 2600:2d00:0:3::/64.
  // If you do not specify the next hop addresses, Google Cloud automatically
  // assigns unused addresses from the 2600:2d00:0:2::/64 or 2600:2d00:0:3::/64 range for you.
  string peer_ipv6_nexthop_address = 14 [json_name = "peer_ipv6_nexthop_address"];

  string project = 15;

  // Region where the router and BgpPeer reside.
  // If it is not provided, the provider region is used.
  string region = 16;

  // The name of the Cloud Router in which this BgpPeer will be configured.
  string router = 17;

  // The URI of the VM instance that is used as third-party router appliances
  // such as Next Gen Firewalls, Virtual Routers, or Router Appliances.
  // The VM instance must be located in zones contained in the same region as
  // this Cloud Router. The VM instance is the peer side of the BGP session.
  string router_appliance_instance = 18 [json_name = "router_appliance_instance"];

  repeated AdvertisedIpRanges advertised_ip_ranges = 19 [json_name = "advertised_ip_ranges"];

  Bfd bfd = 20;

  repeated Timeouts timeouts = 21;

  map<string, string> for_each = 22 [json_name = "for_each"];

  repeated string depends_on = 23 [json_name = "depends_on"];

  int32 count = 24;

  string provider = 25;

  terraform.v1.Lifecycle lifecycle = 26;

  message AdvertisedIpRanges {
    // User-specified description for the IP range.
    string description = 1;

    // The IP range to advertise. The value must be a
    // CIDR-formatted string.
    string range = 2;
  }

  message Bfd {
    // The minimum interval, in milliseconds, between BFD control packets
    // received from the peer router. The actual value is negotiated
    // between the two routers and is equal to the greater of this value
    // and the transmit interval of the other router. If set, this value
    // must be between 1000 and 30000.
    int64 min_receive_interval = 1 [json_name = "min_receive_interval"];

    // The minimum interval, in milliseconds, between BFD control packets
    // transmitted to the peer router. The actual value is negotiated
    // between the two routers and is equal to the greater of this value
    // and the corresponding receive interval of the other router. If set,
    // this value must be between 1000 and 30000.
    int64 min_transmit_interval = 2 [json_name = "min_transmit_interval"];

    // The number of consecutive BFD packets that must be missed before
    // BFD declares that a peer is unavailable. If set, the value must
    // be a value between 5 and 16.
    int64 multiplier = 3;

    // The BFD session initialization mode for this BGP peer.
    // If set to 'ACTIVE', the Cloud Router will initiate the BFD session
    // for this BGP peer. If set to 'PASSIVE', the Cloud Router will wait
    // for the peer router to initiate the BFD session for this BGP peer.
    // If set to 'DISABLED', BFD is disabled for this BGP peer. Possible values: ["ACTIVE", "DISABLED", "PASSIVE"]
    string session_initialization_mode = 4 [json_name = "session_initialization_mode"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeSecurityPolicy version is 0
message GoogleComputeSecurityPolicy {
  // An optional description of this security policy. Max size is 2048.
  string description = 1;

  // Fingerprint of this resource.
  string fingerprint = 2;

  string id = 3;

  // The name of the security policy.
  string name = 4;

  // The project in which the resource belongs. If it is not provided, the provider project is used.
  string project = 5;

  // The URI of the created resource.
  string self_link = 6 [json_name = "self_link"];

  // The type indicates the intended use of the security policy. CLOUD_ARMOR - Cloud Armor backend security policies can be configured to filter incoming HTTP requests targeting backend services. They filter requests before they hit the origin servers. CLOUD_ARMOR_EDGE - Cloud Armor edge security policies can be configured to filter incoming HTTP requests targeting backend services (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage). They filter requests before the request is served from Google's cache.
  string type = 7;

  AdaptiveProtectionConfig adaptive_protection_config = 8 [json_name = "adaptive_protection_config"];

  AdvancedOptionsConfig advanced_options_config = 9 [json_name = "advanced_options_config"];

  RecaptchaOptionsConfig recaptcha_options_config = 10 [json_name = "recaptcha_options_config"];

  repeated Rule rule = 11;

  repeated Timeouts timeouts = 12;

  map<string, string> for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  int32 count = 15;

  string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message AdaptiveProtectionConfig {
    Layer7DdosDefenseConfig layer_7_ddos_defense_config = 1 [json_name = "layer_7_ddos_defense_config"];

    message Layer7DdosDefenseConfig {
      // If set to true, enables CAAP for L7 DDoS detection.
      bool enable = 1;

      // Rule visibility. Supported values include: "STANDARD", "PREMIUM".
      string rule_visibility = 2 [json_name = "rule_visibility"];
    }
  }

  message AdvancedOptionsConfig {
    // JSON body parsing. Supported values include: "DISABLED", "STANDARD".
    string json_parsing = 1 [json_name = "json_parsing"];

    // Logging level. Supported values include: "NORMAL", "VERBOSE".
    string log_level = 2 [json_name = "log_level"];

    JsonCustomConfig json_custom_config = 3 [json_name = "json_custom_config"];

    message JsonCustomConfig {
      // A list of custom Content-Type header values to apply the JSON parsing.
      repeated string content_types = 1 [json_name = "content_types"];
    }
  }

  message RecaptchaOptionsConfig {
    // A field to supply a reCAPTCHA site key to be used for all the rules using the redirect action with the type of GOOGLE_RECAPTCHA under the security policy. The specified site key needs to be created from the reCAPTCHA API. The user is responsible for the validity of the specified site key. If not specified, a Google-managed site key is used.
    string redirect_site_key = 1 [json_name = "redirect_site_key"];
  }

  message Rule {
    // Action to take when match matches the request.
    string action = 1;

    // An optional description of this rule. Max size is 64.
    string description = 2;

    // When set to true, the action specified above is not enforced. Stackdriver logs for requests that trigger a preview action are annotated as such.
    bool preview = 3;

    // An unique positive integer indicating the priority of evaluation for a rule. Rules are evaluated from highest priority (lowest numerically) to lowest priority (highest numerically) in order.
    int64 priority = 4;

    HeaderAction header_action = 5 [json_name = "header_action"];

    Match match = 6;

    RateLimitOptions rate_limit_options = 7 [json_name = "rate_limit_options"];

    RedirectOptions redirect_options = 8 [json_name = "redirect_options"];

    message HeaderAction {
      repeated RequestHeadersToAdds request_headers_to_adds = 1 [json_name = "request_headers_to_adds"];

      message RequestHeadersToAdds {
        // The name of the header to set.
        string header_name = 1 [json_name = "header_name"];

        // The value to set the named header to.
        string header_value = 2 [json_name = "header_value"];
      }
    }

    message Match {
      // Predefined rule expression. If this field is specified, config must also be specified. Available options:   SRC_IPS_V1: Must specify the corresponding src_ip_ranges field in config.
      string versioned_expr = 1 [json_name = "versioned_expr"];

      Config config = 2;

      Expr expr = 3;

      message Config {
        // Set of IP addresses or ranges (IPV4 or IPV6) in CIDR notation to match against inbound traffic. There is a limit of 10 IP ranges per rule. A value of '*' matches all IPs (can be used to override the default behavior).
        repeated string src_ip_ranges = 1 [json_name = "src_ip_ranges"];
      }

      message Expr {
        // Textual representation of an expression in Common Expression Language syntax. The application context of the containing message determines which well-known feature set of CEL is supported.
        string expression = 1;
      }
    }

    message RateLimitOptions {
      // Can only be specified if the action for the rule is "rate_based_ban". If specified, determines the time (in seconds) the traffic will continue to be banned by the rate limit after the rate falls below the threshold.
      int64 ban_duration_sec = 1 [json_name = "ban_duration_sec"];

      // Action to take for requests that are under the configured rate limit threshold. Valid option is "allow" only.
      string conform_action = 2 [json_name = "conform_action"];

      // Determines the key to enforce the rateLimitThreshold on
      string enforce_on_key = 3 [json_name = "enforce_on_key"];

      // Rate limit key name applicable only for the following key types: HTTP_HEADER -- Name of the HTTP header whose value is taken as the key value. HTTP_COOKIE -- Name of the HTTP cookie whose value is taken as the key value.
      string enforce_on_key_name = 4 [json_name = "enforce_on_key_name"];

      // Action to take for requests that are above the configured rate limit threshold, to either deny with a specified HTTP response code, or redirect to a different endpoint. Valid options are "deny()" where valid values for status are 403, 404, 429, and 502, and "redirect" where the redirect parameters come from exceedRedirectOptions below.
      string exceed_action = 5 [json_name = "exceed_action"];

      BanThreshold ban_threshold = 6 [json_name = "ban_threshold"];

      ExceedRedirectOptions exceed_redirect_options = 7 [json_name = "exceed_redirect_options"];

      RateLimitThreshold rate_limit_threshold = 8 [json_name = "rate_limit_threshold"];

      message BanThreshold {
        // Number of HTTP(S) requests for calculating the threshold.
        int64 count = 1;

        // Interval over which the threshold is computed.
        int64 interval_sec = 2 [json_name = "interval_sec"];
      }

      message ExceedRedirectOptions {
        // Target for the redirect action. This is required if the type is EXTERNAL_302 and cannot be specified for GOOGLE_RECAPTCHA.
        string target = 1;

        // Type of the redirect action.
        string type = 2;
      }

      message RateLimitThreshold {
        // Number of HTTP(S) requests for calculating the threshold.
        int64 count = 1;

        // Interval over which the threshold is computed.
        int64 interval_sec = 2 [json_name = "interval_sec"];
      }
    }

    message RedirectOptions {
      // Target for the redirect action. This is required if the type is EXTERNAL_302 and cannot be specified for GOOGLE_RECAPTCHA.
      string target = 1;

      // Type of the redirect action. Available options: EXTERNAL_302: Must specify the corresponding target field in config. GOOGLE_RECAPTCHA: Cannot specify target field in config.
      string type = 2;
    }
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeServiceAttachment version is 0
message GoogleComputeServiceAttachment {
  // An array of the consumer forwarding rules connected to this service
  // attachment.
  repeated ConnectedEndpoints connected_endpoints = 1 [json_name = "connected_endpoints"];

  // The connection preference to use for this service attachment. Valid
  // values include "ACCEPT_AUTOMATIC", "ACCEPT_MANUAL".
  string connection_preference = 2 [json_name = "connection_preference"];

  // An array of projects that are not allowed to connect to this service
  // attachment.
  repeated string consumer_reject_lists = 3 [json_name = "consumer_reject_lists"];

  // An optional description of this resource.
  string description = 4;

  // If specified, the domain name will be used during the integration between
  // the PSC connected endpoints and the Cloud DNS. For example, this is a
  // valid domain name: "p.mycompany.com.". Current max number of domain names
  // supported is 1.
  repeated string domain_names = 5 [json_name = "domain_names"];

  // If true, enable the proxy protocol which is for supplying client TCP/IP
  // address data in TCP connections that traverse proxies on their way to
  // destination servers.
  bool enable_proxy_protocol = 6 [json_name = "enable_proxy_protocol"];

  // Fingerprint of this resource. This field is used internally during
  // updates of this resource.
  string fingerprint = 7;

  string id = 8;

  // Name of the resource. The name must be 1-63 characters long, and
  // comply with RFC1035. Specifically, the name must be 1-63 characters
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?'
  // which means the first character must be a lowercase letter, and all
  // following characters must be a dash, lowercase letter, or digit,
  // except the last character, which cannot be a dash.
  string name = 9;

  // An array of subnets that is provided for NAT in this service attachment.
  repeated string nat_subnets = 10 [json_name = "nat_subnets"];

  string project = 11;

  // URL of the region where the resource resides.
  string region = 12;

  string self_link = 13 [json_name = "self_link"];

  // The URL of a forwarding rule that represents the service identified by
  // this service attachment.
  string target_service = 14 [json_name = "target_service"];

  repeated ConsumerAcceptLists consumer_accept_lists = 15 [json_name = "consumer_accept_lists"];

  repeated Timeouts timeouts = 16;

  map<string, string> for_each = 17 [json_name = "for_each"];

  repeated string depends_on = 18 [json_name = "depends_on"];

  int32 count = 19;

  string provider = 20;

  terraform.v1.Lifecycle lifecycle = 21;

  message ConnectedEndpoints {
    // endpoint: string
    string endpoint = 1;

    // status: string
    string status = 2;
  }

  message ConsumerAcceptLists {
    // The number of consumer forwarding rules the consumer project can
    // create.
    int64 connection_limit = 1 [json_name = "connection_limit"];

    // A project that is allowed to connect to this service attachment.
    string project_id_or_num = 2 [json_name = "project_id_or_num"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeSharedVpcHostProject version is 0
message GoogleComputeSharedVpcHostProject {
  string id = 1;

  // The ID of the project that will serve as a Shared VPC host project
  string project = 2;

  repeated Timeouts timeouts = 3;

  map<string, string> for_each = 4 [json_name = "for_each"];

  repeated string depends_on = 5 [json_name = "depends_on"];

  int32 count = 6;

  string provider = 7;

  terraform.v1.Lifecycle lifecycle = 8;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeSharedVpcServiceProject version is 0
message GoogleComputeSharedVpcServiceProject {
  // The deletion policy for the shared VPC service. Setting ABANDON allows the resource
  // 				to be abandoned rather than deleted. Possible values are: "ABANDON".
  string deletion_policy = 1 [json_name = "deletion_policy"];

  // The ID of a host project to associate.
  string host_project = 2 [json_name = "host_project"];

  string id = 3;

  // The ID of the project that will serve as a Shared VPC service project.
  string service_project = 4 [json_name = "service_project"];

  repeated Timeouts timeouts = 5;

  map<string, string> for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  int32 count = 8;

  string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeSnapshot version is 0
message GoogleComputeSnapshot {
  // Creates the new snapshot in the snapshot chain labeled with the
  // specified name. The chain name must be 1-63 characters long and
  // comply with RFC1035. This is an uncommon option only for advanced
  // service owners who needs to create separate snapshot chains, for
  // example, for chargeback tracking.  When you describe your snapshot
  // resource, this field is visible only if it has a non-empty value.
  string chain_name = 1 [json_name = "chain_name"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 3;

  // Size of the snapshot, specified in GB.
  int64 disk_size_gb = 4 [json_name = "disk_size_gb"];

  string id = 5;

  // The fingerprint used for optimistic locking of this resource. Used
  // internally during updates.
  string label_fingerprint = 6 [json_name = "label_fingerprint"];

  // Labels to apply to this Snapshot.
  map<string, string> labels = 7;

  // A list of public visible licenses that apply to this snapshot. This
  // can be because the original image had licenses attached (such as a
  // Windows image).  snapshotEncryptionKey nested object Encrypts the
  // snapshot using a customer-supplied encryption key.
  repeated string licenses = 8;

  // Name of the resource; provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 9;

  string project = 10;

  string self_link = 11 [json_name = "self_link"];

  // The unique identifier for the resource.
  int64 snapshot_id = 12 [json_name = "snapshot_id"];

  // A reference to the disk used to create this snapshot.
  string source_disk = 13 [json_name = "source_disk"];

  // A size of the storage used by the snapshot. As snapshots share
  // storage, this number is expected to change with snapshot
  // creation/deletion.
  int64 storage_bytes = 14 [json_name = "storage_bytes"];

  // Cloud Storage bucket storage location of the snapshot (regional or multi-regional).
  repeated string storage_locations = 15 [json_name = "storage_locations"];

  // A reference to the zone where the disk is hosted.
  string zone = 16;

  SnapshotEncryptionKey snapshot_encryption_key = 17 [json_name = "snapshot_encryption_key"];

  SourceDiskEncryptionKey source_disk_encryption_key = 18 [json_name = "source_disk_encryption_key"];

  repeated Timeouts timeouts = 19;

  map<string, string> for_each = 20 [json_name = "for_each"];

  repeated string depends_on = 21 [json_name = "depends_on"];

  int32 count = 22;

  string provider = 23;

  terraform.v1.Lifecycle lifecycle = 24;

  message SnapshotEncryptionKey {
    // The name of the encryption key that is stored in Google Cloud KMS.
    string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

    // The service account used for the encryption request for the given KMS key.
    // If absent, the Compute Engine Service Agent service account is used.
    string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    string raw_key = 3 [json_name = "raw_key"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied
    // encryption key that protects this resource.
    string sha256 = 4;
  }

  message SourceDiskEncryptionKey {
    // The service account used for the encryption request for the given KMS key.
    // If absent, the Compute Engine Service Agent service account is used.
    string kms_key_service_account = 1 [json_name = "kms_key_service_account"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    string raw_key = 2 [json_name = "raw_key"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeSnapshotIamBinding version is 0
message GoogleComputeSnapshotIamBinding {
  string etag = 1;

  string id = 2;

  repeated string members = 3;

  string name = 4;

  string project = 5;

  string role = 6;

  Condition condition = 7;

  map<string, string> for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Condition {
    string description = 1;

    string expression = 2;

    string title = 3;
  }
}

// GoogleComputeSnapshotIamMember version is 0
message GoogleComputeSnapshotIamMember {
  string etag = 1;

  string id = 2;

  string member = 3;

  string name = 4;

  string project = 5;

  string role = 6;

  Condition condition = 7;

  map<string, string> for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Condition {
    string description = 1;

    string expression = 2;

    string title = 3;
  }
}

// GoogleComputeSnapshotIamPolicy version is 0
message GoogleComputeSnapshotIamPolicy {
  string etag = 1;

  string id = 2;

  string name = 3;

  string policy_data = 4 [json_name = "policy_data"];

  string project = 5;

  map<string, string> for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  int32 count = 8;

  string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;
}

// GoogleComputeSslCertificate version is 0
message GoogleComputeSslCertificate {
  // The certificate in PEM format.
  // The certificate chain must be no greater than 5 certs long.
  // The chain must include at least one intermediate cert.
  string certificate = 1;

  // The unique identifier for the resource.
  int64 certificate_id = 2 [json_name = "certificate_id"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 3 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 4;

  // Expire time of the certificate in RFC3339 text format.
  string expire_time = 5 [json_name = "expire_time"];

  string id = 6;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  //
  //
  // These are in the same namespace as the managed SSL certificates.
  string name = 7;

  // Creates a unique name beginning with the specified prefix. Conflicts with name.
  string name_prefix = 8 [json_name = "name_prefix"];

  // The write-only private key in PEM format.
  string private_key = 9 [json_name = "private_key"];

  string project = 10;

  string self_link = 11 [json_name = "self_link"];

  repeated Timeouts timeouts = 12;

  map<string, string> for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  int32 count = 15;

  string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeSslPolicy version is 0
message GoogleComputeSslPolicy {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Profile specifies the set of SSL features that can be used by the
  // load balancer when negotiating SSL with clients. This can be one of
  // 'COMPATIBLE', 'MODERN', 'RESTRICTED', or 'CUSTOM'. If using 'CUSTOM',
  // the set of SSL features to enable must be specified in the
  // 'customFeatures' field.
  //
  // See the [official documentation](https://cloud.google.com/compute/docs/load-balancing/ssl-policies#profilefeaturesupport)
  // for which ciphers are available to use. **Note**: this argument
  // *must* be present when using the 'CUSTOM' profile. This argument
  // *must not* be present when using any other profile.
  repeated string custom_features = 2 [json_name = "custom_features"];

  // An optional description of this resource.
  string description = 3;

  // The list of features enabled in the SSL policy.
  repeated string enabled_features = 4 [json_name = "enabled_features"];

  // Fingerprint of this resource. A hash of the contents stored in this
  // object. This field is used in optimistic locking.
  string fingerprint = 5;

  string id = 6;

  // The minimum version of SSL protocol that can be used by the clients
  // to establish a connection with the load balancer. Default value: "TLS_1_0" Possible values: ["TLS_1_0", "TLS_1_1", "TLS_1_2"]
  string min_tls_version = 7 [json_name = "min_tls_version"];

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 8;

  // Profile specifies the set of SSL features that can be used by the
  // load balancer when negotiating SSL with clients. If using 'CUSTOM',
  // the set of SSL features to enable must be specified in the
  // 'customFeatures' field.
  //
  // See the [official documentation](https://cloud.google.com/compute/docs/load-balancing/ssl-policies#profilefeaturesupport)
  // for information on what cipher suites each profile provides. If
  // 'CUSTOM' is used, the 'custom_features' attribute **must be set**. Default value: "COMPATIBLE" Possible values: ["COMPATIBLE", "MODERN", "RESTRICTED", "CUSTOM"]
  string profile = 9;

  string project = 10;

  string self_link = 11 [json_name = "self_link"];

  repeated Timeouts timeouts = 12;

  map<string, string> for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  int32 count = 15;

  string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeSubnetwork version is 0
message GoogleComputeSubnetwork {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional description of this resource. Provide this property when
  // you create the resource. This field can be set only at resource
  // creation time.
  string description = 2;

  // The range of external IPv6 addresses that are owned by this subnetwork.
  string external_ipv6_prefix = 3 [json_name = "external_ipv6_prefix"];

  // Fingerprint of this resource. This field is used internally during updates of this resource.
  string fingerprint = 4;

  // The gateway address for default routes to reach destination addresses
  // outside this subnetwork.
  string gateway_address = 5 [json_name = "gateway_address"];

  string id = 6;

  // The range of internal addresses that are owned by this subnetwork.
  // Provide this property when you create the subnetwork. For example,
  // 10.0.0.0/8 or 192.168.0.0/16. Ranges must be unique and
  // non-overlapping within a network. Only IPv4 is supported.
  string ip_cidr_range = 7 [json_name = "ip_cidr_range"];

  // The access type of IPv6 address this subnet holds. It's immutable and can only be specified during creation
  // or the first time the subnet is updated into IPV4_IPV6 dual stack. If the ipv6_type is EXTERNAL then this subnet
  // cannot enable direct path. Possible values: ["EXTERNAL", "INTERNAL"]
  string ipv6_access_type = 8 [json_name = "ipv6_access_type"];

  // The range of internal IPv6 addresses that are owned by this subnetwork.
  string ipv6_cidr_range = 9 [json_name = "ipv6_cidr_range"];

  // The name of the resource, provided by the client when initially
  // creating the resource. The name must be 1-63 characters long, and
  // comply with RFC1035. Specifically, the name must be 1-63 characters
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which
  // means the first character must be a lowercase letter, and all
  // following characters must be a dash, lowercase letter, or digit,
  // except the last character, which cannot be a dash.
  string name = 10;

  // The network this subnet belongs to.
  // Only networks that are in the distributed mode can have subnetworks.
  string network = 11;

  // When enabled, VMs in this subnetwork without external IP addresses can
  // access Google APIs and services by using Private Google Access.
  bool private_ip_google_access = 12 [json_name = "private_ip_google_access"];

  // The private IPv6 google access type for the VMs in this subnet.
  string private_ipv6_google_access = 13 [json_name = "private_ipv6_google_access"];

  string project = 14;

  // The purpose of the resource. This field can be either 'PRIVATE_RFC_1918', 'INTERNAL_HTTPS_LOAD_BALANCER' or 'REGIONAL_MANAGED_PROXY'.
  // A subnetwork with purpose set to 'INTERNAL_HTTPS_LOAD_BALANCER' is a user-created subnetwork that is reserved for Internal HTTP(S) Load Balancing.
  // A subnetwork in a given region with purpose set to 'REGIONAL_MANAGED_PROXY' is a proxy-only subnet and is shared between all the regional Envoy-based load balancers.
  // If unspecified, the purpose defaults to 'PRIVATE_RFC_1918'.
  // The enableFlowLogs field isn't supported with the purpose field set to 'INTERNAL_HTTPS_LOAD_BALANCER'.
  string purpose = 15;

  // The GCP region for this subnetwork.
  string region = 16;

  // The role of subnetwork.
  // The value can be set to 'ACTIVE' or 'BACKUP'.
  // An 'ACTIVE' subnetwork is one that is currently being used.
  // A 'BACKUP' subnetwork is one that is ready to be promoted to 'ACTIVE' or is currently draining.
  //
  // Subnetwork role must be specified when purpose is set to 'INTERNAL_HTTPS_LOAD_BALANCER' or 'REGIONAL_MANAGED_PROXY'. Possible values: ["ACTIVE", "BACKUP"]
  string role = 17;

  // An array of configurations for secondary IP ranges for VM instances
  // contained in this subnetwork. The primary IP of such VM must belong
  // to the primary ipCidrRange of the subnetwork. The alias IPs may belong
  // to either primary or secondary ranges.
  //
  // **Note**: This field uses [attr-as-block mode](https://www.terraform.io/docs/configuration/attr-as-blocks.html) to avoid
  // breaking users during the 0.12 upgrade. To explicitly send a list
  // of zero objects you must use the following syntax:
  // 'example=[]'
  // For more details about this behavior, see [this section](https://www.terraform.io/docs/configuration/attr-as-blocks.html#defining-a-fixed-object-collection-value).
  repeated SecondaryIpRange secondary_ip_range = 18 [json_name = "secondary_ip_range"];

  string self_link = 19 [json_name = "self_link"];

  // The stack type for this subnet to identify whether the IPv6 feature is enabled or not.
  // If not specified IPV4_ONLY will be used. Possible values: ["IPV4_ONLY", "IPV4_IPV6"]
  string stack_type = 20 [json_name = "stack_type"];

  LogConfig log_config = 21 [json_name = "log_config"];

  repeated Timeouts timeouts = 22;

  map<string, string> for_each = 23 [json_name = "for_each"];

  repeated string depends_on = 24 [json_name = "depends_on"];

  int32 count = 25;

  string provider = 26;

  terraform.v1.Lifecycle lifecycle = 27;

  message SecondaryIpRange {
    // ip_cidr_range: string
    string ip_cidr_range = 1 [json_name = "ip_cidr_range"];

    // range_name: string
    string range_name = 2 [json_name = "range_name"];
  }

  message LogConfig {
    // Can only be specified if VPC flow logging for this subnetwork is enabled.
    // Toggles the aggregation interval for collecting flow logs. Increasing the
    // interval time will reduce the amount of generated flow logs for long
    // lasting connections. Default is an interval of 5 seconds per connection. Default value: "INTERVAL_5_SEC" Possible values: ["INTERVAL_5_SEC", "INTERVAL_30_SEC", "INTERVAL_1_MIN", "INTERVAL_5_MIN", "INTERVAL_10_MIN", "INTERVAL_15_MIN"]
    string aggregation_interval = 1 [json_name = "aggregation_interval"];

    // Export filter used to define which VPC flow logs should be logged, as as CEL expression. See
    // https://cloud.google.com/vpc/docs/flow-logs#filtering for details on how to format this field.
    // The default value is 'true', which evaluates to include everything.
    string filter_expr = 2 [json_name = "filter_expr"];

    // Can only be specified if VPC flow logging for this subnetwork is enabled.
    // The value of the field must be in [0, 1]. Set the sampling rate of VPC
    // flow logs within the subnetwork where 1.0 means all collected logs are
    // reported and 0.0 means no logs are reported. Default is 0.5 which means
    // half of all collected logs are reported.
    int64 flow_sampling = 3 [json_name = "flow_sampling"];

    // Can only be specified if VPC flow logging for this subnetwork is enabled.
    // Configures whether metadata fields should be added to the reported VPC
    // flow logs. Default value: "INCLUDE_ALL_METADATA" Possible values: ["EXCLUDE_ALL_METADATA", "INCLUDE_ALL_METADATA", "CUSTOM_METADATA"]
    string metadata = 4;

    // List of metadata fields that should be added to reported logs.
    // Can only be specified if VPC flow logs for this subnetwork is enabled and "metadata" is set to CUSTOM_METADATA.
    repeated string metadata_fields = 5 [json_name = "metadata_fields"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeSubnetworkIamBinding version is 0
message GoogleComputeSubnetworkIamBinding {
  string etag = 1;

  string id = 2;

  repeated string members = 3;

  string project = 4;

  string region = 5;

  string role = 6;

  string subnetwork = 7;

  Condition condition = 8;

  map<string, string> for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    string description = 1;

    string expression = 2;

    string title = 3;
  }
}

// GoogleComputeSubnetworkIamMember version is 0
message GoogleComputeSubnetworkIamMember {
  string etag = 1;

  string id = 2;

  string member = 3;

  string project = 4;

  string region = 5;

  string role = 6;

  string subnetwork = 7;

  Condition condition = 8;

  map<string, string> for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    string description = 1;

    string expression = 2;

    string title = 3;
  }
}

// GoogleComputeSubnetworkIamPolicy version is 0
message GoogleComputeSubnetworkIamPolicy {
  string etag = 1;

  string id = 2;

  string policy_data = 3 [json_name = "policy_data"];

  string project = 4;

  string region = 5;

  string subnetwork = 6;

  map<string, string> for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// GoogleComputeTargetGrpcProxy version is 0
message GoogleComputeTargetGrpcProxy {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 2;

  // Fingerprint of this resource. A hash of the contents stored in
  // this object. This field is used in optimistic locking. This field
  // will be ignored when inserting a TargetGrpcProxy. An up-to-date
  // fingerprint must be provided in order to patch/update the
  // TargetGrpcProxy; otherwise, the request will fail with error
  // 412 conditionNotMet. To see the latest fingerprint, make a get()
  // request to retrieve the TargetGrpcProxy. A base64-encoded string.
  string fingerprint = 3;

  string id = 4;

  // Name of the resource. Provided by the client when the resource
  // is created. The name must be 1-63 characters long, and comply
  // with RFC1035. Specifically, the name must be 1-63 characters long
  // and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which
  // means the first character must be a lowercase letter, and all
  // following characters must be a dash, lowercase letter, or digit,
  // except the last character, which cannot be a dash.
  string name = 5;

  string project = 6;

  string self_link = 7 [json_name = "self_link"];

  // Server-defined URL with id for the resource.
  string self_link_with_id = 8 [json_name = "self_link_with_id"];

  // URL to the UrlMap resource that defines the mapping from URL to
  // the BackendService. The protocol field in the BackendService
  // must be set to GRPC.
  string url_map = 9 [json_name = "url_map"];

  // If true, indicates that the BackendServices referenced by
  // the urlMap may be accessed by gRPC applications without using
  // a sidecar proxy. This will enable configuration checks on urlMap
  // and its referenced BackendServices to not allow unsupported features.
  // A gRPC application must use "xds:///" scheme in the target URI
  // of the service it is connecting to. If false, indicates that the
  // BackendServices referenced by the urlMap will be accessed by gRPC
  // applications via a sidecar proxy. In this case, a gRPC application
  // must not use "xds:///" scheme in the target URI of the service
  // it is connecting to
  bool validate_for_proxyless = 10 [json_name = "validate_for_proxyless"];

  repeated Timeouts timeouts = 11;

  map<string, string> for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  int32 count = 14;

  string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeTargetHttpProxy version is 0
message GoogleComputeTargetHttpProxy {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 2;

  string id = 3;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 4;

  string project = 5;

  // This field only applies when the forwarding rule that references
  // this target proxy has a loadBalancingScheme set to INTERNAL_SELF_MANAGED.
  bool proxy_bind = 6 [json_name = "proxy_bind"];

  // The unique identifier for the resource.
  int64 proxy_id = 7 [json_name = "proxy_id"];

  string self_link = 8 [json_name = "self_link"];

  // A reference to the UrlMap resource that defines the mapping from URL
  // to the BackendService.
  string url_map = 9 [json_name = "url_map"];

  repeated Timeouts timeouts = 10;

  map<string, string> for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeTargetHttpsProxy version is 0
message GoogleComputeTargetHttpsProxy {
  // A reference to the CertificateMap resource uri that identifies a certificate map
  // associated with the given target proxy. This field can only be set for global target proxies.
  // Accepted format is '//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}'.
  string certificate_map = 1 [json_name = "certificate_map"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 3;

  string id = 4;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 5;

  string project = 6;

  // This field only applies when the forwarding rule that references
  // this target proxy has a loadBalancingScheme set to INTERNAL_SELF_MANAGED.
  bool proxy_bind = 7 [json_name = "proxy_bind"];

  // The unique identifier for the resource.
  int64 proxy_id = 8 [json_name = "proxy_id"];

  // Specifies the QUIC override policy for this resource. This determines
  // whether the load balancer will attempt to negotiate QUIC with clients
  // or not. Can specify one of NONE, ENABLE, or DISABLE. If NONE is
  // specified, Google manages whether QUIC is used. Default value: "NONE" Possible values: ["NONE", "ENABLE", "DISABLE"]
  string quic_override = 9 [json_name = "quic_override"];

  string self_link = 10 [json_name = "self_link"];

  // A list of SslCertificate resources that are used to authenticate
  // connections between users and the load balancer. At least one SSL
  // certificate must be specified.
  repeated string ssl_certificates = 11 [json_name = "ssl_certificates"];

  // A reference to the SslPolicy resource that will be associated with
  // the TargetHttpsProxy resource. If not set, the TargetHttpsProxy
  // resource will not have any SSL policy configured.
  string ssl_policy = 12 [json_name = "ssl_policy"];

  // A reference to the UrlMap resource that defines the mapping from URL
  // to the BackendService.
  string url_map = 13 [json_name = "url_map"];

  repeated Timeouts timeouts = 14;

  map<string, string> for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  int32 count = 17;

  string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeTargetInstance version is 0
message GoogleComputeTargetInstance {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 2;

  string id = 3;

  // The Compute instance VM handling traffic for this target instance.
  // Accepts the instance self-link, relative path
  // (e.g. 'projects/project/zones/zone/instances/instance') or name. If
  // name is given, the zone will default to the given zone or
  // the provider-default zone and the project will default to the
  // provider-level project.
  string instance = 4;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 5;

  // NAT option controlling how IPs are NAT'ed to the instance.
  // Currently only NO_NAT (default value) is supported. Default value: "NO_NAT" Possible values: ["NO_NAT"]
  string nat_policy = 6 [json_name = "nat_policy"];

  string project = 7;

  string self_link = 8 [json_name = "self_link"];

  // URL of the zone where the target instance resides.
  string zone = 9;

  repeated Timeouts timeouts = 10;

  map<string, string> for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeTargetPool version is 0
message GoogleComputeTargetPool {
  // URL to the backup target pool. Must also set failover_ratio.
  string backup_pool = 1 [json_name = "backup_pool"];

  // Textual description field.
  string description = 2;

  // Ratio (0 to 1) of failed nodes before using the backup pool (which must also be set).
  int64 failover_ratio = 3 [json_name = "failover_ratio"];

  // List of zero or one health check name or self_link. Only legacy google_compute_http_health_check is supported.
  repeated string health_checks = 4 [json_name = "health_checks"];

  string id = 5;

  // List of instances in the pool. They can be given as URLs, or in the form of "zone/name". Note that the instances need not exist at the time of target pool creation, so there is no need to use the Terraform interpolators to create a dependency on the instances from the target pool.
  repeated string instances = 6;

  // A unique name for the resource, required by GCE. Changing this forces a new resource to be created.
  string name = 7;

  // The ID of the project in which the resource belongs. If it is not provided, the provider project is used.
  string project = 8;

  // Where the target pool resides. Defaults to project region.
  string region = 9;

  // The URI of the created resource.
  string self_link = 10 [json_name = "self_link"];

  // How to distribute load. Options are "NONE" (no affinity). "CLIENT_IP" (hash of the source/dest addresses / ports), and "CLIENT_IP_PROTO" also includes the protocol (default "NONE").
  string session_affinity = 11 [json_name = "session_affinity"];

  repeated Timeouts timeouts = 12;

  map<string, string> for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  int32 count = 15;

  string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeTargetSslProxy version is 0
message GoogleComputeTargetSslProxy {
  // A reference to the BackendService resource.
  string backend_service = 1 [json_name = "backend_service"];

  // A reference to the CertificateMap resource uri that identifies a certificate map
  // associated with the given target proxy. This field can only be set for global target proxies.
  // Accepted format is '//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}'.
  string certificate_map = 2 [json_name = "certificate_map"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 3 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 4;

  string id = 5;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 6;

  string project = 7;

  // Specifies the type of proxy header to append before sending data to
  // the backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
  string proxy_header = 8 [json_name = "proxy_header"];

  // The unique identifier for the resource.
  int64 proxy_id = 9 [json_name = "proxy_id"];

  string self_link = 10 [json_name = "self_link"];

  // A list of SslCertificate resources that are used to authenticate
  // connections between users and the load balancer. At least one
  // SSL certificate must be specified.
  repeated string ssl_certificates = 11 [json_name = "ssl_certificates"];

  // A reference to the SslPolicy resource that will be associated with
  // the TargetSslProxy resource. If not set, the TargetSslProxy
  // resource will not have any SSL policy configured.
  string ssl_policy = 12 [json_name = "ssl_policy"];

  repeated Timeouts timeouts = 13;

  map<string, string> for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  int32 count = 16;

  string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeTargetTcpProxy version is 0
message GoogleComputeTargetTcpProxy {
  // A reference to the BackendService resource.
  string backend_service = 1 [json_name = "backend_service"];

  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 3;

  string id = 4;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  // first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 5;

  string project = 6;

  // This field only applies when the forwarding rule that references
  // this target proxy has a loadBalancingScheme set to INTERNAL_SELF_MANAGED.
  bool proxy_bind = 7 [json_name = "proxy_bind"];

  // Specifies the type of proxy header to append before sending data to
  // the backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
  string proxy_header = 8 [json_name = "proxy_header"];

  // The unique identifier for the resource.
  int64 proxy_id = 9 [json_name = "proxy_id"];

  string self_link = 10 [json_name = "self_link"];

  repeated Timeouts timeouts = 11;

  map<string, string> for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  int32 count = 14;

  string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeUrlMap version is 0
message GoogleComputeUrlMap {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // The backend service or backend bucket to use when none of the given rules match.
  string default_service = 2 [json_name = "default_service"];

  // An optional description of this resource. Provide this property when you create
  // the resource.
  string description = 3;

  // Fingerprint of this resource. A hash of the contents stored in this object. This
  // field is used in optimistic locking.
  string fingerprint = 4;

  string id = 5;

  // The unique identifier for the resource.
  int64 map_id = 6 [json_name = "map_id"];

  // Name of the resource. Provided by the client when the resource is created. The
  // name must be 1-63 characters long, and comply with RFC1035. Specifically, the
  // name must be 1-63 characters long and match the regular expression
  // '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character must be a lowercase
  // letter, and all following characters must be a dash, lowercase letter, or digit,
  // except the last character, which cannot be a dash.
  string name = 7;

  string project = 8;

  string self_link = 9 [json_name = "self_link"];

  DefaultRouteAction default_route_action = 10 [json_name = "default_route_action"];

  DefaultUrlRedirect default_url_redirect = 11 [json_name = "default_url_redirect"];

  HeaderAction header_action = 12 [json_name = "header_action"];

  repeated HostRule host_rule = 13 [json_name = "host_rule"];

  repeated PathMatcher path_matcher = 14 [json_name = "path_matcher"];

  repeated Test test = 15;

  repeated Timeouts timeouts = 16;

  map<string, string> for_each = 17 [json_name = "for_each"];

  repeated string depends_on = 18 [json_name = "depends_on"];

  int32 count = 19;

  string provider = 20;

  terraform.v1.Lifecycle lifecycle = 21;

  message DefaultRouteAction {
    CorsPolicy cors_policy = 1 [json_name = "cors_policy"];

    FaultInjectionPolicy fault_injection_policy = 2 [json_name = "fault_injection_policy"];

    RequestMirrorPolicy request_mirror_policy = 3 [json_name = "request_mirror_policy"];

    RetryPolicy retry_policy = 4 [json_name = "retry_policy"];

    Timeout timeout = 5;

    UrlRewrite url_rewrite = 6 [json_name = "url_rewrite"];

    repeated WeightedBackendServices weighted_backend_services = 7 [json_name = "weighted_backend_services"];

    message CorsPolicy {
      // In response to a preflight request, setting this to true indicates that the actual request can include user credentials.
      // This translates to the Access-Control-Allow-Credentials header.
      bool allow_credentials = 1 [json_name = "allow_credentials"];

      // Specifies the content for the Access-Control-Allow-Headers header.
      repeated string allow_headers = 2 [json_name = "allow_headers"];

      // Specifies the content for the Access-Control-Allow-Methods header.
      repeated string allow_methods = 3 [json_name = "allow_methods"];

      // Specifies the regular expression patterns that match allowed origins. For regular expression grammar
      // please see en.cppreference.com/w/cpp/regex/ecmascript
      // An origin is allowed if it matches either an item in allowOrigins or an item in allowOriginRegexes.
      repeated string allow_origin_regexes = 4 [json_name = "allow_origin_regexes"];

      // Specifies the list of origins that will be allowed to do CORS requests.
      // An origin is allowed if it matches either an item in allowOrigins or an item in allowOriginRegexes.
      repeated string allow_origins = 5 [json_name = "allow_origins"];

      // If true, specifies the CORS policy is disabled. The default value is false, which indicates that the CORS policy is in effect.
      bool disabled = 6;

      // Specifies the content for the Access-Control-Expose-Headers header.
      repeated string expose_headers = 7 [json_name = "expose_headers"];

      // Specifies how long results of a preflight request can be cached in seconds.
      // This translates to the Access-Control-Max-Age header.
      int64 max_age = 8 [json_name = "max_age"];
    }

    message FaultInjectionPolicy {
      Abort abort = 1;

      Delay delay = 2;

      message Abort {
        // The HTTP status code used to abort the request.
        // The value must be between 200 and 599 inclusive.
        int64 http_status = 1 [json_name = "http_status"];

        // The percentage of traffic (connections/operations/requests) which will be aborted as part of fault injection.
        // The value must be between 0.0 and 100.0 inclusive.
        int64 percentage = 2;
      }

      message Delay {
        // The percentage of traffic (connections/operations/requests) on which delay will be introduced as part of fault injection.
        // The value must be between 0.0 and 100.0 inclusive.
        int64 percentage = 1;

        FixedDelay fixed_delay = 2 [json_name = "fixed_delay"];

        message FixedDelay {
          // Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are
          // represented with a 0 seconds field and a positive nanos field. Must be from 0 to 999,999,999 inclusive.
          int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive.
          // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years
          string seconds = 2;
        }
      }
    }

    message RequestMirrorPolicy {
      // The full or partial URL to the BackendService resource being mirrored to.
      string backend_service = 1 [json_name = "backend_service"];
    }

    message RetryPolicy {
      // Specifies the allowed number retries. This number must be > 0. If not specified, defaults to 1.
      int64 num_retries = 1 [json_name = "num_retries"];

      // Specfies one or more conditions when this retry rule applies. Valid values are:
      //
      // * 5xx: Loadbalancer will attempt a retry if the backend service responds with any 5xx response code,
      //  or if the backend service does not respond at all, example: disconnects, reset, read timeout,
      // * connection failure, and refused streams.
      // * gateway-error: Similar to 5xx, but only applies to response codes 502, 503 or 504.
      // * connect-failure: Loadbalancer will retry on failures connecting to backend services,
      //  for example due to connection timeouts.
      // * retriable-4xx: Loadbalancer will retry for retriable 4xx response codes.
      //  Currently the only retriable error supported is 409.
      // * refused-stream:Loadbalancer will retry if the backend service resets the stream with a REFUSED_STREAM error code.
      //  This reset type indicates that it is safe to retry.
      // * cancelled: Loadbalancer will retry if the gRPC status code in the response header is set to cancelled
      // * deadline-exceeded: Loadbalancer will retry if the gRPC status code in the response header is set to deadline-exceeded
      // * resource-exhausted: Loadbalancer will retry if the gRPC status code in the response header is set to resource-exhausted
      // * unavailable: Loadbalancer will retry if the gRPC status code in the response header is set to unavailable
      repeated string retry_conditions = 2 [json_name = "retry_conditions"];

      PerTryTimeout per_try_timeout = 3 [json_name = "per_try_timeout"];

      message PerTryTimeout {
        // Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are
        // represented with a 0 seconds field and a positive nanos field. Must be from 0 to 999,999,999 inclusive.
        int64 nanos = 1;

        // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive.
        // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years
        string seconds = 2;
      }
    }

    message Timeout {
      // Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented
      // with a 0 seconds field and a positive nanos field. Must be from 0 to 999,999,999 inclusive.
      int64 nanos = 1;

      // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive.
      // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years
      string seconds = 2;
    }

    message UrlRewrite {
      // Prior to forwarding the request to the selected service, the request's host header is replaced
      // with contents of hostRewrite.
      //
      // The value must be between 1 and 255 characters.
      string host_rewrite = 1 [json_name = "host_rewrite"];

      // Prior to forwarding the request to the selected backend service, the matching portion of the
      // request's path is replaced by pathPrefixRewrite.
      //
      // The value must be between 1 and 1024 characters.
      string path_prefix_rewrite = 2 [json_name = "path_prefix_rewrite"];
    }

    message WeightedBackendServices {
      // The full or partial URL to the default BackendService resource. Before forwarding the
      // request to backendService, the loadbalancer applies any relevant headerActions
      // specified as part of this backendServiceWeight.
      string backend_service = 1 [json_name = "backend_service"];

      // Specifies the fraction of traffic sent to backendService, computed as
      // weight / (sum of all weightedBackendService weights in routeAction) .
      //
      // The selection of a backend service is determined only for new traffic. Once a user's request
      // has been directed to a backendService, subsequent requests will be sent to the same backendService
      // as determined by the BackendService's session affinity policy.
      //
      // The value must be between 0 and 1000
      int64 weight = 2;

      HeaderAction header_action = 3 [json_name = "header_action"];

      message HeaderAction {
        // A list of header names for headers that need to be removed from the request prior to
        // forwarding the request to the backendService.
        repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

        // A list of header names for headers that need to be removed from the response prior to sending the
        // response back to the client.
        repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

        repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

        repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

        message RequestHeadersToAdd {
          // The name of the header to add.
          string header_name = 1 [json_name = "header_name"];

          // The value of the header to add.
          string header_value = 2 [json_name = "header_value"];

          // If false, headerValue is appended to any values that already exist for the header.
          // If true, headerValue is set for the header, discarding any values that were set for that header.
          bool replace = 3;
        }

        message ResponseHeadersToAdd {
          // The name of the header to add.
          string header_name = 1 [json_name = "header_name"];

          // The value of the header to add.
          string header_value = 2 [json_name = "header_value"];

          // If false, headerValue is appended to any values that already exist for the header.
          // If true, headerValue is set for the header, discarding any values that were set for that header.
          bool replace = 3;
        }
      }
    }
  }

  message DefaultUrlRedirect {
    // The host that will be used in the redirect response instead of the one that was
    // supplied in the request. The value must be between 1 and 255 characters.
    string host_redirect = 1 [json_name = "host_redirect"];

    // If set to true, the URL scheme in the redirected request is set to https. If set to
    // false, the URL scheme of the redirected request will remain the same as that of the
    // request. This must only be set for UrlMaps used in TargetHttpProxys. Setting this
    // true for TargetHttpsProxy is not permitted. The default is set to false.
    bool https_redirect = 2 [json_name = "https_redirect"];

    // The path that will be used in the redirect response instead of the one that was
    // supplied in the request. pathRedirect cannot be supplied together with
    // prefixRedirect. Supply one alone or neither. If neither is supplied, the path of the
    // original request will be used for the redirect. The value must be between 1 and 1024
    // characters.
    string path_redirect = 3 [json_name = "path_redirect"];

    // The prefix that replaces the prefixMatch specified in the HttpRouteRuleMatch,
    // retaining the remaining portion of the URL before redirecting the request.
    // prefixRedirect cannot be supplied together with pathRedirect. Supply one alone or
    // neither. If neither is supplied, the path of the original request will be used for
    // the redirect. The value must be between 1 and 1024 characters.
    string prefix_redirect = 4 [json_name = "prefix_redirect"];

    // The HTTP Status code to use for this RedirectAction. Supported values are:
    //
    // * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301.
    //
    // * FOUND, which corresponds to 302.
    //
    // * SEE_OTHER which corresponds to 303.
    //
    // * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method
    // will be retained.
    //
    // * PERMANENT_REDIRECT, which corresponds to 308. In this case,
    // the request method will be retained. Possible values: ["FOUND", "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER", "TEMPORARY_REDIRECT"]
    string redirect_response_code = 5 [json_name = "redirect_response_code"];

    // If set to true, any accompanying query portion of the original URL is removed prior
    // to redirecting the request. If set to false, the query portion of the original URL is
    // retained. The default is set to false.
    // This field is required to ensure an empty block is not set. The normal default value is false.
    bool strip_query = 6 [json_name = "strip_query"];
  }

  message HeaderAction {
    // A list of header names for headers that need to be removed from the request
    // prior to forwarding the request to the backendService.
    repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

    // A list of header names for headers that need to be removed from the response
    // prior to sending the response back to the client.
    repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

    repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

    repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

    message RequestHeadersToAdd {
      // The name of the header.
      string header_name = 1 [json_name = "header_name"];

      // The value of the header to add.
      string header_value = 2 [json_name = "header_value"];

      // If false, headerValue is appended to any values that already exist for the
      // header. If true, headerValue is set for the header, discarding any values that
      // were set for that header.
      bool replace = 3;
    }

    message ResponseHeadersToAdd {
      // The name of the header.
      string header_name = 1 [json_name = "header_name"];

      // The value of the header to add.
      string header_value = 2 [json_name = "header_value"];

      // If false, headerValue is appended to any values that already exist for the
      // header. If true, headerValue is set for the header, discarding any values that
      // were set for that header.
      bool replace = 3;
    }
  }

  message HostRule {
    // An optional description of this resource. Provide this property when you create
    // the resource.
    string description = 1;

    // The list of host patterns to match. They must be valid hostnames, except * will
    // match any string of ([a-z0-9-.]*). In that case, * must be the first character
    // and must be followed in the pattern by either - or ..
    repeated string hosts = 2;

    // The name of the PathMatcher to use to match the path portion of the URL if the
    // hostRule matches the URL's host portion.
    string path_matcher = 3 [json_name = "path_matcher"];
  }

  message PathMatcher {
    // The backend service or backend bucket to use when none of the given paths match.
    string default_service = 1 [json_name = "default_service"];

    // An optional description of this resource. Provide this property when you create
    // the resource.
    string description = 2;

    // The name to which this PathMatcher is referred by the HostRule.
    string name = 3;

    DefaultRouteAction default_route_action = 4 [json_name = "default_route_action"];

    DefaultUrlRedirect default_url_redirect = 5 [json_name = "default_url_redirect"];

    HeaderAction header_action = 6 [json_name = "header_action"];

    repeated PathRule path_rule = 7 [json_name = "path_rule"];

    repeated RouteRules route_rules = 8 [json_name = "route_rules"];

    message DefaultRouteAction {
      CorsPolicy cors_policy = 1 [json_name = "cors_policy"];

      FaultInjectionPolicy fault_injection_policy = 2 [json_name = "fault_injection_policy"];

      RequestMirrorPolicy request_mirror_policy = 3 [json_name = "request_mirror_policy"];

      RetryPolicy retry_policy = 4 [json_name = "retry_policy"];

      Timeout timeout = 5;

      UrlRewrite url_rewrite = 6 [json_name = "url_rewrite"];

      repeated WeightedBackendServices weighted_backend_services = 7 [json_name = "weighted_backend_services"];

      message CorsPolicy {
        // In response to a preflight request, setting this to true indicates that the actual request can include user credentials.
        // This translates to the Access-Control-Allow-Credentials header.
        bool allow_credentials = 1 [json_name = "allow_credentials"];

        // Specifies the content for the Access-Control-Allow-Headers header.
        repeated string allow_headers = 2 [json_name = "allow_headers"];

        // Specifies the content for the Access-Control-Allow-Methods header.
        repeated string allow_methods = 3 [json_name = "allow_methods"];

        // Specifies the regular expression patterns that match allowed origins. For regular expression grammar
        // please see en.cppreference.com/w/cpp/regex/ecmascript
        // An origin is allowed if it matches either an item in allowOrigins or an item in allowOriginRegexes.
        repeated string allow_origin_regexes = 4 [json_name = "allow_origin_regexes"];

        // Specifies the list of origins that will be allowed to do CORS requests.
        // An origin is allowed if it matches either an item in allowOrigins or an item in allowOriginRegexes.
        repeated string allow_origins = 5 [json_name = "allow_origins"];

        // If true, specifies the CORS policy is disabled. The default value is false, which indicates that the CORS policy is in effect.
        bool disabled = 6;

        // Specifies the content for the Access-Control-Expose-Headers header.
        repeated string expose_headers = 7 [json_name = "expose_headers"];

        // Specifies how long results of a preflight request can be cached in seconds.
        // This translates to the Access-Control-Max-Age header.
        int64 max_age = 8 [json_name = "max_age"];
      }

      message FaultInjectionPolicy {
        Abort abort = 1;

        Delay delay = 2;

        message Abort {
          // The HTTP status code used to abort the request.
          // The value must be between 200 and 599 inclusive.
          int64 http_status = 1 [json_name = "http_status"];

          // The percentage of traffic (connections/operations/requests) which will be aborted as part of fault injection.
          // The value must be between 0.0 and 100.0 inclusive.
          int64 percentage = 2;
        }

        message Delay {
          // The percentage of traffic (connections/operations/requests) on which delay will be introduced as part of fault injection.
          // The value must be between 0.0 and 100.0 inclusive.
          int64 percentage = 1;

          FixedDelay fixed_delay = 2 [json_name = "fixed_delay"];

          message FixedDelay {
            // Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are
            // represented with a 0 seconds field and a positive nanos field. Must be from 0 to 999,999,999 inclusive.
            int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive.
            // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years
            string seconds = 2;
          }
        }
      }

      message RequestMirrorPolicy {
        // The full or partial URL to the BackendService resource being mirrored to.
        string backend_service = 1 [json_name = "backend_service"];
      }

      message RetryPolicy {
        // Specifies the allowed number retries. This number must be > 0. If not specified, defaults to 1.
        int64 num_retries = 1 [json_name = "num_retries"];

        // Specfies one or more conditions when this retry rule applies. Valid values are:
        //
        // * 5xx: Loadbalancer will attempt a retry if the backend service responds with any 5xx response code,
        //  or if the backend service does not respond at all, example: disconnects, reset, read timeout,
        // * connection failure, and refused streams.
        // * gateway-error: Similar to 5xx, but only applies to response codes 502, 503 or 504.
        // * connect-failure: Loadbalancer will retry on failures connecting to backend services,
        //  for example due to connection timeouts.
        // * retriable-4xx: Loadbalancer will retry for retriable 4xx response codes.
        //  Currently the only retriable error supported is 409.
        // * refused-stream:Loadbalancer will retry if the backend service resets the stream with a REFUSED_STREAM error code.
        //  This reset type indicates that it is safe to retry.
        // * cancelled: Loadbalancer will retry if the gRPC status code in the response header is set to cancelled
        // * deadline-exceeded: Loadbalancer will retry if the gRPC status code in the response header is set to deadline-exceeded
        // * resource-exhausted: Loadbalancer will retry if the gRPC status code in the response header is set to resource-exhausted
        // * unavailable: Loadbalancer will retry if the gRPC status code in the response header is set to unavailable
        repeated string retry_conditions = 2 [json_name = "retry_conditions"];

        PerTryTimeout per_try_timeout = 3 [json_name = "per_try_timeout"];

        message PerTryTimeout {
          // Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are
          // represented with a 0 seconds field and a positive nanos field. Must be from 0 to 999,999,999 inclusive.
          int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive.
          // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years
          string seconds = 2;
        }
      }

      message Timeout {
        // Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented
        // with a 0 seconds field and a positive nanos field. Must be from 0 to 999,999,999 inclusive.
        int64 nanos = 1;

        // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive.
        // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years
        string seconds = 2;
      }

      message UrlRewrite {
        // Prior to forwarding the request to the selected service, the request's host header is replaced
        // with contents of hostRewrite.
        //
        // The value must be between 1 and 255 characters.
        string host_rewrite = 1 [json_name = "host_rewrite"];

        // Prior to forwarding the request to the selected backend service, the matching portion of the
        // request's path is replaced by pathPrefixRewrite.
        //
        // The value must be between 1 and 1024 characters.
        string path_prefix_rewrite = 2 [json_name = "path_prefix_rewrite"];
      }

      message WeightedBackendServices {
        // The full or partial URL to the default BackendService resource. Before forwarding the
        // request to backendService, the loadbalancer applies any relevant headerActions
        // specified as part of this backendServiceWeight.
        string backend_service = 1 [json_name = "backend_service"];

        // Specifies the fraction of traffic sent to backendService, computed as
        // weight / (sum of all weightedBackendService weights in routeAction) .
        //
        // The selection of a backend service is determined only for new traffic. Once a user's request
        // has been directed to a backendService, subsequent requests will be sent to the same backendService
        // as determined by the BackendService's session affinity policy.
        //
        // The value must be between 0 and 1000
        int64 weight = 2;

        HeaderAction header_action = 3 [json_name = "header_action"];

        message HeaderAction {
          // A list of header names for headers that need to be removed from the request prior to
          // forwarding the request to the backendService.
          repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

          // A list of header names for headers that need to be removed from the response prior to sending the
          // response back to the client.
          repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

          repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

          repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

          message RequestHeadersToAdd {
            // The name of the header to add.
            string header_name = 1 [json_name = "header_name"];

            // The value of the header to add.
            string header_value = 2 [json_name = "header_value"];

            // If false, headerValue is appended to any values that already exist for the header.
            // If true, headerValue is set for the header, discarding any values that were set for that header.
            bool replace = 3;
          }

          message ResponseHeadersToAdd {
            // The name of the header to add.
            string header_name = 1 [json_name = "header_name"];

            // The value of the header to add.
            string header_value = 2 [json_name = "header_value"];

            // If false, headerValue is appended to any values that already exist for the header.
            // If true, headerValue is set for the header, discarding any values that were set for that header.
            bool replace = 3;
          }
        }
      }
    }

    message DefaultUrlRedirect {
      // The host that will be used in the redirect response instead of the one that was
      // supplied in the request. The value must be between 1 and 255 characters.
      string host_redirect = 1 [json_name = "host_redirect"];

      // If set to true, the URL scheme in the redirected request is set to https. If set to
      // false, the URL scheme of the redirected request will remain the same as that of the
      // request. This must only be set for UrlMaps used in TargetHttpProxys. Setting this
      // true for TargetHttpsProxy is not permitted. The default is set to false.
      bool https_redirect = 2 [json_name = "https_redirect"];

      // The path that will be used in the redirect response instead of the one that was
      // supplied in the request. pathRedirect cannot be supplied together with
      // prefixRedirect. Supply one alone or neither. If neither is supplied, the path of the
      // original request will be used for the redirect. The value must be between 1 and 1024
      // characters.
      string path_redirect = 3 [json_name = "path_redirect"];

      // The prefix that replaces the prefixMatch specified in the HttpRouteRuleMatch,
      // retaining the remaining portion of the URL before redirecting the request.
      // prefixRedirect cannot be supplied together with pathRedirect. Supply one alone or
      // neither. If neither is supplied, the path of the original request will be used for
      // the redirect. The value must be between 1 and 1024 characters.
      string prefix_redirect = 4 [json_name = "prefix_redirect"];

      // The HTTP Status code to use for this RedirectAction. Supported values are:
      //
      // * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301.
      //
      // * FOUND, which corresponds to 302.
      //
      // * SEE_OTHER which corresponds to 303.
      //
      // * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method
      // will be retained.
      //
      // * PERMANENT_REDIRECT, which corresponds to 308. In this case,
      // the request method will be retained. Possible values: ["FOUND", "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER", "TEMPORARY_REDIRECT"]
      string redirect_response_code = 5 [json_name = "redirect_response_code"];

      // If set to true, any accompanying query portion of the original URL is removed prior
      // to redirecting the request. If set to false, the query portion of the original URL is
      // retained.
      // This field is required to ensure an empty block is not set. The normal default value is false.
      bool strip_query = 6 [json_name = "strip_query"];
    }

    message HeaderAction {
      // A list of header names for headers that need to be removed from the request
      // prior to forwarding the request to the backendService.
      repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

      // A list of header names for headers that need to be removed from the response
      // prior to sending the response back to the client.
      repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

      repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

      repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

      message RequestHeadersToAdd {
        // The name of the header.
        string header_name = 1 [json_name = "header_name"];

        // The value of the header to add.
        string header_value = 2 [json_name = "header_value"];

        // If false, headerValue is appended to any values that already exist for the
        // header. If true, headerValue is set for the header, discarding any values that
        // were set for that header.
        bool replace = 3;
      }

      message ResponseHeadersToAdd {
        // The name of the header.
        string header_name = 1 [json_name = "header_name"];

        // The value of the header to add.
        string header_value = 2 [json_name = "header_value"];

        // If false, headerValue is appended to any values that already exist for the
        // header. If true, headerValue is set for the header, discarding any values that
        // were set for that header.
        bool replace = 3;
      }
    }

    message PathRule {
      // The list of path patterns to match. Each must start with / and the only place a
      // \* is allowed is at the end following a /. The string fed to the path matcher
      // does not include any text after the first ? or #, and those chars are not
      // allowed here.
      repeated string paths = 1;

      // The backend service or backend bucket to use if any of the given paths match.
      string service = 2;

      RouteAction route_action = 3 [json_name = "route_action"];

      UrlRedirect url_redirect = 4 [json_name = "url_redirect"];

      message RouteAction {
        CorsPolicy cors_policy = 1 [json_name = "cors_policy"];

        FaultInjectionPolicy fault_injection_policy = 2 [json_name = "fault_injection_policy"];

        RequestMirrorPolicy request_mirror_policy = 3 [json_name = "request_mirror_policy"];

        RetryPolicy retry_policy = 4 [json_name = "retry_policy"];

        Timeout timeout = 5;

        UrlRewrite url_rewrite = 6 [json_name = "url_rewrite"];

        repeated WeightedBackendServices weighted_backend_services = 7 [json_name = "weighted_backend_services"];

        message CorsPolicy {
          // In response to a preflight request, setting this to true indicates that the
          // actual request can include user credentials. This translates to the Access-
          // Control-Allow-Credentials header. Defaults to false.
          bool allow_credentials = 1 [json_name = "allow_credentials"];

          // Specifies the content for the Access-Control-Allow-Headers header.
          repeated string allow_headers = 2 [json_name = "allow_headers"];

          // Specifies the content for the Access-Control-Allow-Methods header.
          repeated string allow_methods = 3 [json_name = "allow_methods"];

          // Specifies the regular expression patterns that match allowed origins. For
          // regular expression grammar please see en.cppreference.com/w/cpp/regex/ecmascript
          // An origin is allowed if it matches either allow_origins or allow_origin_regex.
          repeated string allow_origin_regexes = 4 [json_name = "allow_origin_regexes"];

          // Specifies the list of origins that will be allowed to do CORS requests. An
          // origin is allowed if it matches either allow_origins or allow_origin_regex.
          repeated string allow_origins = 5 [json_name = "allow_origins"];

          // If true, specifies the CORS policy is disabled.
          bool disabled = 6;

          // Specifies the content for the Access-Control-Expose-Headers header.
          repeated string expose_headers = 7 [json_name = "expose_headers"];

          // Specifies how long the results of a preflight request can be cached. This
          // translates to the content for the Access-Control-Max-Age header.
          int64 max_age = 8 [json_name = "max_age"];
        }

        message FaultInjectionPolicy {
          Abort abort = 1;

          Delay delay = 2;

          message Abort {
            // The HTTP status code used to abort the request. The value must be between 200
            // and 599 inclusive.
            int64 http_status = 1 [json_name = "http_status"];

            // The percentage of traffic (connections/operations/requests) which will be
            // aborted as part of fault injection. The value must be between 0.0 and 100.0
            // inclusive.
            int64 percentage = 2;
          }

          message Delay {
            // The percentage of traffic (connections/operations/requests) on which delay will
            // be introduced as part of fault injection. The value must be between 0.0 and
            // 100.0 inclusive.
            int64 percentage = 1;

            FixedDelay fixed_delay = 2 [json_name = "fixed_delay"];

            message FixedDelay {
              // Span of time that's a fraction of a second at nanosecond resolution. Durations
              // less than one second are represented with a 0 'seconds' field and a positive
              // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
              int64 nanos = 1;

              // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
              // inclusive.
              string seconds = 2;
            }
          }
        }

        message RequestMirrorPolicy {
          // The BackendService resource being mirrored to.
          string backend_service = 1 [json_name = "backend_service"];
        }

        message RetryPolicy {
          // Specifies the allowed number retries. This number must be > 0.
          int64 num_retries = 1 [json_name = "num_retries"];

          // Specifies one or more conditions when this retry rule applies. Valid values are:
          //
          // * 5xx: Loadbalancer will attempt a retry if the backend service responds with
          // any 5xx response code, or if the backend service does not respond at all,
          // example: disconnects, reset, read timeout, connection failure, and refused
          // streams.
          // * gateway-error: Similar to 5xx, but only applies to response codes
          // 502, 503 or 504.
          // * connect-failure: Loadbalancer will retry on failures
          // connecting to backend services, for example due to connection timeouts.
          // * retriable-4xx: Loadbalancer will retry for retriable 4xx response codes.
          // Currently the only retriable error supported is 409.
          // * refused-stream: Loadbalancer will retry if the backend service resets the stream with a
          // REFUSED_STREAM error code. This reset type indicates that it is safe to retry.
          // * cancelled: Loadbalancer will retry if the gRPC status code in the response
          // header is set to cancelled
          // * deadline-exceeded: Loadbalancer will retry if the
          // gRPC status code in the response header is set to deadline-exceeded
          // * resource-exhausted: Loadbalancer will retry if the gRPC status code in the response
          // header is set to resource-exhausted
          // * unavailable: Loadbalancer will retry if
          // the gRPC status code in the response header is set to unavailable
          repeated string retry_conditions = 2 [json_name = "retry_conditions"];

          PerTryTimeout per_try_timeout = 3 [json_name = "per_try_timeout"];

          message PerTryTimeout {
            // Span of time that's a fraction of a second at nanosecond resolution. Durations
            // less than one second are represented with a 0 'seconds' field and a positive
            // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
            int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            // inclusive.
            string seconds = 2;
          }
        }

        message Timeout {
          // Span of time that's a fraction of a second at nanosecond resolution. Durations
          // less than one second are represented with a 0 'seconds' field and a positive
          // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
          int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          // inclusive.
          string seconds = 2;
        }

        message UrlRewrite {
          // Prior to forwarding the request to the selected service, the request's host
          // header is replaced with contents of hostRewrite. The value must be between 1 and
          // 255 characters.
          string host_rewrite = 1 [json_name = "host_rewrite"];

          // Prior to forwarding the request to the selected backend service, the matching
          // portion of the request's path is replaced by pathPrefixRewrite. The value must
          // be between 1 and 1024 characters.
          string path_prefix_rewrite = 2 [json_name = "path_prefix_rewrite"];
        }

        message WeightedBackendServices {
          // The default BackendService resource. Before
          // forwarding the request to backendService, the loadbalancer applies any relevant
          // headerActions specified as part of this backendServiceWeight.
          string backend_service = 1 [json_name = "backend_service"];

          // Specifies the fraction of traffic sent to backendService, computed as weight /
          // (sum of all weightedBackendService weights in routeAction) . The selection of a
          // backend service is determined only for new traffic. Once a user's request has
          // been directed to a backendService, subsequent requests will be sent to the same
          // backendService as determined by the BackendService's session affinity policy.
          // The value must be between 0 and 1000
          int64 weight = 2;

          HeaderAction header_action = 3 [json_name = "header_action"];

          message HeaderAction {
            // A list of header names for headers that need to be removed from the request
            // prior to forwarding the request to the backendService.
            repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

            // A list of header names for headers that need to be removed from the response
            // prior to sending the response back to the client.
            repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

            repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

            repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

            message RequestHeadersToAdd {
              // The name of the header.
              string header_name = 1 [json_name = "header_name"];

              // The value of the header to add.
              string header_value = 2 [json_name = "header_value"];

              // If false, headerValue is appended to any values that already exist for the
              // header. If true, headerValue is set for the header, discarding any values that
              // were set for that header.
              bool replace = 3;
            }

            message ResponseHeadersToAdd {
              // The name of the header.
              string header_name = 1 [json_name = "header_name"];

              // The value of the header to add.
              string header_value = 2 [json_name = "header_value"];

              // If false, headerValue is appended to any values that already exist for the
              // header. If true, headerValue is set for the header, discarding any values that
              // were set for that header.
              bool replace = 3;
            }
          }
        }
      }

      message UrlRedirect {
        // The host that will be used in the redirect response instead of the one
        // that was supplied in the request. The value must be between 1 and 255
        // characters.
        string host_redirect = 1 [json_name = "host_redirect"];

        // If set to true, the URL scheme in the redirected request is set to https.
        // If set to false, the URL scheme of the redirected request will remain the
        // same as that of the request. This must only be set for UrlMaps used in
        // TargetHttpProxys. Setting this true for TargetHttpsProxy is not
        // permitted. The default is set to false.
        bool https_redirect = 2 [json_name = "https_redirect"];

        // The path that will be used in the redirect response instead of the one
        // that was supplied in the request. pathRedirect cannot be supplied
        // together with prefixRedirect. Supply one alone or neither. If neither is
        // supplied, the path of the original request will be used for the redirect.
        // The value must be between 1 and 1024 characters.
        string path_redirect = 3 [json_name = "path_redirect"];

        // The prefix that replaces the prefixMatch specified in the
        // HttpRouteRuleMatch, retaining the remaining portion of the URL before
        // redirecting the request. prefixRedirect cannot be supplied together with
        // pathRedirect. Supply one alone or neither. If neither is supplied, the
        // path of the original request will be used for the redirect. The value
        // must be between 1 and 1024 characters.
        string prefix_redirect = 4 [json_name = "prefix_redirect"];

        // The HTTP Status code to use for this RedirectAction. Supported values are:
        //
        // * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301.
        //
        // * FOUND, which corresponds to 302.
        //
        // * SEE_OTHER which corresponds to 303.
        //
        // * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method
        // will be retained.
        //
        // * PERMANENT_REDIRECT, which corresponds to 308. In this case,
        // the request method will be retained. Possible values: ["FOUND", "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER", "TEMPORARY_REDIRECT"]
        string redirect_response_code = 5 [json_name = "redirect_response_code"];

        // If set to true, any accompanying query portion of the original URL is
        // removed prior to redirecting the request. If set to false, the query
        // portion of the original URL is retained.
        // This field is required to ensure an empty block is not set. The normal default value is false.
        bool strip_query = 6 [json_name = "strip_query"];
      }
    }

    message RouteRules {
      // For routeRules within a given pathMatcher, priority determines the order
      // in which load balancer will interpret routeRules. RouteRules are evaluated
      // in order of priority, from the lowest to highest number. The priority of
      // a rule decreases as its number increases (1, 2, 3, N+1). The first rule
      // that matches the request is applied.
      //
      // You cannot configure two or more routeRules with the same priority.
      // Priority for each rule must be set to a number between 0 and
      // 2147483647 inclusive.
      //
      // Priority numbers can have gaps, which enable you to add or remove rules
      // in the future without affecting the rest of the rules. For example,
      // 1, 2, 3, 4, 5, 9, 12, 16 is a valid series of priority numbers to which
      // you could add rules numbered from 6 to 8, 10 to 11, and 13 to 15 in the
      // future without any impact on existing rules.
      int64 priority = 1;

      // The backend service resource to which traffic is
      // directed if this rule is matched. If routeAction is additionally specified,
      // advanced routing actions like URL Rewrites, etc. take effect prior to sending
      // the request to the backend. However, if service is specified, routeAction cannot
      // contain any weightedBackendService s. Conversely, if routeAction specifies any
      // weightedBackendServices, service must not be specified. Only one of urlRedirect,
      // service or routeAction.weightedBackendService must be set.
      string service = 2;

      HeaderAction header_action = 3 [json_name = "header_action"];

      repeated MatchRules match_rules = 4 [json_name = "match_rules"];

      RouteAction route_action = 5 [json_name = "route_action"];

      UrlRedirect url_redirect = 6 [json_name = "url_redirect"];

      message HeaderAction {
        // A list of header names for headers that need to be removed from the request
        // prior to forwarding the request to the backendService.
        repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

        // A list of header names for headers that need to be removed from the response
        // prior to sending the response back to the client.
        repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

        repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

        repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

        message RequestHeadersToAdd {
          // The name of the header.
          string header_name = 1 [json_name = "header_name"];

          // The value of the header to add.
          string header_value = 2 [json_name = "header_value"];

          // If false, headerValue is appended to any values that already exist for the
          // header. If true, headerValue is set for the header, discarding any values that
          // were set for that header.
          bool replace = 3;
        }

        message ResponseHeadersToAdd {
          // The name of the header.
          string header_name = 1 [json_name = "header_name"];

          // The value of the header to add.
          string header_value = 2 [json_name = "header_value"];

          // If false, headerValue is appended to any values that already exist for the
          // header. If true, headerValue is set for the header, discarding any values that
          // were set for that header.
          bool replace = 3;
        }
      }

      message MatchRules {
        // For satisfying the matchRule condition, the path of the request must exactly
        // match the value specified in fullPathMatch after removing any query parameters
        // and anchor that may be part of the original URL. FullPathMatch must be between 1
        // and 1024 characters. Only one of prefixMatch, fullPathMatch or regexMatch must
        // be specified.
        string full_path_match = 1 [json_name = "full_path_match"];

        // Specifies that prefixMatch and fullPathMatch matches are case sensitive.
        // Defaults to false.
        bool ignore_case = 2 [json_name = "ignore_case"];

        // For satisfying the matchRule condition, the request's path must begin with the
        // specified prefixMatch. prefixMatch must begin with a /. The value must be
        // between 1 and 1024 characters. Only one of prefixMatch, fullPathMatch or
        // regexMatch must be specified.
        string prefix_match = 3 [json_name = "prefix_match"];

        // For satisfying the matchRule condition, the path of the request must satisfy the
        // regular expression specified in regexMatch after removing any query parameters
        // and anchor supplied with the original URL. For regular expression grammar please
        // see en.cppreference.com/w/cpp/regex/ecmascript  Only one of prefixMatch,
        // fullPathMatch or regexMatch must be specified.
        string regex_match = 4 [json_name = "regex_match"];

        repeated HeaderMatches header_matches = 5 [json_name = "header_matches"];

        repeated MetadataFilters metadata_filters = 6 [json_name = "metadata_filters"];

        repeated QueryParameterMatches query_parameter_matches = 7 [json_name = "query_parameter_matches"];

        message HeaderMatches {
          // The value should exactly match contents of exactMatch. Only one of exactMatch,
          // prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch must be set.
          string exact_match = 1 [json_name = "exact_match"];

          // The name of the HTTP header to match. For matching against the HTTP request's
          // authority, use a headerMatch with the header name ":authority". For matching a
          // request's method, use the headerName ":method".
          string header_name = 2 [json_name = "header_name"];

          // If set to false, the headerMatch is considered a match if the match criteria
          // above are met. If set to true, the headerMatch is considered a match if the
          // match criteria above are NOT met. Defaults to false.
          bool invert_match = 3 [json_name = "invert_match"];

          // The value of the header must start with the contents of prefixMatch. Only one of
          // exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch
          // must be set.
          string prefix_match = 4 [json_name = "prefix_match"];

          // A header with the contents of headerName must exist. The match takes place
          // whether or not the request's header has a value or not. Only one of exactMatch,
          // prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch must be set.
          bool present_match = 5 [json_name = "present_match"];

          // The value of the header must match the regular expression specified in
          // regexMatch. For regular expression grammar, please see:
          // en.cppreference.com/w/cpp/regex/ecmascript  For matching against a port
          // specified in the HTTP request, use a headerMatch with headerName set to PORT and
          // a regular expression that satisfies the RFC2616 Host header's port specifier.
          // Only one of exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or
          // rangeMatch must be set.
          string regex_match = 6 [json_name = "regex_match"];

          // The value of the header must end with the contents of suffixMatch. Only one of
          // exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch
          // must be set.
          string suffix_match = 7 [json_name = "suffix_match"];

          RangeMatch range_match = 8 [json_name = "range_match"];

          message RangeMatch {
            // The end of the range (exclusive).
            int64 range_end = 1 [json_name = "range_end"];

            // The start of the range (inclusive).
            int64 range_start = 2 [json_name = "range_start"];
          }
        }

        message MetadataFilters {
          // Specifies how individual filterLabel matches within the list of filterLabels
          // contribute towards the overall metadataFilter match. Supported values are:
          //  - MATCH_ANY: At least one of the filterLabels must have a matching label in the
          // provided metadata.
          //  - MATCH_ALL: All filterLabels must have matching labels in
          // the provided metadata. Possible values: ["MATCH_ALL", "MATCH_ANY"]
          string filter_match_criteria = 1 [json_name = "filter_match_criteria"];

          repeated FilterLabels filter_labels = 2 [json_name = "filter_labels"];

          message FilterLabels {
            // Name of metadata label. The name can have a maximum length of 1024 characters
            // and must be at least 1 character long.
            string name = 1;

            // The value of the label must match the specified value. value can have a maximum
            // length of 1024 characters.
            string value = 2;
          }
        }

        message QueryParameterMatches {
          // The queryParameterMatch matches if the value of the parameter exactly matches
          // the contents of exactMatch. Only one of presentMatch, exactMatch and regexMatch
          // must be set.
          string exact_match = 1 [json_name = "exact_match"];

          // The name of the query parameter to match. The query parameter must exist in the
          // request, in the absence of which the request match fails.
          string name = 2;

          // Specifies that the queryParameterMatch matches if the request contains the query
          // parameter, irrespective of whether the parameter has a value or not. Only one of
          // presentMatch, exactMatch and regexMatch must be set.
          bool present_match = 3 [json_name = "present_match"];

          // The queryParameterMatch matches if the value of the parameter matches the
          // regular expression specified by regexMatch. For the regular expression grammar,
          // please see en.cppreference.com/w/cpp/regex/ecmascript  Only one of presentMatch,
          // exactMatch and regexMatch must be set.
          string regex_match = 4 [json_name = "regex_match"];
        }
      }

      message RouteAction {
        CorsPolicy cors_policy = 1 [json_name = "cors_policy"];

        FaultInjectionPolicy fault_injection_policy = 2 [json_name = "fault_injection_policy"];

        RequestMirrorPolicy request_mirror_policy = 3 [json_name = "request_mirror_policy"];

        RetryPolicy retry_policy = 4 [json_name = "retry_policy"];

        Timeout timeout = 5;

        UrlRewrite url_rewrite = 6 [json_name = "url_rewrite"];

        repeated WeightedBackendServices weighted_backend_services = 7 [json_name = "weighted_backend_services"];

        message CorsPolicy {
          // In response to a preflight request, setting this to true indicates that the
          // actual request can include user credentials. This translates to the Access-
          // Control-Allow-Credentials header. Defaults to false.
          bool allow_credentials = 1 [json_name = "allow_credentials"];

          // Specifies the content for the Access-Control-Allow-Headers header.
          repeated string allow_headers = 2 [json_name = "allow_headers"];

          // Specifies the content for the Access-Control-Allow-Methods header.
          repeated string allow_methods = 3 [json_name = "allow_methods"];

          // Specifies the regular expression patterns that match allowed origins. For
          // regular expression grammar please see en.cppreference.com/w/cpp/regex/ecmascript
          // An origin is allowed if it matches either allow_origins or allow_origin_regex.
          repeated string allow_origin_regexes = 4 [json_name = "allow_origin_regexes"];

          // Specifies the list of origins that will be allowed to do CORS requests. An
          // origin is allowed if it matches either allow_origins or allow_origin_regex.
          repeated string allow_origins = 5 [json_name = "allow_origins"];

          // If true, specifies the CORS policy is disabled.
          // which indicates that the CORS policy is in effect. Defaults to false.
          bool disabled = 6;

          // Specifies the content for the Access-Control-Expose-Headers header.
          repeated string expose_headers = 7 [json_name = "expose_headers"];

          // Specifies how long the results of a preflight request can be cached. This
          // translates to the content for the Access-Control-Max-Age header.
          int64 max_age = 8 [json_name = "max_age"];
        }

        message FaultInjectionPolicy {
          Abort abort = 1;

          Delay delay = 2;

          message Abort {
            // The HTTP status code used to abort the request. The value must be between 200
            // and 599 inclusive.
            int64 http_status = 1 [json_name = "http_status"];

            // The percentage of traffic (connections/operations/requests) which will be
            // aborted as part of fault injection. The value must be between 0.0 and 100.0
            // inclusive.
            int64 percentage = 2;
          }

          message Delay {
            // The percentage of traffic (connections/operations/requests) on which delay will
            // be introduced as part of fault injection. The value must be between 0.0 and
            // 100.0 inclusive.
            int64 percentage = 1;

            FixedDelay fixed_delay = 2 [json_name = "fixed_delay"];

            message FixedDelay {
              // Span of time that's a fraction of a second at nanosecond resolution. Durations
              // less than one second are represented with a 0 'seconds' field and a positive
              // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
              int64 nanos = 1;

              // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
              // inclusive.
              string seconds = 2;
            }
          }
        }

        message RequestMirrorPolicy {
          // The BackendService resource being mirrored to.
          string backend_service = 1 [json_name = "backend_service"];
        }

        message RetryPolicy {
          // Specifies the allowed number retries. This number must be > 0.
          int64 num_retries = 1 [json_name = "num_retries"];

          // Specfies one or more conditions when this retry rule applies. Valid values are:
          //
          // * 5xx: Loadbalancer will attempt a retry if the backend service responds with
          //  any 5xx response code, or if the backend service does not respond at all,
          //  example: disconnects, reset, read timeout, connection failure, and refused
          //  streams.
          // * gateway-error: Similar to 5xx, but only applies to response codes
          //  502, 503 or 504.
          // * connect-failure: Loadbalancer will retry on failures
          //  connecting to backend services, for example due to connection timeouts.
          // * retriable-4xx: Loadbalancer will retry for retriable 4xx response codes.
          //  Currently the only retriable error supported is 409.
          // * refused-stream: Loadbalancer will retry if the backend service resets the stream with a
          //  REFUSED_STREAM error code. This reset type indicates that it is safe to retry.
          // * cancelled: Loadbalancer will retry if the gRPC status code in the response
          //  header is set to cancelled
          // * deadline-exceeded: Loadbalancer will retry if the
          //  gRPC status code in the response header is set to deadline-exceeded
          // * resource-exhausted: Loadbalancer will retry if the gRPC status code in the response
          //  header is set to resource-exhausted
          // * unavailable: Loadbalancer will retry if the gRPC status code in
          //  the response header is set to unavailable
          repeated string retry_conditions = 2 [json_name = "retry_conditions"];

          PerTryTimeout per_try_timeout = 3 [json_name = "per_try_timeout"];

          message PerTryTimeout {
            // Span of time that's a fraction of a second at nanosecond resolution. Durations
            // less than one second are represented with a 0 'seconds' field and a positive
            // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
            int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            // inclusive.
            string seconds = 2;
          }
        }

        message Timeout {
          // Span of time that's a fraction of a second at nanosecond resolution. Durations
          // less than one second are represented with a 0 'seconds' field and a positive
          // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
          int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          // inclusive.
          string seconds = 2;
        }

        message UrlRewrite {
          // Prior to forwarding the request to the selected service, the request's host
          // header is replaced with contents of hostRewrite. The value must be between 1 and
          // 255 characters.
          string host_rewrite = 1 [json_name = "host_rewrite"];

          // Prior to forwarding the request to the selected backend service, the matching
          // portion of the request's path is replaced by pathPrefixRewrite. The value must
          // be between 1 and 1024 characters.
          string path_prefix_rewrite = 2 [json_name = "path_prefix_rewrite"];
        }

        message WeightedBackendServices {
          // The default BackendService resource. Before
          // forwarding the request to backendService, the loadbalancer applies any relevant
          // headerActions specified as part of this backendServiceWeight.
          string backend_service = 1 [json_name = "backend_service"];

          // Specifies the fraction of traffic sent to backendService, computed as weight /
          // (sum of all weightedBackendService weights in routeAction) . The selection of a
          // backend service is determined only for new traffic. Once a user's request has
          // been directed to a backendService, subsequent requests will be sent to the same
          // backendService as determined by the BackendService's session affinity policy.
          // The value must be between 0 and 1000
          int64 weight = 2;

          HeaderAction header_action = 3 [json_name = "header_action"];

          message HeaderAction {
            // A list of header names for headers that need to be removed from the request
            // prior to forwarding the request to the backendService.
            repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

            // A list of header names for headers that need to be removed from the response
            // prior to sending the response back to the client.
            repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

            repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

            repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

            message RequestHeadersToAdd {
              // The name of the header.
              string header_name = 1 [json_name = "header_name"];

              // The value of the header to add.
              string header_value = 2 [json_name = "header_value"];

              // If false, headerValue is appended to any values that already exist for the
              // header. If true, headerValue is set for the header, discarding any values that
              // were set for that header.
              bool replace = 3;
            }

            message ResponseHeadersToAdd {
              // The name of the header.
              string header_name = 1 [json_name = "header_name"];

              // The value of the header to add.
              string header_value = 2 [json_name = "header_value"];

              // If false, headerValue is appended to any values that already exist for the
              // header. If true, headerValue is set for the header, discarding any values that
              // were set for that header.
              bool replace = 3;
            }
          }
        }
      }

      message UrlRedirect {
        // The host that will be used in the redirect response instead of the one that was
        // supplied in the request. The value must be between 1 and 255 characters.
        string host_redirect = 1 [json_name = "host_redirect"];

        // If set to true, the URL scheme in the redirected request is set to https. If set
        // to false, the URL scheme of the redirected request will remain the same as that
        // of the request. This must only be set for UrlMaps used in TargetHttpProxys.
        // Setting this true for TargetHttpsProxy is not permitted. Defaults to false.
        bool https_redirect = 2 [json_name = "https_redirect"];

        // The path that will be used in the redirect response instead of the one that was
        // supplied in the request. Only one of pathRedirect or prefixRedirect must be
        // specified. The value must be between 1 and 1024 characters.
        string path_redirect = 3 [json_name = "path_redirect"];

        // The prefix that replaces the prefixMatch specified in the HttpRouteRuleMatch,
        // retaining the remaining portion of the URL before redirecting the request.
        string prefix_redirect = 4 [json_name = "prefix_redirect"];

        // The HTTP Status code to use for this RedirectAction. Supported values are:
        //
        // * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301.
        //
        // * FOUND, which corresponds to 302.
        //
        // * SEE_OTHER which corresponds to 303.
        //
        // * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method will be retained.
        //
        // * PERMANENT_REDIRECT, which corresponds to 308. In this case, the request method will be retained. Possible values: ["FOUND", "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER", "TEMPORARY_REDIRECT"]
        string redirect_response_code = 5 [json_name = "redirect_response_code"];

        // If set to true, any accompanying query portion of the original URL is removed
        // prior to redirecting the request. If set to false, the query portion of the
        // original URL is retained. Defaults to false.
        bool strip_query = 6 [json_name = "strip_query"];
      }
    }
  }

  message Test {
    // Description of this test case.
    string description = 1;

    // Host portion of the URL.
    string host = 2;

    // Path portion of the URL.
    string path = 3;

    // The backend service or backend bucket link that should be matched by this test.
    string service = 4;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// GoogleComputeVpnGateway version is 0
message GoogleComputeVpnGateway {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 2;

  // The unique identifier for the resource.
  int64 gateway_id = 3 [json_name = "gateway_id"];

  string id = 4;

  // Name of the resource. Provided by the client when the resource is
  // created. The name must be 1-63 characters long, and comply with
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  // the first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last
  // character, which cannot be a dash.
  string name = 5;

  // The network this VPN gateway is accepting traffic for.
  string network = 6;

  string project = 7;

  // The region this gateway should sit in.
  string region = 8;

  string self_link = 9 [json_name = "self_link"];

  repeated Timeouts timeouts = 10;

  map<string, string> for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// GoogleComputeVpnTunnel version is 0
message GoogleComputeVpnTunnel {
  // Creation timestamp in RFC3339 text format.
  string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  string description = 2;

  // Detailed status message for the VPN tunnel.
  string detailed_status = 3 [json_name = "detailed_status"];

  string id = 4;

  // IKE protocol version to use when establishing the VPN tunnel with
  // peer VPN gateway.
  // Acceptable IKE versions are 1 or 2. Default version is 2.
  int64 ike_version = 5 [json_name = "ike_version"];

  // Local traffic selector to use when establishing the VPN tunnel with
  // peer VPN gateway. The value should be a CIDR formatted string,
  // for example '192.168.0.0/16'. The ranges should be disjoint.
  // Only IPv4 is supported.
  repeated string local_traffic_selector = 6 [json_name = "local_traffic_selector"];

  // Name of the resource. The name must be 1-63 characters long, and
  // comply with RFC1035. Specifically, the name must be 1-63
  // characters long and match the regular expression
  // '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character
  // must be a lowercase letter, and all following characters must
  // be a dash, lowercase letter, or digit,
  // except the last character, which cannot be a dash.
  string name = 7;

  // URL of the peer side external VPN gateway to which this VPN tunnel is connected.
  string peer_external_gateway = 8 [json_name = "peer_external_gateway"];

  // The interface ID of the external VPN gateway to which this VPN tunnel is connected.
  int64 peer_external_gateway_interface = 9 [json_name = "peer_external_gateway_interface"];

  // URL of the peer side HA GCP VPN gateway to which this VPN tunnel is connected.
  // If provided, the VPN tunnel will automatically use the same vpn_gateway_interface
  // ID in the peer GCP VPN gateway.
  // This field must reference a 'google_compute_ha_vpn_gateway' resource.
  string peer_gcp_gateway = 10 [json_name = "peer_gcp_gateway"];

  // IP address of the peer VPN gateway. Only IPv4 is supported.
  string peer_ip = 11 [json_name = "peer_ip"];

  string project = 12;

  // The region where the tunnel is located. If unset, is set to the region of 'target_vpn_gateway'.
  string region = 13;

  // Remote traffic selector to use when establishing the VPN tunnel with
  // peer VPN gateway. The value should be a CIDR formatted string,
  // for example '192.168.0.0/16'. The ranges should be disjoint.
  // Only IPv4 is supported.
  repeated string remote_traffic_selector = 14 [json_name = "remote_traffic_selector"];

  // URL of router resource to be used for dynamic routing.
  string router = 15;

  string self_link = 16 [json_name = "self_link"];

  // Shared secret used to set the secure session between the Cloud VPN
  // gateway and the peer VPN gateway.
  string shared_secret = 17 [json_name = "shared_secret"];

  // Hash of the shared secret.
  string shared_secret_hash = 18 [json_name = "shared_secret_hash"];

  // URL of the Target VPN gateway with which this VPN tunnel is
  // associated.
  string target_vpn_gateway = 19 [json_name = "target_vpn_gateway"];

  // The unique identifier for the resource. This identifier is defined by the server.
  string tunnel_id = 20 [json_name = "tunnel_id"];

  // URL of the VPN gateway with which this VPN tunnel is associated.
  // This must be used if a High Availability VPN gateway resource is created.
  // This field must reference a 'google_compute_ha_vpn_gateway' resource.
  string vpn_gateway = 21 [json_name = "vpn_gateway"];

  // The interface ID of the VPN gateway with which this VPN tunnel is associated.
  int64 vpn_gateway_interface = 22 [json_name = "vpn_gateway_interface"];

  repeated Timeouts timeouts = 23;

  map<string, string> for_each = 24 [json_name = "for_each"];

  repeated string depends_on = 25 [json_name = "depends_on"];

  int32 count = 26;

  string provider = 27;

  terraform.v1.Lifecycle lifecycle = 28;

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}
