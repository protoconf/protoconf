---
phase: quick-260902-f8i
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [QUICK-260902-f8i]

files_modified:
  - pb/protoconf/v1/protoconf.proto
  - pb/protoconf/v1/protoconf.pb.go            # REGENERATED — do not hand-edit
  - pb/protoconf/v1/protoconf_grpc.pb.go       # REGENERATED — do not hand-edit
  - agent/api/proto/v1/protoconf_service.proto
  - agent/api/proto/v1/protoconf_service.pb.go       # REGENERATED — do not hand-edit
  - agent/api/proto/v1/protoconf_service_grpc.pb.go  # REGENERATED — do not hand-edit
  - agent/kv_agent_impl.go
  - agent/kv_agent_impl_test.go
  - agent/kv_agent_rollout_impl.go
  - agent/kv_agent_rollout_impl_test.go
  - agent/dummykv/dummykv.go
  - agent/filekv/filekv.go
  - agent/filekv/filekv_test.go
  - agent/legacy.go
  - test/e2e_test.go

estimate:
  tokens: 62000
  raw_tokens: 41000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "A client can call `GetConfig(path)` once on the current service and receive the same `ConfigUpdate.Value` that `SubscribeForConfig`'s first message would carry — no stream setup, no teardown"
    - "GetConfig works against BOTH agent implementations, each reading the key shape its own SubscribeForConfig watches (`prefix/path` for ProtoconfKVAgent, `prefix/path/config.data` for ProtoconfKVAgentRollout)"
    - "GetConfig against the dev/devserver path (filekv) returns a config instead of panicking"
    - "A missing config returns a gRPC `codes.NotFound` status, not a nil dereference and not `codes.Internal`"
    - "Legacy `protoconfservice` clients get GetConfig too, via the existing `legacyProtoconfServer` marshal-round-trip bridge"
    - "`ConfigUpdate` is unchanged — no `raw` field, no googleapis import, no HTTP handler"
  artifacts:
    - pb/protoconf/v1/protoconf.proto
    - agent/api/proto/v1/protoconf_service.proto
    - agent/filekv/filekv.go
    - agent/legacy.go
  key_links:
    - "`pb/protoconf/v1/protoconf.proto` MUST be generated with `-I=pb` — `generate.go` deliberately excludes `pb/`. Generating with `-I=.` renames the symbol to `File_pb_protoconf_v1_protoconf_proto` and breaks `server/server.go:285`"
    - "Both agent structs embed the ProtoconfServiceServer INTERFACE (nil), not `UnimplementedProtoconfServiceServer` — the compiler will NOT catch a missing GetConfig; a call would nil-panic at runtime. Both impls are mandatory, and only tests prove it"
    - "`filekv.Get` is `panic(\"implement me\")` today; devserver and dev-mode agent both use filekv, so GetConfig panics there until Task 3 lands"
    - "`agent/filekv` key validation is the only thing stopping `request.Path` from escaping `protoconfRoot` via `..` — Get must mirror Watch's `filepath.ToSlash(filepath.Clean(key))` check"
    - "`parseProtoconfValue` in `kv_agent_rollout_impl.go` returns `*datatypes.ProtoconfValue`, whose `.Value` is `*anypb.Any` — assignable to BOTH `ConfigUpdate.Value` types, so one helper serves both agents"
---

<objective>
Add a unary `GetConfig` RPC to `ProtoconfService` so a CLI, job, health check, or script can read one config without opening a server stream and tearing it down. Backport of upstream PR #496, GetConfig only.

Purpose: `SubscribeForConfig` is the only read path today. One-shot readers pay for stream setup, first-message extraction, and cancellation to get a value they will never watch.
Output: `GetConfig` on both proto service definitions, both agent implementations, the legacy bridge, and a working `filekv.Get` so the dev path does not panic.
</objective>

<execution_context>
@/Users/smintz/go/src/github.com/protoconf/protoconf/.claude/gsd-core/workflows/execute-plan.md
@/Users/smintz/go/src/github.com/protoconf/protoconf/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@pb/protoconf/v1/protoconf.proto
@agent/api/proto/v1/protoconf_service.proto
@agent/kv_agent_impl.go
@agent/kv_agent_rollout_impl.go
@agent/legacy.go
@agent/filekv/filekv.go
</context>

<scope_fence>
IN scope: the GetConfig RPC and only what it needs to work on every backend.

OUT of scope — do NOT port from PR #496, do NOT touch:
- TLS (already on main), `OTEL_SDK_DISABLED` toggles, noop stats handler, `conf.yaml`
- a `raw` / `google.api.HttpBody` field on `ConfigUpdate`, any googleapis import
- connectrpc vanguard-go, any REST transcoding, any `net/http` handler
- `GetJsonConfig` and `GetJsonConfigHttp` (that is the follow-up vanguard PR's job)
- `inserter/`, the store health probe (separate open PRs)
- `Test_cliCommand_Run/run_consul_server` — it hangs on this machine because a real Consul listens on 127.0.0.1:8500. Pre-existing and environmental. It is skipped in the verify command. Do not try to fix it.
</scope_fence>

<toolchain_setup>
`protoc` and both plugins are already installed and pinned, but `$(go env GOPATH)/bin` is NOT on PATH in a fresh shell. Every task that regenerates protos must first run:

```
export PATH="$PATH:$(go env GOPATH)/bin"
```

Installed: protoc 34.0, protoc-gen-go v1.36.11, protoc-gen-go-grpc 1.6.2.
</toolchain_setup>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Proto contract + codegen + GetConfig on ProtoconfKVAgent, proven end-to-end</name>
  <files>pb/protoconf/v1/protoconf.proto, agent/api/proto/v1/protoconf_service.proto, pb/protoconf/v1/protoconf.pb.go, pb/protoconf/v1/protoconf_grpc.pb.go, agent/api/proto/v1/protoconf_service.pb.go, agent/api/proto/v1/protoconf_service_grpc.pb.go, agent/dummykv/dummykv.go, agent/kv_agent_impl.go, agent/kv_agent_impl_test.go</files>

  <read_first>
    - `agent/kv_agent_impl.go` — `SubscribeForConfig` watches `path.Join(s.config.Prefix, request.Path)` with NO suffix. GetConfig here reads that exact key shape.
    - `agent/kv_agent_rollout_impl.go` — the `parseProtoconfValue(kvPair)` helper near the bottom. Reuse it; do not write a third base64+unmarshal copy.
    - `agent/kv_agent_impl_test.go` — `testServer(ctx, srv)` bufconn helper and `newAny(msg)`. Extend this file; do not create a new one.
    - `agent/dummykv/dummykv.go` — `Get` returns `fmt.Errorf("could not find key %s", key)` for a miss. Real backends (`kvtools/consul@v1.0.2` consul.go:109) return `store.ErrKeyNotFound`.
    - `agent/dummykv/dummykv_test.go` around line 66 — `TestGet_NotFound` asserts only `assert.Error`, so switching the sentinel does not break it.
  </read_first>

  <behavior>
    - GetConfig on a key that was Put returns a `ConfigUpdate` whose `Value` proto-equals what `SubscribeForConfig`'s first message carries for the same key.
    - GetConfig on an absent key returns a gRPC error with `codes.NotFound`.
    - GetConfig on a key holding non-base64 or non-proto bytes returns `codes.Internal`, not a panic.
  </behavior>

  <action>
Split this task into TWO atomic commits.

COMMIT A — proto + regeneration, mechanical only, no hand-written logic:

Add to `pb/protoconf/v1/protoconf.proto` (package `protoconf.v1`), next to `ConfigSubscriptionRequest`:

  message ConfigRequest with `string path = 1;` and `string channel = 2;`

and add to the `ProtoconfService` service block, after the existing SubscribeForConfig rpc, a unary rpc GetConfig taking ConfigRequest and returning ConfigUpdate.

Make the identical addition to `agent/api/proto/v1/protoconf_service.proto` (package `v1`, go_package `protoconfservice`). The two definitions are kept in lockstep because `agent/legacy.go` bridges them by proto-marshal round-trip. Do NOT change `ConfigUpdate` in either file.

Then regenerate with these two commands EXACTLY. Proto generation is not uniform here: `generate.go` deliberately excludes `pb/`, so the pb module is generated with `-I=pb`. Using `-I=.` for it renames the generated file symbol and breaks `server/server.go:285`.

    export PATH="$PATH:$(go env GOPATH)/bin"
    protoc -I=pb --go_out=pb --go_opt=paths=source_relative --go-grpc_out=pb --go-grpc_opt=paths=source_relative protoconf/v1/protoconf.proto
    protoc -I=. --go_out=. --go_opt=paths=source_relative --go-grpc_out=. --go-grpc_opt=paths=source_relative agent/api/proto/v1/protoconf_service.proto

These four `.pb.go` files are currently on protoc-gen-go v1.28.1 and will be rewritten by v1.36.11. Expect roughly 380 insertions and 670 deletions of purely mechanical churn. That is expected and acceptable — it was dry-run and the build and tests stay green. Do NOT hand-edit any `.pb.go` file, and do NOT try to suppress the churn. Confirm `grep -c File_protoconf_v1_protoconf_proto pb/protoconf/v1/protoconf.pb.go` is non-zero before committing.

Commit A message: `chore(proto): add GetConfig rpc and regenerate`

COMMIT B — dummykv sentinel + ProtoconfKVAgent.GetConfig + tests:

In `agent/dummykv/dummykv.go`, change `Get`'s miss return from the `fmt.Errorf` to `store.ErrKeyNotFound` so the in-repo fake honors the valkeyrie contract the real backends already follow. Drop the now-unused `fmt` import if nothing else uses it. This is what lets GetConfig distinguish a genuine miss from a store failure in tests.

In `agent/kv_agent_impl.go`, add a `GetConfig(ctx, *protoconf_pb.ConfigRequest) (*protoconf_pb.ConfigUpdate, error)` method on `*ProtoconfKVAgent`. Open a span named GetConfig off the package `tracer`, mirroring the span in SubscribeForConfig. Build the key with `path.Join(s.config.Prefix, request.Path)` — the SAME shape SubscribeForConfig watches, no suffix, so a caller's path means the same thing for Get as for Subscribe. Call `s.store.Get(ctx, key, &store.ReadOptions{})`. On error, map with `errors.Is(err, store.ErrKeyNotFound)` to a `status.Error(codes.NotFound, ...)` and anything else to `codes.Internal`; log via `s.Logger.With(slog.String("key", request.Path))` the way SubscribeForConfig does. Guard `kvPair == nil` separately — some backends return nil,nil — and return `codes.NotFound` there rather than dereferencing. Decode with the existing `parseProtoconfValue` helper from `kv_agent_rollout_impl.go` (same package, and its `*datatypes.ProtoconfValue` result has an `*anypb.Any` Value that assigns straight into `protoconf_pb.ConfigUpdate.Value`); on decode failure return `codes.Internal`. Return the ConfigUpdate carrying `result.Value`. Add the `codes`/`status` imports.

Leave the inline base64+unmarshal block inside SubscribeForConfig alone — it sends a partial `ConfigUpdate{Error: ...}` on each failure mode and keeps streaming, which `parseProtoconfValue` does not express. Add a one-line note in the summary that it stayed duplicated and why.

Write tests in `agent/kv_agent_impl_test.go` covering the three behaviors above, using the existing `testServer` bufconn helper and `dummykv`, in the style of `TestProtoconfKVAgent_SubscribeForConfig`. Assert NotFound with `status.Code(err)`.

Commit B message: `feat(agent): add GetConfig one-shot rpc to ProtoconfKVAgent`
  </action>

  <verify>
    <automated>go build ./... && go test ./agent/ ./agent/dummykv/ ./server/... -count=1 -run 'GetConfig|TestGet_NotFound|SubscribeForConfig'</automated>
  </verify>

  <done>Both `.proto` files declare ConfigRequest and the GetConfig rpc; the four `.pb.go` files are regenerated (never hand-edited) and `server/server.go` still builds against `File_protoconf_v1_protoconf_proto`; a bufconn client calling GetConfig against a dummykv-backed `ProtoconfKVAgent` gets the same Value SubscribeForConfig's first message carries, and a NotFound status for an absent key. Two separate commits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GetConfig on ProtoconfKVAgentRollout, production key shape</name>
  <files>agent/kv_agent_rollout_impl.go, agent/kv_agent_rollout_impl_test.go</files>

  <read_first>
    - `agent/kv_agent_rollout_impl.go` — `SubscribeForConfig` builds `kvStableConfigPath := path.Join(s.config.Prefix, request.Path, "config.data")`. That suffix matches what `inserter.XXXinsertVersion` (inserter/inserter.go:349) actually writes. This file already imports `codes` and `status`.
    - `agent/kv_agent_rollout_impl_test.go` — `newProtoconfKVAgentRollout` + `testServer` + `inserter.InsertConfig` setup. Extend this file.
  </read_first>

  <behavior>
    - After `inserter.InsertConfig(name, value, metadata)`, GetConfig on `name` returns a ConfigUpdate whose Value proto-equals the inserted value.
    - GetConfig on a path with no inserted config returns `codes.NotFound`.
  </behavior>

  <action>
Add a `GetConfig(ctx, *protoconfservice.ConfigRequest) (*protoconfservice.ConfigUpdate, error)` method on `*ProtoconfKVAgentRollout`, mirroring Task 1's method body but with the key built as `path.Join(s.config.Prefix, request.Path, "config.data")` — the same shape this agent's own SubscribeForConfig watches. The two agents' key layouts genuinely differ; do NOT unify them, and do not factor the two GetConfig bodies into a shared helper, because the differing key shape is the whole point.

Use `s.tracer` (the per-instance tracer field on this struct, not the package-level `tracer` var used in Task 1) for the span. Same error mapping: `store.ErrKeyNotFound` or a nil kvPair maps to `codes.NotFound`, other store errors to `codes.Internal`, decode failure to `codes.Internal`. Decode with the `parseProtoconfValue` helper already in this file.

Read the stable config path only. Do NOT walk `rollout.json` or resolve a matching stage — a one-shot read returns the default config. Mark that ceiling with a one-line comment prefixed `ponytail:` naming it and the upgrade path (resolve the stage the way SubscribeForConfig does, if a caller ever needs channel-aware one-shot reads). Note that `ConfigRequest.channel` is therefore carried but unused here.

Add a test in `agent/kv_agent_rollout_impl_test.go` in the style of the existing table test: stand up `newProtoconfKVAgentRollout` + `testServer` + a `dummykv` store + `inserter.NewProtoconfInserter(testdata.SmallTestDir(), kvStore)`, insert a config, then assert GetConfig returns it and that an uninserted path yields `codes.NotFound`. Keep it a plain sequential test — this one needs none of the goroutine/timing scaffolding the rollout subscribe test carries, because a unary call has no delivery race.
  </action>

  <verify>
    <automated>go test ./agent/ -count=1 -run 'Rollout.*GetConfig|GetConfig.*Rollout'</automated>
  </verify>

  <done>`ProtoconfKVAgentRollout.GetConfig` reads `prefix/path/config.data`, returns the inserted value, and returns NotFound for an uninserted path. The two agents' key shapes remain distinct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement filekv.Get and make Watch reuse it</name>
  <files>agent/filekv/filekv.go, agent/filekv/filekv_test.go</files>

  <read_first>
    - `agent/filekv/filekv.go` — `Get` is `panic("implement me")`. The body it needs already exists inline in `Watch`'s goroutine: `s.parser.ReadConfig(absPath, protoconfValue)`, `proto.Marshal`, base64-encode into a `store.KVPair{Key: key, Value: ...}`.
    - Same file, `Watch` — key validation `key != filepath.ToSlash(filepath.Clean(key)) || key == ""` and `absPath := filepath.Join(s.protoconfRoot, key+consts.CompiledConfigExtension)`. Get must mirror both.
    - `agent/filekv/filekv_test.go` — `newTestStore(t)` helper over `testdata.SmallTestDir()`. Extend this file.
    - `test/e2e_test.go:249-320` — the dev agent runs filekv with `Prefix: consts.CompiledConfigPath`, so a real key looks like `materialized_config/load_mutable_test`.
  </read_first>

  <behavior>
    - Get on a valid key whose materialized file exists returns a KVPair whose Value base64-decodes and proto-unmarshals into a ProtoconfValue.
    - Get on an empty key, or one that does not survive `filepath.Clean` (`../escape`), returns an error and never touches the filesystem.
    - Get on a valid key with no file on disk returns `store.ErrKeyNotFound`.
    - Watch still delivers the same KVPair it delivered before.
  </behavior>

  <action>
Implement `Get` on `Store` (keep the existing value receiver and signature — `Watch` is on `*Store` and can call it). Order: validate the key exactly as Watch does and return an error on a bad key BEFORE building any path, because this validation is the only thing keeping a caller-supplied `request.Path` from escaping `protoconfRoot`. Then build `absPath` the same way Watch does, `os.Stat` it and return `store.ErrKeyNotFound` if it is absent so the result composes with the rest of the valkeyrie contract, then read via `s.parser.ReadConfig`, `proto.Marshal`, and return the base64-encoded `store.KVPair`.

Then replace the read/marshal/encode block inside `Watch`'s goroutine with a call to `s.Get(ctx, key, opts)` so there is exactly one copy. On error, log and return as the current code does — collapse the two distinct slog.Error lines into one, since Get now carries the specific cause. Watch's own pre-goroutine validation and `absPath` construction stay where they are; Get revalidating is harmless.

Drop any import left unused after the extraction and add `os`.

Add tests to `agent/filekv/filekv_test.go` for the four behaviors. For the happy path use a key that resolves under `testdata.SmallTestDir()` — the dev agent's real prefix means `materialized_config/<name>`, and `utils/testdata/small/materialized_config/` holds `test`, `enum_test`, `load_mutable_test` and others. KNOWN RISK: `newTestStore` does not run module init/sync, so `parser.ReadConfig` may fail to resolve descriptors for configs depending on remote protos. If the first key you pick fails that way, try a config whose proto is local before doing anything else; only if none resolve, replicate the `lib.NewModuleService` + `Init` + `Sync` setup from `test/e2e_test.go:257-263` in a test helper. Do not stub the parser.
  </action>

  <verify>
    <automated>go test ./agent/filekv/... -count=1</automated>
  </verify>

  <done>`filekv.Get` returns a real KVPair for an existing materialized config, rejects a non-clean or empty key before touching disk, returns `store.ErrKeyNotFound` for a missing file, and `Watch` produces its KVPair by calling `Get`. `go test ./agent/filekv/...` passes with no panic.</done>
</task>

<task type="auto">
  <name>Task 4: Legacy bridge passthrough, registration check, full verification</name>
  <files>agent/legacy.go, test/e2e_test.go</files>

  <read_first>
    - `agent/legacy.go` — the `upgrade(in, out)` proto-marshal round-trip helper and the `SubscribeForConfig` passthrough that uses it. `legacyProtoconfServer` embeds `protoconfagent.UnimplementedProtoconfServiceServer`.
    - `agent/agent.go:157-158` — both `protoconf_pb.RegisterProtoconfServiceServer(rpcServer, agent)` and `protoconfagent.RegisterProtoconfServiceServer(rpcServer, legacy)` are already wired.
    - `test/e2e_test.go:307-316` — the dev agent's `SubscribeForConfig` on `load_mutable_test` and the `expected` anypb value it is checked against.
  </read_first>

  <action>
Add a `GetConfig(ctx, *protoconfagent.ConfigRequest) (*protoconfagent.ConfigUpdate, error)` method on `*legacyProtoconfServer`, following the existing `SubscribeForConfig` passthrough exactly: `upgrade()` the request into a `protoconf_pb.ConfigRequest`, call `s.stub.GetConfig(ctx, next)`, `upgrade()` the reply into a `protoconfagent.ConfigUpdate`, return it. It is unary, so there is no stream loop — return the error unwrapped on any step so the caller keeps the gRPC status code from the inner call. Do NOT port `GetJsonConfig` or `GetJsonConfigHttp` from PR #496.

Verify rather than assume the registration claim: confirm `agent/agent.go` needs no edit because `legacyProtoconfServer` embeds `UnimplementedProtoconfServiceServer` and both agent structs embed the ProtoconfServiceServer interface. Record in the summary the hazard this creates — those two structs embed the INTERFACE, not the Unimplemented struct, so a missing GetConfig compiles fine and nil-panics at call time; the Task 1 and Task 2 tests are the only thing catching that.

In `test/e2e_test.go`, inside the existing dev-client subtest region, add an assertion that `devAgentClient.GetConfig` on `load_mutable_test` returns the same `expected` anypb value the `devWatcher.Recv()` check uses. This is the one place that exercises GetConfig over a real filekv store end-to-end and proves Task 3 removed the panic on the dev path.

Then run the full verification below.
  </action>

  <verify>
    <automated>go build ./... && go test ./agent/... ./test/... ./server/... -count=1 -skip Test_cliCommand_Run</automated>
  </verify>

  <done>Legacy `protoconfservice` clients can call GetConfig and receive the upgraded ConfigUpdate; `agent/agent.go` is unmodified and that was confirmed by reading it, not assumed; the e2e test asserts GetConfig over filekv matches the streamed value; the full verify command passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gRPC client → agent | `ConfigRequest.path` is fully attacker-controlled and is joined onto a store key / filesystem path |
| agent → KV store | Store-returned bytes are base64+protobuf decoded into a ProtoconfValue |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-f8i-01 | Information disclosure | `ConfigRequest.path` → `filekv.Get` absPath | high | mitigate | Task 3: Get rejects any key where `key != filepath.ToSlash(filepath.Clean(key))` or `key == ""` BEFORE building absPath, mirroring Watch — this is what stops `../` escaping `protoconfRoot` |
| T-f8i-02 | Denial of service | nil `kvPair` from a backend returning `(nil, nil)` | medium | mitigate | Tasks 1 and 2: explicit `kvPair == nil` guard returning `codes.NotFound` instead of dereferencing |
| T-f8i-03 | Denial of service | malformed store value reaching `parseProtoconfValue` | medium | mitigate | Tasks 1 and 2: decode failure returns `codes.Internal`; the existing helper already returns errors rather than panicking, and Task 1 tests the non-base64 and non-proto cases |
| T-f8i-04 | Information disclosure | GetConfig exposes any readable config path unauthenticated | medium | accept | No new trust boundary — `SubscribeForConfig` already exposes exactly the same config set to the same unauthenticated callers. Authn/authz on the agent read path is out of scope for this backport |
| T-f8i-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs in this plan; protoc and both plugins are already installed and pinned in `$(go env GOPATH)/bin`, no new Go module dependency is added |
</threat_model>

<verification>
```
go build ./...
go test ./agent/... ./test/... ./server/... -count=1 -skip Test_cliCommand_Run
```

`Test_cliCommand_Run/run_consul_server` is skipped deliberately: it hangs on this dev machine because a real Consul is listening on 127.0.0.1:8500. Pre-existing and environmental — do not attempt to fix it.

Manual spot checks:
- `grep -c File_protoconf_v1_protoconf_proto pb/protoconf/v1/protoconf.pb.go` returns non-zero (proves the `-I=pb` generation was used and `server/server.go:285` still resolves).
- `git log --oneline -5` shows the regeneration as its own commit, separate from the hand-written code.
</verification>

<success_criteria>
- `GetConfig` exists on both `.proto` service definitions and all four `.pb.go` files are regenerated, never hand-edited.
- `GetConfig` is implemented on `ProtoconfKVAgent` (key `prefix/path`) and `ProtoconfKVAgentRollout` (key `prefix/path/config.data`), each matching its own SubscribeForConfig key shape.
- `filekv.Get` is implemented and `Watch` calls it — one copy of the read/marshal/encode logic.
- `legacyProtoconfServer.GetConfig` bridges to the new service via `upgrade()`.
- Missing config returns `codes.NotFound`; store failure and decode failure return `codes.Internal`; nothing nil-panics.
- `ConfigUpdate` is byte-for-byte unchanged in both protos; no googleapis import, no vanguard, no HTTP handler, no inserter or health-probe changes.
- The full verify command passes.
</success_criteria>

<output>
Create `.planning/quick/260902-f8i-add-getconfig-one-shot-rpc-to-protoconfs/260902-f8i-SUMMARY.md` when done
</output>
