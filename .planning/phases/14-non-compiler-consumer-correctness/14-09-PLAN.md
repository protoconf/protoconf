---
phase: 14-non-compiler-consumer-correctness
plan: 09
type: execute
wave: 5
depends_on: []
gap_closure: true
files_modified:
  - agent/filekv/filekv.go
  - agent/filekv/filekv_test.go
  - .planning/phases/14-non-compiler-consumer-correctness/14-02-PLAN.md
  - .planning/phases/14-non-compiler-consumer-correctness/14-07-PLAN.md
  - .planning/phases/14-non-compiler-consumer-correctness/deferred-items.md
autonomous: true
requirements: [CONS-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A caller-supplied key whose joined path resolves outside protoconfRoot is rejected by BOTH Get and Watch before any filesystem access — no os.Stat, no ReadConfig, no fsnotify registration on the escaped path."
    - "With a real, readable, schema-valid materialized config present outside protoconfRoot at the traversal target, Get returns an error and never returns that file's content; the test asserts on the absence of the content, not merely on the presence of an error."
    - "Watch is guarded by the same single validated helper Get uses, so the two call sites cannot drift apart (WR-03). The key-to-path join exists in exactly one place in agent/filekv/filekv.go."
    - "The empty key is still rejected, and a legitimate key naming a config that does not exist still returns store.ErrKeyNotFound — the fix does not reclassify a missing key as an invalid key."
    - "Every key shape the existing filekv suite already exercises keeps its current outcome: materialized_config/test and materialized_config/ondemand resolve, materialized_config/does_not_exist returns store.ErrKeyNotFound, and materialized_config/unresolvable still returns the Phase 13 D-02 diagnostic naming the symbol."
    - "CONS-03 idempotency edge: rejecting the same traversal key twice returns the same sentinel-matching error both times, and accepting the same valid key twice still returns byte-identical values (TestGetIsIdempotentForSameKey stays green) — validation is a pure function of the key and the immutable protoconfRoot."
    - "CONS-03 concurrency edge: the validation helper reads only s.protoconfRoot, which is written once in New and never mutated, so it introduces no lock, no shared mutable state, and no new race. The agent's existing -race proofs stay green unchanged."
    - statement: "SAFE-02 no-regression: concurrent Get and Watch against one long-lived store stay race-free after the guard is introduced, proven by re-running the phase's existing filekv and agent race suites rather than by a new concurrency test."
      verification: backstop
    - "The two threat-model rows that claimed this risk was already mitigated (14-02's T-14-03, 14-07's T-14-21) and the docstring of TestGetRejectsTraversalKey describe the shipped state truthfully after this plan."
  prohibitions:
    - statement: "A test cited anywhere as the control for a security threat must be demonstrated capable of failing against the unguarded code before the guard lands. Asserting only that some error occurred, when the fixture cannot produce the success case, is not a regression pin."
      status: active
      verification: "Task 1 runs the strengthened tests against the unfixed guard and requires the FAIL lines in the output; the SUMMARY records those lines verbatim. Task 3's threat-model rows cite only controls this plan established."
    - statement: "The key-to-path validation must not be duplicated per call site. A second hand-copied guard in a third Store method is prohibited; new methods that map a caller key to a filesystem path call the same helper."
      status: active
      verification: "Source assertion in Task 2: Get and Watch each obtain their absolute path from one helper call, and the filepath.Join of protoconfRoot with a caller key appears exactly once in agent/filekv/filekv.go."
  artifacts:
    - path: "agent/filekv/filekv.go"
      provides: "One validated key-to-path helper, resolveKeyPath, called by both Get and Watch; the ErrInvalidKey sentinel"
      contains: "resolveKeyPath"
    - path: "agent/filekv/filekv_test.go"
      provides: "A traversal proof for Get and for Watch, backed by a real outside-root file, asserting the content is never served"
      contains: "TestWatchRejectsTraversalKey"
  key_links:
    - from: "agent/filekv/filekv.go Get"
      to: "agent/filekv/filekv.go resolveKeyPath"
      via: "Get obtains absPath from the helper instead of validating and joining inline"
      pattern: "resolveKeyPath\\("
    - from: "agent/filekv/filekv.go Watch"
      to: "agent/filekv/filekv.go resolveKeyPath"
      via: "Watch obtains absPath from the same helper before addWatch registers an fsnotify watch"
      pattern: "resolveKeyPath\\("
---

<objective>
Close the single gap recorded in `14-VERIFICATION.md`: `agent/filekv`'s path-traversal guard
does not reject a leading `..` key segment, the test this phase shipped to "pin" that guard
cannot fail, and two threat-model rows plus one docstring assert a mitigation that does not
exist.

Purpose: the phase goal promises the four consumers resolve types "correctly and **safely**
... no silent wrong answers." A gRPC client choosing `ConfigSubscriptionRequest.Path` can
currently make the agent read and return a file from outside the config tree. The
vulnerability predates Phase 14; the false assurance about it does not.

Output: one shared validated key-to-path helper used by both `Get` and `Watch`, two traversal
tests that are demonstrated capable of failing against the unfixed code, and three corrected
documentation claims.
</objective>

<execution_context>
@/Users/smintz/go/src/github.com/protoconf/protoconf/.claude/gsd-core/workflows/execute-plan.md
@/Users/smintz/go/src/github.com/protoconf/protoconf/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-non-compiler-consumer-correctness/14-VERIFICATION.md
@.planning/phases/14-non-compiler-consumer-correctness/14-REVIEW.md
@agent/filekv/filekv.go
@agent/filekv/filekv_test.go
</context>

<gap_source>
`14-VERIFICATION.md`, `status: gaps_found`, 5/6 truths. One gap, three parts:

1. `agent/filekv/filekv.go` `Get` and `Watch` both guard with `key != filepath.ToSlash(filepath.Clean(key)) || key == ""`. `Clean` cannot collapse a leading `..` — it has no preceding component to cancel against — so `../secret/leak` is already its own clean form and passes.
2. `agent/filekv/filekv_test.go` `TestGetRejectsTraversalKey` asserts only `require.Error` against a fixture with no file at the traversed target, so it passes on `os.Stat`'s `ErrNotExist`, not on the guard.
3. `14-02-PLAN.md` T-14-03, `14-02-PLAN.md` `<prohibition_breadcrumbs>`, and `14-07-PLAN.md` T-14-21 each claim the risk is pinned/mitigated.

**Planner reproduction (this session, current HEAD, scratch test written and deleted):** a store rooted at `<tmp>/protoconfRoot` with `src/ondemand/v1/thing.proto`, and a schema-valid config at `<tmp>/secret/leak.materialized_JSON` carrying `x: "TOP-SECRET-OUTSIDE-ROOT"`, produced:

- `Get(ctx, "../secret/leak", nil)` → `err=<nil>`, and the decoded `ProtoconfValue` was `value:{type_url:"type.googleapis.com/ondemand.v1.Thing" value:"\n\x17TOP-SECRET-OUTSIDE-ROOT"}`.
- `Watch(ctx, "../secret/leak", nil)` → `err=<nil>` (the outside-root file is registered with fsnotify and its content streams through the goroutine's own `Get`).

Both halves are live. `Watch` is not protected by `Get`'s guard: `addWatch` runs on the escaped absolute path before the goroutine's first `Get`.
</gap_source>

<sibling_call_site_audit>
Performed as instructed before writing tasks — every place a caller-supplied key is joined onto a filesystem root.

| Site | Caller-supplied key? | Escapes? | Disposition |
|---|---|---|---|
| `agent/filekv/filekv.go:102` (`Get`) | yes, via `SubscribeForConfig`/`GetConfig` | **yes, confirmed live** | Fixed by this plan |
| `agent/filekv/filekv.go:142` (`Watch`) | yes, same surface | **yes, confirmed live** | Fixed by this plan |
| `agent/otelkv/otelkv.go:53,74` | passthrough | no join of its own; delegates to `next` | No change — guarding the wrapper would duplicate the fix |
| `agent/dummykv/dummykv.go:108,131` | in-memory map lookup | no filesystem path | No change |
| `agent/configmaps/configmaps.go:159,230` | key becomes a Kubernetes ConfigMap **name**, not a path | no filesystem join | No change |
| `inserter/inserter.go:234-350` | joins are over `i.Prefix` and operator-supplied CLI file arguments, not remote-caller input | out of the remote trust boundary | No change |
| `server/server.go:517` (`MutateConfig`) | **yes** — `in.Path` from a gRPC request | **yes** — `filepath.Clean(in.Path)` has the identical leading-`..` defect, and the joined path is an `os.MkdirAll` + `os.WriteFile` target | **NOT fixed here — see `<discovered_adjacent_finding>`** |

`agent/kv_agent_impl.go:72` and `:131` prepend `s.config.Prefix` with `path.Join`, which does not neutralise the escape: with an empty prefix the key survives verbatim, and with a non-empty prefix enough leading `..` segments still escape. The agent is a real reachability path, not a theoretical one.
</sibling_call_site_audit>

<discovered_adjacent_finding>
**`server/server.go:517` carries the same defect class and is deliberately NOT fixed by this plan.**

`filename := filepath.Join(s.protoconfRoot, consts.MutableConfigPath, filepath.Clean(in.Path)+consts.CompiledConfigExtension)` — a `MutateConfig` caller supplying `in.Path` with leading `..` segments escapes `protoconfRoot/mutable_config` and directs an `os.MkdirAll` plus `os.WriteFile` of attacker-supplied JSON at the resulting path.

Scope call, stated explicitly rather than absorbed silently, per the gap-closure contract:

- It is **separable**: a different package, a different function, no shared code path with `filekv`. Fixing it requires either a second local guard or a new shared package, and `filekv`'s helper is package-private by design.
- It is **not the recorded gap**. `14-VERIFICATION.md` records `agent/filekv` only.
- It is **not** WR-01 or WR-02 (the two pre-existing warnings the contract excludes); it is a new finding from this plan's sibling audit.

Task 3 records it in `deferred-items.md` and it appears in this plan's threat register as `T-14-25` with disposition `transfer`, making **no** mitigation claim. This plan does not assert that the mutation server's write path is safe.
</discovered_adjacent_finding>

<symlink_scope_decision>
Required explicit decision. **Out of scope, by choice.**

`filepath.Clean` and `filepath.Rel` are lexical, so a symlink *inside* `protoconfRoot` pointing outside it defeats the containment check. `filepath.EvalSymlinks` closes it at the cost of an extra syscall on every `Get`, on the agent's hot read path.

Declined because planting a symlink inside `protoconfRoot` requires write access to the config repository — a strictly larger compromise than the unauthenticated remote read this plan closes, and one that already permits writing arbitrary config content directly. Recorded as `T-14-24` (`accept`) rather than left undecided, and marked in-source with a `ponytail:` comment naming the ceiling and the upgrade path.
</symlink_scope_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove the traversal end-to-end — strengthen the Get test, add the Watch test, require both to FAIL against the unfixed guard</name>

  <read_first>
    - `agent/filekv/filekv_test.go` — in full. Note `newTestStore` (line 21) roots at the shared `testdata.SmallTestDir()`, which is unsuitable here; `newOnDemandFixture` (line 203) is the correct analog: it builds a `t.TempDir()` root with `src/ondemand/v1/thing.proto` and a `materialized_config/` holding a schema-valid `ondemand.materialized_JSON`. Reuse its fixture shape and its JSON literal form.
    - `agent/filekv/filekv.go` — `Get` (lines 94-122) and `Watch` (lines 135-175), specifically that `Watch` calls `addWatch(absPath, ...)` before the goroutine's first `Get`, so `Get`'s guard does not cover it.
    - `.planning/phases/14-non-compiler-consumer-correctness/14-VERIFICATION.md` — the `gaps:` frontmatter block, for the acceptance bar this test has to meet.
  </read_first>

  <files>agent/filekv/filekv_test.go</files>

  <behavior>
    - `Get` with a leading-`..` key that resolves to a real, readable, schema-valid materialized config outside the root: returns an error and a nil pair, and the outside file's distinctive payload string never appears in anything returned.
    - `Watch` with the same key: returns an error and a nil channel, before any fsnotify registration.
    - The empty key is still rejected by both.
    - A key naming a config that genuinely does not exist under the root still returns `store.ErrKeyNotFound`, not the invalid-key error.
  </behavior>

  <action>
Add a fixture helper `newTraversalFixture(t *testing.T) (root string, secretPayload string)` to `agent/filekv/filekv_test.go`, modelled on `newOnDemandFixture`. It builds `base := t.TempDir()`, then:
  - `root := filepath.Join(base, "protoconfRoot")` containing `src/ondemand/v1/thing.proto` (the same three-line `ondemand.v1.Thing` proto `newOnDemandFixture` writes) and an empty `materialized_config/` directory;
  - a sibling directory `filepath.Join(base, "secret")` — **outside** `root` — holding `leak.materialized_JSON`, whose JSON is `newOnDemandFixture`'s good-config shape with the `x` field set to the distinctive constant returned as `secretPayload`. Use a payload string that appears nowhere else in the repository so a substring assertion is unambiguous.
It returns `root` and `secretPayload`. The traversal key that reaches the planted file from `root` is `"../secret/leak"`.

Rewrite `TestGetRejectsTraversalKey` (currently lines 306-320) to build a store over `newTraversalFixture`'s root and assert, for key `"../secret/leak"`:
  - `require.Error(t, err)` and `require.Nil(t, pair)`;
  - **and** that the planted content is never served — assert the returned pair is nil *and* that `secretPayload` is absent from `err.Error()`. Because a non-nil pair is the leak vector, guard the content assertion so that if `pair` is somehow non-nil the test base64-decodes and `proto.Unmarshal`s it and fails on `assert.NotContains(t, protoconfValue.String(), secretPayload)`. This is the assertion the old test lacked: it fails on a *successful* traversal, not only on a missing file.
  - Keep the existing empty-key case (`s.Get(ctx, "", nil)` errors, nil pair).
  - Replace the docstring. It must no longer claim to pin an existing guard. State instead that it proves a caller-supplied key cannot read a file outside `protoconfRoot`, that the fixture plants a real readable file at the traversal target so the test fails on a successful read rather than passing on `os.Stat`'s `ErrNotExist`, and name `14-VERIFICATION.md` as the gap this closes.

Add `TestWatchRejectsTraversalKey` over the same fixture: `s.Watch(ctx, "../secret/leak", nil)` returns an error and a nil channel, and `s.Watch(ctx, "", nil)` returns an error. Its docstring states that `Watch` needs its own guard because `addWatch` registers the escaped absolute path with fsnotify before the goroutine's first `Get` runs, so `Get`'s validation does not cover it.

Add `TestGetMissingKeyIsNotAnInvalidKey` over the same fixture: `s.Get(ctx, "materialized_config/absent", nil)` returns an error satisfying `errors.Is(err, store.ErrKeyNotFound)`. This pins the error-class boundary Task 2 must not blur.

**Then run the three tests against the unfixed guard and capture the output.** All three traversal assertions must FAIL at this point — that failure is this task's deliverable. Record the verbatim `--- FAIL:` lines in the SUMMARY. Do not modify `agent/filekv/filekv.go` in this task.
  </action>

  <verify>
    <automated>go test ./agent/filekv/ -run 'TestGetRejectsTraversalKey|TestWatchRejectsTraversalKey' -v 2>&1 | tee /tmp/filekv-red.txt; grep -q -- "--- FAIL: TestGetRejectsTraversalKey" /tmp/filekv-red.txt &amp;&amp; grep -q -- "--- FAIL: TestWatchRejectsTraversalKey" /tmp/filekv-red.txt</automated>
    <fails_when>non-zero exit from the final grep pair — i.e. either "--- FAIL: TestGetRejectsTraversalKey" or "--- FAIL: TestWatchRejectsTraversalKey" is absent from the output. A green run here means the new tests are as unfalsifiable as the one they replace and the task is not done.</fails_when>
    <automated>go test ./agent/filekv/ -run TestGetMissingKeyIsNotAnInvalidKey -v</automated>
    <fails_when>non-zero exit, or "--- FAIL: TestGetMissingKeyIsNotAnInvalidKey" in the output — this one must pass already, since current behavior returns store.ErrKeyNotFound for a missing key</fails_when>
    <automated>go vet ./agent/filekv/...</automated>
    <fails_when>any output on stdout or stderr, or non-zero exit</fails_when>
  </verify>

  <acceptance_criteria>
    - `agent/filekv/filekv_test.go` defines `newTraversalFixture`, and the file it plants at `<base>/secret/leak.materialized_JSON` sits outside the `root` the store is constructed with.
    - Running `go test ./agent/filekv/ -run 'TestGetRejectsTraversalKey|TestWatchRejectsTraversalKey' -v` against unmodified `agent/filekv/filekv.go` emits both `--- FAIL:` lines; those two lines are quoted verbatim in the SUMMARY.
    - `agent/filekv/filekv.go` is not edited by this task — the RED evidence must come from the unguarded production code, so the whole diff of this task lives in the test file.
    - `TestGetRejectsTraversalKey`'s docstring contains no claim that it pins a pre-existing guard, and names `14-VERIFICATION.md`.
    - `TestWatchRejectsTraversalKey` exists and asserts a nil channel alongside the error.
    - `go test ./agent/filekv/ -run TestGetMissingKeyIsNotAnInvalidKey` exits 0.
  </acceptance_criteria>

  <done>Two traversal tests exist that demonstrably fail against the current code, with the failure output recorded. The error-class boundary test passes. Production code is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: One validated key-to-path helper, called by both Get and Watch</name>

  <read_first>
    - `agent/filekv/filekv.go` — in full. `Get` lines 94-122 and `Watch` lines 135-175 hold the two hand-copied guards and the two identical `filepath.Join(s.protoconfRoot, key+consts.CompiledConfigExtension)` calls. `New` (lines 60-86) sets `protoconfRoot` once from `filepath.Abs` and never mutates it.
    - `.planning/phases/14-non-compiler-consumer-correctness/14-REVIEW.md` — WR-03, which asks for exactly one shared helper so `Get` and `Watch` cannot drift.
    - `server/server.go` around lines 555-565 — the in-repo `errors.Join(SentinelErr, err)` convention this task follows (`ErrPreMutationScriptError` and siblings).
    - `agent/filekv/filekv_test.go` — the tests written in Task 1, which this task must turn green without disturbing any other test in the file.
  </read_first>

  <files>agent/filekv/filekv.go</files>

  <reversibility rating="reversible">Adds one unexported helper and one sentinel; reverting is deleting the helper and restoring two inline guards. No published contract changes — the `store.Store` interface, both signatures, and `store.ErrKeyNotFound` semantics are untouched.</reversibility>

  <action>
Declare a package-level sentinel next to the existing `StoreName` const: `var ErrInvalidKey = errors.New("invalid key")`, using `errors.New` per CLAUDE.md. Add `errors` and `strings` to the import block.

Add one unexported method on `*Store`:

  `func (s *Store) resolveKeyPath(key string) (string, error)`

It is the only place in the package that turns a caller key into a filesystem path. Its logic, in order:
  1. Reject `key == ""`, and reject any key that is not already equal to its own `filepath.ToSlash(filepath.Clean(key))` form. This preserves today's normalization rejection exactly — `./x`, `a//b`, and `a/../b` keep failing as they do now.
  2. Join: `absPath := filepath.Join(s.protoconfRoot, key+consts.CompiledConfigExtension)`.
  3. Containment: compute `filepath.Rel(s.protoconfRoot, absPath)` and reject when that returns an error, when the result is exactly `".."`, or when it starts with `".." + string(filepath.Separator)`. Use `strings.HasPrefix` for the last. This is the check that stands on its own — it rejects an escape regardless of whether step 1 is present, which is what stops the two conditions from being one condition restated.
  4. Return `absPath, nil`.

On every rejection return `"", errors.Join(ErrInvalidKey, fmt.Errorf("path=%s", key))` so callers can use `errors.Is` and the offending key still appears in the message.

Place a `ponytail:` comment on the containment step naming the ceiling and upgrade path recorded in this plan's `<symlink_scope_decision>`: the check is lexical, a symlink planted inside `protoconfRoot` defeats it, and `filepath.EvalSymlinks` is the upgrade at the cost of a stat per read. Do not write the phrase this plan's threat register uses for the accepted disposition; write the ceiling in your own words.

Rewrite `Get` to open with `absPath, err := s.resolveKeyPath(key)` and return `nil, err` on failure, deleting its inline guard and its inline `filepath.Join`. Everything after — the `os.Stat` block returning `store.ErrKeyNotFound`, `s.parser.ReadConfig`, `proto.Marshal`, the base64 `store.KVPair` — is unchanged.

Rewrite `Watch` the same way: `absPath, err := s.resolveKeyPath(key)` returning `nil, err`, then the existing `addWatch`/goroutine body unchanged. The helper call must precede `addWatch`, so no fsnotify registration ever happens on an escaped path. Leave the `readEvents` send-on-closed-channel ceiling (WR-01) and its existing `ponytail:` comment exactly as they are — that is out of this plan's scope.

Change nothing else in the file. Add no dependency.
  </action>

  <verify>
    <automated>go test -race -count=1 ./agent/filekv/ -run 'TestGetRejectsTraversalKey|TestWatchRejectsTraversalKey|TestGetMissingKeyIsNotAnInvalidKey' -v</automated>
    <fails_when>non-zero exit, or any "--- FAIL:" line in the output, or the string "WARNING: DATA RACE" in the output</fails_when>
    <automated>go test -race -count=1 ./agent/...</automated>
    <fails_when>non-zero exit, or "WARNING: DATA RACE" in the output, or any package line that is not "ok" or "no test files" — in particular TestGet_ValidKey, TestGetIsIdempotentForSameKey, TestGetResolvesTypeAbsentFromConstructionSnapshot, TestGetUnresolvableTypeReturnsDiagnostic, TestWatch_ContextCancellation and the filekv race tests must all still pass</fails_when>
    <automated>bash -c 'test "$(grep -v "^[[:space:]]*//" agent/filekv/filekv.go | grep -o "resolveKeyPath" | wc -l | tr -d " ")" -ge 3'</automated>
    <fails_when>non-zero exit — fewer than 3 non-comment matches, meaning the helper is declared but one of Get/Watch does not call it (expected: 1 declaration + 1 call in Get + 1 call in Watch)</fails_when>
    <automated>bash -c 'test "$(grep -o "filepath.Join(s.protoconfRoot" agent/filekv/filekv.go | wc -l | tr -d " ")" -eq 1'</automated>
    <fails_when>non-zero exit — the protoconfRoot join appears other than exactly once (inside the helper), which is the guard drift WR-03 warns about</fails_when>
    <automated>go vet ./agent/filekv/...</automated>
    <fails_when>any output on stdout or stderr, or non-zero exit</fails_when>
    <automated>git diff --exit-code go.mod go.sum</automated>
    <fails_when>non-zero exit — a dependency was added; this fix is stdlib-only</fails_when>
    <automated>go test -race -timeout 300s -skip 'Test_cliCommand_Run' ./...</automated>
    <fails_when>non-zero exit, or "WARNING: DATA RACE" in the output, or any "FAIL" package line</fails_when>
  </verify>

  <acceptance_criteria>
    - `agent/filekv/filekv.go` declares `ErrInvalidKey` via `errors.New` and one `resolveKeyPath` method on `*Store`.
    - `Get` and `Watch` each obtain `absPath` from a single `resolveKeyPath` call and contain no inline key validation and no inline root join.
    - In `Watch`, the `resolveKeyPath` call appears before the `addWatch` call.
    - The three tests from Task 1 that failed now pass, and the SUMMARY records the before/after transition for each named test.
    - `go test -race -count=1 ./agent/...` is green with zero `WARNING: DATA RACE`.
    - `go test -race -timeout 300s -skip 'Test_cliCommand_Run' ./...` is green — the eight executed plans' tests are the regression suite and stay passing.
    - `git diff --exit-code go.mod go.sum` exits 0.
    - The containment step carries a `ponytail:` comment naming the lexical-check ceiling and `filepath.EvalSymlinks` as the upgrade path.
  </acceptance_criteria>

  <done>A caller-supplied key that resolves outside `protoconfRoot` is rejected by both `Get` and `Watch` before any filesystem access, through one shared helper; the empty key and `store.ErrKeyNotFound` semantics are unchanged; the full bounded repo suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the three false mitigation claims and record the adjacent finding</name>

  <read_first>
    - `.planning/phases/14-non-compiler-consumer-correctness/14-02-PLAN.md` — the `<threat_model>` row `T-14-03` (around line 191) and the `<prohibition_breadcrumbs>` block (around lines 197-200).
    - `.planning/phases/14-non-compiler-consumer-correctness/14-07-PLAN.md` — the `<threat_model>` row `T-14-21` (around line 214).
    - `.planning/phases/14-non-compiler-consumer-correctness/deferred-items.md` — the existing entry format (`- <what>` / `status: open` / `**What:** <detail>`), to be matched exactly.
    - `.planning/phases/14-non-compiler-consumer-correctness/14-VERIFICATION.md` — the `gaps:` block, so each correction cites the right source.
    - `server/server.go` lines 512-522 — confirm the `MutateConfig` path join first-hand before writing it into `deferred-items.md`; do not transcribe the line number from this plan.
  </read_first>

  <files>.planning/phases/14-non-compiler-consumer-correctness/14-02-PLAN.md, .planning/phases/14-non-compiler-consumer-correctness/14-07-PLAN.md, .planning/phases/14-non-compiler-consumer-correctness/deferred-items.md</files>

  <action>
**Scoped edits only.** These two plan files are executed and have SUMMARYs. Use `Edit` with a scoped replacement on the exact passages named below. Do not renumber, supersede, restructure, or otherwise alter either file — no frontmatter changes, no task changes, no other threat-model rows.

In `14-02-PLAN.md`, rewrite only the Mitigation Plan cell of row `T-14-03`. The corrected cell states: the guard in place when 14-02 shipped rejected only a key that differed from its own cleaned form, which does not reject a leading `..` segment; `TestGetRejectsTraversalKey` as written by 14-02 could not detect that, because its fixture had no file at the traversal target; the risk was live until plan 14-09, which added the containment check and a test proven capable of failing. Change the Disposition cell for that row from its current value to `transferred → 14-09`. Leave the Threat ID, Category, Component and Severity cells as they are.

In `14-02-PLAN.md`, amend the `<prohibition_breadcrumbs>` block. Keep the canon-referral line intact — treating path traversal as canon security rather than minting a bespoke prohibition was the correct call and is not what failed. Append one line recording that the accompanying `T-14-03` mitigation claim was false and is corrected above, and that the fix landed in 14-09.

In `14-07-PLAN.md`, rewrite only the Mitigation Plan cell of row `T-14-21`. The corrected cell states: 14-07 correctly left both guards untouched, but the cited pin did not hold for a leading `..` segment in either `Get` or `Watch`; the containment check and the `Watch`-side test arrived in 14-09. Change that row's Disposition cell to `transferred → 14-09`. Leave the other cells as they are.

In `deferred-items.md`, append one entry in the file's existing format for the `server/server.go` `MutateConfig` finding. It must state: the site, that `filepath.Clean` on a caller-supplied `in.Path` has the same leading-`..` defect `filekv` had, that the joined path is an `os.MkdirAll` plus `os.WriteFile` target so this is a write rather than a read, that it was discovered by 14-09's sibling call-site audit and is deliberately out of that plan's scope because `14-VERIFICATION.md` records `agent/filekv` only and the site is in a different package with no shared code path, and that `/gsd-secure-phase` is the venue. Include the git-blame date of the line so a future reader can tell it predates this milestone. Set `status: open`.
  </action>

  <verify>
    <automated>bash -c 'grep -c "14-09" .planning/phases/14-non-compiler-consumer-correctness/14-02-PLAN.md .planning/phases/14-non-compiler-consumer-correctness/14-07-PLAN.md .planning/phases/14-non-compiler-consumer-correctness/deferred-items.md'</automated>
    <fails_when>non-zero exit, or any of the three lines reports a count of 0 — a file was not updated</fails_when>
    <automated>bash -c 'node .claude/gsd-core/bin/gsd-tools.cjs query verify.plan-structure .planning/phases/14-non-compiler-consumer-correctness/14-02-PLAN.md | grep -q "\"task_count\": 2"'</automated>
    <fails_when>non-zero exit — 14-02's task count is no longer 2, meaning the scoped edit restructured an executed plan instead of rewriting one table cell and appending one breadcrumb line</fails_when>
    <automated>bash -c 'node .claude/gsd-core/bin/gsd-tools.cjs query verify.plan-structure .planning/phases/14-non-compiler-consumer-correctness/14-07-PLAN.md | grep -q "\"task_count\": 2"'</automated>
    <fails_when>non-zero exit — 14-07's task count is no longer 2, meaning the scoped edit restructured an executed plan</fails_when>
    <automated>bash -c 'OUT=$(git diff HEAD --numstat -- .planning/phases/14-non-compiler-consumer-correctness/14-02-PLAN.md .planning/phases/14-non-compiler-consumer-correctness/14-07-PLAN.md) || exit 2; printf "%s\n" "$OUT" | awk "{if (\$1>6||\$2>6) exit 1}"'</automated>
    <fails_when>exit 1 — one of the two executed plans shows more than 6 added or more than 6 deleted lines, i.e. the edit grew past the two threat-model cells and one appended breadcrumb line. Exit 2 instead means the `git diff` itself failed (bad path, detached state) and the budget was never actually measured — `git` is deliberately outside the pipeline and its status captured first, so a broken command cannot read as clean the way it would if `awk` were simply handed empty input. Run this BEFORE committing the task: `git diff HEAD` covers staged and unstaged changes, so it reports nothing once the edit is committed. The three `grep -c "14-09"` gates above independently prove the edits exist, so an empty numstat here cannot pass alongside them.</fails_when>
    <automated>bash -c 'node .claude/gsd-core/bin/gsd-tools.cjs query frontmatter.validate .planning/phases/14-non-compiler-consumer-correctness/14-02-PLAN.md --schema plan | grep -q "\"valid\": *true"'</automated>
    <fails_when>non-zero exit — the scoped edit broke 14-02's frontmatter</fails_when>
    <automated>bash -c 'node .claude/gsd-core/bin/gsd-tools.cjs query frontmatter.validate .planning/phases/14-non-compiler-consumer-correctness/14-07-PLAN.md --schema plan | grep -q "\"valid\": *true"'</automated>
    <fails_when>non-zero exit — the scoped edit broke 14-07's frontmatter</fails_when>
    <automated>bash -c 'grep -c "server/server.go" .planning/phases/14-non-compiler-consumer-correctness/deferred-items.md'</automated>
    <fails_when>non-zero exit, or a count of 0 — the adjacent finding was not recorded</fails_when>
  </verify>

  <acceptance_criteria>
    - `14-02-PLAN.md`'s `T-14-03` row no longer asserts that `TestGetRejectsTraversalKey` pins the guard; its Disposition cell reads `transferred → 14-09`.
    - `14-02-PLAN.md`'s `<prohibition_breadcrumbs>` still contains its original canon-referral line, plus one appended line naming the false claim and its correction.
    - `14-07-PLAN.md`'s `T-14-21` row no longer asserts that 14-02's test pins the `Get` half; its Disposition cell reads `transferred → 14-09`.
    - `git diff --numstat` shows at most 6 added and at most 6 deleted lines for each of the two plan files.
    - Both plan files still validate against the `plan` schema.
    - `deferred-items.md` gains one entry naming `server/server.go`'s `MutateConfig` path join, `status: open`, carrying the blame date read from `git blame` rather than copied from this plan.
    - No file other than the three listed in `<files>` is modified by this task.
  </acceptance_criteria>

  <done>All three passages that claimed this risk was mitigated now describe the shipped state truthfully, the executed plans are otherwise byte-unchanged, and the adjacent `server/server.go` finding is recorded rather than silently absorbed or silently dropped.</done>
</task>

</tasks>

<threat_model>
**ASVS level: 1. Blocking severity: high.**

Every row below states what is true after this plan's own tasks land. No row cites a test as a control unless a task in this plan establishes that the test fails against the unguarded code.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subscribed gRPC client → `filekv.Store.Get` key | `ConfigSubscriptionRequest.Path` / `ConfigRequest.Path` reaches `Get` via `agent/kv_agent_impl.go`, prefixed with `path.Join` but not neutralised by it, and is joined onto `protoconfRoot` to form a filesystem read path |
| subscribed gRPC client → `filekv.Store.Watch` key | the same untrusted key is joined onto `protoconfRoot` and registered with fsnotify *before* the goroutine's first `Get` |
| `protoconfRoot` filesystem contents → containment check | a symlink inside the config tree is followed by the OS but is invisible to a lexical containment check |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-23 | Information Disclosure | `Store.Get` and `Store.Watch` key-to-path join, `agent/filekv/filekv.go` | high | mitigate | Task 2's `resolveKeyPath` rejects any key whose joined path leaves `protoconfRoot`, via a `filepath.Rel` containment check that holds independently of the pre-existing normalization check, and both methods route through it before any filesystem access. The control's test is established as falsifiable by Task 1, which runs the strengthened `Get` and `Watch` tests against the unguarded code and requires both `--- FAIL:` lines before Task 2 may begin — this is the specific discipline `14-VERIFICATION.md` found missing from 14-02. |
| T-14-24 | Tampering | symlink inside `protoconfRoot` resolving outside it | low | accept | The containment check is lexical, so a symlink planted inside the config tree defeats it. Accepted: planting one requires write access to the config repository, which already permits writing arbitrary config content directly — a strictly larger compromise than the unauthenticated remote read T-14-23 closes. `filepath.EvalSymlinks` would close it at the cost of a stat syscall on the agent's hot read path; declined deliberately and marked in-source with a `ponytail:` comment. See `<symlink_scope_decision>`. |
| T-14-25 | Information Disclosure / Tampering | `ProtoconfMutationServer.MutateConfig` path join, `server/server.go` | high | transfer | **This plan does not fix this and makes no mitigation claim about it.** A caller-supplied `in.Path` with leading `..` segments escapes `protoconfRoot/mutable_config` and directs an `os.MkdirAll` plus `os.WriteFile` of caller-supplied JSON. Same defect class as T-14-23, different package, no shared code path; outside the gap `14-VERIFICATION.md` records. Transferred to `/gsd-secure-phase` and recorded in `deferred-items.md` by Task 3. Listed here rather than omitted precisely because omitting an inconvenient finding is the failure mode this plan exists to correct. |
| T-14-26 | Repudiation | a future `Store` method mapping a caller key to a path without the guard | medium | mitigate | Task 2 leaves exactly one `filepath.Join` of `protoconfRoot` with a caller key in the package, asserted by a source gate in Task 2's `<verify>`, so a second call site cannot silently reintroduce the defect by copy-paste (WR-03's drift concern). |
| T-14-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero packages added — the fix is `path/filepath`, `strings` and `errors` from the standard library. `git diff --exit-code go.mod go.sum` is a gate in Task 2's `<verify>`. RESEARCH.md's Package Legitimacy Audit records "No new packages" for this phase and that remains true. |
</threat_model>

<prohibition_breadcrumbs>
- Path traversal on a caller-supplied `Store` key is **canon** security (OWASP), recorded above as `T-14-23`; per the canon-referral rule it is not minted as a bespoke prohibition. 14-02 made this same call and it was correct — what failed there was the separate, untrue mitigation claim, not the referral.
- Two **non-canon** prohibitions specific to this gap ARE minted, under `must_haves.prohibitions`: a test cited as a security control must be shown capable of failing, and the key-to-path validation must not be duplicated per call site.
</prohibition_breadcrumbs>

<edge_coverage_notes>
Spec-less probe fallback (no SPEC.md for this phase). Rows relevant to this gap, resolved per §A:

| Requirement | Category | Status | Verification | Where |
|---|---|---|---|---|
| CONS-03 | idempotency | resolved | explicit | `must_haves.truths` — repeated rejection is stable, repeated acceptance is byte-identical |
| CONS-03 | concurrency | resolved | explicit | `must_haves.truths` — the helper reads only the immutable `protoconfRoot`, adding no lock and no shared state |
| SAFE-02 | concurrency | resolved | backstop | `must_haves.truths` structured marker — no-regression, proven by re-running the existing race suites |

Rows for CONS-02 (`unclassified`), CONS-04 (`adjacency`, `empty`, `ordering`, `concurrency`) and SAFE-03 (`adjacency`, `empty`, `ordering`) are **out of scope for this gap-closure plan** — they belong to requirements already covered by the executed plans 14-01 and 14-04 through 14-08. They are noted here rather than dropped. None was auto-dismissed.
</edge_coverage_notes>

<artifacts_this_phase_produces>
New symbols and files this plan creates. The source-grounding pass should exclude these from drift verification — none exists at HEAD.

**`agent/filekv/filekv.go`**
- `ErrInvalidKey` — package-level sentinel (`errors.New`)
- `(*Store).resolveKeyPath(key string) (string, error)` — the single validated key-to-path helper

**`agent/filekv/filekv_test.go`**
- `newTraversalFixture(t *testing.T) (root string, secretPayload string)` — test helper
- `TestWatchRejectsTraversalKey` — new test
- `TestGetMissingKeyIsNotAnInvalidKey` — new test
- `TestGetRejectsTraversalKey` — **not new**; an existing test at lines 306-320 whose body and docstring are rewritten

**Planning artifacts**
- `.planning/phases/14-non-compiler-consumer-correctness/14-09-SUMMARY.md` — new
- one new entry in `deferred-items.md` (existing file)

No new packages, no new files, no new dependencies.
</artifacts_this_phase_produces>

<verification>
- `go test -race -count=1 ./agent/...` is green with zero `WARNING: DATA RACE`
- `go test -race -timeout 300s -skip 'Test_cliCommand_Run' ./...` is green — the bounded form, because a bare `go test ./...` stalls ~10 minutes on a Consul-dependent test
- `go vet ./agent/filekv/...` is silent
- `git diff --exit-code go.mod go.sum` is clean
- The SUMMARY quotes, verbatim, the `--- FAIL:` lines Task 1 produced against the unfixed guard and the corresponding `--- PASS:` lines after Task 2 — the before/after pair is the evidence that this plan's control is real
- `agent/filekv/filekv.go` contains exactly one join of `protoconfRoot` with a caller key
</verification>

<success_criteria>
The phase goal's safety clause holds for the agent's filekv store: a caller-supplied key
cannot read or watch a file outside `protoconfRoot`, through either `Get` or `Watch`. The
test that proves it has been observed failing against the unguarded code. The three
passages that previously claimed this was already handled now say something true, and the
one sibling call site carrying the same defect is recorded with its venue rather than
quietly inherited.
</success_criteria>

<output>
Create `.planning/phases/14-non-compiler-consumer-correctness/14-09-SUMMARY.md` when done.
</output>
