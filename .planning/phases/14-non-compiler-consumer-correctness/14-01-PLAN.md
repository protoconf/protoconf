---
phase: 14-non-compiler-consumer-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inserter/inserter.go
  - inserter/lazy_resolution_test.go
autonomous: true
requirements: [CONS-02]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The inserter constructs its module service lazily and still writes a config.json whose nested google.protobuf.Any renders the wrapped message's fields (D-01, D-03)."
    - "A materialized config whose message type is absent from the inserter's construction-time snapshot is still inserted correctly, because resolution falls through the shared tiered resolver (D-03)."
    - "The inserter CLI still exits 0 when one file among several fails to insert, logging that failure per file — its current skip-and-log-continue shape is unchanged by the resolver rewiring (RESEARCH correction to CONTEXT.md's discretion premise)."
    - "An unresolvable type URL on the inserter's write path returns the Phase 13 D-02 diagnostic error naming the symbol, the import roots and the index state — it is never swallowed into a successful insert."
  prohibitions:
    - statement: "MUST NOT emit a config.json that silently omits or empties an unresolvable nested Any's content — a partial render must surface as an error, never as truncated output an operator would read as complete."
      category: safety
      status: resolved
      verification: judgment
  artifacts:
    - path: "inserter/inserter.go"
      provides: "Lazy module-service construction plus tiered type-URL resolution on the config.json write path"
      contains: "lib.NewLazyModuleService"
    - path: "inserter/lazy_resolution_test.go"
      provides: "CONS-02 on-demand resolution proof and CLI exit-code shape guard"
      contains: "TestInserterResolvesTypeAbsentFromConstructionSnapshot"
  key_links:
    - from: "inserter/inserter.go"
      to: "compiler/lib/parser/parser.go"
      via: "config.json marshal and FindMessageByURL both route through i.parser.TypeResolver"
      pattern: "i\\.parser\\.TypeResolver"
---

<objective>
Prove the whole Phase 14 architecture on one real consumer, end to end: the inserter
constructs lazily, resolves a materialized config's type on demand through the shared
tiered resolver, renders its nested `Any` content, and writes both `config.data` and
`config.json` to the KV store.

Purpose: this is the tracer slice. Every other plan in this phase is the same two moves
(`D-01` construction flip, `D-03` resolver rewiring) applied to another consumer. Proving
it once, on the consumer with the strongest pre-existing regression test, catches an
architectural dead end after one commit instead of after five.
Output: `inserter/inserter.go` on the lazy path with both resolver call sites rewired, plus
a dedicated test that the type genuinely was not in the construction snapshot.
</objective>

<execution_context>
@/Users/smintz/go/src/github.com/protoconf/protoconf/.claude/gsd-core/workflows/execute-plan.md
@/Users/smintz/go/src/github.com/protoconf/protoconf/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-non-compiler-consumer-correctness/14-CONTEXT.md
@.planning/phases/14-non-compiler-consumer-correctness/14-RESEARCH.md
@.planning/phases/14-non-compiler-consumer-correctness/14-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — the inserter constructs lazily and inserts a nested-Any config through the tiered resolver</name>
  <reversibility rating="costly">Undo means reverting this construction site plus both resolver call sites; no published contract or wire format changes, so it is not one-way (rating carried forward from CONTEXT.md D-01).</reversibility>
  <files>inserter/inserter.go</files>
  <read_first>
    - `inserter/inserter.go` — the file being modified: `NewProtoconfInserter` around line 192, and the `config.json` write block around lines 366-385
    - `inserter/inserter_test.go` — `TestProtoconfInserter_InsertConfig_AnyResolution` (around line 80) is the existing regression test this task must keep green; it is the tracer's end-to-end check
    - `compiler/lib/parser/parser.go` lines 24-121 — `Parser.TypeResolver` and `RegistryTypeResolver`'s tier chain, so the executor sees that `*RegistryTypeResolver` satisfies both `protoregistry.MessageTypeResolver` and `protojson.MarshalOptions.Resolver` and the swap needs no signature change
    - `compiler/lib/module_service.go` lines 44-70 and 445-462 — `NewLazyModuleService` and the lazy branch of `GetProtoRegistry` that sets `ImportPaths` and `CacheDir`, which is what arms the Phase 13 scan and symbol-index tiers
    - `.planning/phases/14-non-compiler-consumer-correctness/14-PATTERNS.md` § `inserter/inserter.go` — the verified analog with the exact before/after lines
  </read_first>
  <action>
Apply D-01 and D-03 to `inserter/inserter.go` and nothing else in that file.

D-01 — in `NewProtoconfInserter`, change the module-service constructor call from
`lib.NewModuleService(protoconfRoot)` to `lib.NewLazyModuleService(protoconfRoot)`. Leave
the surrounding lines untouched: the `logger.Error` branch, the `return nil` on failure,
and the following `ms.LoadFromLockFile()` call all stay exactly as they are.

D-03 — in the `config.json` write block of `InsertConfigFile`'s version-writing helper,
both resolver references on the receiver's parser must become `i.parser.TypeResolver`:
(a) the `FindMessageByURL(protoconfValue.Value.TypeUrl)` receiver, and (b) the `Resolver:`
field of the `protojson.MarshalOptions{Multiline: true, ...}` literal that marshals the
`dynamicpb` message into `data`. Keep `Multiline: true`. Keep the surrounding
`dynamicpb.NewMessage(mt.Descriptor())` / `UnmarshalTo` / `errors.Join` error wrapping
unchanged. <!-- planner-discipline-allow: LocalResolver -->

The construction-time snapshot field on `Parser` (named in `14-PATTERNS.md`) stays declared
and stays used by `ExtensionResolver` consumers elsewhere in the repo; this task only stops
the inserter from reading it for type-URL and `Any` resolution.

Do NOT touch the goroutine loop in `cliCommand.Run` (around lines 180-198) that fans each
file out and logs per-file failures. That loop's skip-and-log-continue shape and its
`return 0` are the inserter's current CLI contract; RESEARCH.md verified this at HEAD and
CONTEXT.md's discretion constraint forbids changing a CLI's exit-code behavior as a side
effect of the resolver rewiring.
  </action>
  <acceptance_criteria>
    - Source: `grep -c 'lib.NewLazyModuleService(protoconfRoot)' inserter/inserter.go` is 1
    - Source: `grep -c 'i.parser.TypeResolver' inserter/inserter.go` is 2
    - Source: `grep -c 'return 0' inserter/inserter.go` is unchanged from HEAD — the CLI's exit-code shape was not edited
    - Behavior: inserting `field_type_any_test.materialized_JSON` against `testdata.SmallTestDir()` writes a `field_type_any_test/config.json` value containing the literals `type.googleapis.com/test.v1.TestMessage`, `test_any`, `test_any_repeated` and `test_any_map` — the nested `Any` still renders under lazy construction
    - Test: the command in `<verify>` exits 0 and prints `--- PASS: TestProtoconfInserter_InsertConfig_AnyResolution`
  </acceptance_criteria>
  <verify>
    <automated>go test -race ./inserter/... -run 'TestProtoconfInserter_InsertConfig$|TestProtoconfInserter_InsertConfig_AnyResolution|Test_cliCommand_Run' -v</automated>
    <fails_when>non-zero exit, or `--- FAIL:` appears in the output, or the output contains `no tests to run` (go test -run exits 0 when the filter matches nothing), or `=== RUN   TestProtoconfInserter_InsertConfig_AnyResolution` is absent from the output</fails_when>
    <automated>go build ./... &amp;&amp; go vet ./inserter/...</automated>
    <fails_when>non-zero exit, or any line of output at all (both commands are silent on success)</fails_when>
  </verify>
  <done>The inserter is on the lazy construction path, both of its type-URL/`Any` resolution sites read the tiered resolver, and the pre-existing nested-`Any` regression test is green under `-race`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CONS-02 proof — the resolved type was genuinely absent from the construction snapshot</name>
  <files>inserter/lazy_resolution_test.go</files>
  <read_first>
    - `inserter/inserter.go` — as modified by Task 1; the test reaches `i.parser` (unexported field, same package)
    - `inserter/inserter_test.go` — `TestProtoconfInserter_InsertConfig_AnyResolution` for the `dummykv` + `testdata.SmallTestDir()` + `kvStore.Get` assertion idiom to copy
    - `compiler/lib/parser/parser.go` lines 24-121 — both exported resolver fields on `Parser`, so the test can contrast them
    - `utils/testdata/embed.go` `SmallTestDir` (around line 109) — returns a fresh temp copy of `utils/testdata/small` per call, including `src/test.proto` and `materialized_config/field_type_any_test.materialized_JSON`
  </read_first>
  <behavior>
    - Test 1 (`TestInserterResolvesTypeAbsentFromConstructionSnapshot`): after `NewProtoconfInserter(testdata.SmallTestDir(), dummykv)`, a direct lookup of `type.googleapis.com/test.v1.TestMessage` on the parser's construction-time snapshot returns an error satisfying `errors.Is(err, protoregistry.NotFound)`, while the same lookup on `i.parser.TypeResolver` returns a non-nil `protoreflect.MessageType` and no error. This is what makes CONS-02 an on-demand claim rather than a tautology.
    - Test 2 (`TestInserterUnresolvableTypeReturnsDiagnostic`): inserting a materialized config whose `@type` names a symbol declared nowhere under `src/` returns a non-nil error whose message contains `not found`, the queried symbol name, and `symbol index:` — the Phase 13 D-02 diagnostic, surfaced rather than swallowed.
    - Test 3 (`TestInserterCLIExitsZeroOnPerFileFailure`): running `cliCommand.Run` over one good and one nonexistent config file returns exit code 0 — the per-file skip-and-log-continue contract is intact.
  </behavior>
  <action>
Create `inserter/lazy_resolution_test.go` in package `inserter` with the three tests named
in `<behavior>`.

For Test 1, build the inserter exactly as `TestProtoconfInserter_InsertConfig_AnyResolution`
does (a `dummykv.New` store and `testdata.SmallTestDir()`), then call `InsertConfigFile`
for `field_type_any_test.materialized_JSON` first so the lazy tiers have actually run, and
assert the snapshot-vs-tiered contrast described in `<behavior>`. Take the snapshot lookup
BEFORE the insert as well, and assert it is `protoregistry.NotFound` at that point too, so
the test cannot pass merely because the snapshot object is shared and later grows —
`*protoregistry.Types` is written only at construction, so both readings must stay
NotFound. <!-- planner-discipline-allow: LocalResolver -->

For Test 2, write a purpose-built materialized config into the temp root's
`materialized_config/` directory before constructing the inserter: valid `ProtoconfValue`
JSON whose `value` carries `"@type": "type.googleapis.com/absent.v1.NoSuchMessage"`. Assert
`InsertConfigFile` returns a non-nil error and use `assert.Contains` on `err.Error()` for
the three substrings in `<behavior>`. Do not assert on the full error string — its import
roots segment embeds a temp path.

For Test 3, follow `Test_cliCommand_Run`'s existing construction of the command under test
in `inserter/inserter_test.go`, pass the protoconf root plus two config arguments (one that
exists in the fixture, one that does not), and `require.Equal(t, 0, code)`.

Use `github.com/stretchr/testify/require` for preconditions and `assert` for content
checks, matching this package's existing convention.
  </action>
  <acceptance_criteria>
    - Test: `go test -race ./inserter/... -run 'TestInserterResolvesTypeAbsentFromConstructionSnapshot|TestInserterUnresolvableTypeReturnsDiagnostic|TestInserterCLIExitsZeroOnPerFileFailure' -v` exits 0 and prints `--- PASS:` for all three names
    - Source: `inserter/lazy_resolution_test.go` exists and declares `package inserter`
    - Behavior: Test 1 fails if the construction-snapshot lookup ever succeeds — the assertion is `errors.Is(err, protoregistry.NotFound)`, not merely `err != nil`
    - Behavior: Test 2's asserted error text contains all three of `not found`, `absent.v1.NoSuchMessage`, and `symbol index:`
    - Behavior: Test 3 asserts the exact integer `0`, not `>= 0`
  </acceptance_criteria>
  <verify>
    <automated>go test -race ./inserter/... -run 'TestInserterResolvesTypeAbsentFromConstructionSnapshot|TestInserterUnresolvableTypeReturnsDiagnostic|TestInserterCLIExitsZeroOnPerFileFailure' -v</automated>
    <fails_when>non-zero exit, or `--- FAIL:` in the output, or `no tests to run` in the output, or fewer than three `--- PASS:` lines</fails_when>
    <automated>go test -race ./inserter/...</automated>
    <fails_when>non-zero exit, or `FAIL` appears in the output</fails_when>
  </verify>
  <done>CONS-02 is proven on-demand rather than by construction: the type is demonstrably absent from the snapshot, resolvable through the tiers, loud when genuinely missing, and the CLI's exit-code contract is pinned by a test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `materialized_config/*.materialized_JSON` on disk → inserter parser | Repository-authored JSON carrying an arbitrary `@type` string crosses into descriptor resolution |
| inserter → KV store (Consul/etcd/ZooKeeper/ConfigMaps) | Rendered `config.json` bytes are written to a store the agent serves verbatim to REST clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | `LoadSymbolByScan` reached from `inserter.go`'s `FindMessageByURL` | medium | mitigate | The scan tier's lexical match is a candidate only: `utils/symbol_scan.go` `LoadSymbolByScan` re-checks `MessageRegistry.FindMessageTypeByUrl` after `ParseOne` before returning true. Task 2's Test 1 asserts a resolved type matches the queried URL exactly; Test 2 asserts an absent symbol fails loudly rather than binding a near-match. |
| T-14-02 | Information Disclosure | `InsertConfigFile`'s error return | low | accept | The D-02 diagnostic embeds local import-root filesystem paths. These are operator-side paths already visible in the inserter's own CLI arguments and logs; the inserter is an operator tool, not a network service. Task 2 deliberately asserts on substrings rather than the full path to avoid pinning them. |
| T-14-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable and enforced as such: this plan adds zero dependencies. `git diff --exit-code go.mod go.sum` must be clean at plan end. RESEARCH.md § Package Legitimacy Audit records "No new packages" for the phase. |
</threat_model>

<flagged_assumptions>
Probe row left `unresolved` (surfaced, not dropped, per the spec-less fallback protocol):

- **CONS-02 · category `unclassified` · status `unresolved`** — the edge probe could not classify
  this requirement into one of its eight shape categories and emitted "unclassified — review
  manually". It is therefore NOT auto-resolved into a `must_haves` truth. The plan's four
  authored truths cover the requirement's stated behavior (lazy construction, on-demand
  resolution, loud failure, unchanged exit code); if a reviewer identifies a further edge shape
  for CONS-02, it belongs in a gap-closure plan, not silently in this one.
</flagged_assumptions>

<verification>
- `go build ./...` succeeds
- `go vet ./inserter/...` is silent
- `go test -race ./inserter/...` is green, including the two pre-existing tests and the three new ones
- `git diff --exit-code go.mod go.sum` is clean
</verification>

<success_criteria>
ROADMAP criterion 1 holds: the inserter reads and inserts a materialized config whose type
is not already resolved, resolving it correctly — and a test proves the "not already
resolved" half rather than assuming it.
</success_criteria>

<artifacts_this_phase_produces>
Symbols and paths this plan creates (new — exclude from drift verification):

- `inserter/lazy_resolution_test.go` (new file, package `inserter`)
- `TestInserterResolvesTypeAbsentFromConstructionSnapshot` (new test)
- `TestInserterUnresolvableTypeReturnsDiagnostic` (new test)
- `TestInserterCLIExitsZeroOnPerFileFailure` (new test)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/14-non-compiler-consumer-correctness/14-01-SUMMARY.md` when done
</output>
