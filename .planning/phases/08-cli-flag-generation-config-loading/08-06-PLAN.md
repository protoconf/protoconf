---
phase: 08-cli-flag-generation-config-loading
plan: 06
type: execute
wave: 2
depends_on: [08-05]
gap_closure: true
files_modified:
  - server/server.go
  - compiler/command.go
  - inserter/inserter.go
  - mutate/mutate.go
  - command/configfile.go
  - server/server_test.go
  - inserter/inserter_test.go
  - CHANGELOG.md
autonomous: true
requirements: [PCLI-05, PCLI-06, PCLI-07, PCLI-08, PCLI-09]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All five components (agent, serve, compile, insert, mutate) resolve configuration through the same command.ConfigLayerer instance per Command(), so the multi-config-file precedence fix reaches every CLI surface and not only the agent (D-03, D-05, D-13)"
    - "With PROTOCONF_SERVER_GRPC_ADDRESS set to :9999, protoconf serve -config-file f1.json -config-file f2.json where f1 coincidentally sets :9999 and f2 sets :8888 ends with :9999 (D-05; the gap-1 scenario at the server CLI)"
    - "An explicit -grpc-address flag equal to consts.ServerDefaultAddress parsed before -config-file still beats the config file for protoconf serve (D-05)"
    - "With PROTOCONF_INSERTER_STORE_ADDRESS set, protoconf insert -config-file f1.json -config-file f2.json keeps the env-supplied store-address list even when f1's list is byte-identical to it — the repeated-string half of gap 1 (D-05, PCLI-07)"
    - "protoconf insert -config-file f1.json -config-file f2.json with no env var keeps ONLY the later file's store-address entries — the list is replaced, never appended (D-03)"
    - "Loading the same config file twice through any component is idempotent: the resulting config equals the result of loading it once (PCLI-08 ordering edge)"
    - "An empty JSON config file leaves the factory default and any env-var value untouched, and a config file whose extension libprotoconf cannot unmarshal makes flag.Parse return a non-nil error (PCLI-08 empty edge)"
    - "command/configfile.go declares exactly one layering entry point after this plan: the superseded free function and its matchesBase helper are gone, so no component can accidentally be wired to the defective path (D-03)"
    - "CHANGELOG.md's Unreleased section records that repeated -config-file flags now resolve later-file-wins for message-typed fields and that env vars survive a coinciding earlier file, naming PCLI-09 (D-05)"
    - statement: "Each Command() invocation builds its own config message, flag set, defaults snapshot and ConfigLayerer; two commands constructed in the same process share no config or provenance state"
      verification: backstop
    - statement: "Precedence resolution completes synchronously inside Command() plus flag.Parse on a single goroutine; no background goroutine mutates the config after Parse returns"
      verification: backstop
  prohibitions:
    - "PCLI-09's stated precedence must not be weakened to match the implementation — the REQUIREMENTS.md PCLI-09 text is the target, not a variable, and this plan must not edit it"
    - "The superseded value-comparison layering path must not survive as a second, parallel implementation once every component is migrated — two layering entry points would let a future component pick the defective one"
  artifacts:
    - path: "server/server.go"
      provides: "Server config-file handler driven by a per-Command() ConfigLayerer"
      contains: "command.NewConfigLayerer"
    - path: "compiler/command.go"
      provides: "Compiler config-file handler driven by a per-Command() ConfigLayerer"
      contains: "command.NewConfigLayerer"
    - path: "inserter/inserter.go"
      provides: "Inserter config-file handler driven by a per-Command() ConfigLayerer"
      contains: "command.NewConfigLayerer"
    - path: "mutate/mutate.go"
      provides: "Mutate config-file handler driven by a per-Command() ConfigLayerer"
      contains: "command.NewConfigLayerer"
    - path: "command/configfile.go"
      provides: "A single layering entry point — the superseded free function and matchesBase removed"
      contains: "func NewConfigLayerer"
    - path: "server/server_test.go"
      provides: "Test_cliCommand_MultiConfigFilePrecedence — server CLI regression coverage for the scalar gap"
      contains: "Test_cliCommand_MultiConfigFilePrecedence"
    - path: "inserter/inserter_test.go"
      provides: "Test_cliCommand_MultiConfigFilePrecedence — inserter CLI regression coverage for the repeated-field gap"
      contains: "Test_cliCommand_MultiConfigFilePrecedence"
    - path: "CHANGELOG.md"
      provides: "Unreleased entry recording the multi-config-file precedence correction"
      contains: "PCLI-09"
  key_links:
    - from: "server/server.go"
      to: "command/configfile.go"
      via: "import github.com/protoconf/protoconf/command"
      pattern: "command\\.NewConfigLayerer"
    - from: "compiler/command.go"
      to: "command/configfile.go"
      via: "import github.com/protoconf/protoconf/command"
      pattern: "command\\.NewConfigLayerer"
    - from: "inserter/inserter.go"
      to: "command/configfile.go"
      via: "import github.com/protoconf/protoconf/command"
      pattern: "command\\.NewConfigLayerer"
    - from: "mutate/mutate.go"
      to: "command/configfile.go"
      via: "import github.com/protoconf/protoconf/command"
      pattern: "command\\.NewConfigLayerer"
---

<objective>
Generalise 08-05's proven fix from the agent to the remaining four components, then delete the superseded layering path so no component can be wired to it again.

08-05 built `command.ConfigLayerer`, wired `agent/command.go` onto it, and proved both `08-VERIFICATION.md` gaps closed end-to-end. `server/server.go`, `compiler/command.go`, `inserter/inserter.go` and `mutate/mutate.go` are still on the defective three-argument free function, so `protoconf serve -config-file a.json -config-file b.json` combined with an env var still silently drops the env value. This plan finishes the migration, adds CLI-level regression coverage for the two field kinds the agent test could not reach (`ServerConfig`'s plain scalars under coincidence, and `InserterConfig`'s `repeated string`), and records the behavior change in `CHANGELOG.md`.

Purpose: PCLI-09 holds for every CLI surface, not four fifths of them, and `command/configfile.go` carries exactly one implementation of the ordering.
Output: four rewired components, `LayerConfigFile`/`matchesBase` removed, two new CLI regression tests, one CHANGELOG entry.

CONTEXT.md decision disposition (all thirteen, so nothing is silently dropped):

| Decision | Disposition in this plan |
|---|---|
| D-01, D-02 | Folded — env prefixes delivered by 08-01/08-02; untouched, must stay green. |
| **D-03** | **Completed here.** The layering mechanism named by D-03 becomes `command.ConfigLayerer` across all five components; the superseded free function is removed. D-03's other terms — in-place mutation of the live message, never reassigning the component's `config` field, `lpc.Unmarshal` for json/yaml/pb — remain binding and are re-asserted by this plan's acceptance criteria. |
| D-04 | Folded — format detection by extension, untouched. |
| **D-05** | **Completed here.** `flags > env vars > config file > proto defaults` now holds for repeated `-config-file` flags on all five components. D-05's recorded known limitation (a proto3 zero-valued enum supplied by an env var is indistinguishable from unset) is unchanged and still pinned by the inserter's existing subtests. |
| D-06, D-07, D-08 | Delivered by 08-01/08-02; untouched, must stay green — re-asserted by the `--help` and per-package test gates. |
| D-09, D-10 | Informational (devserver) — explicitly not tracked deliverables of Phase 8. No work. |
| D-11, D-12 | Delivered by 08-02 (KVStoreConfig removal); untouched. |
| **D-13** | **Extended by one line per component**, identically to 08-05's agent change: `layerer := command.NewConfigLayerer(base, c.flag)` between step 6 (`PopulateFlagSet`) and step 7 (`flag.Func("config-file", …)`). The seven-step sequence is otherwise preserved verbatim in all four files. |
</objective>

<gap_closure_context>
**Source:** `.planning/phases/08-cli-flag-generation-config-loading/08-VERIFICATION.md` — the same two `status: failed` gaps 08-05 addresses. Gap 1's `artifacts:` names `command/configfile.go`, and gap 2's names both `command/configfile.go` and `agent/config/v1/agent_config.proto`, but the `key_link` table records that all five components call the same helper — so the defect is live in all five and closing it in one is not closing it.

**What 08-05 leaves for this plan.** 08-05 deliberately did NOT remove `LayerConfigFile` or `matchesBase`, because the other four components still call them and Go compiles per package tree. Removing them is this plan's Task 2, after Task 1 has moved every call site.

**Requirement status entering this plan:**

| ID | Status | Touched here? |
|----|--------|---------------|
| PCLI-05 | SATISFIED by 08-01/08-02 | No — carry-over, must stay satisfied (guarded by four `--help` checks) |
| PCLI-06 | SATISFIED by 08-01/08-02 | No — carry-over, must stay satisfied |
| PCLI-07 | Strengthened for the agent by 08-05 | Strengthened for the remaining four |
| PCLI-08 | Strengthened for the agent by 08-05 | Strengthened for the remaining four |
| **PCLI-09** | **Closed for the agent by 08-05; still BLOCKED for serve/compile/insert/mutate** | **Yes — closed here** |

`08-01-PLAN.md` … `08-05-PLAN.md` are executed or in-flight and MUST NOT be modified, renumbered, or superseded.

**Explicitly out of scope** (recorded non-gating in 08-VERIFICATION.md — do not plan or perform work for these): `mutate/mutate.go:159-163` `TYPE_SINT32` using `uint32()` (WR-01, pre-existing, predates phase 8), the discarded `lpc.Environment()` error return at five call sites (WR-02, a no-op in vendored `libprotoconf@v0.1.0`), and the pre-existing environmental `compiler/lib` `load_remote_with_load_local.pconf` failure. `mutate/mutate.go` is edited by this plan for the layerer wiring only — resist the adjacency temptation to fix WR-01 in the same file.
</gap_closure_context>

<execution_context>
@/Users/smintz/go/src/github.com/protoconf/protoconf/.claude/gsd-core/workflows/execute-plan.md
@/Users/smintz/go/src/github.com/protoconf/protoconf/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cli-flag-generation-config-loading/08-VERIFICATION.md
@.planning/phases/08-cli-flag-generation-config-loading/08-05-PLAN.md
@.planning/phases/08-cli-flag-generation-config-loading/08-05-SUMMARY.md
@.planning/phases/08-cli-flag-generation-config-loading/08-04-SUMMARY.md
</context>

<tasks>

<!-- planner-discipline-allow: command.LayerConfigFile(c.config, base, preFile) -->
<!-- planner-discipline-allow: func LayerConfigFile( -->
<!-- planner-discipline-allow: func matchesBase( -->
<!-- planner-discipline-allow: command\.LayerConfigFile -->
<!-- planner-discipline-allow: matchesBase -->

<task type="auto" tdd="true">
  <name>Task 1: Migrate server, compiler, inserter and mutate onto the ConfigLayerer</name>

  <precondition>`command.NewConfigLayerer` and `(*command.ConfigLayerer).LayerConfigFile` exist in `command/configfile.go`, delivered by 08-05. Assert with `grep -q 'func NewConfigLayerer(' command/configfile.go` before editing anything; halt if absent, because every edit in this task depends on that API.</precondition>

  <read_first>
    - `command/configfile.go` — the API delivered by 08-05: the `ConfigLayerer` struct fields, `NewConfigLayerer(defaults proto.Message, fs *flag.FlagSet)`, and the six steps of `(*ConfigLayerer).LayerConfigFile(live, preFile)`. Note explicitly that the constructor must be called AFTER `PopulateFlagSet` so `fs` is populated.
    - `agent/command.go` — the reference wiring produced by 08-05. Read the `base := proto.Clone(c.config)` line and its comment, the `layerer := command.NewConfigLayerer(base, c.flag)` statement and its position relative to `PopulateFlagSet` and `flag.Func`, and the closure body with `preFile := proto.Clone(c.config)` and `layerer.LayerConfigFile(c.config, preFile)`. This is the shape to replicate exactly four times.
    - `server/server.go` — read the whole `Command()` factory: `consts.ServerDefaultAddress` as the literal default, `configtool.NewConfig` / `SetEnvKeyPrefix("PROTOCONF_SERVER")` / `base := proto.Clone(c.config)` / `lpc.Environment()` / `flag.NewFlagSet` / `lpc.PopulateFlagSet(c.flag)`, and the `c.flag.Func("config-file", …)` closure with its three-line precedence comment. Note the file's import block is known NOT to be gofmt-clean in places.
    - `compiler/command.go` — the same factory shape, prefix `PROTOCONF_COMPILER`, config `&protoconf_compiler_config.CompilerConfig{}` with no literal defaults.
    - `inserter/inserter.go` — the same factory shape, prefix `PROTOCONF_INSERTER`, config `&protoconf_inserter_config.InserterConfig{}`. This is the component with the `repeated string store_address` field and the `StoreType` enum whose zero value is `consul`.
    - `mutate/mutate.go` — the same factory shape, prefix `PROTOCONF_MUTATE`, config with literal defaults `ProtoconfRoot: "./src"` and `ServerAddress: "localhost:4301"`. Read ONLY the `Command()` factory; the `TYPE_SINT32` switch at lines ~159-163 is explicitly out of scope.
    - `.planning/phases/08-cli-flag-generation-config-loading/08-05-SUMMARY.md` — what 08-05 actually built and any recorded deviation from its plan.
  </read_first>

  <files>server/server.go, compiler/command.go, inserter/inserter.go, mutate/mutate.go</files>

  <behavior>
    This task is a mechanical, four-times-identical replication of the wiring 08-05 proved for the agent. Its correctness signal is not a new test but the existing per-component `Test_cliCommand_ConfigPrecedence` suites staying green while the CLI-level multi-file behavior changes — the new multi-file rows land in Task 2.

    Every component ends with the identical three-part shape:
    1. `base := proto.Clone(c.config)` stays exactly where it is, before `lpc.Environment()`, and is now a pristine defaults snapshot that is never mutated.
    2. `layerer := command.NewConfigLayerer(base, c.flag)` is a new statement placed after `lpc.PopulateFlagSet(c.flag)` and immediately before the `c.flag.Func("config-file", …)` call.
    3. Inside the closure, `preFile := proto.Clone(c.config)` stays where it is, and the single three-argument free-function call is replaced by `layerer.LayerConfigFile(c.config, preFile)`.

    No component's `config` field may be assigned anywhere inside the closure — that would orphan the flag set and silently discard every flag parsed after `-config-file`.
  </behavior>

  <action>
    For each of `server/server.go`, `compiler/command.go`, `inserter/inserter.go` and `mutate/mutate.go`, make exactly these edits and nothing else:

    - Insert `layerer := command.NewConfigLayerer(base, c.flag)` immediately before the `c.flag.Func("config-file", …)` statement, with a one-line comment stating that it owns this `Command()` instance's accumulated config-file layer and its env/flag provenance set, and that it must be constructed after `PopulateFlagSet` so the flag set is populated when it reads flag provenance.
    - Update the comment above `base := proto.Clone(c.config)` in each file: `base` is now the pristine factory-default snapshot handed to `NewConfigLayerer` and is no longer mutated as an accumulator. Keep each file's existing specifics — `server/server.go` and `compiler/command.go` have a three-line generic comment; `mutate/mutate.go`'s additionally names `ProtoconfRoot: "./src"` and `ServerAddress: "localhost:4301"`, and that enumeration must be preserved.
    - Inside the `config-file` closure, replace the single three-argument free-function call with `layerer.LayerConfigFile(c.config, preFile)`. Do not move, rename or duplicate `preFile`.
    - Update the three-line precedence comment above that call so it still contains the literal `flags > env vars > config file > proto defaults` and additionally states that the ordering now holds across repeated `-config-file` flags.
    - `github.com/protoconf/protoconf/command` is already imported in all four files (each currently calls the free function); do not add or remove imports beyond what the compiler requires.
    - Do not change any `-config-file` usage string, any flag registration, any `Run` method, or anything outside the `Command()` factory.
    - Run `gofmt -w` on all four files. `server/server.go` carries a pre-existing import-ordering violation; if `gofmt` reformats lines this task did not otherwise touch, that is expected — note it in the SUMMARY rather than reverting it.

    Do NOT delete `LayerConfigFile` or `matchesBase` in this task. Task 2 removes them once every call site has moved and the tree is green.
  </action>

  <acceptance_criteria>
    - Each of `server/server.go`, `compiler/command.go`, `inserter/inserter.go`, `mutate/mutate.go` contains `command.NewConfigLayerer(base, c.flag)` exactly once.
    - Each of those four files contains `layerer.LayerConfigFile(c.config, preFile)` exactly once.
    - Each of those four files contains zero occurrences of the superseded three-argument free-function call.
    - Each of those four files contains zero occurrences of a `c.config` assignment (`c.config =` or `c.config, _ =`).
    - Each of those four files still contains the literal `flags > env vars > config file > proto defaults`.
    - `mutate/mutate.go`'s `base` comment still names both `ProtoconfRoot: "./src"` and `ServerAddress: "localhost:4301"`.
    - `mutate/mutate.go`'s `TYPE_SINT32` case still reads exactly as it does today — this task must not touch it (out of scope per 08-VERIFICATION.md).
    - `go test ./server ./compiler ./inserter ./mutate ./agent ./command -count=1` reports `ok` for all six packages.
    - `go test ./server -run Test_cliCommand_ConfigPrecedence -count=1 -v` and the same command for `./compiler`, `./inserter` and `./mutate` each print `--- PASS` and zero `--- FAIL` lines.
    - `go run cmd/protoconf/main.go serve --help`, `compile --help`, `insert --help` and `mutate --help` each still print a `-config-file` entry.
    - `git diff --exit-code go.mod go.sum` reports no changes.
  </acceptance_criteria>

  <reversibility rating="costly">
    Changes the observable multi-`-config-file` precedence of four shipped CLI surfaces. Rated `costly` rather than `one-way` on a checked fact: no proto, wire-format or gRPC service definition changes, no data migration, and `git revert` restores the previous behavior exactly. The four components' public CLI flag interface is unchanged (guarded by the four `--help` checks), so no user-visible contract is broken beyond the precedence correction that PCLI-09 requires. No `checkpoint:decision` is warranted.
  </reversibility>

  <verify>
    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && go build ./...</automated>
    <fails_when>non-zero exit, or any compiler diagnostic line matching `.go:<line>:<col>:` appears in the output</fails_when>

    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && for f in server/server.go compiler/command.go inserter/inserter.go mutate/mutate.go; do test "$(grep -c 'command.NewConfigLayerer(base, c.flag)' $f)" = "1" || { echo "BAD_CONSTRUCTOR_COUNT $f"; exit 1; }; test "$(grep -c 'layerer.LayerConfigFile(c.config, preFile)' $f)" = "1" || { echo "BAD_CALL_COUNT $f"; exit 1; }; test "$(grep -c 'base, preFile)' $f)" = "0" || { echo "OLD_CALL_SURVIVES $f"; exit 1; }; test "$(grep -c 'c\.config *=' $f)" = "0" || { echo "CONFIG_REASSIGNED $f"; exit 1; }; done; echo ALL_FOUR_MIGRATED_OK</automated>
    <fails_when>non-zero exit, or any line beginning `BAD_CONSTRUCTOR_COUNT`, `BAD_CALL_COUNT`, `OLD_CALL_SURVIVES` or `CONFIG_REASSIGNED`, or the line `ALL_FOUR_MIGRATED_OK` is absent</fails_when>

    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && for f in server/server.go compiler/command.go inserter/inserter.go mutate/mutate.go; do grep -q 'flags > env vars > config file > proto defaults' $f || { echo "MISSING_PRECEDENCE_COMMENT $f"; exit 1; }; done; echo PRECEDENCE_COMMENTS_OK</automated>
    <fails_when>non-zero exit, or any line beginning `MISSING_PRECEDENCE_COMMENT`, or the line `PRECEDENCE_COMMENTS_OK` is absent — a component's handler no longer documents the ordering it implements</fails_when>

    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && go test ./server ./compiler ./inserter ./mutate ./agent ./command -count=1</automated>
    <fails_when>non-zero exit, or any line beginning with `FAIL` or `--- FAIL`, or fewer than six `ok ` lines in the output</fails_when>

    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && go test ./server ./compiler ./inserter ./mutate -run Test_cliCommand_ConfigPrecedence -count=1 -v 2>&1 | grep -c -- '--- PASS: Test_cliCommand_ConfigPrecedence/'</automated>
    <fails_when>the printed count is less than 20 — across the four components these tables hold well over twenty rows today, so a lower number means rows stopped running rather than stopped failing</fails_when>

    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && for c in serve compile insert mutate; do go run cmd/protoconf/main.go $c --help 2>&1 | grep -q -- '-config-file' || { echo "MISSING_CONFIG_FILE_FLAG $c"; exit 1; }; done; echo ALL_FOUR_HELP_OK</automated>
    <fails_when>non-zero exit, or any line beginning `MISSING_CONFIG_FILE_FLAG`, or the line `ALL_FOUR_HELP_OK` is absent — a component's generated CLI surface regressed (PCLI-05/PCLI-06)</fails_when>

    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && grep -c 'FieldDescriptorProto_TYPE_SINT32' mutate/mutate.go</automated>
    <fails_when>the printed count is not `1` — the out-of-scope `TYPE_SINT32` branch was removed, duplicated, or otherwise disturbed by adjacency editing</fails_when>

    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && test -z "$(gofmt -l server/server.go compiler/command.go inserter/inserter.go mutate/mutate.go)"</automated>
    <fails_when>non-zero exit — `gofmt -l` printed at least one path, meaning that file is not gofmt-formatted (Trunk enforces gofmt in CI)</fails_when>

    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && git diff --exit-code go.mod go.sum</automated>
    <fails_when>non-zero exit — a module dependency was added or changed, which this plan must not do</fails_when>
  </verify>

  <done>
    All five components construct one `command.ConfigLayerer` per `Command()` and route their `-config-file` handler through it. Every pre-existing per-component precedence suite still passes, every component's `--help` still lists `-config-file`, and no component reassigns its live config pointer.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the superseded layering path and add CLI regression coverage for the scalar and repeated-field gaps</name>

  <precondition>All four components migrated by Task 1 no longer reference the superseded free function: `grep -rl 'command.LayerConfigFile' --include='*.go' .` must return nothing before the removal step, otherwise deleting it breaks the build.</precondition>

  <read_first>
    - `command/configfile.go` — the current file after 08-05 and Task 1: the `ConfigLayerer` block, the still-present `LayerConfigFile` free function and `matchesBase`, and `setFieldReplacing` (which STAYS — it is used by the layerer).
    - `server/server_test.go` — the file being extended. Note it is `package server`, uses `testify` `require`/`assert`, already provides `makeTempScript` and `writeConfigJSON`, and that `Test_cliCommand_ConfigPrecedence` drives only `cc.flag.Parse` and never `cc.Run`.
    - `inserter/inserter_test.go` — the file being extended. Read `Test_cliCommand_ConfigPrecedence` in full: it covers three field kinds (a plain string `prefix`, a `repeated string` `store-address`, and the `store` enum), and it documents the `consul == enum 0` limitation. Note its config-file writing helper and its env keys (`PROTOCONF_INSERTER_PREFIX`, and the `store-address` field's env key is derived from the proto field name `servers`/`store_address` — read the actual key from the existing test rather than guessing).
    - `agent/command_test.go` — `Test_cliCommand_MultiConfigFilePrecedence` written by 08-05. The two new tests replicate its shape (table-driven, `t.Setenv` before `Command()`, `cc.flag.Parse`, per-row accessor, no `t.Parallel()`).
    - `inserter/config/v1/inserter_config.proto` — `store_address` (`repeated string`, `json_name` `store-address`) and the `StoreType` enum.
    - `server/config/v1/server_config.proto` — `grpc_address` (`json_name` `grpc-address`) and `consts.ServerDefaultAddress` as its factory default.
    - `CHANGELOG.md` — the existing `## Unreleased` / `### ⚠ BREAKING CHANGES` entry added by 08-04, which already names PCLI-09 and the agent's precedence change. The new entry extends that section rather than replacing it.
  </read_first>

  <files>command/configfile.go, server/server_test.go, inserter/inserter_test.go, CHANGELOG.md</files>

  <behavior>
    Write both new tests FIRST against the Task 1 output, confirm the coincidence rows pass (Task 1's migration is what makes them pass — if they fail, the migration is wrong, not the test), and only then delete the superseded code. Record in the SUMMARY whether the new rows would have failed against the pre-Task-1 sources; the honest way to establish that is `git stash` on the four component files, run, unstash.

    **`server/server_test.go` — `Test_cliCommand_MultiConfigFilePrecedence`**, table-driven, `package server`, `testify` assertions, asserting on `cc.config.GrpcAddress`:
    - `env_wins_over_two_files_when_first_file_coincides` — env `PROTOCONF_SERVER_GRPC_ADDRESS=:9999`; file A `{"grpc-address":":9999"}`, file B `{"grpc-address":":8888"}`; args `-config-file A -config-file B` → want `:9999`. THE GAP-1 SCENARIO.
    - `flag_before_config_file_wins_when_flag_equals_factory_default` — no env; args `-grpc-address <consts.ServerDefaultAddress> -config-file A` where A sets `:8888` → want `consts.ServerDefaultAddress`.
    - `later_file_wins_for_scalar_without_env` — no env; A `:8888`, B `:7777` → want `:7777`. Regression guard.
    - `same_config_file_twice_is_idempotent_across_two_flags` — no env; args `-config-file A -config-file A` with A setting `:8888` → want `:8888`. PCLI-08 ordering edge.
    - `empty_second_config_file_does_not_erase_first` — no env; A sets `:8888`, B is `{}` → want `:8888`. PCLI-08 empty edge.
    - `unparsable_config_file_extension_errors` — args `-config-file <path ending in .txt>` → `cc.flag.Parse` returns a non-nil error (`require.Error`). PCLI-08 empty/format edge.

    **`inserter/inserter_test.go` — `Test_cliCommand_MultiConfigFilePrecedence`**, same shape, asserting on `cc.config.StoreAddress` (a `[]string`, compared with `assert.Equal` against an explicit slice):
    - `env_list_wins_over_two_files_when_first_file_coincides` — env store-address set to the two-element list; file A sets the SAME two elements, file B sets a different single element → want the env list. The repeated-string half of gap 1.
    - `later_file_replaces_earlier_list_without_env` — no env; A `["file1:1"]`, B `["file2:2"]` → want exactly `["file2:2"]`, length 1. Proves `proto.Merge`'s append semantics are still neutralised after the provenance rewrite — assert the LENGTH explicitly, because an append bug shows up as `["file1:1","file2:2"]`.
    - `three_files_last_list_wins` — no env; three files, each a distinct single-element list → want exactly the third file's element, length 1.

    Both tests use `t.TempDir()` for config files, `.json` extensions (except the deliberate `.txt` row), `t.Setenv` before `Command()`, and no `t.Parallel()`.
  </behavior>

  <action>
    **Part A — new CLI regression tests.**

    Add `Test_cliCommand_MultiConfigFilePrecedence` to `server/server_test.go` and a same-named function to `inserter/inserter_test.go`, exactly as specified in `<behavior>`. Reuse each file's existing config-file writing helper where its signature fits; where a row needs a body the existing helper cannot produce (an empty `{}`, a `.txt` path, or a `store-address` list), add a file-local raw-body helper named `writeRawConfig(t *testing.T, dir, name, body string) string` rather than changing the existing helper's signature. Do not modify `Test_cliCommand_ConfigPrecedence`, `Test_cliCommand_Run`, or any other existing test in either file.

    For the inserter's env row, read the env key from the existing `Test_cliCommand_ConfigPrecedence` table in that same file — the `store_address` field's env key is derived by `libprotoconf` from the proto FIELD NAME, not from `json_name`, so do not construct it from `store-address`.

    **Part B — remove the superseded layering path from `command/configfile.go`.**

    Delete the free function `LayerConfigFile` and the helper `matchesBase` in their entirety, including their doc comments. Keep `ConfigLayerer`, `NewConfigLayerer`, `LayerConfigFile` as a METHOD, `markExplicitFlags`, `fieldEqual` and `setFieldReplacing` — `setFieldReplacing` is called by the layerer and must not be removed.

    Carry forward the two documented limitations that lived in `matchesBase`'s doc comment, by moving them into `ConfigLayerer`'s doc comment so they are not lost: (1) an env var whose value equals the factory default is indistinguishable from unset; (2) proto3 implicit presence makes a zero-valued scalar, `false`, or a zero-numbered enum invisible to `Range`, so `PROTOCONF_INSERTER_STORE=consul` (where `consul = 0`) cannot override a config file and the `-store consul` flag is the escape hatch. State that limitation (2) is narrowed but not eliminated by flag provenance: a FLAG setting a field to its zero value is now correctly recorded as explicit by `markExplicitFlags`, but its value is still invisible to the `Range` that re-applies it, so the flag escape hatch works because `flag.Parse` writes directly into the live message, not because the layerer re-applies it.

    Verify nothing else in the repository references the removed symbols before deleting: `grep -rn 'command\.LayerConfigFile\|matchesBase' --include='*.go' .` must return no hits afterwards.

    Run `gofmt -w` on all three Go files.

    **Part C — `CHANGELOG.md`.**

    Extend the existing `## Unreleased` / `### ⚠ BREAKING CHANGES` section — do not create a second `## Unreleased` heading and do not touch any released section. Add one bullet under the existing `**cli:**` bullet that records, in operator-facing terms: repeated `-config-file` flags now resolve so that the LATER file wins for message-typed fields (`agent`'s `tls-config` and `store-tls`), where previously the FIRST file's value silently persisted; and an environment variable now wins over a config file even when an earlier `-config-file` happened to set the same value. Name PCLI-09. State the security-relevant consequence explicitly: an operator using `protoconf agent -config-file base.json -config-file override.json` with different TLS material previously got `base.json`'s certificate, key and CA, and now gets `override.json`'s.
  </action>

  <acceptance_criteria>
    - `grep -c 'func LayerConfigFile(' command/configfile.go` returns 0 — the free function is gone.
    - `grep -c 'func matchesBase(' command/configfile.go` returns 0.
    - `grep -c 'func setFieldReplacing(' command/configfile.go` returns 1 — the shared replacement routine survives.
    - `grep -c 'func NewConfigLayerer(' command/configfile.go` returns 1.
    - `grep -rn 'command\.LayerConfigFile' --include='*.go' .` returns no hits.
    - `command/configfile.go` still contains the literal `flags > env vars > config file > proto defaults`, and its `ConfigLayerer` doc comment mentions the zero-value/`consul` limitation carried forward from `matchesBase`.
    - `go test ./server -run Test_cliCommand_MultiConfigFilePrecedence -count=1 -v` prints `--- PASS` for all six named subtests.
    - `go test ./inserter -run Test_cliCommand_MultiConfigFilePrecedence -count=1 -v` prints `--- PASS` for all three named subtests.
    - `go test ./command -run TestLayerConfigFile -count=1 -v` still prints at least 21 `--- PASS: TestLayerConfigFile/` lines — removing the free function must not disturb the ported suite.
    - `CHANGELOG.md` contains exactly one `## Unreleased` heading, and the text under it mentions `PCLI-09`, `tls-config` and `-config-file`.
    - `go build ./...` and `go test ./server ./compiler ./inserter ./mutate ./agent ./command -count=1` both succeed.
    - The SUMMARY records whether `env_wins_over_two_files_when_first_file_coincides` fails against the pre-Task-1 component sources (established by stashing the four component files, running, unstashing).
  </acceptance_criteria>

  <reversibility rating="costly">
    Removes an exported symbol (`command.LayerConfigFile`) from a public Go module path. Rated `costly` rather than `one-way` on a checked fact: the symbol was introduced by 08-03 and lives entirely under `## Unreleased` in `CHANGELOG.md` — the last tagged release is 0.1.5 (2022-01-17) — so it has never shipped and no downstream importer can depend on it. `git revert` restores it. No proto, wire-format or gRPC change. No `checkpoint:decision` is warranted.
  </reversibility>

  <verify>
    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && go build ./... && go test ./server ./compiler ./inserter ./mutate ./agent ./command -count=1</automated>
    <fails_when>non-zero exit, or any line beginning with `FAIL` or `--- FAIL`, or fewer than six `ok ` lines in the output</fails_when>

    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && go test ./server -run Test_cliCommand_MultiConfigFilePrecedence -count=1 -v</automated>
    <fails_when>non-zero exit; or the output contains `--- FAIL`, `[no tests to run]`, or `no test files`; or it is missing any of the lines `--- PASS: Test_cliCommand_MultiConfigFilePrecedence/env_wins_over_two_files_when_first_file_coincides`, `--- PASS: Test_cliCommand_MultiConfigFilePrecedence/flag_before_config_file_wins_when_flag_equals_factory_default`, `--- PASS: Test_cliCommand_MultiConfigFilePrecedence/later_file_wins_for_scalar_without_env`, `--- PASS: Test_cliCommand_MultiConfigFilePrecedence/same_config_file_twice_is_idempotent_across_two_flags`, `--- PASS: Test_cliCommand_MultiConfigFilePrecedence/empty_second_config_file_does_not_erase_first`, `--- PASS: Test_cliCommand_MultiConfigFilePrecedence/unparsable_config_file_extension_errors`</fails_when>

    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && go test ./inserter -run Test_cliCommand_MultiConfigFilePrecedence -count=1 -v</automated>
    <fails_when>non-zero exit; or the output contains `--- FAIL`, `[no tests to run]`, or `no test files`; or it is missing any of the lines `--- PASS: Test_cliCommand_MultiConfigFilePrecedence/env_list_wins_over_two_files_when_first_file_coincides`, `--- PASS: Test_cliCommand_MultiConfigFilePrecedence/later_file_replaces_earlier_list_without_env`, `--- PASS: Test_cliCommand_MultiConfigFilePrecedence/three_files_last_list_wins`</fails_when>

    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && test "$(grep -c 'func LayerConfigFile(' command/configfile.go)" = "0" && test "$(grep -c 'func matchesBase(' command/configfile.go)" = "0" && test "$(grep -c 'func setFieldReplacing(' command/configfile.go)" = "1" && test "$(grep -c 'func NewConfigLayerer(' command/configfile.go)" = "1" && echo SINGLE_LAYERING_PATH_OK</automated>
    <fails_when>non-zero exit, or the line `SINGLE_LAYERING_PATH_OK` is absent — either the superseded free function or `matchesBase` survived as a second layering path, or the shared `setFieldReplacing` routine or the constructor was removed by mistake</fails_when>

    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && ! grep -rn 'command\.LayerConfigFile' --include='*.go' . && echo NO_DANGLING_REFERENCES_OK</automated>
    <fails_when>non-zero exit, or the line `NO_DANGLING_REFERENCES_OK` is absent — some Go source still names the removed free function</fails_when>

    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && go test ./command -run TestLayerConfigFile -count=1 -v 2>&1 | grep -c '^    --- PASS: TestLayerConfigFile/'</automated>
    <fails_when>the printed count is less than 21 — the ported and extended unit suite from 08-05 lost rows when the free function was removed</fails_when>

    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && test "$(grep -c '^## Unreleased' CHANGELOG.md)" = "1" && sed -n '/^## Unreleased/,/^## /p' CHANGELOG.md | grep -q 'PCLI-09' && sed -n '/^## Unreleased/,/^## /p' CHANGELOG.md | grep -q 'tls-config' && echo CHANGELOG_OK</automated>
    <fails_when>non-zero exit, or the line `CHANGELOG_OK` is absent — either a second `## Unreleased` heading was created, or the entry does not name PCLI-09 and the affected TLS field</fails_when>

    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && test -z "$(gofmt -l command/configfile.go server/server_test.go inserter/inserter_test.go)"</automated>
    <fails_when>non-zero exit — `gofmt -l` printed at least one path, meaning that file is not gofmt-formatted</fails_when>

    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && git diff --exit-code go.mod go.sum</automated>
    <fails_when>non-zero exit — a module dependency was added or changed, which this plan must not do</fails_when>
  </verify>

  <done>
    `command/configfile.go` exposes exactly one layering entry point. The scalar coincidence gap and the repeated-string coincidence gap are both pinned by CLI-level regression tests on `serve` and `insert`, the same-file-twice and empty/unparsable-file edges are covered, and `CHANGELOG.md` tells an operator that repeated `-config-file` flags now resolve later-file-wins for TLS material.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols and files created by this plan — none exist before it runs, so drift verification must not treat them as pre-existing:

| Symbol / path | Kind | Location |
|---|---|---|
| `layerer` (local) | new local variable in `Command()` | `server/server.go`, `compiler/command.go`, `inserter/inserter.go`, `mutate/mutate.go` |
| `Test_cliCommand_MultiConfigFilePrecedence` | new test func | `server/server_test.go` |
| `Test_cliCommand_MultiConfigFilePrecedence` | new test func | `inserter/inserter_test.go` |
| `writeRawConfig` | new unexported test helper (only if the file's existing helper cannot produce the body) | `server/server_test.go`, `inserter/inserter_test.go` |
| `env_wins_over_two_files_when_first_file_coincides`, `flag_before_config_file_wins_when_flag_equals_factory_default`, `later_file_wins_for_scalar_without_env`, `same_config_file_twice_is_idempotent_across_two_flags`, `empty_second_config_file_does_not_erase_first`, `unparsable_config_file_extension_errors` | new subtest names | `server/server_test.go` |
| `env_list_wins_over_two_files_when_first_file_coincides`, `later_file_replaces_earlier_list_without_env`, `three_files_last_list_wins` | new subtest names | `inserter/inserter_test.go` |
| `## Unreleased` bullet on multi-config-file precedence | new CHANGELOG entry (extends the existing section) | `CHANGELOG.md` |

Symbols this plan REMOVES (they exist before it runs and must not exist after): `LayerConfigFile` (free function) and `matchesBase` in `command/configfile.go`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator environment → process config | `PROTOCONF_SERVER_*`, `PROTOCONF_COMPILER_*`, `PROTOCONF_INSERTER_*`, `PROTOCONF_MUTATE_*` variables are read by `lpc.Environment()` and become authoritative configuration. |
| config file(s) on disk → process config | `-config-file` is repeatable on all four components; each occurrence reads an operator-named path via `os.ReadFile`. |
| CLI argv → process config | Flags parsed by `flag.Parse`; highest precedence by design. |
| resolved config → outbound credentials | `ServerConfig.auth_token`, `tls_cert`/`tls_key`/`tls_ca`, `MutateConfig`'s TLS fields, and `InserterConfig.store_address` determine what the process authenticates with and which KV store it writes to. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06-01 | Elevation of Privilege | `server/server.go`, `mutate/mutate.go` config-file handlers | high | mitigate | Until Task 1 lands, a `PROTOCONF_SERVER_AUTH_TOKEN` (or the mutate equivalent) supplied by the container runtime is silently dropped whenever an earlier `-config-file` set the same value and a later one changed it, so the process authenticates with file-supplied rather than environment-supplied credentials. Mitigated by routing all four components through the provenance-tracking layerer and pinned by `env_wins_over_two_files_when_first_file_coincides` in `server/server_test.go`. `security_block_on: high` makes this a release gate. |
| T-08-06-02 | Tampering | `inserter/inserter.go` `store_address` resolution | high | mitigate | The same defect on a `repeated string` field: an operator pinning the KV-store endpoint list through `PROTOCONF_INSERTER_*` can have it silently replaced by a later config file's list, redirecting materialized config writes to an unintended store. Mitigated identically; pinned by `env_list_wins_over_two_files_when_first_file_coincides` in `inserter/inserter_test.go`, which asserts the exact slice, not just its first element. |
| T-08-06-03 | Tampering | `command/configfile.go` after Task 2 | medium | mitigate | Leaving two layering entry points in one package would let a future component be wired to the defective value-comparison path and silently reintroduce both gaps with no test failure anywhere. Mitigated by deleting the free function and `matchesBase` outright and gating on `SINGLE_LAYERING_PATH_OK` plus a repository-wide reference grep. |
| T-08-06-04 | Information Disclosure | `server/server_test.go`, `inserter/inserter_test.go` | low | mitigate | New precedence fixtures use only address strings and store-address lists — never a real token, key or certificate, and never the `-text` TLS variants. Config files are written into `t.TempDir()` (auto-removed), never a shared `os.TempDir()` path. |
| T-08-06-05 | Repudiation | precedence resolution across five components | low | accept | Nothing logs which source won for a given field. Accepted for this gap closure — per-field provenance logging is new surface beyond the two gaps. `CHANGELOG.md` and every component's `-config-file` usage text document the ordering instead. |
| T-08-06-06 | Denial of Service | `-config-file` handlers | low | accept | `os.ReadFile` with no size cap on an operator-supplied, repeatable path. Pre-existing on all five components and unchanged by this plan; the paths are supplied by whoever starts the process. |
| T-08-06-SC | Tampering (supply chain) | Go module graph | high | mitigate | This plan installs nothing: no npm/pip/cargo step exists, and every symbol used is stdlib or an existing direct dependency. No `## Package Legitimacy Audit` is required because there are no package-manager installs. Enforced by the `git diff --exit-code go.mod go.sum` gates in both tasks' `<verify>`. |

Canon-referral breadcrumbs (surfaced, deliberately not minted as prohibitions): config-input trust ordering, credential-bearing configuration fields, and arbitrary-path file reads are canon security territory covered by `/gsd-secure-phase` and the register above.
</threat_model>

<probe_accounting>
This plan authors the four probe items 08-05 deferred to it, plus the one flagged assumption. The full twelve-item ledger and the split between the two plans live in `08-05-PLAN.md`'s `<probe_accounting>`; it is not restated here so the two plans cannot drift apart.

| Requirement | Category | Authored as | Where |
|---|---|---|---|
| PCLI-08 | empty | explicit truth — an empty second config file does not erase the first; an unknown extension makes `flag.Parse` error | this plan (`empty_second_config_file_does_not_erase_first`, `unparsable_config_file_extension_errors`) |
| PCLI-08 | ordering | explicit truth — loading the same file twice is idempotent | this plan (`same_config_file_twice_is_idempotent_across_two_flags`) |
| PCLI-08 | concurrency | backstop truth — each `Command()` owns an isolated layerer, so two commands in one process share no config or provenance state | this plan |
| PCLI-09 | concurrency | backstop truth — resolution completes synchronously inside `Command()` plus `flag.Parse` on one goroutine | this plan |
| PCLI-06 | unclassified | **flagged planner assumption** | this plan |

**Flagged planner assumption (PCLI-06, `unclassified`, unresolved).** The probe row reads "unclassified — review manually" and names no predicate, so no defensible acceptance criterion can be derived from the available artifacts. The planner's assumption is that PCLI-06 ("generated CLI matches the current flag interface, backward compatible") is fully satisfied by 08-01/08-02's `json_name` annotations and is untouched by a change confined to precedence resolution. It is guarded only by the four `--help` presence checks in Task 1, which prove `-config-file` still exists on every component but do not re-verify every flag name against the pre-migration interface. This is surfaced as an assumption, NOT auto-resolved with a backstop, and NOT dismissed.

Ledger for the plan set: 12 surfaced = 11 authored (7 in 08-05, 4 here) + 1 flagged assumption (here). No silent drops.

Prohibitions kept after the recall→precision pass: 2 in this plan (3 in 08-05). Dropped as routine engineering rather than minted: "must not break the build", "must not leave unused imports", "must not delete a test to make the suite pass" (structurally prevented by the `ALL_TWELVE_ORIGINAL_ROWS_PRESENT` and subtest-count gates instead). Dropped as canon and breadcrumbed in `<threat_model>`: config-input trust ordering, credential handling, arbitrary path read. Both prohibitions here are authored descriptor-less, so each disposes flagged-unverified.
</probe_accounting>

<verification>
1. `go build ./...` exits 0.
2. `go test ./server ./compiler ./inserter ./mutate ./agent ./command -count=1` exits 0 with `ok` for all six packages.
3. `go test ./server -run Test_cliCommand_MultiConfigFilePrecedence -count=1 -v` shows `--- PASS` for all six subtests; the inserter equivalent shows `--- PASS` for all three.
4. Every component's pre-existing `Test_cliCommand_ConfigPrecedence` still passes: `go test ./server ./compiler ./inserter ./mutate ./agent -run Test_cliCommand_ConfigPrecedence -count=1 -v` shows zero `--- FAIL` lines.
5. `go test ./command -run TestLayerConfigFile -count=1 -v` still shows at least 21 `--- PASS: TestLayerConfigFile/` lines.
6. `grep -rn 'command\.LayerConfigFile' --include='*.go' .` returns no hits, and `command/configfile.go` declares neither `func LayerConfigFile(` nor `func matchesBase(`.
7. `serve`, `compile`, `insert` and `mutate` `--help` each still list `-config-file`.
8. `CHANGELOG.md` has exactly one `## Unreleased` heading whose body names PCLI-09 and `tls-config`.
9. `git diff --exit-code go.mod go.sum` exits 0.
10. `08-01-PLAN.md` … `08-05-PLAN.md` are byte-identical to their committed state: `git diff --exit-code .planning/phases/08-cli-flag-generation-config-loading/08-0[1-5]-PLAN.md` exits 0.
11. `.planning/REQUIREMENTS.md` PCLI-09 text is unchanged.

Scope note: `go test ./...` is deliberately NOT the gate. Phase 8 verification recorded a pre-existing, environmental `compiler/lib` failure (`load_remote_with_load_local.pconf`, missing `.protoconf_cache` fixture); gating on the whole tree would let that failure mask or falsely fail this plan.
</verification>

<success_criteria>
- All five CLI components resolve `flags > env vars > config file > proto defaults` across repeated `-config-file` flags, for scalar, repeated-string and message-typed fields alike.
- `command/configfile.go` has exactly one layering implementation; the defective value-comparison path cannot be reached because it no longer exists.
- The scalar coincidence gap and the repeated-string coincidence gap each have a committed CLI-level regression test on a real component, not only a helper unit test.
- The PCLI-08 empty, ordering and format edges are covered by named subtests rather than assumed.
- An operator reading `CHANGELOG.md` learns that a later `-config-file` now wins for TLS material and that an environment variable survives a coinciding earlier file.
- Nothing outside the two gaps was changed: `mutate/mutate.go`'s `TYPE_SINT32` branch, the five discarded `lpc.Environment()` error returns, and the environmental `compiler/lib` failure are all untouched.
</success_criteria>

<output>
Create `.planning/phases/08-cli-flag-generation-config-loading/08-06-SUMMARY.md` when done. Record: the pre-migration run of the two new CLI tests (established by stashing the four component files), the final diff of `command/configfile.go` after the removal, any gofmt reformatting of pre-existing lines in `server/server.go`, and any place where observed `libprotoconf` behavior differed from `08-05-PLAN.md`'s `<library_facts>`.
</output>
