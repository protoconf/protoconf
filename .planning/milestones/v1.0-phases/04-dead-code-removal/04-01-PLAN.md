---
phase: 04-dead-code-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inserter/inserter.go
  - agent/filekv/filekv.go
autonomous: true
requirements:
  - REFC-09
  - REFC-10

must_haves:
  truths:
    - "inserter/inserter.go has no init() function"
    - "inserter/inserter.go has no runtime import"
    - "agent/filekv/filekv.go has no dead error check after the watchCh send"
    - "All existing tests pass after removal"
    - "Project builds cleanly"
  artifacts:
    - path: "inserter/inserter.go"
      provides: "Inserter without dead init()"
      contains: "func newFlagSet"
    - path: "agent/filekv/filekv.go"
      provides: "Watch goroutine without dead error check"
      contains: "watchCh <- &store.KVPair"
  key_links:
    - from: "inserter/inserter.go"
      to: "go build"
      via: "no unused imports"
      pattern: "^import"
    - from: "agent/filekv/filekv.go"
      to: "Watch goroutine logic"
      via: "channel send followed by select"
      pattern: "watchCh.*KVPair"
---

<objective>
Remove two instances of dead code: the unnecessary runtime.GOMAXPROCS init() function in inserter/inserter.go and the unreachable error check in agent/filekv/filekv.go Watch goroutine.

Purpose: Eliminate noise and dead code paths that obscure the real logic, satisfying REFC-09 and REFC-10.
Output: Two cleaned-up Go source files with all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dead-code-removal/04-CONTEXT.md
@.planning/phases/04-dead-code-removal/04-RESEARCH.md

<interfaces>
<!-- No new interfaces created or consumed. Pure removal of dead code. -->

From inserter/inserter.go (lines 1-53, current state):
- Line 14: `"runtime"` import -- REMOVE (unused after init() removal)
- Lines 47-53: `func init() { runtime.GOMAXPROCS(runtime.NumCPU()) }` -- REMOVE entirely

From agent/filekv/filekv.go (lines 142-148, current state):
- Lines 142-145: `watchCh <- &store.KVPair{...}` -- KEEP
- Lines 146-148: `if err != nil { return }` -- REMOVE (dead check, err guaranteed nil here)
- Lines 28-30: `func init() { valkeyrie.Register(...) }` -- DO NOT TOUCH (functional init)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead init() and runtime import from inserter (REFC-09, D-01, D-02)</name>
  <files>inserter/inserter.go</files>
  <read_first>
    - inserter/inserter.go (full file to see current state of imports and init)
  </read_first>
  <action>
    Remove the entire init() function block at lines 47-53 per D-01:
    ```
    func init() {
    	// set the number of CPUs to use.
    	// By default, the number of CPUs is the number of CPUs on the machine.
    	// Just to show we can change the number of CPUs
    	// I have added this below command.
    	runtime.GOMAXPROCS(runtime.NumCPU())
    }
    ```

    Also remove `"runtime"` from the import block (line 14) since it has no other references in the file. This is required -- Go compiler rejects unused imports.

    Per D-02: Do NOT scan or modify init() functions in any other package.

    After editing, the file should go from line 37 imports closing paren directly to `func newFlagSet()` (with appropriate blank lines per gofmt).

    Run `gofmt -w inserter/inserter.go` after editing to normalize formatting.
  </action>
  <verify>
    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && go build ./inserter/... && go test ./inserter/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'func init()' inserter/inserter.go` returns 0
    - `grep -c '"runtime"' inserter/inserter.go` returns 0
    - `grep -c 'GOMAXPROCS' inserter/inserter.go` returns 0
    - `go build ./inserter/...` exits 0
    - `go test ./inserter/...` exits 0 (existing tests pass)
  </acceptance_criteria>
  <done>inserter/inserter.go contains no init() function, no runtime import, and no GOMAXPROCS call. Package builds and all existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead error check from filekv Watch goroutine (REFC-10, D-03, D-04)</name>
  <files>agent/filekv/filekv.go</files>
  <read_first>
    - agent/filekv/filekv.go (full file to see current state of Watch function)
  </read_first>
  <action>
    Remove exactly lines 146-148 from agent/filekv/filekv.go per D-03:
    ```
    if err != nil {
    	return
    }
    ```

    This block checks `err` from the `proto.Marshal` call on line 137, but that error was already handled at lines 138-141 with `if err != nil { slog.Error(...); return }`. If execution reaches line 146, err is guaranteed nil.

    Per D-04: Do NOT modify surrounding code, TODO comments, or other logic. Do NOT touch the functional init() at lines 28-30 that registers the store with Valkeyrie.

    After removal, line 145 (closing brace of the watchCh send) should be followed by a blank line then the `select {` statement.

    Run `gofmt -w agent/filekv/filekv.go` after editing to normalize formatting.
  </action>
  <verify>
    <automated>cd /Users/smintz/go/src/github.com/protoconf/protoconf && go build ./agent/filekv/...</automated>
  </verify>
  <acceptance_criteria>
    - The sequence `watchCh <- &store.KVPair{` is present in filekv.go (channel send preserved)
    - `grep -A2 'watchCh <- &store.KVPair{' agent/filekv/filekv.go` does NOT contain `if err != nil`
    - `grep -c 'func init()' agent/filekv/filekv.go` returns 1 (functional init preserved)
    - `grep 'valkeyrie.Register' agent/filekv/filekv.go` returns a match (functional init intact)
    - `go build ./agent/filekv/...` exits 0
  </acceptance_criteria>
  <done>agent/filekv/filekv.go Watch goroutine has no dead error check after the channel send. The functional init() is untouched. Package builds cleanly.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full project build and test suite to confirm nothing is broken:

```bash
go build ./...
go test ./inserter/... ./agent/filekv/...
```

Note: `go vet ./agent/filekv/...` has pre-existing "passes lock by value" warnings unrelated to this phase. These are out of scope.
</verification>

<success_criteria>
1. `grep -c 'func init()' inserter/inserter.go` returns 0
2. `grep -c '"runtime"' inserter/inserter.go` returns 0
3. `grep -c 'GOMAXPROCS' inserter/inserter.go` returns 0
4. Dead `if err != nil { return }` block removed from filekv.go Watch goroutine
5. filekv.go functional init() with valkeyrie.Register is preserved
6. `go build ./...` succeeds
7. `go test ./inserter/...` passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-dead-code-removal/04-01-SUMMARY.md`
</output>
