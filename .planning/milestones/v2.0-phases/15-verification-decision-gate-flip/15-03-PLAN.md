---
phase: 15-verification-decision-gate-flip
plan: 03
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - .planning/research/compiler-performance/BASELINE.md
autonomous: true
requirements: [GATE-04, GATE-05]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.planning/research/compiler-performance/BASELINE.md` gains a milestone-close section recording the in-process `compileCorpus` figure over the 2400-proto calibrated corpus, the CI-observed figure, the calibrated threshold, and one hand-timed real `protoconf compile` invocation (D-02, D-09)."
    - "That section states plainly that the figure was measured over a corpus generated by `utils/testdata.GenerateCorpus`, not over the real 799-proto protoconf-terraform tree, and that the ~3x per-file multiplier used to size it is unsourced and accepted at face value (D-07, D-08)."
    - "The GATE-04 evidence names the exact difference between the two test commands: the pinned local command skips `Test_cliCommand_Run`, which exists in `inserter/`, `server/`, `agent/` and `compiler/` and stalls on Consul locally, while the authoritative CI run executes the full `./...` under `-race` with no skips. [GATE-04 adjacency edge]"
    - "The GATE-04 evidence cites a Go workflow run by numeric run id and by the commit SHA it ran on, and that run's conclusion is `success`; a cancelled run, or one whose log contains no test results, is not accepted as evidence. [GATE-04 empty edge]"
    - "When more than one Go workflow run exists on the phase's commits, the evidence cites the latest completed successful run on the branch head at the time of writing, and states that the `.planning/` edits committed after it do not change test outcomes. [GATE-04 ordering edge]"
    - "The evidence names all five GATE-04 cases explicitly: `with_config_rollout_validator.pconf`, `validator_test.pconf`, `validator_repeated_test.pconf`, `validator_map_test.pconf` (all expecting `ErrInvalidConfig`) and `field_type_any_test.pconf` (expecting `nil`), with each one's observed outcome in the cited run."
    - "The evidence records the GATE-05 requirement-text mismatch: GATE-05 as written asks for the real protoconf-terraform corpus to be compiled once at milestone close, and D-07 supersedes that with a calibrated generated corpus so nothing depends on a sibling checkout CI cannot satisfy."
  artifacts:
    - .planning/research/compiler-performance/BASELINE.md
  key_links:
    - "Milestone-close section -> BASELINE.md's existing before-numbers (6.97s headline, the 4,639ms/260ms/197ms stage breakdown, the 1,286ms unlinked-parse row): the after-section is appended so the comparison is immediate; the before-numbers are not edited."
    - "GATE-04 evidence -> a specific GitHub Actions run id and commit SHA: without both, the claim is unauditable."
    - "GATE-05 figure -> `TestCompilerStartupBudget`'s corpus (`corpusProtos = 2400`): the recorded evidence number and the CI-asserted gate are the same measurement over the same corpus, not two independent claims."
  prohibitions:
    - statement: "The milestone-close evidence must not present the generated-corpus figure as a measurement of the real 799-proto protoconf-terraform corpus, and must not present the ~3x per-file multiplier that sizes the corpus as a sourced or measured number. Overstating either turns a calibrated estimate into a claim the milestone did not earn."
      status: resolved
      verification: judgment
    - statement: "The evidence must not be made to look better by editing BASELINE.md's existing before-numbers, its stage breakdown, or its negative-result section. The after-section is appended; the record of what was measured in 2026-09 stays as measured."
      status: resolved
      verification: judgment
---

<objective>
Record the milestone's closing measurements and its test-suite evidence in one place, with
the caveats stated rather than papered over.

Purpose: GATE-05 asks for a real-corpus number recorded as evidence, and GATE-04 asks for
proof that every pre-existing test stayed green with the named validator cases unchanged in
outcome. Neither is a CI gate; both are claims that need an audit trail. D-09 puts them in
`BASELINE.md`, beside the 6.97s before-numbers they are the "after" for.

Output: one appended milestone-close section in
`.planning/research/compiler-performance/BASELINE.md`.
</objective>

<execution_context>
@/Users/smintz/go/src/github.com/protoconf/protoconf/.claude/gsd-core/workflows/execute-plan.md
@/Users/smintz/go/src/github.com/protoconf/protoconf/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-verification-decision-gate-flip/15-CONTEXT.md
@.planning/research/compiler-performance/BASELINE.md
@.planning/phases/15-verification-decision-gate-flip/15-01-SUMMARY.md
@compiler/lib/startup_bench_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure the two closing figures — in-process and real CLI</name>
  <files>(no repository files modified; measurements are recorded in the task output and consumed by Task 3)</files>
  <read_first>
    - `compiler/lib/startup_bench_test.go` — `TestCompilerStartupBudget` as landed by plan 15-01, to confirm the corpus size constant and the exact shape of the `startup budget: n=` log line
    - `utils/testdata/corpus.go` — `GenerateCorpus`'s signature and its caller-owns-dir contract, since this task generates a corpus outside a test
    - `compiler/command.go` lines 38-80 — how `protoconf compile` takes its root directory and config file arguments, so the hand-timed invocation exercises `runLocally` -> `compilerlib.NewCompiler` + `CompileFile`, the same call path `compileCorpus` measures
    - `.planning/phases/15-verification-decision-gate-flip/15-CONTEXT.md` — D-02 (why a CLI number is taken at all), D-07, D-09
  </read_first>
  <precondition>Plan 15-01 is complete: `TestCompilerStartupBudget` exists with its calibrated threshold, and plan 15-01's SUMMARY records the CI-observed millisecond figure and the run id it came from.</precondition>
  <action>
Collect the two numbers the milestone-close section needs. Nothing in the repository changes
in this task; every artifact it creates lives outside the working tree.

**1. The in-process figure (GATE-05).** Run `TestCompilerStartupBudget` locally, without
`-race`, and capture its `startup budget: n=2400` line verbatim. This is the same
measurement the CI gate asserts, over the same corpus — record it as such, not as a second
independent claim.

**2. The real CLI figure (D-02).** PROJECT.md states the goal as "`protoconf compile`
completes in under 200ms", which names the binary and therefore includes Go runtime init,
flag parsing and config loading that the in-process figure excludes. Close that gap with one
real invocation:

  - Build the binary: `go build -o <scratch>/protoconf ./cmd/protoconf`.
  - Generate a 2400-proto corpus into a durable directory outside the repository. Write a
    throwaway main package that calls `testdata.GenerateCorpus(dir, 2400)` and run it with
    `go run /absolute/path/to/that/file.go <dir>` from the repository root — verified while
    planning: `go run` on an absolute path outside the module still resolves module imports
    from the current module, so nothing needs to be created inside the repository. If that
    fails on this machine, create the file under a temporary directory inside the repository
    instead and delete it before committing; either way nothing generated here is committed.
  - Time the invocation: `protoconf compile <dir> main.mpconf`, using `/usr/bin/time -p` and
    taking the best of three runs to remove first-run page-cache noise. Record all three.
  - Confirm from the CLI's own log line that it took the lazy path — `protoFilesLoaded=5`
    over a 2400-proto tree is the check that the number means what it claims.

For reference only, measured on darwin/arm64 while planning: corpus generation at n=2400 took
~311ms, and three CLI runs measured 0.11s, 0.08s, 0.08s. Do not copy these into the evidence
— re-measure and record the observed values. They are here so a wildly different result is
recognized as a problem rather than written down.

Record every figure in the task output, each labelled with what produced it, the machine
(`go env GOOS GOARCH`), and the commit SHA measured at.
  </action>
  <verify>
    <automated>go test -count=1 -run '^TestCompilerStartupBudget$' -v ./compiler/lib/... 2>&amp;1 | tee /dev/stderr | grep -q 'startup budget: n=2400'</automated>
    <fails_when>non-zero exit — the gate did not pass locally, or the measurement line is absent, so there is no in-process figure to record</fails_when>
    <automated>go build ./... &amp;&amp; go vet ./compiler/lib/...</automated>
    <fails_when>non-zero exit, or a `vet:` diagnostic line appears in the output</fails_when>
    <automated>git status --porcelain</automated>
    <fails_when>non-zero exit, or the output lists any generated corpus directory, built binary, or throwaway Go file inside the repository — this task must leave the working tree clean</fails_when>
  </verify>
  <acceptance_criteria>
    - The task output quotes the local `startup budget: n=2400` line verbatim, with its elapsed duration.
    - The task output records three `/usr/bin/time -p` real-time figures for `protoconf compile <dir> main.mpconf` over a freshly generated 2400-proto corpus, and identifies which is taken as the recorded figure.
    - The task output quotes the CLI's `compile finished` log line showing `protoFilesLoaded=5`, proving the lazy path was exercised over the 2400-proto tree.
    - The task output records `go env GOOS GOARCH` and the commit SHA the measurements were taken at.
    - `git status --porcelain` is empty at the end of the task.
  </acceptance_criteria>
  <done>Both closing figures exist, each traceable to the command that produced it and to the same 2400-proto corpus, with nothing left behind in the working tree.</done>
</task>

<task type="auto">
  <name>Task 2: Obtain the authoritative green CI run for GATE-04</name>
  <files>(no repository files modified; the run id, SHA and outcomes are recorded in the task output)</files>
  <read_first>
    - `compiler/lib/compiler_test.go` lines 35-70 — the `t.Run` table, specifically `with_config_rollout_validator.pconf` (41), the pre-existing skip at 47-51, `validator_test.pconf` (56), `validator_repeated_test.pconf` (57), `validator_map_test.pconf` (58), `validator_passing_test.pconf` (59) and `field_type_any_test.pconf` (66)
    - `.github/workflows/go.yml` — the `Run coverage` step, which is what runs the full `./...` under `-race` with no skips
    - `.planning/config.json` — `workflow.test_command`, the pinned local command and its `-skip 'Test_cliCommand_Run'`
    - `.planning/phases/15-verification-decision-gate-flip/15-CONTEXT.md` — D-10 (both suites, CI authoritative, gap stated) and D-11 (a green run is sufficient proof; the pre-existing skip is deliberately not named in the evidence)
  </read_first>
  <precondition>Plans 15-01 and 15-02 are committed. The current branch has an open pull request, so `.github/workflows/go.yml`'s `pull_request` trigger fires on push.</precondition>
  <action>
Produce the run GATE-04 is closed against. D-10 makes CI authoritative because only CI runs
the full `./...` under `-race` with no skips; the pinned local command is the runnable
check a reviewer can execute, and the two differ in a known, stateable way.

**1. Run the pinned local command** exactly as configured:
`go test -race -timeout 300s -skip 'Test_cliCommand_Run' ./...`. Record its result. This is
the reviewer-reproducible half.

**2. Push the branch and obtain a green run.** Push, then `gh run watch` the resulting Go
workflow run to completion. Note that `go.yml` sets `cancel-in-progress: true`, so any
concurrent push cancels the run — a cancelled run is not evidence; push again and re-watch.
The observed baseline for this job is ~11.5-12 minutes.

**3. Verify the run actually proves something.** Confirm `gh run view <run-id>` reports
conclusion `success`. Then, from `gh run view <run-id> --log`, extract and record:

  - The `Run startup budget gate` step's `startup budget: n=2400` line (the CI-side GATE-05
    and GATE-02 figure).
  - The outcome of each of the five GATE-04 named cases:
    `with_config_rollout_validator.pconf`, `validator_test.pconf`,
    `validator_repeated_test.pconf`, `validator_map_test.pconf` — all four expecting
    `ErrInvalidConfig` — and `field_type_any_test.pconf`, expecting `nil`. Name all five;
    the fifth is explicitly part of GATE-04's text and sits in the same `t.Run` table as
    two dozen unremarkable cases, so it is the one most easily lost.
  - The `TestCompilerStartupScaling` result and its logged allocation ratio.
  - The commit SHA the run executed against (`gh run view <run-id> --json headSha`).

A run whose log contains no test results, or which was cancelled, does not count. If more
than one successful run exists on this branch, take the latest completed successful one on
the current branch head.

**4. State the gap, do not leave it implicit** (D-10). Record in the task output which cases
the local command omits that CI covers: `Test_cliCommand_Run`, which exists in `inserter/`,
`server/`, `agent/` and `compiler/`, and is skipped locally because it stalls on Consul.

Note for the executor, so it is not a surprise (D-11): one pre-existing compiler case,
`load_remote_with_load_local.pconf`, is skipped rather than green — a stale `vizceral_repo`
module pin from 2026-09-01, 244 commits back and unrelated to this milestone. The user
declined naming it as an explicit exception in the GATE-04 evidence, so do not add it to the
BASELINE.md section and do not treat its skip as a failure. Record the observation in this
task's output only.
  </action>
  <verify>
    <automated>go test -race -timeout 300s -skip 'Test_cliCommand_Run' ./...</automated>
    <fails_when>non-zero exit, or `--- FAIL` appears in the output</fails_when>
    <automated>BR=$(git rev-parse --abbrev-ref HEAD) &amp;&amp; gh run list --workflow=go.yml --branch "$BR" --limit 1 --json conclusion,databaseId,headSha</automated>
    <fails_when>non-zero exit, or the reported `conclusion` is anything other than `success`</fails_when>
    <automated>BR=$(git rev-parse --abbrev-ref HEAD) &amp;&amp; RID=$(gh run list --workflow=go.yml --branch "$BR" --limit 1 --json databaseId --jq '.[0].databaseId') &amp;&amp; gh run view "$RID" --log &gt; /tmp/gsd-15-run.log &amp;&amp; grep -c 'startup budget: n=2400' /tmp/gsd-15-run.log</automated>
    <fails_when>the printed count is 0, or non-zero exit — the budget step did not execute in the cited run, or the run produced no log</fails_when>
  </verify>
  <acceptance_criteria>
    - The task output records the pinned local command's full result line and its exit status.
    - The task output records a Go workflow run id whose `conclusion` is `success` and the `headSha` it ran against.
    - The task output records the observed outcome of each of the five GATE-04 named cases from that run's log, listed by fixture name.
    - The task output records the `startup budget: n=2400` line and the `TestCompilerStartupScaling` allocation ratio from that run's log.
    - The task output states the local-versus-CI gap by naming `Test_cliCommand_Run` and the four packages it lives in.
    - The task output notes the pre-existing `load_remote_with_load_local.pconf` skip as an observation, and confirms it is not carried into the BASELINE.md section.
  </acceptance_criteria>
  <done>A named, successful GitHub Actions run on a named commit exists, with the five GATE-04 cases' outcomes and both performance figures extracted from its log, and the local-versus-CI coverage gap written down rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 3: Append the milestone-close section to BASELINE.md</name>
  <files>.planning/research/compiler-performance/BASELINE.md</files>
  <read_first>
    - `.planning/research/compiler-performance/BASELINE.md` — the whole file: the `## Headline` 6.97s claim, the `## Stage breakdown` table (4,639ms registry / 260ms resolvers / 197ms compile, `NewCompiler` = 5.06s), `## What the config actually needs` (5 requested -> 7 total files in 2.686ms), the `## Related measurements` table (1,286ms unlinked parse, 623ms lexical scan, 4,639ms parse+link), and the `## Reproduction` closer — the new section is appended in this file's table-driven voice
    - `.planning/REQUIREMENTS.md` lines 76-81 — GATE-01 through GATE-05 verbatim, so the recorded evidence is checked against the requirement text rather than against memory
    - `.planning/phases/15-verification-decision-gate-flip/15-CONTEXT.md` — D-02, D-07, D-08, D-09, D-10, D-11
    - Task 1 and Task 2 outputs — every number and identifier written here comes from those, not from any planning document
  </read_first>
  <action>
Append one new section to `.planning/research/compiler-performance/BASELINE.md`. Append —
do not edit the headline, the stage breakdown, the CPU profile, the negative result, or the
related-measurements table. Those are the "before" this section is the "after" for, and they
stay as measured on 2026-09-04. Place the new section before `## Reproduction` so the
reproduction notes remain the file's closer, or after it if that reads better in the final
file; state which and why in the summary.

Head the section for the milestone close and open it with the measurement conditions:
machine, commit SHA, and the date, in the same style as the file's existing header block.

Then record, using the numbers from Tasks 1 and 2 and nothing else:

**The after-numbers**, as a table that invites comparison with the stage breakdown above:
the in-process `compileCorpus` figure over the 2400-proto corpus, the CI-observed figure
from `ubuntu-latest`, the calibrated threshold now asserted, and the hand-timed
`protoconf compile` CLI figure. Say for each what produced it. State that the CLI figure
exists because PROJECT.md's goal names the binary and therefore includes Go runtime init,
flag parsing and config loading that the in-process figure excludes (D-02).

**What the corpus is, stated so it cannot be misread** (D-07, D-08). The figure was measured
over a corpus generated by `utils/testdata.GenerateCorpus`, not over the real 799-proto
protoconf-terraform tree at `../protoconf-terraform/example/src`. It is sized at 2400 protos
to *cost what those 799 real protos cost*, using the ~3x per-file multiplier GATE-05 states
— and that multiplier is unsourced: it appears nowhere in
`.planning/research/compiler-performance/`, and `utils/testdata/corpus.go`'s own tuning
history (earlier cuts 47x then 21x cheaper per file, closed by the current message count and
field options) suggests the generator is closer to parity than 3x. Record why that is
acceptable rather than merely admitted: startup cost is now flat against corpus size (39ms
at n=50, 35ms at n=500), so a wrong multiplier changes the file count without materially
changing the measured time or the gate's outcome. The cost of being wrong is a slightly
weaker evidential claim, not a wrong gate.

**The GATE-05 requirement-text mismatch, surfaced not silently reinterpreted** (D-07). Quote
what GATE-05 asks for — the real protoconf-terraform corpus compiled once at milestone close
— and state that D-07 supersedes it, because that corpus is a sibling checkout absent on CI
and on every other developer's machine and drifting independently, and that the requirement's
underlying intent is preserved by the calibration. Note that amending REQUIREMENTS.md was
offered and deferred out of this phase.

**The GATE-04 evidence.** The run id and commit SHA from Task 2, with the run's conclusion.
The five named cases and their observed outcomes: `with_config_rollout_validator.pconf`,
`validator_test.pconf`, `validator_repeated_test.pconf`, `validator_map_test.pconf` all
producing `ErrInvalidConfig`, and `field_type_any_test.pconf` producing `nil`. Then the D-10
statement of what the two commands cover: the pinned local command
`go test -race -timeout 300s -skip 'Test_cliCommand_Run' ./...` is the reviewer-runnable
check; CI is authoritative because it runs the full `./...` under `-race` with no skips, and
therefore covers the `Test_cliCommand_Run` cases in `inserter/`, `server/`, `agent/` and
`compiler/` that the local command omits because they stall on Consul. Add one line noting
that `.planning/` edits committed after the cited run — this section among them — do not
change test outcomes, so the run remains the evidence for the phase's code state.

Do not add the `load_remote_with_load_local.pconf` skip to this section (D-11).

**The gates' resting state.** One short list: GATE-01 asserted by
`TestCompilerStartupScaling`; GATE-02 asserted by `TestCompilerStartupBudget` in the
`Run startup budget gate` CI step; GATE-03 documented in `CHANGELOG.md` and `README.md`;
GATE-04 and GATE-05 evidenced above. This is what makes the section readable as the
milestone's closing record rather than as a loose pile of numbers.
  </action>
  <verify>
    <automated>grep -c 'GenerateCorpus' .planning/research/compiler-performance/BASELINE.md</automated>
    <fails_when>the printed count is 0, or non-zero exit — the section does not identify what the corpus actually was</fails_when>
    <automated>grep -q 'field_type_any_test.pconf' .planning/research/compiler-performance/BASELINE.md &amp;&amp; grep -q 'validator_map_test.pconf' .planning/research/compiler-performance/BASELINE.md &amp;&amp; grep -q 'Test_cliCommand_Run' .planning/research/compiler-performance/BASELINE.md &amp;&amp; echo GATE04_OK</automated>
    <fails_when>non-zero exit, or the literal `GATE04_OK` is absent from the output — one of the named GATE-04 elements is missing</fails_when>
    <automated>grep -q '6.97s' .planning/research/compiler-performance/BASELINE.md &amp;&amp; grep -q '4,639ms' .planning/research/compiler-performance/BASELINE.md &amp;&amp; grep -q '1,286ms' .planning/research/compiler-performance/BASELINE.md &amp;&amp; grep -q 'a ~1,700x gap' .planning/research/compiler-performance/BASELINE.md &amp;&amp; echo BEFORE_INTACT</automated>
    <fails_when>non-zero exit, or the literal `BEFORE_INTACT` is absent from the output — one of the 2026-09-04 before-numbers was edited or removed instead of the section being appended</fails_when>
    <automated>go test -race -timeout 300s -skip 'Test_cliCommand_Run' ./...</automated>
    <fails_when>non-zero exit, or `--- FAIL` appears in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/research/compiler-performance/BASELINE.md` gains exactly one new `##`-level section, and all seven pre-existing `##` headings (`Headline`, `Stage breakdown`, `What the config actually needs`, `CPU profile`, `Negative result: the linker lookup is not a fixable upstream bug`, `Related measurements`, `Reproduction`) are still present with their original figures (`6.97s`, `4,639ms`, `1,286ms`, `a ~1,700x gap`).
    - The new section contains a numeric GitHub Actions run id and a commit SHA, and states that run's conclusion.
    - The new section names all five GATE-04 fixtures and each one's observed outcome.
    - The new section contains the strings `GenerateCorpus`, `Test_cliCommand_Run`, and `protoconf-terraform`.
    - The new section states that the ~3x per-file multiplier is unsourced and explains why the exposure is low, citing the 39ms-at-n=50 / 35ms-at-n=500 flatness.
    - The new section quotes GATE-05's requirement text and states that D-07 supersedes it, rather than restating GATE-05 as if it had been satisfied literally.
    - The new section does not mention `load_remote_with_load_local.pconf` (D-11).
    - The four recorded figures (in-process, CI-observed, calibrated threshold, CLI) each name the command that produced them.
    - `go test -race -timeout 300s -skip 'Test_cliCommand_Run' ./...` exits 0.
  </acceptance_criteria>
  <done>BASELINE.md holds both halves of the milestone in one document: the 6.97s before-numbers untouched, and an appended closing section whose every figure names its source, whose corpus is described for what it is, and whose GATE-04 claim points at a specific auditable CI run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Milestone evidence -> future reader | A recorded number is trusted without re-measurement. Anything overstated here is believed later. |
| GitHub Actions run log -> evidence record | Numbers and outcomes are transcribed by hand from a run log into a Markdown file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-06 | Repudiation | Milestone-close evidence in `.planning/research/compiler-performance/BASELINE.md` | medium | mitigate | Every claim carries its provenance: the CI figures cite a numeric run id and a commit SHA, the local figures cite the command and the machine, and the corpus is identified as `GenerateCorpus`-generated. An unattributable number cannot be written here. |
| T-15-07 | Tampering | Transcription from `gh run view --log` into Markdown | low | mitigate | Task 2 extracts figures with `gh run view --log` and records them verbatim in its output; Task 3 copies only from that output, never from a planning document. Task 3's verify greps the committed file for the run id, the fixture names and the corpus generator name. |
| T-15-08 | Tampering | Existing BASELINE.md before-numbers | medium | mitigate | The section is appended, and Task 3's verify counts removed lines in the diff — any edit to the 2026-09-04 record registers there instead of passing quietly. Captured as this plan's second prohibition. |

No code paths, no input parsing and no package-manager installs are touched by this plan, so
no `T-15-SC` supply-chain row applies.
</threat_model>

## Artifacts this phase produces

| Artifact | Kind | Location |
|----------|------|----------|
| milestone-close `##` section (after-numbers, corpus description, GATE-04 evidence, gate resting state) | new document section | `.planning/research/compiler-performance/BASELINE.md` |

Symbols this plan reads but does not create (they are produced by plan 15-01):
`TestCompilerStartupBudget`, `corpusProtos`, `budget`, the `startup budget: n=` log literal,
and the `Run startup budget gate` CI step.

## Probe coverage and flagged assumptions

**Edge items carried by this plan** (three of the nine surfaced items, all auto-resolved with
`verification: explicit` acceptance criteria in `must_haves.truths` above):

| Requirement | Category | Where resolved |
|---|---|---|
| GATE-04 | adjacency | truth 3 — the exact difference between the local and CI test sets is named, not left implicit |
| GATE-04 | empty | truth 4 — a cancelled run, or one with no test results, is not accepted as evidence |
| GATE-04 | ordering | truth 5 — when several runs exist, the latest completed successful run on the branch head is the one cited |

The remaining three of the nine (GATE-05 boundary, precision, concurrency) are carried by plan
15-01; the three `unclassified` items (GATE-01, GATE-02, GATE-03) are flagged as assumptions in
plans 15-01 and 15-02. Nine surfaced, six authored into `must_haves.truths`, three flagged —
no silent drops.

**Flagged assumptions**

- **D-11 overclaim, accepted by the user.** One pre-existing compiler case,
  `load_remote_with_load_local.pconf` (`compiler/lib/compiler_test.go:47-51`), is skipped
  rather than green — a stale `vizceral_repo` module pin from 2026-09-01, 244 commits back and
  unrelated to lazy loading. GATE-04's flat "every pre-existing test stays green" therefore
  overclaims by exactly this one test. The user was offered the option of naming it as an
  explicit exception in the evidence and declined; the plan honors that and does not carry it
  into BASELINE.md. Repinning the module is a deferred idea, not phase scope.
- **D-08 accepted risk.** The ~3x per-file multiplier is unsourced. Recorded in the evidence
  itself rather than resolved, per CONTEXT.md.

<verification>
- `.planning/research/compiler-performance/BASELINE.md` carries a milestone-close section with a run id, a commit SHA, four labelled figures, the five GATE-04 fixture names, and the corpus caveats.
- `git diff HEAD -- .planning/research/compiler-performance/BASELINE.md` shows additions only.
- The cited GitHub Actions run reports conclusion `success`.
- `go test -race -timeout 300s -skip 'Test_cliCommand_Run' ./...` passes.
</verification>

<success_criteria>
- GATE-04: a specific successful CI run on a specific commit is cited as evidence, the five named cases' outcomes are recorded, and the local-versus-CI coverage gap is stated rather than implied.
- GATE-05: the closing figure is recorded with its corpus described for what it is, the ~3x multiplier's unsourced status disclosed, and the requirement-text mismatch surfaced rather than silently reinterpreted.
- BASELINE.md's 2026-09-04 before-numbers are unchanged.
</success_criteria>

<output>
Create `.planning/phases/15-verification-decision-gate-flip/15-03-SUMMARY.md` when done.
Record in it the four figures, the cited run id and SHA, and where in BASELINE.md the section
was placed and why.
</output>
