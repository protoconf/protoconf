---
phase: 14-non-compiler-consumer-correctness
plan: 05
type: execute
wave: 4
depends_on: [14-01, 14-02, 14-03, 14-04]
files_modified:
  - mutate/mutate.go
  - compiler/lib/module_service.go
  - mutate/mutate_test.go
autonomous: true
requirements: [CONS-02, CONS-03, CONS-04]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The mutate CLI constructs its module service lazily and resolves its target message name through the shared tiered resolver, so a type absent from the construction snapshot still resolves (D-01, D-03)."
    - "The mutate CLI still returns exit code 1 when its target message cannot be found — its abort shape is preserved, and is deliberately NOT unified with the inserter's skip-and-log-continue exit 0 (Claude's Discretion constraint, RESEARCH correction)."
    - "No construction-time snapshot remains as a type-URL or nested-Any resolution source anywhere in inserter, mutate, agent or server — a repo-wide check finds zero surviving sites outside the two deliberately-excluded ExtensionResolver fields (D-03, D-04)."
    - "NewLazyModuleService's doc comment states the truth after this phase: mod sync is the only remaining NewModuleService consumer, and the other four construct lazily (D-01)."
  prohibitions:
    - statement: "MUST NOT loosen symbol matching — case folding, prefix or suffix matching, or accepting the first lexical candidate without the post-parse registry re-check — to make a failing lookup succeed; an ambiguous or absent symbol must fail loudly rather than be approximated into a plausible wrong type."
      category: safety
      status: resolved
      verification: judgment
  artifacts:
    - path: "mutate/mutate.go"
      provides: "Lazy construction and tiered message-name resolution for the mutate CLI, with its exit-1 abort preserved"
      contains: "lib.NewLazyModuleService"
    - path: "compiler/lib/module_service.go"
      provides: "A doc comment that matches the post-phase consumer set"
    - path: "mutate/mutate_test.go"
      provides: "Exit-code shape guard for an unresolvable target message"
  key_links:
    - from: "mutate/mutate.go"
      to: "compiler/lib/parser/parser.go"
      via: "the anyResolver local is assigned from the tiered resolver before FindMessageByName"
      pattern: "parser\\.TypeResolver"
---

<objective>
Close the last resolution site and make the codebase say so: flip the `mutate` CLI, rewrite
the now-false `NewLazyModuleService` doc comment, and assert as a checkable end-state that
no construction-time snapshot survives anywhere as a type-URL or nested-`Any` resolution
source.

Purpose: Phase 13's D-03 left ROADMAP criterion 1's grep clause — *"grepping for type-URL
resolution logic finds that single implementation"* — only partly satisfied, because four
call sites remained. This plan is where it completes, and the completion is verified by a
gate rather than asserted in prose.
Output: two line-level changes in `mutate/mutate.go`, a corrected doc comment, an exit-code
guard test, and a repo-wide end-state gate.
</objective>

<execution_context>
@/Users/smintz/go/src/github.com/protoconf/protoconf/.claude/gsd-core/workflows/execute-plan.md
@/Users/smintz/go/src/github.com/protoconf/protoconf/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-non-compiler-consumer-correctness/14-CONTEXT.md
@.planning/phases/14-non-compiler-consumer-correctness/14-RESEARCH.md
@.planning/phases/14-non-compiler-consumer-correctness/14-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mutate CLI — lazy construction, tiered resolution, and its exit-1 abort preserved</name>
  <reversibility rating="costly">Undo means reverting this construction site and resolver assignment; it is the fourth of four coordinated flips and no published contract or wire format changes, so it is not one-way (rating carried forward from CONTEXT.md D-01).</reversibility>
  <files>mutate/mutate.go, mutate/mutate_test.go</files>
  <read_first>
    - `mutate/mutate.go` — the file being modified: the construction block around lines 67-73 and the resolver assignment plus `FindMessageByName` call around lines 74-81, including the `slog.Error(...)` / `return 1` abort branch that must survive unchanged
    - `compiler/lib/parser/parser.go` lines 24-121 — `RegistryTypeResolver.FindMessageByName`, confirming the tiered chain covers the by-name entry point as well as the by-URL one
    - `mutate/mutate_test.go` — the existing construction of `cliCommand` and its `Run` invocation, for the exit-code assertion idiom to reuse
    - `.planning/phases/14-non-compiler-consumer-correctness/14-PATTERNS.md` § `mutate/mutate.go` — the verified analog with the exact before/after lines
    - `.planning/phases/14-non-compiler-consumer-correctness/14-RESEARCH.md` § Common Pitfalls, Pitfall 2 — the verified finding that `mutate` is the only one of the four consumers that currently aborts
  </read_first>
  <behavior>
    - Test (`TestMutateExitsOneOnUnresolvableMessage`): running the mutate command with a `-proto-msg` naming a message declared nowhere under `src/` returns exit code exactly `1`. This pins the abort shape against accidental unification with the inserter's exit-0 skip-and-continue.
    - Test (`TestMutateResolvesMessageAbsentFromConstructionSnapshot`): running the mutate command against a root whose target message exists only in `src/` (not in the construction-time snapshot) returns exit code `0` and writes the expected mutable config file.
  </behavior>
  <action>
Two line-level changes in `mutate/mutate.go`'s `Run`.

D-01 — change `lib.NewModuleService(root)` to `lib.NewLazyModuleService(root)`. The
`slog.Error("error creating module service", ...)` branch and its `return 1` stay exactly
as they are, as does the following `ms.LoadFromLockFile()`.

D-03 — the local `anyResolver` is assigned from the parser's construction-time snapshot
field; reassign it from `parser.TypeResolver` instead. Keep the local variable name and keep
the `anyResolver.FindMessageByName(protoreflect.FullName(c.config.ProtoMsg))` call that
consumes it unchanged, so the diff is one line. <!-- planner-discipline-allow: LocalResolver -->

Do NOT touch the `slog.Error("could not find typeUrl for", ...)` branch or its `return 1`.
That abort is `mutate`'s current and only-one-of-the-four CLI contract; RESEARCH.md verified
at HEAD that the inserter does the opposite (per-file skip-and-log, `return 0`), and
CONTEXT.md's discretion constraint forbids changing either as a side effect of the resolver
rewiring. Preserve each consumer's own shape; do not unify them.

Then add the two tests named in `<behavior>` to `mutate/mutate_test.go`, following the
existing test's construction of the command under test. For the second test, build a temp
root containing `src/ondemand/v1/thing.proto` (package `ondemand.v1`, message `Thing`, one
string field) laid out so the directory path mirrors the package name, and drive the command
with `-proto-msg ondemand.v1.Thing` plus one `field=value` argument.
  </action>
  <acceptance_criteria>
    - Source: `grep -c 'lib.NewLazyModuleService(root)' mutate/mutate.go` is 1 and `grep -c 'lib.NewModuleService' mutate/mutate.go` is 0
    - Source: `grep -c 'anyResolver := parser.TypeResolver' mutate/mutate.go` is 1
    - Source: `grep -c 'return 1' mutate/mutate.go` is unchanged from HEAD — no abort branch was added or removed
    - Behavior: `TestMutateExitsOneOnUnresolvableMessage` asserts the exact integer `1`, not merely a non-zero value
    - Behavior: `TestMutateResolvesMessageAbsentFromConstructionSnapshot` asserts the exact integer `0` and that the expected file under `mutable_config/` exists afterwards
    - Test: the `<verify>` command exits 0 and prints two `--- PASS:` lines
  </acceptance_criteria>
  <verify>
    <automated>go test -race ./mutate/... -run 'TestMutateExitsOneOnUnresolvableMessage|TestMutateResolvesMessageAbsentFromConstructionSnapshot' -v</automated>
    <fails_when>non-zero exit, or `--- FAIL:` in the output, or `no tests to run` in the output, or fewer than two `--- PASS:` lines</fails_when>
    <automated>go build ./... &amp;&amp; go vet ./mutate/...</automated>
    <fails_when>non-zero exit, or any line of output at all (both commands are silent on success)</fails_when>
  </verify>
  <done>The mutate CLI is lazy, resolves by name through the tiered chain, and its exit-1 abort is pinned by a test that would fail if it were unified with the inserter's exit-0 shape.</done>
</task>

<task type="auto">
  <name>Task 2: Doc-comment truth and the repo-wide single-resolution-path end-state gate</name>
  <precondition>Plans 14-01, 14-02, 14-03 and 14-04 are committed, so all four construction flips and all four resolver rewirings are present in the working tree.</precondition>
  <files>compiler/lib/module_service.go</files>
  <read_first>
    - `compiler/lib/module_service.go` lines 44-70 — the doc comment being rewritten and the function it documents
    - `mod/command.go` around line 81 — the one remaining eager consumer; confirm the call site is still `lib.NewModuleService(".")` before writing the comment that names it
    - `compiler/lib/parser/parser.go` lines 24-46 and 116-127 — the `Parser` field declarations and `FindExtensionByName`/`FindExtensionByNumber`, which document why the extension resolver is excluded from the end-state gate
    - `server/server.go` around lines 432-444 — the two `ExtensionResolver` fields the gate deliberately allows through
    - `agent/kv_agent_rollout_impl.go` around line 352 and `inserter/inserter_test.go` around line 75 — two prose comments that mention the snapshot field by name; the gate filters comment lines so these must not trip it
  </read_first>
  <action>
Two things: a comment rewrite and a gate.

**Doc comment.** Rewrite `NewLazyModuleService`'s doc comment in
`compiler/lib/module_service.go`. Its current text claims that "every other consumer
(server, inserter, agent/filekv, mutate, mod sync) must keep using `NewModuleService`",
which is false after this phase. The replacement states: `GetProtoRegistry()` returns a
near-empty registry configured for on-demand parsing rather than eagerly walking and
parsing all of `src/`; the compiler, the mutation server, the inserter, `agent/filekv` and
the `mutate` CLI all construct through it; `mod sync` is the sole remaining
`NewModuleService` consumer, because it needs the whole tree by definition. Verify the
`mod/command.go` call site by grep before naming it, rather than trusting the line number.
No code inside the function changes.

**End-state gate.** Add the gate below to `<verify>` and record its meaning in the SUMMARY:
after this phase, the only permitted references to the construction-time snapshot field
outside `compiler/lib/parser/` are (a) comment lines and (b) the two `ExtensionResolver:`
fields in `server/server.go`'s reflection wiring. The gate greps `inserter/`, `mutate/`,
`agent/` and `server/` for the field name, drops lines whose content begins with `//`
(so prose mentions in `agent/kv_agent_rollout_impl.go` and `inserter/inserter_test.go` do
not trip it), drops the two allowed `ExtensionResolver:` assignments, and requires the
remainder to be empty. Comment filtering is mandatory here: an unfiltered count would be
self-invalidating, since the surrounding explanatory comments name the field.
<!-- planner-discipline-allow: LocalResolver -->

At HEAD the gate reports four offending lines (`inserter/inserter.go` twice,
`mutate/mutate.go` once, `server/server.go` once). After plans 14-01, 14-03 and this plan's
Task 1, it must report none. Do not "complete" the gate by touching the `ExtensionResolver`
fields — the tiered resolver's extension lookups delegate straight to the same snapshot, so
swapping them would change nothing and would only remove the gate's own allowance.
  </action>
  <acceptance_criteria>
    - Source: `grep -c 'must keep using NewModuleService' compiler/lib/module_service.go` is 0
    - Source: `grep -c 'mod sync' compiler/lib/module_service.go` is at least 1 — the rewritten comment names the one remaining eager consumer
    - Source: `grep -c 'lib.NewModuleService' mod/command.go` is 1 — the comment's claim is true of the code
    - CLI: the end-state gate command in `<verify>` prints no `path:line:` lines and exits 0
    - Source: `grep -cE 'ExtensionResolver:[[:space:]]*s\.parser\.' server/server.go` is still 2 — the deliberate exclusion is intact, not "completed"
  </acceptance_criteria>
  <verify>
    <automated>test -f inserter/inserter.go &amp;&amp; test -f mutate/mutate.go &amp;&amp; test -f server/server.go &amp;&amp; test -f agent/filekv/filekv.go &amp;&amp; OFFENDERS=$(grep -rn --include='*.go' 'LocalResolver' inserter/ mutate/ agent/ server/ | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//' | grep -vE 'ExtensionResolver:[[:space:]]*s\.parser\.LocalResolver'); printf '%s' "$OFFENDERS"; test -z "$OFFENDERS"</automated>
    <fails_when>non-zero exit, or any `path:line:content` line printed before the exit — each printed line is a surviving non-comment construction-time-snapshot resolution site outside the two allowed reflection extension-resolver fields. A missing source file also exits non-zero at the leading `test -f` guard, so the gate cannot pass vacuously.</fails_when>
    <automated>go build ./... &amp;&amp; go vet ./...</automated>
    <fails_when>non-zero exit, or any line of output at all (both commands are silent on success)</fails_when>
    <automated>go test -race -count=1 ./...</automated>
    <fails_when>non-zero exit, or `FAIL` appears in the output</fails_when>
  </verify>
  <done>The doc comment matches the code, `mod sync` is confirmed as the only remaining eager consumer, and a runnable gate proves no second type-URL or nested-`Any` resolution source survives in the four consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI arguments → `mutate` | `-proto-msg` and `field=value` arguments name a message and set its fields; an unresolvable name must abort rather than write a partial config |
| `mutate` → `mutable_config/` on disk | The written file is later read by the mutation server's reflection walk and by the compiler |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-13 | Spoofing | `FindMessageByName` reached through the scan tier | medium | mitigate | The scan tier treats a lexical declaration match as a candidate only and re-checks `MessageRegistry.FindMessageTypeByUrl` after `ParseOne` before returning true (`utils/symbol_scan.go` `LoadSymbolByScan`); the index tier keys on the exact fully-qualified name's bytes with no case folding or affix matching (`utils/symbol_index.go` `SymbolFile`). `TestMutateExitsOneOnUnresolvableMessage` asserts an absent name aborts rather than binding a near-match. Recorded as a prohibition in `must_haves`. |
| T-14-14 | Tampering | Second surviving resolution implementation | medium | mitigate | Task 2's end-state gate is the control: any non-comment reference to the construction-time snapshot outside `compiler/lib/parser/` and the two allowed extension-resolver fields fails the plan. This is what keeps D-04's narrowness an invariant rather than a convention. |
| T-14-15 | Repudiation | `mutate`'s exit code | low | accept | Preserving exit 1 on an unresolvable message means an operator's shell and any CI wrapping it still see the failure. No new audit surface is warranted for a single-shot operator CLI; the abort plus the D-02 diagnostic in the log is the record. |
| T-14-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero dependencies added; `git diff --exit-code go.mod go.sum` must be clean at plan end. RESEARCH.md § Package Legitimacy Audit records "No new packages" for the phase. |
</threat_model>

<verification>
- The end-state gate prints nothing and exits 0
- `go build ./...` succeeds and `go vet ./...` is silent
- `go test -race -count=1 ./...` is green (this matches CI's `.github/workflows/go.yml` command shape)
- `git diff --exit-code go.mod go.sum` is clean
</verification>

<success_criteria>
Exactly one type-URL resolution implementation remains reachable from every consumer, the
`mutate` CLI's abort contract is unchanged, and the doc comment that told a future reader
the opposite is gone.
</success_criteria>

<artifacts_this_phase_produces>
Symbols this plan creates (new — exclude from drift verification):

- `TestMutateExitsOneOnUnresolvableMessage` (new test in `mutate/mutate_test.go`)
- `TestMutateResolvesMessageAbsentFromConstructionSnapshot` (new test)
- the rewritten `NewLazyModuleService` doc comment in `compiler/lib/module_service.go`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/14-non-compiler-consumer-correctness/14-05-SUMMARY.md` when done
</output>
